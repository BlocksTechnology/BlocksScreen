from devices.amu.models import GateStatus
from lib.utils.blocks_button import BlocksCustomButton
from lib.utils.blocks_linedit import BlocksCustomLinEdit
from lib.utils.icon_button import IconButton
from PyQt6 import QtCore, QtGui, QtWidgets
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


# ──────────────────────────────────────────────────────────────────────────────
# Spool button
# ──────────────────────────────────────────────────────────────────────────────
class Spoll_button(QtWidgets.QAbstractButton):
    def __init__(self, parent=None):
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁ__init____mutmut_mutants'), args, kwargs, self)
    def xǁSpoll_buttonǁ__init____mutmut_orig(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_1(self, parent=None):
        super().__init__(None)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_2(self, parent=None):
        super().__init__(parent)
        self.color = None
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_3(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(None, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_4(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, None, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_5(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, None)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_6(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_7(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_8(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, )
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_9(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(1, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_10(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 1, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_11(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 1)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_12(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = None
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_13(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = None
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_14(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = "XXXX"
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_15(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = None
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_16(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 1
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_17(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = None
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_18(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = "XXXX"
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_19(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = None
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_20(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 1
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_21(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(None)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_22(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(False)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_23(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(None)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_24(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(101)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_25(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = None
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_26(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            None
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_27(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            "XX:/filament_related/media/btn_icons/loaded_spool.svgXX"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_28(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/FILAMENT_RELATED/MEDIA/BTN_ICONS/LOADED_SPOOL.SVG"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/half_spoll.svg"
        )
    def xǁSpoll_buttonǁ__init____mutmut_29(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = None
    def xǁSpoll_buttonǁ__init____mutmut_30(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            None
        )
    def xǁSpoll_buttonǁ__init____mutmut_31(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            "XX:/filament_related/media/btn_icons/half_spoll.svgXX"
        )
    def xǁSpoll_buttonǁ__init____mutmut_32(self, parent=None):
        super().__init__(parent)
        self.color = QtGui.QColor(0, 0, 0)
        self.status = GateStatus.UNKNOWN
        self.slot_id = ""
        self.temp = 0
        self.material = ""
        self.weight = 0
        self.setCheckable(True)
        self.setMinimumHeight(100)
        self._icon = QtGui.QPixmap(
            ":/filament_related/media/btn_icons/loaded_spool.svg"
        )
        self._unloaded_icon = QtGui.QPixmap(
            ":/FILAMENT_RELATED/MEDIA/BTN_ICONS/HALF_SPOLL.SVG"
        )
    
    xǁSpoll_buttonǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁ__init____mutmut_1': xǁSpoll_buttonǁ__init____mutmut_1, 
        'xǁSpoll_buttonǁ__init____mutmut_2': xǁSpoll_buttonǁ__init____mutmut_2, 
        'xǁSpoll_buttonǁ__init____mutmut_3': xǁSpoll_buttonǁ__init____mutmut_3, 
        'xǁSpoll_buttonǁ__init____mutmut_4': xǁSpoll_buttonǁ__init____mutmut_4, 
        'xǁSpoll_buttonǁ__init____mutmut_5': xǁSpoll_buttonǁ__init____mutmut_5, 
        'xǁSpoll_buttonǁ__init____mutmut_6': xǁSpoll_buttonǁ__init____mutmut_6, 
        'xǁSpoll_buttonǁ__init____mutmut_7': xǁSpoll_buttonǁ__init____mutmut_7, 
        'xǁSpoll_buttonǁ__init____mutmut_8': xǁSpoll_buttonǁ__init____mutmut_8, 
        'xǁSpoll_buttonǁ__init____mutmut_9': xǁSpoll_buttonǁ__init____mutmut_9, 
        'xǁSpoll_buttonǁ__init____mutmut_10': xǁSpoll_buttonǁ__init____mutmut_10, 
        'xǁSpoll_buttonǁ__init____mutmut_11': xǁSpoll_buttonǁ__init____mutmut_11, 
        'xǁSpoll_buttonǁ__init____mutmut_12': xǁSpoll_buttonǁ__init____mutmut_12, 
        'xǁSpoll_buttonǁ__init____mutmut_13': xǁSpoll_buttonǁ__init____mutmut_13, 
        'xǁSpoll_buttonǁ__init____mutmut_14': xǁSpoll_buttonǁ__init____mutmut_14, 
        'xǁSpoll_buttonǁ__init____mutmut_15': xǁSpoll_buttonǁ__init____mutmut_15, 
        'xǁSpoll_buttonǁ__init____mutmut_16': xǁSpoll_buttonǁ__init____mutmut_16, 
        'xǁSpoll_buttonǁ__init____mutmut_17': xǁSpoll_buttonǁ__init____mutmut_17, 
        'xǁSpoll_buttonǁ__init____mutmut_18': xǁSpoll_buttonǁ__init____mutmut_18, 
        'xǁSpoll_buttonǁ__init____mutmut_19': xǁSpoll_buttonǁ__init____mutmut_19, 
        'xǁSpoll_buttonǁ__init____mutmut_20': xǁSpoll_buttonǁ__init____mutmut_20, 
        'xǁSpoll_buttonǁ__init____mutmut_21': xǁSpoll_buttonǁ__init____mutmut_21, 
        'xǁSpoll_buttonǁ__init____mutmut_22': xǁSpoll_buttonǁ__init____mutmut_22, 
        'xǁSpoll_buttonǁ__init____mutmut_23': xǁSpoll_buttonǁ__init____mutmut_23, 
        'xǁSpoll_buttonǁ__init____mutmut_24': xǁSpoll_buttonǁ__init____mutmut_24, 
        'xǁSpoll_buttonǁ__init____mutmut_25': xǁSpoll_buttonǁ__init____mutmut_25, 
        'xǁSpoll_buttonǁ__init____mutmut_26': xǁSpoll_buttonǁ__init____mutmut_26, 
        'xǁSpoll_buttonǁ__init____mutmut_27': xǁSpoll_buttonǁ__init____mutmut_27, 
        'xǁSpoll_buttonǁ__init____mutmut_28': xǁSpoll_buttonǁ__init____mutmut_28, 
        'xǁSpoll_buttonǁ__init____mutmut_29': xǁSpoll_buttonǁ__init____mutmut_29, 
        'xǁSpoll_buttonǁ__init____mutmut_30': xǁSpoll_buttonǁ__init____mutmut_30, 
        'xǁSpoll_buttonǁ__init____mutmut_31': xǁSpoll_buttonǁ__init____mutmut_31, 
        'xǁSpoll_buttonǁ__init____mutmut_32': xǁSpoll_buttonǁ__init____mutmut_32
    }
    xǁSpoll_buttonǁ__init____mutmut_orig.__name__ = 'xǁSpoll_buttonǁ__init__'

    def setColor(self, qc: QtGui.QColor):
        args = [qc]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁsetColor__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁsetColor__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁsetColor__mutmut_orig(self, qc: QtGui.QColor):
        self.color = qc
        self.update()

    def xǁSpoll_buttonǁsetColor__mutmut_1(self, qc: QtGui.QColor):
        self.color = None
        self.update()
    
    xǁSpoll_buttonǁsetColor__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁsetColor__mutmut_1': xǁSpoll_buttonǁsetColor__mutmut_1
    }
    xǁSpoll_buttonǁsetColor__mutmut_orig.__name__ = 'xǁSpoll_buttonǁsetColor'

    def setStatus(self, s: GateStatus):
        args = [s]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁsetStatus__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁsetStatus__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁsetStatus__mutmut_orig(self, s: GateStatus):
        self.status = s
        self.update()

    def xǁSpoll_buttonǁsetStatus__mutmut_1(self, s: GateStatus):
        self.status = None
        self.update()
    
    xǁSpoll_buttonǁsetStatus__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁsetStatus__mutmut_1': xǁSpoll_buttonǁsetStatus__mutmut_1
    }
    xǁSpoll_buttonǁsetStatus__mutmut_orig.__name__ = 'xǁSpoll_buttonǁsetStatus'

    def setGateId(self, i: int):
        args = [i]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁsetGateId__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁsetGateId__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁsetGateId__mutmut_orig(self, i: int):
        self.slot_id = i
        self.update()

    def xǁSpoll_buttonǁsetGateId__mutmut_1(self, i: int):
        self.slot_id = None
        self.update()
    
    xǁSpoll_buttonǁsetGateId__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁsetGateId__mutmut_1': xǁSpoll_buttonǁsetGateId__mutmut_1
    }
    xǁSpoll_buttonǁsetGateId__mutmut_orig.__name__ = 'xǁSpoll_buttonǁsetGateId'

    def setMaterial(self, mat: str):
        args = [mat]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁsetMaterial__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁsetMaterial__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁsetMaterial__mutmut_orig(self, mat: str):
        self.material = mat
        self.update()

    def xǁSpoll_buttonǁsetMaterial__mutmut_1(self, mat: str):
        self.material = None
        self.update()
    
    xǁSpoll_buttonǁsetMaterial__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁsetMaterial__mutmut_1': xǁSpoll_buttonǁsetMaterial__mutmut_1
    }
    xǁSpoll_buttonǁsetMaterial__mutmut_orig.__name__ = 'xǁSpoll_buttonǁsetMaterial'

    def setWeight(self, w: int):
        args = [w]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁsetWeight__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁsetWeight__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁsetWeight__mutmut_orig(self, w: int):
        self.weight = w
        self.update()

    def xǁSpoll_buttonǁsetWeight__mutmut_1(self, w: int):
        self.weight = None
        self.update()
    
    xǁSpoll_buttonǁsetWeight__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁsetWeight__mutmut_1': xǁSpoll_buttonǁsetWeight__mutmut_1
    }
    xǁSpoll_buttonǁsetWeight__mutmut_orig.__name__ = 'xǁSpoll_buttonǁsetWeight'

    def setTemp(self, t: int):
        args = [t]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁsetTemp__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁsetTemp__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁsetTemp__mutmut_orig(self, t: int):
        self.temp = t
        self.update()

    def xǁSpoll_buttonǁsetTemp__mutmut_1(self, t: int):
        self.temp = None
        self.update()
    
    xǁSpoll_buttonǁsetTemp__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁsetTemp__mutmut_1': xǁSpoll_buttonǁsetTemp__mutmut_1
    }
    xǁSpoll_buttonǁsetTemp__mutmut_orig.__name__ = 'xǁSpoll_buttonǁsetTemp'

    def update_entry(
        self, color: QtGui.QColor, slot_id: int, status: GateStatus, material: str
    ):
        args = [color, slot_id, status, material]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁupdate_entry__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁupdate_entry__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁupdate_entry__mutmut_orig(
        self, color: QtGui.QColor, slot_id: int, status: GateStatus, material: str
    ):
        self.setColor(color)
        self.setGateId(slot_id)
        self.setStatus(status)
        self.setMaterial(material)
        self.update()

    def xǁSpoll_buttonǁupdate_entry__mutmut_1(
        self, color: QtGui.QColor, slot_id: int, status: GateStatus, material: str
    ):
        self.setColor(None)
        self.setGateId(slot_id)
        self.setStatus(status)
        self.setMaterial(material)
        self.update()

    def xǁSpoll_buttonǁupdate_entry__mutmut_2(
        self, color: QtGui.QColor, slot_id: int, status: GateStatus, material: str
    ):
        self.setColor(color)
        self.setGateId(None)
        self.setStatus(status)
        self.setMaterial(material)
        self.update()

    def xǁSpoll_buttonǁupdate_entry__mutmut_3(
        self, color: QtGui.QColor, slot_id: int, status: GateStatus, material: str
    ):
        self.setColor(color)
        self.setGateId(slot_id)
        self.setStatus(None)
        self.setMaterial(material)
        self.update()

    def xǁSpoll_buttonǁupdate_entry__mutmut_4(
        self, color: QtGui.QColor, slot_id: int, status: GateStatus, material: str
    ):
        self.setColor(color)
        self.setGateId(slot_id)
        self.setStatus(status)
        self.setMaterial(None)
        self.update()
    
    xǁSpoll_buttonǁupdate_entry__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁupdate_entry__mutmut_1': xǁSpoll_buttonǁupdate_entry__mutmut_1, 
        'xǁSpoll_buttonǁupdate_entry__mutmut_2': xǁSpoll_buttonǁupdate_entry__mutmut_2, 
        'xǁSpoll_buttonǁupdate_entry__mutmut_3': xǁSpoll_buttonǁupdate_entry__mutmut_3, 
        'xǁSpoll_buttonǁupdate_entry__mutmut_4': xǁSpoll_buttonǁupdate_entry__mutmut_4
    }
    xǁSpoll_buttonǁupdate_entry__mutmut_orig.__name__ = 'xǁSpoll_buttonǁupdate_entry'

    def paintEvent(self, e: QtGui.QPaintEvent | None) -> None:
        args = [e]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoll_buttonǁpaintEvent__mutmut_orig'), object.__getattribute__(self, 'xǁSpoll_buttonǁpaintEvent__mutmut_mutants'), args, kwargs, self)

    def xǁSpoll_buttonǁpaintEvent__mutmut_orig(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_1(self, e: QtGui.QPaintEvent | None) -> None:
        opt = None
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_2(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(None)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_3(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = None
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_4(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(None)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_5(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(None)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_6(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(None)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_7(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(None)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_8(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            None, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_9(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, None, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_10(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, None, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_11(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, None
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_12(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_13(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_14(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_15(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_16(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = None
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_17(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(None)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_18(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = None

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_19(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(None, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_20(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, None, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_21(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, None)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_22(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_23(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_24(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, )

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_25(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(256, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_26(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 256, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_27(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 256)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_28(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = None
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_29(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(None)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_30(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(None)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_31(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(3)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_32(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(None)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_33(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = None
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_34(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(None)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_35(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(13)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_36(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(None)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_37(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(False)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_38(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(None)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_39(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = None
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_40(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(None)

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_41(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(None))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_42(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() * 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_43(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 10))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_44(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(None)

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_45(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(None))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_46(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() * 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_47(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 5))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_48(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(None)
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_49(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(None))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_50(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() + self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_51(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() / 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_52(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 2.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_53(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(None)

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_54(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(None))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_55(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            None,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_56(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            None,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_57(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            None,
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_58(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_59(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_60(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_61(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic & QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_62(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " - str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_63(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "XXGate XX" + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_64(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_65(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "GATE " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_66(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(None),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_67(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_68(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(None)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_69(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(131)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_70(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(None)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_71(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(131)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_72(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = None
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_73(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(None, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_74(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, None, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_75(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, None, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_76(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, None)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_77(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_78(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_79(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_80(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, )
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_81(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(2, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_82(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 2, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_83(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, +1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_84(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -2, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_85(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, +1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_86(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -2)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_87(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(None)
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_88(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(None))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_89(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() + rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_90(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() / 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_91(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 1.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_92(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(None, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_93(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, None)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_94(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_95(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, )

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_96(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = None
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_97(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(None)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_98(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(None)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_99(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(3)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_100(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(None)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_101(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = None
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_102(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(None, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_103(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, None, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_104(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, None, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_105(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, None)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_106(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_107(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_108(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_109(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, )
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_110(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(2, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_111(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 2, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_112(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, +1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_113(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -2, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_114(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, +1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_115(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -2)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_116(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(None)
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_117(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(None))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_118(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() + rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_119(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() / 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_120(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 1.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_121(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(None)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_122(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = None
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_123(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 66
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_124(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = None
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_125(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status not in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_126(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = None
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_127(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            None,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_128(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            None,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_129(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            None,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_130(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            None,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_131(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_132(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_133(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_134(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_135(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = None
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_136(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) / 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_137(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() + scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_138(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 3
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_139(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = None

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_140(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int(None)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_141(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) / 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_142(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() + scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_143(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 2.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_144(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = None
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_145(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(None)
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_146(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(None)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_147(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = None
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_148(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(None)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_149(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(None, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_150(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, None, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_151(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, None)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_152(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_153(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_154(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, )
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_155(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(1, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_156(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 1, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_157(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(None)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_158(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(None, white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_159(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), None)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_160(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_161(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), )
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_162(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(None, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_163(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, None, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_164(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, None)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_165(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_166(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_167(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, )

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_168(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = None
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_169(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(None)
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_170(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(None)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.end()

    def xǁSpoll_buttonǁpaintEvent__mutmut_171(self, e: QtGui.QPaintEvent | None) -> None:
        opt = QtWidgets.QStyleOption()
        opt.initFrom(self)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering)

        self.style().drawPrimitive(
            QtWidgets.QStyle.PrimitiveElement.PE_Widget, opt, painter, self
        )

        color = QtGui.QColor(self.color)
        white = QtGui.QColor(255, 255, 255)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)

        font = painter.font()
        font.setPointSize(12)
        font.setBold(True)
        painter.setFont(font)

        _text_rect = self.rect()
        _text_rect.setTop(int(self.rect().height() / 9))

        _text_rect.setBottom(int(self.rect().height() / 4))

        _text_rect.setLeft(int(self.rect().width() - self.rect().width() * 1.6))
        _text_rect.setRight(int(self.rect().width()))

        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            "Gate " + str(self.slot_id),
        )
        if not self.isChecked():
            white.setAlpha(130)
            color.setAlpha(130)

        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.fillRect(rect, color)

        pen = QtGui.QPen(white)
        pen.setWidth(2)
        painter.setPen(pen)
        rect = self.rect().adjusted(1, 1, -1, -1)
        rect.setY(int(rect.height() - rect.height() * 0.15))
        painter.drawRect(rect)

        # Draw icon centered
        icon_size = 65
        icon = (
            self._icon
            if self.status in [GateStatus.AVAILABLE, GateStatus.AVAILABLE_FROM_BUFFER]
            else self._unloaded_icon
        )
        scaled = icon.scaled(
            icon_size,
            icon_size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        x = (self.width() - scaled.width()) // 2
        y = int((self.height() - scaled.height()) // 1.1)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)
        p2 = QtGui.QPainter(tinted)
        p2.drawPixmap(0, 0, scaled)
        p2.setCompositionMode(QtGui.QPainter.CompositionMode.CompositionMode_SourceIn)
        p2.fillRect(tinted.rect(), white)
        p2.end()
        painter.drawPixmap(x, y, tinted)

        tinted = QtGui.QPixmap(scaled.size())
        tinted.fill(QtCore.Qt.GlobalColor.transparent)

        painter.setPen(None)
        painter.end()
    
    xǁSpoll_buttonǁpaintEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoll_buttonǁpaintEvent__mutmut_1': xǁSpoll_buttonǁpaintEvent__mutmut_1, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_2': xǁSpoll_buttonǁpaintEvent__mutmut_2, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_3': xǁSpoll_buttonǁpaintEvent__mutmut_3, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_4': xǁSpoll_buttonǁpaintEvent__mutmut_4, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_5': xǁSpoll_buttonǁpaintEvent__mutmut_5, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_6': xǁSpoll_buttonǁpaintEvent__mutmut_6, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_7': xǁSpoll_buttonǁpaintEvent__mutmut_7, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_8': xǁSpoll_buttonǁpaintEvent__mutmut_8, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_9': xǁSpoll_buttonǁpaintEvent__mutmut_9, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_10': xǁSpoll_buttonǁpaintEvent__mutmut_10, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_11': xǁSpoll_buttonǁpaintEvent__mutmut_11, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_12': xǁSpoll_buttonǁpaintEvent__mutmut_12, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_13': xǁSpoll_buttonǁpaintEvent__mutmut_13, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_14': xǁSpoll_buttonǁpaintEvent__mutmut_14, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_15': xǁSpoll_buttonǁpaintEvent__mutmut_15, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_16': xǁSpoll_buttonǁpaintEvent__mutmut_16, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_17': xǁSpoll_buttonǁpaintEvent__mutmut_17, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_18': xǁSpoll_buttonǁpaintEvent__mutmut_18, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_19': xǁSpoll_buttonǁpaintEvent__mutmut_19, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_20': xǁSpoll_buttonǁpaintEvent__mutmut_20, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_21': xǁSpoll_buttonǁpaintEvent__mutmut_21, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_22': xǁSpoll_buttonǁpaintEvent__mutmut_22, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_23': xǁSpoll_buttonǁpaintEvent__mutmut_23, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_24': xǁSpoll_buttonǁpaintEvent__mutmut_24, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_25': xǁSpoll_buttonǁpaintEvent__mutmut_25, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_26': xǁSpoll_buttonǁpaintEvent__mutmut_26, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_27': xǁSpoll_buttonǁpaintEvent__mutmut_27, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_28': xǁSpoll_buttonǁpaintEvent__mutmut_28, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_29': xǁSpoll_buttonǁpaintEvent__mutmut_29, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_30': xǁSpoll_buttonǁpaintEvent__mutmut_30, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_31': xǁSpoll_buttonǁpaintEvent__mutmut_31, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_32': xǁSpoll_buttonǁpaintEvent__mutmut_32, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_33': xǁSpoll_buttonǁpaintEvent__mutmut_33, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_34': xǁSpoll_buttonǁpaintEvent__mutmut_34, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_35': xǁSpoll_buttonǁpaintEvent__mutmut_35, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_36': xǁSpoll_buttonǁpaintEvent__mutmut_36, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_37': xǁSpoll_buttonǁpaintEvent__mutmut_37, 
        'xǁSpoll_buttonǁpaintEvent__mutmut
 
        'xǁSpoll_buttonǁpaintEvent__mutmut_138': xǁSpoll_buttonǁpaintEvent__mutmut_138, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_139': xǁSpoll_buttonǁpaintEvent__mutmut_139, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_140': xǁSpoll_buttonǁpaintEvent__mutmut_140, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_141': xǁSpoll_buttonǁpaintEvent__mutmut_141, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_142': xǁSpoll_buttonǁpaintEvent__mutmut_142, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_143': xǁSpoll_buttonǁpaintEvent__mutmut_143, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_144': xǁSpoll_buttonǁpaintEvent__mutmut_144, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_145': xǁSpoll_buttonǁpaintEvent__mutmut_145, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_146': xǁSpoll_buttonǁpaintEvent__mutmut_146, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_147': xǁSpoll_buttonǁpaintEvent__mutmut_147, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_148': xǁSpoll_buttonǁpaintEvent__mutmut_148, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_149': xǁSpoll_buttonǁpaintEvent__mutmut_149, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_150': xǁSpoll_buttonǁpaintEvent__mutmut_150, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_151': xǁSpoll_buttonǁpaintEvent__mutmut_151, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_152': xǁSpoll_buttonǁpaintEvent__mutmut_152, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_153': xǁSpoll_buttonǁpaintEvent__mutmut_153, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_154': xǁSpoll_buttonǁpaintEvent__mutmut_154, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_155': xǁSpoll_buttonǁpaintEvent__mutmut_155, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_156': xǁSpoll_buttonǁpaintEvent__mutmut_156, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_157': xǁSpoll_buttonǁpaintEvent__mutmut_157, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_158': xǁSpoll_buttonǁpaintEvent__mutmut_158, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_159': xǁSpoll_buttonǁpaintEvent__mutmut_159, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_160': xǁSpoll_buttonǁpaintEvent__mutmut_160, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_161': xǁSpoll_buttonǁpaintEvent__mutmut_161, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_162': xǁSpoll_buttonǁpaintEvent__mutmut_162, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_163': xǁSpoll_buttonǁpaintEvent__mutmut_163, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_164': xǁSpoll_buttonǁpaintEvent__mutmut_164, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_165': xǁSpoll_buttonǁpaintEvent__mutmut_165, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_166': xǁSpoll_buttonǁpaintEvent__mutmut_166, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_167': xǁSpoll_buttonǁpaintEvent__mutmut_167, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_168': xǁSpoll_buttonǁpaintEvent__mutmut_168, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_169': xǁSpoll_buttonǁpaintEvent__mutmut_169, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_170': xǁSpoll_buttonǁpaintEvent__mutmut_170, 
        'xǁSpoll_buttonǁpaintEvent__mutmut_171': xǁSpoll_buttonǁpaintEvent__mutmut_171
    }
    xǁSpoll_buttonǁpaintEvent__mutmut_orig.__name__ = 'xǁSpoll_buttonǁpaintEvent'


# ──────────────────────────────────────────────────────────────────────────────
# Carousel (scrollable spool row)
# ──────────────────────────────────────────────────────────────────────────────
class SpoolCarousel(QtWidgets.QWidget):
    selectionChanged = QtCore.pyqtSignal(int)  # emits selected slot index (0-based)

    VISIBLE = 4  # how many spools show at once

    def __init__(self, parent=None):
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁ__init____mutmut_orig(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(True)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_1(self, parent=None):
        super().__init__(None)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(True)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_2(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = None
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(True)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_3(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = None
        self.button_group.setExclusive(True)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_4(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(None)
        self.button_group.setExclusive(True)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_5(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(None)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_6(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(False)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_7(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(True)
        self._offset = None  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_8(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(True)
        self._offset = 1  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = None

        self._build_ui()

    def xǁSpoolCarouselǁ__init____mutmut_9(self, parent=None):
        super().__init__(parent)
        self.buttons: list[Spoll_button] = []
        self.button_group = QtWidgets.QButtonGroup(self)
        self.button_group.setExclusive(True)
        self._offset = 0  # first visible index
        # self.setStyleSheet("border: 1px solid white")

        self._anim_group: QtCore.QParallelAnimationGroup | None = ""

        self._build_ui()
    
    xǁSpoolCarouselǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁ__init____mutmut_1': xǁSpoolCarouselǁ__init____mutmut_1, 
        'xǁSpoolCarouselǁ__init____mutmut_2': xǁSpoolCarouselǁ__init____mutmut_2, 
        'xǁSpoolCarouselǁ__init____mutmut_3': xǁSpoolCarouselǁ__init____mutmut_3, 
        'xǁSpoolCarouselǁ__init____mutmut_4': xǁSpoolCarouselǁ__init____mutmut_4, 
        'xǁSpoolCarouselǁ__init____mutmut_5': xǁSpoolCarouselǁ__init____mutmut_5, 
        'xǁSpoolCarouselǁ__init____mutmut_6': xǁSpoolCarouselǁ__init____mutmut_6, 
        'xǁSpoolCarouselǁ__init____mutmut_7': xǁSpoolCarouselǁ__init____mutmut_7, 
        'xǁSpoolCarouselǁ__init____mutmut_8': xǁSpoolCarouselǁ__init____mutmut_8, 
        'xǁSpoolCarouselǁ__init____mutmut_9': xǁSpoolCarouselǁ__init____mutmut_9
    }
    xǁSpoolCarouselǁ__init____mutmut_orig.__name__ = 'xǁSpoolCarouselǁ__init__'

    def _build_ui(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁ_build_ui__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁ_build_ui__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁ_build_ui__mutmut_orig(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_1(self):
        root = None

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_2(self):
        root = QtWidgets.QHBoxLayout(None)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_3(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = None
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_4(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(None)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_5(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            None
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_6(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(None)
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_7(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap("XX:/arrow_icons/media/btn_icons/arrow_left.svgXX")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_8(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/ARROW_ICONS/MEDIA/BTN_ICONS/ARROW_LEFT.SVG")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_9(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = None
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_10(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(None)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_11(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            None
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_12(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(None)
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_13(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap("XX:/arrow_icons/media/btn_icons/arrow_right.svgXX")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_14(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/ARROW_ICONS/MEDIA/BTN_ICONS/ARROW_RIGHT.SVG")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_15(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(None)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_16(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(61)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_17(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(None)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_18(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(61)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_19(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(None)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_20(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(101)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_21(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(None)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_22(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(101)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_23(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(None)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_24(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(None)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_25(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = None
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_26(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(None)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_27(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = None
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_28(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(None)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_29(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(None, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_30(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, None, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_31(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, None, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_32(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, None)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_33(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_34(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_35(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_36(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, )

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_37(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(1, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_38(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 1, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_39(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 1, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_40(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 1)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_41(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(None)
        root.addWidget(self._slot_area)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_42(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(None)
        root.addWidget(self.right_arrow)

        self._update_arrows()

    def xǁSpoolCarouselǁ_build_ui__mutmut_43(self):
        root = QtWidgets.QHBoxLayout(self)

        self.left_arrow = IconButton(self)
        self.left_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_left.svg")
        )
        self.right_arrow = IconButton(self)
        self.right_arrow.setPixmap(
            QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
        )

        self.right_arrow.setFixedWidth(60)
        self.left_arrow.setFixedWidth(60)

        self.left_arrow.setFixedHeight(100)
        self.right_arrow.setFixedHeight(100)

        # sizePolicy = QtWidgets.QSizePolicy(QtWidgets.QSizePolicy.Policy.Expanding, QtWidgets.QSizePolicy.Policy.Expanding)
        # self.right_arrow.setSizePolicy(sizePolicy)
        # self.left_arrow.setSizePolicy(sizePolicy)

        self.left_arrow.clicked.connect(self._scroll_left)
        self.right_arrow.clicked.connect(self._scroll_right)

        self._slot_area = QtWidgets.QWidget(self)
        self._slot_layout = QtWidgets.QHBoxLayout(self._slot_area)
        # on self._slot_layout.setSpacing(0)
        self._slot_layout.setContentsMargins(0, 0, 0, 0)

        root.addWidget(self.left_arrow)
        root.addWidget(self._slot_area)
        root.addWidget(None)

        self._update_arrows()
    
    xǁSpoolCarouselǁ_build_ui__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSp

        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        args = [color, slot_id, material, temp, status]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁaddSpool__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁaddSpool__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁaddSpool__mutmut_orig(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_1(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "XXPLAXX",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_2(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "pla",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_3(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 1000,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_4(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(None):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_5(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id != slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_6(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(None, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_7(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, None, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_8(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, None, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_9(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, None)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_10(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_11(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_12(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_13(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, )
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_14(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(None)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_15(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = None
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_16(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(None)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_17(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(None)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_18(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(None)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_19(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(None)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_20(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(None)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_21(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            None,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_22(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            None,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_23(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_24(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_25(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(None, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_26(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, None)
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_27(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_28(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, )
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_29(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(None)
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_30(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: None)
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_31(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(None))
        self.buttons.append(btn)
        self._refresh_visible()

    def xǁSpoolCarouselǁaddSpool__mutmut_32(
        self,
        color: QtGui.QColor,
        slot_id: int,
        material: str = "PLA",
        temp: int = 999,
        status: GateStatus = GateStatus.UNKNOWN,
    ):
        if any(btn.slot_id == slot_id for btn in self.buttons):
            self.buttons[int(slot_id)].update_entry(color, slot_id, status, material)
            self.update()
            return
        btn = Spoll_button()
        btn.setColor(color)
        btn.setGateId(slot_id)
        btn.setStatus(status)
        btn.setMaterial(material)
        btn.setTemp(temp)
        btn.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Preferred,
        )
        self.button_group.addButton(btn, len(self.buttons))
        btn.clicked.connect(lambda checked, b=btn: self._on_btn_clicked(b))
        self.buttons.append(None)
        self._refresh_visible()
    
    xǁSpoolCarouselǁaddSpool__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁaddSpool__mutmut_1': xǁSpoolCarouselǁaddSpool__mutmut_1, 
        'xǁSpoolCarouselǁaddSpool__mutmut_2': xǁSpoolCarouselǁaddSpool__mutmut_2, 
        'xǁSpoolCarouselǁaddSpool__mutmut_3': xǁSpoolCarouselǁaddSpool__mutmut_3, 
        'xǁSpoolCarouselǁaddSpool__mutmut_4': xǁSpoolCarouselǁaddSpool__mutmut_4, 
        'xǁSpoolCarouselǁaddSpool__mutmut_5': xǁSpoolCarouselǁaddSpool__mutmut_5, 
        'xǁSpoolCarouselǁaddSpool__mutmut_6': xǁSpoolCarouselǁaddSpool__mutmut_6, 
        'xǁSpoolCarouselǁaddSpool__mutmut_7': xǁSpoolCarouselǁaddSpool__mutmut_7, 
        'xǁSpoolCarouselǁaddSpool__mutmut_8': xǁSpoolCarouselǁaddSpool__mutmut_8, 
        'xǁSpoolCarouselǁaddSpool__mutmut_9': xǁSpoolCarouselǁaddSpool__mutmut_9, 
        'xǁSpoolCarouselǁaddSpool__mutmut_10': xǁSpoolCarouselǁaddSpool__mutmut_10, 
        'xǁSpoolCarouselǁaddSpool__mutmut_11': xǁSpoolCarouselǁaddSpool__mutmut_11, 
        'xǁSpoolCarouselǁaddSpool__mutmut_12': xǁSpoolCarouselǁaddSpool__mutmut_12, 
        'xǁSpoolCarouselǁaddSpool__mutmut_13': xǁSpoolCarouselǁaddSpool__mutmut_13, 
        'xǁSpoolCarouselǁaddSpool__mutmut_14': xǁSpoolCarouselǁaddSpool__mutmut_14, 
        'xǁSpoolCarouselǁaddSpool__mutmut_15': xǁSpoolCarouselǁaddSpool__mutmut_15, 
        'xǁSpoolCarouselǁaddSpool__mutmut_16': xǁSpoolCarouselǁaddSpool__mutmut_16, 
        'xǁSpoolCarouselǁaddSpool__mutmut_17': xǁSpoolCarouselǁaddSpool__mutmut_17, 
        'xǁSpoolCarouselǁaddSpool__mutmut_18': xǁSpoolCarouselǁaddSpool__mutmut_18, 
        'xǁSpoolCarouselǁaddSpool__mutmut_19': xǁSpoolCarouselǁaddSpool__mutmut_19, 
        'xǁSpoolCarouselǁaddSpool__mutmut_20': xǁSpoolCarouselǁaddSpool__mutmut_20, 
        'xǁSpoolCarouselǁaddSpool__mutmut_21': xǁSpoolCarouselǁaddSpool__mutmut_21, 
        'xǁSpoolCarouselǁaddSpool__mutmut_22': xǁSpoolCarouselǁaddSpool__mutmut_22, 
        'xǁSpoolCarouselǁaddSpool__mutmut_23': xǁSpoolCarouselǁaddSpool__mutmut_23, 
        'xǁSpoolCarouselǁaddSpool__mutmut_24': xǁSpoolCarouselǁaddSpool__mutmut_24, 
        'xǁSpoolCarouselǁaddSpool__mutmut_25': xǁSpoolCarouselǁaddSpool__mutmut_25, 
        'xǁSpoolCarouselǁaddSpool__mutmut_26': xǁSpoolCarouselǁaddSpool__mutmut_26, 
        'xǁSpoolCarouselǁaddSpool__mutmut_27': xǁSpoolCarouselǁaddSpool__mutmut_27, 
        'xǁSpoolCarouselǁaddSpool__mutmut_28': xǁSpoolCarouselǁaddSpool__mutmut_28, 
        'xǁSpoolCarouselǁaddSpool__mutmut_29': xǁSpoolCarouselǁaddSpool__mutmut_29, 
        'xǁSpoolCarouselǁaddSpool__mutmut_30': xǁSpoolCarouselǁaddSpool__mutmut_30, 
        'xǁSpoolCarouselǁaddSpool__mutmut_31': xǁSpoolCarouselǁaddSpool__mutmut_31, 
        'xǁSpoolCarouselǁaddSpool__mutmut_32': xǁSpoolCarouselǁaddSpool__mutmut_32
    }
    xǁSpoolCarouselǁaddSpool__mutmut_orig.__name__ = 'xǁSpoolCarouselǁaddSpool'

    def _on_btn_clicked(self, btn: Spoll_button):
        args = [btn]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁ_on_btn_clicked__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁ_on_btn_clicked__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁ_on_btn_clicked__mutmut_orig(self, btn: Spoll_button):
        idx = self.buttons.index(btn)
        self.selectionChanged.emit(idx)

    def xǁSpoolCarouselǁ_on_btn_clicked__mutmut_1(self, btn: Spoll_button):
        idx = None
        self.selectionChanged.emit(idx)

    def xǁSpoolCarouselǁ_on_btn_clicked__mutmut_2(self, btn: Spoll_button):
        idx = self.buttons.index(None)
        self.selectionChanged.emit(idx)

    def xǁSpoolCarouselǁ_on_btn_clicked__mutmut_3(self, btn: Spoll_button):
        idx = self.buttons.rindex(btn)
        self.selectionChanged.emit(idx)

    def xǁSpoolCarouselǁ_on_btn_clicked__mutmut_4(self, btn: Spoll_button):
        idx = self.buttons.index(btn)
        self.selectionChanged.emit(None)
    
    xǁSpoolCarouselǁ_on_btn_clicked__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁ_on_btn_clicked__mutmut_1': xǁSpoolCarouselǁ_on_btn_clicked__mutmut_1, 
        'xǁSpoolCarouselǁ_on_btn_clicked__mutmut_2': xǁSpoolCarouselǁ_on_btn_clicked__mutmut_2, 
        'xǁSpoolCarouselǁ_on_btn_clicked__mutmut_3': xǁSpoolCarouselǁ_on_btn_clicked__mutmut_3, 
        'xǁSpoolCarouselǁ_on_btn_clicked__mutmut_4': xǁSpoolCarouselǁ_on_btn_clicked__mutmut_4
    }
    xǁSpoolCarouselǁ_on_btn_clicked__mutmut_orig.__name__ = 'xǁSpoolCarouselǁ_on_btn_clicked'

    def _refresh_visible(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁ_refresh_visible__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁ_refresh_visible__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_orig(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_1(self):
        # Clear layout
        while self._slot_layout.count():
            item = None
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_2(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(None)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_3(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(1)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_4(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = None
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_5(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(None, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_6(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, None)
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_7(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_8(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, )
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_9(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset - self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_10(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(None)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_11(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                None,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_12(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                None,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_13(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_14(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                )
            btn.show()

        if len(self.button_group.buttons()) <= 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_15(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) < 4:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()

    def xǁSpoolCarouselǁ_refresh_visible__mutmut_16(self):
        # Clear layout
        while self._slot_layout.count():
            item = self._slot_layout.takeAt(0)
            if item.widget():
                item.widget().setParent(None)

        end = min(self._offset + self.VISIBLE, len(self.buttons))
        for btn in self.buttons[self._offset : end]:
            self._slot_layout.addWidget(btn)
            btn.setSizePolicy(
                QtWidgets.QSizePolicy.Policy.Expanding,
                QtWidgets.QSizePolicy.Policy.Preferred,
            )
            btn.show()

        if len(self.button_group.buttons()) <= 5:
            self.left_arrow.hide()
            self.right_arrow.hide()
        else:
            self.left_arrow.show()
            self.right_arrow.show()

        self._update_arrows()
    
    xǁSpoolCarouselǁ_refresh_visible__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁ_refresh_visible__mutmut_1': xǁSpoolCarouselǁ_refresh_visible__mutmut_1, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_2': xǁSpoolCarouselǁ_refresh_visible__mutmut_2, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_3': xǁSpoolCarouselǁ_refresh_visible__mutmut_3, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_4': xǁSpoolCarouselǁ_refresh_visible__mutmut_4, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_5': xǁSpoolCarouselǁ_refresh_visible__mutmut_5, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_6': xǁSpoolCarouselǁ_refresh_visible__mutmut_6, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_7': xǁSpoolCarouselǁ_refresh_visible__mutmut_7, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_8': xǁSpoolCarouselǁ_refresh_visible__mutmut_8, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_9': xǁSpoolCarouselǁ_refresh_visible__mutmut_9, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_10': xǁSpoolCarouselǁ_refresh_visible__mutmut_10, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_11': xǁSpoolCarouselǁ_refresh_visible__mutmut_11, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_12': xǁSpoolCarouselǁ_refresh_visible__mutmut_12, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_13': xǁSpoolCarouselǁ_refresh_visible__mutmut_13, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_14': xǁSpoolCarouselǁ_refresh_visible__mutmut_14, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_15': xǁSpoolCarouselǁ_refresh_visible__mutmut_15, 
        'xǁSpoolCarouselǁ_refresh_visible__mutmut_16': xǁSpoolCarouselǁ_refresh_visible__mutmut_16
    }
    xǁSpoolCarouselǁ_refresh_visible__mutmut_orig.__name__ = 'xǁSpoolCarouselǁ_refresh_visible'

    def _scroll_left(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁ_scroll_left__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁ_scroll_left__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁ_scroll_left__mutmut_orig(self):
        if self._offset > 0:
            self._offset -= 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_left__mutmut_1(self):
        if self._offset >= 0:
            self._offset -= 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_left__mutmut_2(self):
        if self._offset > 1:
            self._offset -= 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_left__mutmut_3(self):
        if self._offset > 0:
            self._offset = 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_left__mutmut_4(self):
        if self._offset > 0:
            self._offset += 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_left__mutmut_5(self):
        if self._offset > 0:
            self._offset -= 2
            self._refresh_visible()
    
    xǁSpoolCarouselǁ_scroll_left__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁ_scroll_left__mutmut_1': xǁSpoolCarouselǁ_scroll_left__mutmut_1, 
        'xǁSpoolCarouselǁ_scroll_left__mutmut_2': xǁSpoolCarouselǁ_scroll_left__mutmut_2, 
        'xǁSpoolCarouselǁ_scroll_left__mutmut_3': xǁSpoolCarouselǁ_scroll_left__mutmut_3, 
        'xǁSpoolCarouselǁ_scroll_left__mutmut_4': xǁSpoolCarouselǁ_scroll_left__mutmut_4, 
        'xǁSpoolCarouselǁ_scroll_left__mutmut_5': xǁSpoolCarouselǁ_scroll_left__mutmut_5
    }
    xǁSpoolCarouselǁ_scroll_left__mutmut_orig.__name__ = 'xǁSpoolCarouselǁ_scroll_left'

    def _scroll_right(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁ_scroll_right__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁ_scroll_right__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁ_scroll_right__mutmut_orig(self):
        if self._offset + self.VISIBLE < len(self.buttons):
            self._offset += 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_right__mutmut_1(self):
        if self._offset - self.VISIBLE < len(self.buttons):
            self._offset += 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_right__mutmut_2(self):
        if self._offset + self.VISIBLE <= len(self.buttons):
            self._offset += 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_right__mutmut_3(self):
        if self._offset + self.VISIBLE < len(self.buttons):
            self._offset = 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_right__mutmut_4(self):
        if self._offset + self.VISIBLE < len(self.buttons):
            self._offset -= 1
            self._refresh_visible()

    def xǁSpoolCarouselǁ_scroll_right__mutmut_5(self):
        if self._offset + self.VISIBLE < len(self.buttons):
            self._offset += 2
            self._refresh_visible()
    
    xǁSpoolCarouselǁ_scroll_right__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁ_scroll_right__mutmut_1': xǁSpoolCarouselǁ_scroll_right__mutmut_1, 
        'xǁSpoolCarouselǁ_scroll_right__mutmut_2': xǁSpoolCarouselǁ_scroll_right__mutmut_2, 
        'xǁSpoolCarouselǁ_scroll_right__mutmut_3': xǁSpoolCarouselǁ_scroll_right__mutmut_3, 
        'xǁSpoolCarouselǁ_scroll_right__mutmut_4': xǁSpoolCarouselǁ_scroll_right__mutmut_4, 
        'xǁSpoolCarouselǁ_scroll_right__mutmut_5': xǁSpoolCarouselǁ_scroll_right__mutmut_5
    }
    xǁSpoolCarouselǁ_scroll_right__mutmut_orig.__name__ = 'xǁSpoolCarouselǁ_scroll_right'

    def _update_arrows(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁ_update_arrows__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁ_update_arrows__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁ_update_arrows__mutmut_orig(self):
        self.left_arrow.setEnabled(self._offset > 0)
        self.right_arrow.setEnabled(self._offset + self.VISIBLE < len(self.buttons))

    def xǁSpoolCarouselǁ_update_arrows__mutmut_1(self):
        self.left_arrow.setEnabled(None)
        self.right_arrow.setEnabled(self._offset + self.VISIBLE < len(self.buttons))

    def xǁSpoolCarouselǁ_update_arrows__mutmut_2(self):
        self.left_arrow.setEnabled(self._offset >= 0)
        self.right_arrow.setEnabled(self._offset + self.VISIBLE < len(self.buttons))

    def xǁSpoolCarouselǁ_update_arrows__mutmut_3(self):
        self.left_arrow.setEnabled(self._offset > 1)
        self.right_arrow.setEnabled(self._offset + self.VISIBLE < len(self.buttons))

    def xǁSpoolCarouselǁ_update_arrows__mutmut_4(self):
        self.left_arrow.setEnabled(self._offset > 0)
        self.right_arrow.setEnabled(None)

    def xǁSpoolCarouselǁ_update_arrows__mutmut_5(self):
        self.left_arrow.setEnabled(self._offset > 0)
        self.right_arrow.setEnabled(self._offset - self.VISIBLE < len(self.buttons))

    def xǁSpoolCarouselǁ_update_arrows__mutmut_6(self):
        self.left_arrow.setEnabled(self._offset > 0)
        self.right_arrow.setEnabled(self._offset + self.VISIBLE <= len(self.buttons))
    
    xǁSpoolCarouselǁ_update_arrows__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁ_update_arrows__mutmut_1': xǁSpoolCarouselǁ_update_arrows__mutmut_1, 
        'xǁSpoolCarouselǁ_update_arrows__mutmut_2': xǁSpoolCarouselǁ_update_arrows__mutmut_2, 
        'xǁSpoolCarouselǁ_update_arrows__mutmut_3': xǁSpoolCarouselǁ_update_arrows__mutmut_3, 
        'xǁSpoolCarouselǁ_update_arrows__mutmut_4': xǁSpoolCarouselǁ_update_arrows__mutmut_4, 
        'xǁSpoolCarouselǁ_update_arrows__mutmut_5': xǁSpoolCarouselǁ_update_arrows__mutmut_5, 
        'xǁSpoolCarouselǁ_update_arrows__mutmut_6': xǁSpoolCarouselǁ_update_arrows__mutmut_6
    }
    xǁSpoolCarouselǁ_update_arrows__mutmut_orig.__name__ = 'xǁSpoolCarouselǁ_update_arrows'

    def selectedIndex(self) -> int:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁselectedIndex__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁselectedIndex__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁselectedIndex__mutmut_orig(self) -> int:
        btn = self.button_group.checkedButton()
        if btn:
            return self.buttons.index(btn)
        return -1

    def xǁSpoolCarouselǁselectedIndex__mutmut_1(self) -> int:
        btn = None
        if btn:
            return self.buttons.index(btn)
        return -1

    def xǁSpoolCarouselǁselectedIndex__mutmut_2(self) -> int:
        btn = self.button_group.checkedButton()
        if btn:
            return self.buttons.index(None)
        return -1

    def xǁSpoolCarouselǁselectedIndex__mutmut_3(self) -> int:
        btn = self.button_group.checkedButton()
        if btn:
            return self.buttons.rindex(btn)
        return -1

    def xǁSpoolCarouselǁselectedIndex__mutmut_4(self) -> int:
        btn = self.button_group.checkedButton()
        if btn:
            return self.buttons.index(btn)
        return +1

    def xǁSpoolCarouselǁselectedIndex__mutmut_5(self) -> int:
        btn = self.button_group.checkedButton()
        if btn:
            return self.buttons.index(btn)
        return -2
    
    xǁSpoolCarouselǁselectedIndex__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁselectedIndex__mutmut_1': xǁSpoolCarouselǁselectedIndex__mutmut_1, 
        'xǁSpoolCarouselǁselectedIndex__mutmut_2': xǁSpoolCarouselǁselectedIndex__mutmut_2, 
        'xǁSpoolCarouselǁselectedIndex__mutmut_3': xǁSpoolCarouselǁselectedIndex__mutmut_3, 
        'xǁSpoolCarouselǁselectedIndex__mutmut_4': xǁSpoolCarouselǁselectedIndex__mutmut_4, 
        'xǁSpoolCarouselǁselectedIndex__mutmut_5': xǁSpoolCarouselǁselectedIndex__mutmut_5
    }
    xǁSpoolCarouselǁselectedIndex__mutmut_orig.__name__ = 'xǁSpoolCarouselǁselectedIndex'

    def selectIndex(self, idx: int):
        args = [idx]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolCarouselǁselectIndex__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolCarouselǁselectIndex__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolCarouselǁselectIndex__mutmut_orig(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_1(self, idx: int):
        if 1 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_2(self, idx: int):
        if 0 < idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_3(self, idx: int):
        if 0 <= idx <= len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_4(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(None)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_5(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(False)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_6(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx <= self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_7(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = None
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_8(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx > self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_9(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset - self.VISIBLE:
                self._offset = idx - self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_10(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = None
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_11(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE - 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_12(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx + self.VISIBLE + 1
                self._refresh_visible()

    def xǁSpoolCarouselǁselectIndex__mutmut_13(self, idx: int):
        if 0 <= idx < len(self.buttons):
            self.buttons[idx].setChecked(True)
            # scroll so it's visible
            if idx < self._offset:
                self._offset = idx
                self._refresh_visible()
            elif idx >= self._offset + self.VISIBLE:
                self._offset = idx - self.VISIBLE + 2
                self._refresh_visible()
    
    xǁSpoolCarouselǁselectIndex__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolCarouselǁselectIndex__mutmut_1': xǁSpoolCarouselǁselectIndex__mutmut_1, 
        'xǁSpoolCarouselǁselectIndex__mutmut_2': xǁSpoolCarouselǁselectIndex__mutmut_2, 
        'xǁSpoolCarouselǁselectIndex__mutmut_3': xǁSpoolCarouselǁselectIndex__mutmut_3, 
        'xǁSpoolCarouselǁselectIndex__mutmut_4': xǁSpoolCarouselǁselectIndex__mutmut_4, 
        'xǁSpoolCarouselǁselectIndex__mutmut_5': xǁSpoolCarouselǁselectIndex__mutmut_5, 
        'xǁSpoolCarouselǁselectIndex__mutmut_6': xǁSpoolCarouselǁselectIndex__mutmut_6, 
        'xǁSpoolCarouselǁselectIndex__mutmut_7': xǁSpoolCarouselǁselectIndex__mutmut_7, 
        'xǁSpoolCarouselǁselectIndex__mutmut_8': xǁSpoolCarouselǁselectIndex__mutmut_8, 
        'xǁSpoolCarouselǁselectIndex__mutmut_9': xǁSpoolCarouselǁselectIndex__mutmut_9, 
        'xǁSpoolCarouselǁselectIndex__mutmut_10': xǁSpoolCarouselǁselectIndex__mutmut_10, 
        'xǁSpoolCarouselǁselectIndex__mutmut_11': xǁSpoolCarouselǁselectIndex__mutmut_11, 
        'xǁSpoolCarouselǁselectIndex__mutmut_12': xǁSpoolCarouselǁselectIndex__mutmut_12, 
        'xǁSpoolCarouselǁselectIndex__mutmut_13': xǁSpoolCarouselǁselectIndex__mutmut_13
    }
    xǁSpoolCarouselǁselectIndex__mutmut_orig.__name__ = 'xǁSpoolCarouselǁselectIndex'


# ──────────────────────────────────────────────────────────────────────────────
# Info table panel (BlocksCustomFrame + detail grid + op buttons)
# ──────────────────────────────────────────────────────────────────────────────
class SpoolInfoPanel(QtWidgets.QWidget):
    loadRequested = QtCore.pyqtSignal()
    unloadRequested = QtCore.pyqtSignal()
    ejectRequested = QtCore.pyqtSignal()
    checkRequested = QtCore.pyqtSignal()

    request_keypad = QtCore.pyqtSignal(
        "PyQt_PyObject", str, str, str, int, name="request-keyboard"
    )  # value, slot index, caller widget

    def __init__(self, amu_manager, parent=None):
        args = [amu_manager, parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolInfoPanelǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁSpoolInfoPanelǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁSpoolInfoPanelǁ__init____mutmut_orig(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = -1
        self.FStatus = "Unknown"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_1(self, amu_manager, parent=None):
        super().__init__(None)
        self.amu_manager = amu_manager
        self._slot_index = -1
        self.FStatus = "Unknown"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_2(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = None
        self._slot_index = -1
        self.FStatus = "Unknown"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_3(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = None
        self.FStatus = "Unknown"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_4(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = +1
        self.FStatus = "Unknown"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_5(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = -2
        self.FStatus = "Unknown"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_6(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = -1
        self.FStatus = None
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_7(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = -1
        self.FStatus = "XXUnknownXX"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_8(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = -1
        self.FStatus = "unknown"
        self._build_ui()

    def xǁSpoolInfoPanelǁ__init____mutmut_9(self, amu_manager, parent=None):
        super().__init__(parent)
        self.amu_manager = amu_manager
        self._slot_index = -1
        self.FStatus = "UNKNOWN"
        self._build_ui()
    
    xǁSpoolInfoPanelǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolInfoPanelǁ__init____mutmut_1': xǁSpoolInfoPanelǁ__init____mutmut_1, 
        'xǁSpoolInfoPanelǁ__init____mutmut_2': xǁSpoolInfoPanelǁ__init____mutmut_2, 
        'xǁSpoolInfoPanelǁ__init____mutmut_3': xǁSpoolInfoPanelǁ__init____mutmut_3, 
        'xǁSpoolInfoPanelǁ__init____mutmut_4': xǁSpoolInfoPanelǁ__init____mutmut_4, 
        'xǁSpoolInfoPanelǁ__init____mutmut_5': xǁSpoolInfoPanelǁ__init____mutmut_5, 
        'xǁSpoolInfoPanelǁ__init____mutmut_6': xǁSpoolInfoPanelǁ__init____mutmut_6, 
        'xǁSpoolInfoPanelǁ__init____mutmut_7': xǁSpoolInfoPanelǁ__init____mutmut_7, 
        'xǁSpoolInfoPanelǁ__init____mutmut_8': xǁSpoolInfoPanelǁ__init____mutmut_8, 
        'xǁSpoolInfoPanelǁ__init____mutmut_9': xǁSpoolInfoPanelǁ__init____mutmut_9
    }
    xǁSpoolInfoPanelǁ__init____mutmut_orig.__name__ = 'xǁSpoolInfoPanelǁ__init__'

    def _build_ui(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolInfoPanelǁ_build_ui__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolInfoPanelǁ_build_ui__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_orig(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_1(self):
        root = None
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_2(self):
        root = QtWidgets.QHBoxLayout(None)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_3(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(None, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_4(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, None, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_5(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, None, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_6(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, None)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_7(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_8(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_9(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_10(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, )
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_11(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(15, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_12(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 13, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_13(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 15, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_14(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 13)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_15(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(None)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_16(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(21)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_17(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = None
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_18(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(None, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_19(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, None)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_20(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_21(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, )
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_22(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(53, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_23(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 53)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_24(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet(None)
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_25(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("XXborder-radius: 2px; background: #222;XX")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_26(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("BORDER-RADIUS: 2PX; BACKGROUND: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_27(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(None, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_28(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, None, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_29(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, None)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_30(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_31(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_32(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, )

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_33(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 1, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_34(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = None
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_35(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = None
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_36(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(None)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_37(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(None, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_38(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, None, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_39(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, None, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_40(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, None)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_41(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_42(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_43(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_44(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, )
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_45(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(1, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_46(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 1, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_47(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 1, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_48(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 1)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_49(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(None)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_50(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(13)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_51(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(None)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_52(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(3)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_53(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = None
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_54(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(None)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_55(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(13)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_56(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = None
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_57(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(None)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_58(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet(None)
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_59(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("XXcolor: rgba(255,255,255,100);XX")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_60(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("COLOR: RGBA(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_61(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(None)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_62(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="XX—XX", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_63(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = False, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_64(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "XXkeypadXX"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_65(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "KEYPAD"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_66(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = None
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_67(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(None)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_68(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(None)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_69(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(None)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_70(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = None
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_71(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(None)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_72(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet(None)
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_73(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("XXcolor: rgb(255,255,255);XX")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_74(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("COLOR: RGB(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_75(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(None)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_76(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = None
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_77(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=None)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_78(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=True)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_79(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = None
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_80(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = None
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_81(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = None
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_82(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = None

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_83(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            None
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_84(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: None
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_85(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                None, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_86(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, None, "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_87(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", None, "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_88(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", None, 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_89(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", None
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_90(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_91(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_92(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_93(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_94(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_95(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["XXPyQt_PyObjectXX", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_96(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["pyqt_pyobject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_97(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PYQT_PYOBJECT", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_98(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "XX#XX", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_99(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "XXXX", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_100(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "XXXX", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_101(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 7
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_102(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            None
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_103(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: None
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_104(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                None, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_105(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, None, "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_106(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", None, "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_107(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", None, 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_108(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", None
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_109(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_110(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_111(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_112(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_113(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_114(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["XXPyQt_PyObjectXX", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_115(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["pyqt_pyobject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_116(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PYQT_PYOBJECT", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_117(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "XXXX", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_118(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "XXXX", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_119(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "XXXX", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_120(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 1
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_121(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            None
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_122(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: None
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_123(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                None, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_124(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, None, "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_125(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", None, "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_126(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", None, 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_127(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", None
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_128(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_129(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_130(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_131(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_132(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_133(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["XXPyQt_PyObjectXX", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_134(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["pyqt_pyobject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_135(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PYQT_PYOBJECT", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_136(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "XXgXX", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_137(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "G", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_138(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "XXXX", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_139(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "XXXX", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_140(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 1
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_141(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            None
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_142(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: None
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_143(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                None, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_144(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, None, "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_145(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", None, "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_146(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", None, 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_147(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", None
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_148(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_149(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_150(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_151(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_152(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_153(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["XXPyQt_PyObjectXX", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_154(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["pyqt_pyobject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_155(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PYQT_PYOBJECT", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_156(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "XXXX", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_157(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "XXºXX", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_158(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "XXintXX", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_159(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "INT", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_160(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 4
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_161(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = None
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_162(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("XXStatusXX", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_163(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_164(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("STATUS", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_165(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("XXTemperatureXX", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_166(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_167(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("TEMPERATURE", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_168(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "XXnumpadXX"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_169(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "NUMPAD"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_170(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("XXColorXX", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_171(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_172(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("COLOR", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_173(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "XXkeypadXX"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_174(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "KEYPAD"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_175(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("XXMaterialXX", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_176(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_177(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("MATERIAL", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_178(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "XXkeypadXX"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_179(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "KEYPAD"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_180(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("XXWeightXX", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_181(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_182(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("WEIGHT", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_183(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "XXnumpadXX"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_184(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "NUMPAD"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_185(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(None):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_186(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(None, i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_187(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), None, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_188(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, None, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_189(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, None)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_190(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_191(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_192(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_193(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, )
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_194(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(None), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_195(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 1, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_196(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(None, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_197(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, None, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_198(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, None)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_199(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_200(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_201(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, )

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_202(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 2)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_203(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(None, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_204(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, None)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_205(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_206(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, )

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_207(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 2)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_208(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = None
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_209(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(None)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_210(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet(None)
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_211(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("XXcolor: white;XX")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_212(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("COLOR: WHITE;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_213(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(None)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_214(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = None
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_215(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(None)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_216(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(7)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_217(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = None
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_218(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(None)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_219(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(13)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_220(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = None
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_221(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(None)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_222(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText(None)
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_223(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("XXLoadXX")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_224(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_225(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("LOAD")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_226(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(None, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_227(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, None)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_228(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_229(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, )
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_230(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(141, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_231(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 61)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_232(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(None)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_233(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            None
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_234(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(None)
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_235(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap("XX:/filament_related/media/btn_icons/load_filament.svgXX")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_236(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/FILAMENT_RELATED/MEDIA/BTN_ICONS/LOAD_FILAMENT.SVG")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_237(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = None
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_238(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(None)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_239(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText(None)
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_240(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("XXUnloadXX")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_241(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_242(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("UNLOAD")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_243(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(None)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_244(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            None
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_245(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(None)
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_246(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap("XX:/filament_related/media/btn_icons/unload_filament.svgXX")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_247(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/FILAMENT_RELATED/MEDIA/BTN_ICONS/UNLOAD_FILAMENT.SVG")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_248(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(None, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_249(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, None)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_250(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_251(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, )
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_252(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(141, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_253(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 61)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_254(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = None
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_255(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(None)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_256(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText(None)
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_257(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("XXEjectXX")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_258(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_259(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("EJECT")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_260(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(None)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_261(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            None
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_262(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(None)
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_263(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap("XX:/filament_related/media/btn_icons/eject.svgXX")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_264(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/FILAMENT_RELATED/MEDIA/BTN_ICONS/EJECT.SVG")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_265(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(None, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_266(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, None)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_267(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_268(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, )
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_269(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(141, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_270(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 61)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_271(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = None
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_272(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(None)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_273(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(None)
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_274(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(None))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_275(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap("XX:/load_icons/media/btn_icons/cut.svgXX"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_276(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/LOAD_ICONS/MEDIA/BTN_ICONS/CUT.SVG"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_277(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText(None)
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_278(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("XXCheck\nGatesXX")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_279(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("check\ngates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_280(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("CHECK\nGATES")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_281(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(None)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_282(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(None, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_283(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, None)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_284(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_285(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, )

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_286(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(141, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_287(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 61)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_288(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(None)
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_289(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: None)
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_290(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(None)
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_291(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: None)
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_292(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(None)
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_293(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: None)
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_294(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(None)

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_295(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: None)

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_296(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = None
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_297(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(None)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_298(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(7)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_299(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(None)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_300(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(None)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_301(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = None
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_302(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(None)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_303(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(7)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_304(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(None)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_305(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(None)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_306(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(None)
        btn_col.addLayout(bot_row)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_307(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(None)
        root.addLayout(btn_col)

    def xǁSpoolInfoPanelǁ_build_ui__mutmut_308(self):
        root = QtWidgets.QHBoxLayout(self)
        root.setContentsMargins(14, 12, 14, 12)
        root.setSpacing(20)

        # ── Color swatch ──
        self._swatch = QtWidgets.QLabel()
        self._swatch.setFixedSize(52, 52)
        self._swatch.setStyleSheet("border-radius: 2px; background: #222;")
        root.addWidget(self._swatch, 0, QtCore.Qt.AlignmentFlag.AlignVCenter)

        # ── Info grid ──
        grid_widget = QtWidgets.QWidget()
        grid = QtWidgets.QGridLayout(grid_widget)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(12)
        grid.setVerticalSpacing(2)

        font = QtGui.QFont()
        font.setPointSize(12)

        def make_key(text):
            l = QtWidgets.QLabel(text)
            l.setStyleSheet("color: rgba(255,255,255,100);")
            l.setFont(font)
            return l

        def make_val(text="—", edit: bool = True, type: str = "keypad"):
            """Make either an editable line edit or a static label, depending on the *edit* flag. The *type* arg determines the signal emitted on edit (numpad vs qwerty).

            Args:
                text (str, optional): _description_. Defaults to "—".
                edit (bool, optional): _description_. Defaults to True.
                type (str, optional): type of the input field gets ignored if edit is False. Defaults to "keypad".

            Returns:
                _type_: retuns label or line edit widget depending on the edit flag.
            """

            if edit:
                l = BlocksCustomLinEdit(self)
                l.setText(text)
                l.setFont(font)
                # elif type == "qwerty":
                #     l.editingFinished.connect(lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(l.text(), self._slot_index, l, 0, 0))
            else:
                l = QtWidgets.QLabel(text)
                l.setStyleSheet("color: rgb(255,255,255);")
                l.setFont(font)
            return l

        self._lbl_status = make_val(edit=False)
        self._lbl_temp = make_val()
        self._lbl_color = make_val()
        self._lbl_mat = make_val()
        self._lbl_weight = make_val()

        self._lbl_color.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_color, "#", "", "", 6
            )
        )
        self._lbl_mat.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_mat, "", "", "", 0
            )
        )
        self._lbl_weight.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_weight, "g", "", "", 0
            )
        )
        self._lbl_temp.clicked.connect(
            lambda: self.request_keypad["PyQt_PyObject", str, str, str, int].emit(
                self._lbl_temp, "", "º", "int", 3
            )
        )

        rows = [
            ("Status", self._lbl_status, None),
            ("Temperature", self._lbl_temp, "numpad"),
            ("Color", self._lbl_color, "keypad"),
            ("Material", self._lbl_mat, "keypad"),
            ("Weight", self._lbl_weight, "numpad"),
        ]
        for i, (key, val, _) in enumerate(rows):
            grid.addWidget(make_key(key), i, 0, QtCore.Qt.AlignmentFlag.AlignCenter)
            grid.addWidget(val, i, 1)

        root.addWidget(grid_widget, 1)

        # ── Divider ──
        div = QtWidgets.QFrame()
        div.setFrameShape(QtWidgets.QFrame.Shape.VLine)
        div.setStyleSheet("color: white;")
        root.addWidget(div)

        # ── Operation buttons ──
        btn_col = QtWidgets.QVBoxLayout()
        btn_col.setSpacing(6)

        font = QtGui.QFont()
        font.setPointSize(12)

        self._btn_load = BlocksCustomButton(self)
        self._btn_load.setText("Load")
        self._btn_load.setFixedSize(140, 60)
        self._btn_load.setFont(font)
        self._btn_load.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/load_filament.svg")
        )
        self._btn_unload = BlocksCustomButton(self)
        self._btn_unload.setText("Unload")
        self._btn_unload.setFont(font)
        self._btn_unload.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/unload_filament.svg")
        )
        self._btn_unload.setFixedSize(140, 60)
        self._btn_purge = BlocksCustomButton(self)
        self._btn_purge.setText("Eject")
        self._btn_purge.setFont(font)
        self._btn_purge.setPixmap(
            QtGui.QPixmap(":/filament_related/media/btn_icons/eject.svg")
        )
        self._btn_purge.setFixedSize(140, 60)
        self._btn_cut = BlocksCustomButton(self)
        self._btn_cut.setPixmap(QtGui.QPixmap(":/load_icons/media/btn_icons/cut.svg"))
        self._btn_cut.setText("Check\nGates")
        self._btn_cut.setFont(font)
        self._btn_cut.setFixedSize(140, 60)

        self._btn_load.clicked.connect(lambda: self.loadRequested.emit())
        self._btn_unload.clicked.connect(lambda: self.unloadRequested.emit())
        self._btn_purge.clicked.connect(lambda: self.ejectRequested.emit())
        self._btn_cut.clicked.connect(lambda: self.checkRequested.emit())

        top_row = QtWidgets.QHBoxLayout()
        top_row.setSpacing(6)
        top_row.addWidget(self._btn_load)
        top_row.addWidget(self._btn_unload)

        bot_row = QtWidgets.QHBoxLayout()
        bot_row.setSpacing(6)
        bot_row.addWidget(self._btn_purge)
        bot_row.addWidget(self._btn_cut)

        btn_col.addLayout(top_row)
        btn_col.addLayout(bot_row)
        root.addLayout(None)
    
    xǁSpoolInfoPanelǁ_build_ui__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolInfoPanelǁ_build_ui__mutmut_1': xǁSpoolInfoPanelǁ_build_ui__mutmut_1, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_2': xǁSpoolInfoPanelǁ_build_ui__mutmut_2, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_3': xǁSpoolInfoPanelǁ_build_ui__mutmut_3, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_4': xǁSpoolInfoPanelǁ_build_ui__mutmut_4, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_5': xǁSpoolInfoPanelǁ_build_ui__mutmut_5, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_6': xǁSpoolInfoPanelǁ_build_ui__mutmut_6, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_7': xǁSpoolInfoPanelǁ_build_ui__mutmut_7, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_8': xǁSpoolInfoPanelǁ_build_ui__mutmut_8, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_9': xǁSpoolInfoPanelǁ_build_ui__mutmut_9, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_10': xǁSpoolInfoPanelǁ_build_ui__mutmut_10, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_11': xǁSpoolInfoPanelǁ_build_ui__mutmut_11, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_12': xǁSpoolInfoPanelǁ_build_ui__mutmut_12, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_13': xǁSpoolInfoPanelǁ_build_ui__mutmut_13, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_14': xǁSpoolInfoPanelǁ_build_ui__mutmut_14, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_15': xǁSpoolInfoPanelǁ_build_ui__mutmut_15, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_16': xǁSpoolInfoPanelǁ_build_ui__mutmut_16, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_17': xǁSpoolInfoPanelǁ_build_ui__mutmut_17, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_18': xǁSpoolInfoPanelǁ_build_ui__mutmut_18, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_19': xǁSpoolInfoPanelǁ_build_ui__mutmut_19, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut
 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_301': xǁSpoolInfoPanelǁ_build_ui__mutmut_301, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_302': xǁSpoolInfoPanelǁ_build_ui__mutmut_302, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_303': xǁSpoolInfoPanelǁ_build_ui__mutmut_303, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_304': xǁSpoolInfoPanelǁ_build_ui__mutmut_304, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_305': xǁSpoolInfoPanelǁ_build_ui__mutmut_305, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_306': xǁSpoolInfoPanelǁ_build_ui__mutmut_306, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_307': xǁSpoolInfoPanelǁ_build_ui__mutmut_307, 
        'xǁSpoolInfoPanelǁ_build_ui__mutmut_308': xǁSpoolInfoPanelǁ_build_ui__mutmut_308
    }
    xǁSpoolInfoPanelǁ_build_ui__mutmut_orig.__name__ = 'xǁSpoolInfoPanelǁ_build_ui'

    def setFilamentStatus(self, mmu_state):
        args = [mmu_state]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_orig(self, mmu_state):
        self.FStatus = mmu_state.filament
        self.Gate = mmu_state.gate

    def xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_1(self, mmu_state):
        self.FStatus = None
        self.Gate = mmu_state.gate

    def xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_2(self, mmu_state):
        self.FStatus = mmu_state.filament
        self.Gate = None
    
    xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_1': xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_1, 
        'xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_2': xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_2
    }
    xǁSpoolInfoPanelǁsetFilamentStatus__mutmut_orig.__name__ = 'xǁSpoolInfoPanelǁsetFilamentStatus'

    def update_for_slot(self, index: int, btn: Spoll_button):
        args = [index, btn]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_orig'), object.__getattribute__(self, 'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_mutants'), args, kwargs, self)

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_orig(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_1(self, index: int, btn: Spoll_button):
        self._slot_index = None
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_2(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = None
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_3(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = None

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_4(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            None
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_5(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_6(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_7(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_8(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_9(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = None
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_10(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "XX<span style='color:#2ec4a0'>● PRE-LOADED</span>XX",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_11(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● pre-loaded</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_12(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<SPAN STYLE='COLOR:#2EC4A0'>● PRE-LOADED</SPAN>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_13(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_14(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    True,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_15(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    False,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_16(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    False,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_17(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = None
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_18(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "XX<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>XX",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_19(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● pre-loaded (buffer)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_20(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<SPAN STYLE='COLOR:#2EC4A0'>● PRE-LOADED (BUFFER)</SPAN>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_21(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_22(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    True,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_23(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    False,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_24(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    False,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_25(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = None
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_26(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "XX<span style='color:#e8445a'>○ EMPTY</span>XX",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_27(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ empty</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_28(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<SPAN STYLE='COLOR:#E8445A'>○ EMPTY</SPAN>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_29(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_30(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    True,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_31(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    False,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_32(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    False,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_33(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = None

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_34(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "XX<span style='color:#aaa'>? UNKNOWN</span>XX",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_35(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? unknown</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_36(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<SPAN STYLE='COLOR:#AAA'>? UNKNOWN</SPAN>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_37(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    False,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_38(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    False,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_39(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    True,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_40(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    False,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_41(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" or self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_42(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus != "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_43(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "XXLoadedXX" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_44(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_45(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "LOADED" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_46(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index != self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_47(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = None
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_48(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "XX<span style='color:#2ec4a0'>● LOADED</span>XX"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_49(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● loaded</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_50(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<SPAN STYLE='COLOR:#2EC4A0'>● LOADED</SPAN>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_51(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = None
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_52(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = True
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_53(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = None

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_54(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = False

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_55(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(None)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_56(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(None)
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_57(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(None)
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_58(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "XX—XX")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_59(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(None)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_60(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(None)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_61(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(None)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_62(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(None)
        self._lbl_color.setText(f"#{r:02X}{g:02X}{b:02X}")

    def xǁSpoolInfoPanelǁupdate_for_slot__mutmut_63(self, index: int, btn: Spoll_button):
        self._slot_index = index
        color = btn.color
        r, g, b = color.red(), color.green(), color.blue()

        self._swatch.setStyleSheet(
            f"border-radius: 12px;background: rgb({r},{g},{b});border: 2px solid white"
        )

        match btn.status:
            case GateStatus.AVAILABLE:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.AVAILABLE_FROM_BUFFER:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#2ec4a0'>● PRE-LOADED (BUFFER)</span>",
                    True,
                    False,
                    True,
                    True,
                )
            case GateStatus.EMPTY:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#e8445a'>○ EMPTY</span>",
                    False,
                    False,
                    True,
                    True,
                )
            case _:
                text, en_load, en_unload, en_purge, en_cut = (
                    "<span style='color:#aaa'>? UNKNOWN</span>",
                    True,
                    True,
                    False,
                    True,
                )

        if self.FStatus == "Loaded" and self._slot_index == self.Gate:
            text = "<span style='color:#2ec4a0'>● LOADED</span>"
            en_load = False
            en_unload = True

        self._lbl_status.setText(text)
        self._lbl_temp.setText(f"{btn.temp}º")
        self._lbl_mat.setText(btn.material if btn.material else "—")
        self._btn_load.setEnabled(en_load)
        self._btn_unload.setEnabled(en_unload)
        self._btn_purge.setEnabled(en_purge)
        self._btn_cut.setEnabled(en_cut)
        self._lbl_color.setText(None)
    
    xǁSpoolInfoPanelǁupdate_for_slot__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_1': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_1, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_2': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_2, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_3': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_3, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_4': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_4, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_5': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_5, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_6': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_6, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_7': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_7, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_8': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_8, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_9': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_9, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_10': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_10, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_11': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_11, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_12': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_12, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_13': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_13, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_14': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_14, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_15': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_15, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_16': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_16, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_17': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_17, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_18': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_18, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_19': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_19, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_20': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_20, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_21': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_21, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_22': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_22, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_23': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_23, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_24': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_24, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_25': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_25, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_26': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_26, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_27': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_27, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_28': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_28, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_29': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_29, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_30': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_30, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_31': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_31, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_32': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_32, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_33': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_33, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_34': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_34, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_35': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_35, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_36': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_36, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_37': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_37, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_38': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_38, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_39': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_39, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_40': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_40, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_41': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_41, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_42': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_42, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_43': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_43, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_44': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_44, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_45': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_45, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_46': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_46, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_47': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_47, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_48': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_48, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_49': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_49, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_50': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_50, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_51': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_51, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_52': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_52, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_53': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_53, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_54': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_54, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_55': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_55, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_56': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_56, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_57': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_57, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_58': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_58, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_59': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_59, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_60': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_60, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_61': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_61, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_62': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_62, 
        'xǁSpoolInfoPanelǁupdate_for_slot__mutmut_63': xǁSpoolInfoPanelǁupdate_for_slot__mutmut_63
    }
    xǁSpoolInfoPanelǁupdate_for_slot__mutmut_orig.__name__ = 'xǁSpoolInfoPanelǁupdate_for_slot'
