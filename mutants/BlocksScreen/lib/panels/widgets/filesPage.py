import json
import logging
import typing

import helper_methods
from lib.utils.blocks_Scrollbar import CustomScrollBar
from lib.utils.icon_button import IconButton
from lib.utils.list_model import EntryDelegate, EntryListModel, ListItem
from PyQt6 import QtCore, QtGui, QtWidgets

logger = logging.getLogger(__name__)
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class FilesPage(QtWidgets.QWidget):
    # Signals
    request_back = QtCore.pyqtSignal(name="request_back")
    file_selected = QtCore.pyqtSignal(str, dict, name="file_selected")
    request_file_info = QtCore.pyqtSignal(str, name="request_file_info")
    request_dir_info = QtCore.pyqtSignal(
        [], [str], [str, bool], name="api_get_dir_info"
    )
    request_file_list = QtCore.pyqtSignal([], [str], name="api_get_files_list")
    request_file_metadata = QtCore.pyqtSignal(str, name="api_get_gcode_metadata")
    request_scan_metadata = QtCore.pyqtSignal(str, name="api_scan_gcode_metadata")

    # Constants
    GCODE_EXTENSION = ".gcode"
    USB_PREFIX = "USB-"
    ITEM_HEIGHT = 80
    LEFT_FONT_SIZE = 17
    RIGHT_FONT_SIZE = 12

    # Icon paths
    ICON_PATHS = {
        "back_folder": ":/ui/media/btn_icons/back_folder.svg",
        "folder": ":/ui/media/btn_icons/folderIcon.svg",
        "right_arrow": ":/arrow_icons/media/btn_icons/right_arrow.svg",
        "usb": ":/ui/media/btn_icons/usb_icon.svg",
        "back": ":/ui/media/btn_icons/back.svg",
        "refresh": ":/ui/media/btn_icons/refresh.svg",
    }

    def __init__(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ__init____mutmut_orig(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_1(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(None)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_2(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = None
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_3(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = None  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_4(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = None
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_5(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = None
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_6(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = "XXXX"
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_7(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = None
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_8(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = True
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_9(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = None  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_10(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = None  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_11(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = None

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_12(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = None
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_13(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = None

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_14(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(None)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_15(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(None)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_16(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(None)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_17(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(None)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_18(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(False)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)

    def xǁFilesPageǁ__init____mutmut_19(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(None, True)

    def xǁFilesPageǁ__init____mutmut_20(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, None)

    def xǁFilesPageǁ__init____mutmut_21(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(True)

    def xǁFilesPageǁ__init____mutmut_22(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, )

    def xǁFilesPageǁ__init____mutmut_23(self, parent: typing.Optional[QtWidgets.QWidget] = None) -> None:
        super().__init__(parent)

        self._file_list: list[dict] = []
        self._files_data: dict[str, dict] = {}  # filename -> metadata dict
        self._directories: list[dict] = []
        self._curr_dir: str = ""
        self._pending_action: bool = False
        self._pending_metadata_requests: set[str] = set()  # Track pending requests
        self._metadata_retry_count: dict[
            str, int
        ] = {}  # Track retry count per file (max 3)
        self._icons: dict[str, QtGui.QPixmap] = {}

        self._model = EntryListModel()
        self._entry_delegate = EntryDelegate()

        self._model.rowsInserted.connect(self._delayed_scrollbar_update)
        self._model.rowsRemoved.connect(self._delayed_scrollbar_update)
        self._model.modelReset.connect(self._delayed_scrollbar_update)

        self._setup_ui()
        self._load_icons()
        self._connect_signals()

        self.setMouseTracking(True)
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, False)
    
    xǁFilesPageǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ__init____mutmut_1': xǁFilesPageǁ__init____mutmut_1, 
        'xǁFilesPageǁ__init____mutmut_2': xǁFilesPageǁ__init____mutmut_2, 
        'xǁFilesPageǁ__init____mutmut_3': xǁFilesPageǁ__init____mutmut_3, 
        'xǁFilesPageǁ__init____mutmut_4': xǁFilesPageǁ__init____mutmut_4, 
        'xǁFilesPageǁ__init____mutmut_5': xǁFilesPageǁ__init____mutmut_5, 
        'xǁFilesPageǁ__init____mutmut_6': xǁFilesPageǁ__init____mutmut_6, 
        'xǁFilesPageǁ__init____mutmut_7': xǁFilesPageǁ__init____mutmut_7, 
        'xǁFilesPageǁ__init____mutmut_8': xǁFilesPageǁ__init____mutmut_8, 
        'xǁFilesPageǁ__init____mutmut_9': xǁFilesPageǁ__init____mutmut_9, 
        'xǁFilesPageǁ__init____mutmut_10': xǁFilesPageǁ__init____mutmut_10, 
        'xǁFilesPageǁ__init____mutmut_11': xǁFilesPageǁ__init____mutmut_11, 
        'xǁFilesPageǁ__init____mutmut_12': xǁFilesPageǁ__init____mutmut_12, 
        'xǁFilesPageǁ__init____mutmut_13': xǁFilesPageǁ__init____mutmut_13, 
        'xǁFilesPageǁ__init____mutmut_14': xǁFilesPageǁ__init____mutmut_14, 
        'xǁFilesPageǁ__init____mutmut_15': xǁFilesPageǁ__init____mutmut_15, 
        'xǁFilesPageǁ__init____mutmut_16': xǁFilesPageǁ__init____mutmut_16, 
        'xǁFilesPageǁ__init____mutmut_17': xǁFilesPageǁ__init____mutmut_17, 
        'xǁFilesPageǁ__init____mutmut_18': xǁFilesPageǁ__init____mutmut_18, 
        'xǁFilesPageǁ__init____mutmut_19': xǁFilesPageǁ__init____mutmut_19, 
        'xǁFilesPageǁ__init____mutmut_20': xǁFilesPageǁ__init____mutmut_20, 
        'xǁFilesPageǁ__init____mutmut_21': xǁFilesPageǁ__init____mutmut_21, 
        'xǁFilesPageǁ__init____mutmut_22': xǁFilesPageǁ__init____mutmut_22, 
        'xǁFilesPageǁ__init____mutmut_23': xǁFilesPageǁ__init____mutmut_23
    }
    xǁFilesPageǁ__init____mutmut_orig.__name__ = 'xǁFilesPageǁ__init__'

    @property
    def current_directory(self) -> str:
        """Get current directory path."""
        return self._curr_dir

    @current_directory.setter
    def current_directory(self, value: str) -> None:
        """Set current directory path."""
        self._curr_dir = value

    def reload_gcodes_folder(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁreload_gcodes_folder__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁreload_gcodes_folder__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁreload_gcodes_folder__mutmut_orig(self) -> None:
        """Request reload of the gcodes folder from root."""
        logger.debug("Reloading gcodes folder")
        self.request_dir_info[str].emit("")

    def xǁFilesPageǁreload_gcodes_folder__mutmut_1(self) -> None:
        """Request reload of the gcodes folder from root."""
        logger.debug(None)
        self.request_dir_info[str].emit("")

    def xǁFilesPageǁreload_gcodes_folder__mutmut_2(self) -> None:
        """Request reload of the gcodes folder from root."""
        logger.debug("XXReloading gcodes folderXX")
        self.request_dir_info[str].emit("")

    def xǁFilesPageǁreload_gcodes_folder__mutmut_3(self) -> None:
        """Request reload of the gcodes folder from root."""
        logger.debug("reloading gcodes folder")
        self.request_dir_info[str].emit("")

    def xǁFilesPageǁreload_gcodes_folder__mutmut_4(self) -> None:
        """Request reload of the gcodes folder from root."""
        logger.debug("RELOADING GCODES FOLDER")
        self.request_dir_info[str].emit("")

    def xǁFilesPageǁreload_gcodes_folder__mutmut_5(self) -> None:
        """Request reload of the gcodes folder from root."""
        logger.debug("Reloading gcodes folder")
        self.request_dir_info[str].emit(None)

    def xǁFilesPageǁreload_gcodes_folder__mutmut_6(self) -> None:
        """Request reload of the gcodes folder from root."""
        logger.debug("Reloading gcodes folder")
        self.request_dir_info[str].emit("XXXX")
    
    xǁFilesPageǁreload_gcodes_folder__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁreload_gcodes_folder__mutmut_1': xǁFilesPageǁreload_gcodes_folder__mutmut_1, 
        'xǁFilesPageǁreload_gcodes_folder__mutmut_2': xǁFilesPageǁreload_gcodes_folder__mutmut_2, 
        'xǁFilesPageǁreload_gcodes_folder__mutmut_3': xǁFilesPageǁreload_gcodes_folder__mutmut_3, 
        'xǁFilesPageǁreload_gcodes_folder__mutmut_4': xǁFilesPageǁreload_gcodes_folder__mutmut_4, 
        'xǁFilesPageǁreload_gcodes_folder__mutmut_5': xǁFilesPageǁreload_gcodes_folder__mutmut_5, 
        'xǁFilesPageǁreload_gcodes_folder__mutmut_6': xǁFilesPageǁreload_gcodes_folder__mutmut_6
    }
    xǁFilesPageǁreload_gcodes_folder__mutmut_orig.__name__ = 'xǁFilesPageǁreload_gcodes_folder'

    def clear_files_data(self) -> None:
        """Clear all cached file data."""
        self._files_data.clear()
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

    def retry_metadata_request(self, file_path: str) -> bool:
        args = [file_path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁretry_metadata_request__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁretry_metadata_request__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁretry_metadata_request__mutmut_orig(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_1(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = None

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_2(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix(None)

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_3(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removesuffix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_4(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("XX/XX")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_5(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_6(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(None):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_7(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.upper().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_8(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return True

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_9(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = None

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_10(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(None, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_11(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, None)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_12(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_13(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, )

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_14(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 1)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_15(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count >= 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_16(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 4:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_17(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(None)
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_18(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return True

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_19(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = None

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_20(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count - 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_21(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 2

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_22(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count != 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_23(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 1:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_24(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(None)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_25(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(None)
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_26(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(None)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_27(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                None
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_28(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count - 1}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_29(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 2}) for: {clean_path}"
            )

        return True

    def xǁFilesPageǁretry_metadata_request__mutmut_30(self, file_path: str) -> bool:
        """
        Request metadata with a maximum of 3 retries per file.
        Args:
            file_path: Path to the file
        Returns:
            True if request was made, False if max retries reached
        """
        clean_path = file_path.removeprefix("/")

        if not clean_path.lower().endswith(self.GCODE_EXTENSION):
            return False

        current_count = self._metadata_retry_count.get(clean_path, 0)

        if current_count > 3:
            # Maximum 3 force scan per file
            logger.debug(f"Metadata retry limit reached for: {clean_path}")
            return False

        self._metadata_retry_count[clean_path] = current_count + 1

        if current_count == 0:
            # First attempt: regular metadata request
            self.request_file_metadata.emit(clean_path)
            logger.debug(f"Metadata request (attempt 1) for: {clean_path}")
        else:
            # Subsequent attempts: force scan
            self.request_scan_metadata.emit(clean_path)
            logger.debug(
                f"Metadata scan (attempt {current_count + 1}) for: {clean_path}"
            )

        return False
    
    xǁFilesPageǁretry_metadata_request__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁretry_metadata_request__mutmut_1': xǁFilesPageǁretry_metadata_request__mutmut_1, 
        'xǁFilesPageǁretry_metadata_request__mutmut_2': xǁFilesPageǁretry_metadata_request__mutmut_2, 
        'xǁFilesPageǁretry_metadata_request__mutmut_3': xǁFilesPageǁretry_metadata_request__mutmut_3, 
        'xǁFilesPageǁretry_metadata_request__mutmut_4': xǁFilesPageǁretry_metadata_request__mutmut_4, 
        'xǁFilesPageǁretry_metadata_request__mutmut_5': xǁFilesPageǁretry_metadata_request__mutmut_5, 
        'xǁFilesPageǁretry_metadata_request__mutmut_6': xǁFilesPageǁretry_metadata_request__mutmut_6, 
        'xǁFilesPageǁretry_metadata_request__mutmut_7': xǁFilesPageǁretry_metadata_request__mutmut_7, 
        'xǁFilesPageǁretry_metadata_request__mutmut_8': xǁFilesPageǁretry_metadata_request__mutmut_8, 
        'xǁFilesPageǁretry_metadata_request__mutmut_9': xǁFilesPageǁretry_metadata_request__mutmut_9, 
        'xǁFilesPageǁretry_metadata_request__mutmut_10': xǁFilesPageǁretry_metadata_request__mutmut_10, 
        'xǁFilesPageǁretry_metadata_request__mutmut_11': xǁFilesPageǁretry_metadata_request__mutmut_11, 
        'xǁFilesPageǁretry_metadata_request__mutmut_12': xǁFilesPageǁretry_metadata_request__mutmut_12, 
        'xǁFilesPageǁretry_metadata_request__mutmut_13': xǁFilesPageǁretry_metadata_request__mutmut_13, 
        'xǁFilesPageǁretry_metadata_request__mutmut_14': xǁFilesPageǁretry_metadata_request__mutmut_14, 
        'xǁFilesPageǁretry_metadata_request__mutmut_15': xǁFilesPageǁretry_metadata_request__mutmut_15, 
        'xǁFilesPageǁretry_metadata_request__mutmut_16': xǁFilesPageǁretry_metadata_request__mutmut_16, 
        'xǁFilesPageǁretry_metadata_request__mutmut_17': xǁFilesPageǁretry_metadata_request__mutmut_17, 
        'xǁFilesPageǁretry_metadata_request__mutmut_18': xǁFilesPageǁretry_metadata_request__mutmut_18, 
        'xǁFilesPageǁretry_metadata_request__mutmut_19': xǁFilesPageǁretry_metadata_request__mutmut_19, 
        'xǁFilesPageǁretry_metadata_request__mutmut_20': xǁFilesPageǁretry_metadata_request__mutmut_20, 
        'xǁFilesPageǁretry_metadata_request__mutmut_21': xǁFilesPageǁretry_metadata_request__mutmut_21, 
        'xǁFilesPageǁretry_metadata_request__mutmut_22': xǁFilesPageǁretry_metadata_request__mutmut_22, 
        'xǁFilesPageǁretry_metadata_request__mutmut_23': xǁFilesPageǁretry_metadata_request__mutmut_23, 
        'xǁFilesPageǁretry_metadata_request__mutmut_24': xǁFilesPageǁretry_metadata_request__mutmut_24, 
        'xǁFilesPageǁretry_metadata_request__mutmut_25': xǁFilesPageǁretry_metadata_request__mutmut_25, 
        'xǁFilesPageǁretry_metadata_request__mutmut_26': xǁFilesPageǁretry_metadata_request__mutmut_26, 
        'xǁFilesPageǁretry_metadata_request__mutmut_27': xǁFilesPageǁretry_metadata_request__mutmut_27, 
        'xǁFilesPageǁretry_metadata_request__mutmut_28': xǁFilesPageǁretry_metadata_request__mutmut_28, 
        'xǁFilesPageǁretry_metadata_request__mutmut_29': xǁFilesPageǁretry_metadata_request__mutmut_29, 
        'xǁFilesPageǁretry_metadata_request__mutmut_30': xǁFilesPageǁretry_metadata_request__mutmut_30
    }
    xǁFilesPageǁretry_metadata_request__mutmut_orig.__name__ = 'xǁFilesPageǁretry_metadata_request'

    @QtCore.pyqtSlot(list, name="on_file_list")
    def on_file_list(self, file_list: list) -> None:
        """Handle receiving full files list."""
        self._file_list = file_list.copy() if file_list else []
        logger.debug(f"Received file list with {len(self._file_list)} files")

    @QtCore.pyqtSlot(list, name="on_dirs")
    def on_directories(self, directories_data: list) -> None:
        """Handle receiving full directories list."""
        self._directories = directories_data.copy() if directories_data else []
        logger.debug(f"Received {len(self._directories)} directories")

        if self.isVisible():
            self._build_file_list()

    @QtCore.pyqtSlot(dict, name="on_fileinfo")
    def on_fileinfo(self, filedata: dict) -> None:
        """
        Handle receiving file metadata.

        Updates existing file entry in the list with better info (time, filament).
        """
        if not filedata or not self.isVisible():
            return

        filename = filedata.get("filename", "")
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Cache the file data
        self._files_data[filename] = filedata

        # Remove from pending requests and reset retry count (success)
        self._pending_metadata_requests.discard(filename)
        self._metadata_retry_count.pop(filename, None)

        # Check if this file should be displayed in current view
        file_dir = self._get_parent_directory(filename)
        current = self._curr_dir.removeprefix("/")

        # Both empty = root directory, otherwise must match exactly
        if file_dir != current:
            return

        display_name = self._get_display_name(filename)
        item = self._create_file_list_item(filedata)
        if not item:
            return

        self._model.remove_item_by_text(display_name)

        # Find correct position (sorted by modification time, newest first)
        insert_position = self._find_file_insert_position(filedata.get("modified", 0))
        self._model.insert_item(insert_position, item)

        logger.debug(f"Updated file in list: {display_name}")

    @QtCore.pyqtSlot(str, name="on_metadata_error")
    def on_metadata_error(self, filename: str) -> None:
        """
        Handle metadata request failure.

        Triggers retry with scan_gcode_metadata if under retry limit.
        Called when metadata request fails.
        """
        if not filename:
            return

        clean_filename = filename.removeprefix("/")

        if clean_filename not in self._pending_metadata_requests:
            return

        # Try again (will use force scan to the max  of 3 times)
        if not self.retry_metadata_request(clean_filename):
            # Max retries reached, remove from pending
            self._pending_metadata_requests.discard(clean_filename)
            logger.debug(f"Giving up on metadata for: {clean_filename}")

    @QtCore.pyqtSlot(str, list, name="on_usb_files_loaded")
    def on_usb_files_loaded(self, usb_path: str, files: list) -> None:
        """
        Handle preloaded USB files.

        Called when USB files are preloaded in background.
        If we're currently viewing this USB, update the display.

        Args:
            usb_path: The USB mount path
            files: List of file dicts from the USB
        """
        current = self._curr_dir.removeprefix("/")

        # If we're currently in this USB folder, update the file list
        if current == usb_path:
            self._file_list = files.copy()
            if self.isVisible():
                self._build_file_list()
            logger.debug(f"Updated view with preloaded USB files: {usb_path}")

    def _find_file_insert_position(self, modified_time: float) -> int:
        args = [modified_time]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_find_file_insert_position__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_find_file_insert_position__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_find_file_insert_position__mutmut_orig(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_1(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = None

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_2(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 1

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_3(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(None):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_4(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = None
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_5(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(None)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_6(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.rindex(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_7(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = None

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_8(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(None, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_9(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, None)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_10(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_11(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, )

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_12(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_13(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                break

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_14(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = None
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_15(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i - 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_16(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 2
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_17(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                break

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_18(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = None
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_19(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(None)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_20(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = None
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_21(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(None, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_22(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, None)
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_23(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get({})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_24(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, )
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_25(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = None

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_26(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get(None, 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_27(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", None)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_28(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get(0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_29(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", )

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_30(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("XXmodifiedXX", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_31(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("MODIFIED", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_32(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 1)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_33(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time >= file_modified:
                    return i

            insert_pos = i + 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_34(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = None

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_35(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i - 1

        return insert_pos

    def xǁFilesPageǁ_find_file_insert_position__mutmut_36(self, modified_time: float) -> int:
        """
        Find the correct position to insert a new file.

        Files should be:
        1. After all directories
        2. Sorted by modification time (newest first)

        Returns:
            The index at which to insert the new file.
        """
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip directories (items with left_icon)
            if item.left_icon:
                insert_pos = i + 1
                continue

            # This is a file - check its modification time
            file_key = self._find_file_key_by_display_name(item.text)
            if file_key:
                file_data = self._files_data.get(file_key, {})
                file_modified = file_data.get("modified", 0)

                # Files are sorted newest first, so insert before older files
                if modified_time > file_modified:
                    return i

            insert_pos = i + 2

        return insert_pos
    
    xǁFilesPageǁ_find_file_insert_position__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_find_file_insert_position__mutmut_1': xǁFilesPageǁ_find_file_insert_position__mutmut_1, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_2': xǁFilesPageǁ_find_file_insert_position__mutmut_2, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_3': xǁFilesPageǁ_find_file_insert_position__mutmut_3, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_4': xǁFilesPageǁ_find_file_insert_position__mutmut_4, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_5': xǁFilesPageǁ_find_file_insert_position__mutmut_5, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_6': xǁFilesPageǁ_find_file_insert_position__mutmut_6, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_7': xǁFilesPageǁ_find_file_insert_position__mutmut_7, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_8': xǁFilesPageǁ_find_file_insert_position__mutmut_8, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_9': xǁFilesPageǁ_find_file_insert_position__mutmut_9, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_10': xǁFilesPageǁ_find_file_insert_position__mutmut_10, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_11': xǁFilesPageǁ_find_file_insert_position__mutmut_11, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_12': xǁFilesPageǁ_find_file_insert_position__mutmut_12, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_13': xǁFilesPageǁ_find_file_insert_position__mutmut_13, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_14': xǁFilesPageǁ_find_file_insert_position__mutmut_14, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_15': xǁFilesPageǁ_find_file_insert_position__mutmut_15, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_16': xǁFilesPageǁ_find_file_insert_position__mutmut_16, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_17': xǁFilesPageǁ_find_file_insert_position__mutmut_17, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_18': xǁFilesPageǁ_find_file_insert_position__mutmut_18, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_19': xǁFilesPageǁ_find_file_insert_position__mutmut_19, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_20': xǁFilesPageǁ_find_file_insert_position__mutmut_20, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_21': xǁFilesPageǁ_find_file_insert_position__mutmut_21, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_22': xǁFilesPageǁ_find_file_insert_position__mutmut_22, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_23': xǁFilesPageǁ_find_file_insert_position__mutmut_23, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_24': xǁFilesPageǁ_find_file_insert_position__mutmut_24, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_25': xǁFilesPageǁ_find_file_insert_position__mutmut_25, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_26': xǁFilesPageǁ_find_file_insert_position__mutmut_26, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_27': xǁFilesPageǁ_find_file_insert_position__mutmut_27, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_28': xǁFilesPageǁ_find_file_insert_position__mutmut_28, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_29': xǁFilesPageǁ_find_file_insert_position__mutmut_29, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_30': xǁFilesPageǁ_find_file_insert_position__mutmut_30, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_31': xǁFilesPageǁ_find_file_insert_position__mutmut_31, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_32': xǁFilesPageǁ_find_file_insert_position__mutmut_32, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_33': xǁFilesPageǁ_find_file_insert_position__mutmut_33, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_34': xǁFilesPageǁ_find_file_insert_position__mutmut_34, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_35': xǁFilesPageǁ_find_file_insert_position__mutmut_35, 
        'xǁFilesPageǁ_find_file_insert_position__mutmut_36': xǁFilesPageǁ_find_file_insert_position__mutmut_36
    }
    xǁFilesPageǁ_find_file_insert_position__mutmut_orig.__name__ = 'xǁFilesPageǁ_find_file_insert_position'

    def _find_file_key_by_display_name(self, display_name: str) -> typing.Optional[str]:
        args = [display_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_find_file_key_by_display_name__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_find_file_key_by_display_name__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_find_file_key_by_display_name__mutmut_orig(self, display_name: str) -> typing.Optional[str]:
        """Find the file key in _files_data by its display name."""
        for key in self._files_data:
            if self._get_display_name(key) == display_name:
                return key
        return None

    def xǁFilesPageǁ_find_file_key_by_display_name__mutmut_1(self, display_name: str) -> typing.Optional[str]:
        """Find the file key in _files_data by its display name."""
        for key in self._files_data:
            if self._get_display_name(None) == display_name:
                return key
        return None

    def xǁFilesPageǁ_find_file_key_by_display_name__mutmut_2(self, display_name: str) -> typing.Optional[str]:
        """Find the file key in _files_data by its display name."""
        for key in self._files_data:
            if self._get_display_name(key) != display_name:
                return key
        return None
    
    xǁFilesPageǁ_find_file_key_by_display_name__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_find_file_key_by_display_name__mutmut_1': xǁFilesPageǁ_find_file_key_by_display_name__mutmut_1, 
        'xǁFilesPageǁ_find_file_key_by_display_name__mutmut_2': xǁFilesPageǁ_find_file_key_by_display_name__mutmut_2
    }
    xǁFilesPageǁ_find_file_key_by_display_name__mutmut_orig.__name__ = 'xǁFilesPageǁ_find_file_key_by_display_name'

    @QtCore.pyqtSlot(dict, name="on_file_added")
    def on_file_added(self, file_data: dict) -> None:
        """
        Handle a single file being added.

        Called when Moonraker sends notify_filelist_changed with create_file action.
        Adds file to list immediately, metadata updates later.
        """
        path = file_data.get("path", file_data.get("filename", ""))
        if not path:
            return

        # Normalize paths
        path = path.removeprefix("/")
        file_dir = self._get_parent_directory(path)
        current = self._curr_dir.removeprefix("/")

        # Check if file belongs to current directory
        if file_dir != current:
            logger.debug(
                f"File '{path}' (dir: '{file_dir}') not in current directory ('{current}'), skipping"
            )
            return

        # Only update UI if visible
        if not self.isVisible():
            logger.debug("Widget not visible, will refresh on show")
            return

        display_name = self._get_display_name(path)

        if not self._model_contains_item(display_name):
            # Create basic item with unknown info
            modified = file_data.get("modified", 0)

            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

            # Find correct position
            insert_position = self._find_file_insert_position(modified)
            self._model.insert_item(insert_position, item)
            self._hide_placeholder()
            logger.debug(f"Added new file to list: {display_name}")

        # Request metadata for gcode files using retry mechanism
        if path.lower().endswith(self.GCODE_EXTENSION):
            if path not in self._pending_metadata_requests:
                self._pending_metadata_requests.add(path)
                self.retry_metadata_request(path)
                logger.debug(f"Requested metadata for new file: {path}")

    @QtCore.pyqtSlot(str, name="on_file_removed")
    def on_file_removed(self, filepath: str) -> None:
        """
        Handle a file being removed.

        Called when Moonraker sends notify_filelist_changed with delete_file action.
        """
        filepath = filepath.removeprefix("/")
        file_dir = self._get_parent_directory(filepath)
        current = self._curr_dir.removeprefix("/")

        # Always clean up cache
        self._files_data.pop(filepath, None)
        self._pending_metadata_requests.discard(filepath)
        self._metadata_retry_count.pop(filepath, None)

        # Only update UI if visible and in current directory
        if not self.isVisible():
            return

        if file_dir != current:
            logger.debug(
                f"Deleted file '{filepath}' not in current directory ('{current}'), skipping UI update"
            )
            return

        filename = self._get_basename(filepath)
        display_name = self._get_display_name(filename)

        # Remove from model
        removed = self._model.remove_item_by_text(display_name)

        if removed:
            self._check_empty_state()
            logger.debug(f"File removed from view: {filepath}")

    @QtCore.pyqtSlot(dict, name="on_file_modified")
    def on_file_modified(self, file_data: dict) -> None:
        """
        Handle a file being modified.

        Called when Moonraker sends notify_filelist_changed with modify_file action.
        """
        path = file_data.get("path", file_data.get("filename", ""))
        if path:
            # Remove old entry and request fresh metadata
            self.on_file_removed(path)
            self.on_file_added(file_data)

    @QtCore.pyqtSlot(dict, name="on_dir_added")
    def on_dir_added(self, dir_data: dict) -> None:
        """
        Handle a directory being added.

        Called when Moonraker sends notify_filelist_changed with create_dir action.
        Inserts the directory in the correct sorted position (alphabetically, after Go Back).
        """
        # Extract dirname from path or dirname field
        path = dir_data.get("path", "")
        dirname = dir_data.get("dirname", "")

        if not dirname and path:
            dirname = self._get_basename(path)

        if not dirname or dirname.startswith("."):
            return

        path = path.removeprefix("/")
        parent_dir = self._get_parent_directory(path) if path else ""
        current = self._curr_dir.removeprefix("/")

        if parent_dir != current:
            logger.debug(
                f"Directory '{dirname}' (parent: '{parent_dir}') not in current directory ('{current}'), skipping"
            )
            return

        if not self.isVisible():
            logger.debug(
                f"Widget not visible, skipping UI update for added dir: {dirname}"
            )
            return

        # Check if already exists
        if self._model_contains_item(dirname):
            return

        # Ensure dirname is in dir_data
        dir_data["dirname"] = dirname

        # Find the correct sorted position for this directory
        insert_position = self._find_directory_insert_position(dirname)

        # Create the list item
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dirname):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dirname),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )

        # Insert at the correct position
        self._model.insert_item(insert_position, item)

        self._hide_placeholder()
        logger.debug(
            f"Directory added to view at position {insert_position}: {dirname}"
        )

    def _find_directory_insert_position(self, new_dirname: str) -> int:
        args = [new_dirname]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_find_directory_insert_position__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_find_directory_insert_position__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_orig(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_1(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = None
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_2(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.upper()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_3(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = None

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_4(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 1

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_5(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(None):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_6(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = None
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_7(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(None)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_8(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.rindex(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_9(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = None

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_10(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(None, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_11(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, None)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_12(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_13(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, )

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_14(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_15(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                break

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_16(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text != "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_17(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "XXGo BackXX":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_18(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "go back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_19(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "GO BACK":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_20(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = None
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_21(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i - 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_22(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 2
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_23(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                break

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_24(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.upper() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_25(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() >= new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_26(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = None
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_27(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i - 1
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos

    def xǁFilesPageǁ_find_directory_insert_position__mutmut_28(self, new_dirname: str) -> int:
        """
        Find the correct position to insert a new directory.

        Directories should be:
        1. After "Go Back" (if present)
        2. Before all files
        3. Sorted alphabetically among other directories

        Returns:
            The index at which to insert the new directory.
        """
        new_dirname_lower = new_dirname.lower()
        insert_pos = 0

        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)

            if not item:
                continue

            # Skip "Go Back" - always stays at top
            if item.text == "Go Back":
                insert_pos = i + 1
                continue

            # If this item has a left_icon, it's a directory
            if item.left_icon:
                # Compare alphabetically
                if item.text.lower() > new_dirname_lower:
                    # Found a directory that should come after the new one
                    return i
                else:
                    # This directory comes before, keep looking
                    insert_pos = i + 2
            else:
                # Hit a file - insert before it (directories come before files)
                return i

        # Insert at the end of directories (or end of list if no files)
        return insert_pos
    
    xǁFilesPageǁ_find_directory_insert_position__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_find_directory_insert_position__mutmut_1': xǁFilesPageǁ_find_directory_insert_position__mutmut_1, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_2': xǁFilesPageǁ_find_directory_insert_position__mutmut_2, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_3': xǁFilesPageǁ_find_directory_insert_position__mutmut_3, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_4': xǁFilesPageǁ_find_directory_insert_position__mutmut_4, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_5': xǁFilesPageǁ_find_directory_insert_position__mutmut_5, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_6': xǁFilesPageǁ_find_directory_insert_position__mutmut_6, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_7': xǁFilesPageǁ_find_directory_insert_position__mutmut_7, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_8': xǁFilesPageǁ_find_directory_insert_position__mutmut_8, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_9': xǁFilesPageǁ_find_directory_insert_position__mutmut_9, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_10': xǁFilesPageǁ_find_directory_insert_position__mutmut_10, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_11': xǁFilesPageǁ_find_directory_insert_position__mutmut_11, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_12': xǁFilesPageǁ_find_directory_insert_position__mutmut_12, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_13': xǁFilesPageǁ_find_directory_insert_position__mutmut_13, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_14': xǁFilesPageǁ_find_directory_insert_position__mutmut_14, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_15': xǁFilesPageǁ_find_directory_insert_position__mutmut_15, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_16': xǁFilesPageǁ_find_directory_insert_position__mutmut_16, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_17': xǁFilesPageǁ_find_directory_insert_position__mutmut_17, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_18': xǁFilesPageǁ_find_directory_insert_position__mutmut_18, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_19': xǁFilesPageǁ_find_directory_insert_position__mutmut_19, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_20': xǁFilesPageǁ_find_directory_insert_position__mutmut_20, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_21': xǁFilesPageǁ_find_directory_insert_position__mutmut_21, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_22': xǁFilesPageǁ_find_directory_insert_position__mutmut_22, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_23': xǁFilesPageǁ_find_directory_insert_position__mutmut_23, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_24': xǁFilesPageǁ_find_directory_insert_position__mutmut_24, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_25': xǁFilesPageǁ_find_directory_insert_position__mutmut_25, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_26': xǁFilesPageǁ_find_directory_insert_position__mutmut_26, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_27': xǁFilesPageǁ_find_directory_insert_position__mutmut_27, 
        'xǁFilesPageǁ_find_directory_insert_position__mutmut_28': xǁFilesPageǁ_find_directory_insert_position__mutmut_28
    }
    xǁFilesPageǁ_find_directory_insert_position__mutmut_orig.__name__ = 'xǁFilesPageǁ_find_directory_insert_position'

    @QtCore.pyqtSlot(str, name="on_dir_removed")
    def on_dir_removed(self, dirname_or_path: str) -> None:
        """
        Handle a directory being removed.

        Called when Moonraker sends notify_filelist_changed with delete_dir action.
        Also handles USB mount removal (which Moonraker reports as delete_file).
        """
        dirname_or_path = dirname_or_path.removeprefix("/")
        dirname = (
            self._get_basename(dirname_or_path)
            if "/" in dirname_or_path
            else dirname_or_path
        )

        if not dirname:
            return

        # Check if user is currently inside the removed directory (e.g., USB removed)
        current = self._curr_dir.removeprefix("/")
        if current == dirname or current.startswith(dirname + "/"):
            logger.warning(
                f"Current directory '{current}' was removed, returning to root"
            )
            self.on_directory_error()
            return

        # Skip UI update if not visible
        if not self.isVisible():
            return

        removed = self._model.remove_item_by_text(dirname)

        if removed:
            self._check_empty_state()
            logger.debug(f"Directory removed from view: {dirname}")

    @QtCore.pyqtSlot(name="on_full_refresh_needed")
    def on_full_refresh_needed(self) -> None:
        """
        Handle full refresh request.

        Called when Moonraker sends root_update or when major changes occur.
        """
        logger.info("Full refresh requested")
        self._curr_dir = ""
        self.request_dir_info[str].emit(self._curr_dir)

    @QtCore.pyqtSlot(name="on_directory_error")
    def on_directory_error(self) -> None:
        """
        Handle Directory Error.

        Immediately navigates back to root gcodes folder.
        Call this from MainWindow when detecting USB removal or directory errors.
        """
        logger.info("Directory Error - returning to root directory")

        # Reset to root directory
        self._curr_dir = ""

        # Clear any pending actions
        self._pending_action = False
        self._pending_metadata_requests.clear()

        # Request fresh data for root directory
        self.request_dir_info[str].emit("")

    @QtCore.pyqtSlot(ListItem, name="on_item_selected")
    def _on_item_selected(self, item: ListItem) -> None:
        """Handle list item selection."""
        if not item.left_icon:
            # File selected (files don't have left icon)
            filename = self._build_filepath(item.text + self.GCODE_EXTENSION)
            self._on_file_item_clicked(filename)
        elif item.text == "Go Back":
            # Go back selected
            go_back_path = self._get_parent_directory(self._curr_dir)
            if go_back_path == "/":
                go_back_path = ""
            self._on_go_back_dir(go_back_path)
        else:
            # Directory selected
            self._on_dir_item_clicked("/" + item.text)

    @QtCore.pyqtSlot(name="reset_dir")
    def reset_dir(self) -> None:
        """Reset to root directory."""
        self._curr_dir = ""
        self.request_dir_info[str].emit(self._curr_dir)

    def showEvent(self, event: QtGui.QShowEvent) -> None:
        args = [event]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁshowEvent__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁshowEvent__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁshowEvent__mutmut_orig(self, event: QtGui.QShowEvent) -> None:
        """Handle widget becoming visible."""
        # Request fresh data when becoming visible
        self.request_dir_info[str].emit(self._curr_dir)
        super().showEvent(event)

    def xǁFilesPageǁshowEvent__mutmut_1(self, event: QtGui.QShowEvent) -> None:
        """Handle widget becoming visible."""
        # Request fresh data when becoming visible
        self.request_dir_info[str].emit(None)
        super().showEvent(event)

    def xǁFilesPageǁshowEvent__mutmut_2(self, event: QtGui.QShowEvent) -> None:
        """Handle widget becoming visible."""
        # Request fresh data when becoming visible
        self.request_dir_info[str].emit(self._curr_dir)
        super().showEvent(None)
    
    xǁFilesPageǁshowEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁshowEvent__mutmut_1': xǁFilesPageǁshowEvent__mutmut_1, 
        'xǁFilesPageǁshowEvent__mutmut_2': xǁFilesPageǁshowEvent__mutmut_2
    }
    xǁFilesPageǁshowEvent__mutmut_orig.__name__ = 'xǁFilesPageǁshowEvent'

    def hideEvent(self, event: QtGui.QHideEvent) -> None:
        args = [event]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁhideEvent__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁhideEvent__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁhideEvent__mutmut_orig(self, event: QtGui.QHideEvent) -> None:
        """Handle widget being hidden."""
        # Clear pending requests when hidden
        self._pending_metadata_requests.clear()
        super().hideEvent(event)

    def xǁFilesPageǁhideEvent__mutmut_1(self, event: QtGui.QHideEvent) -> None:
        """Handle widget being hidden."""
        # Clear pending requests when hidden
        self._pending_metadata_requests.clear()
        super().hideEvent(None)
    
    xǁFilesPageǁhideEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁhideEvent__mutmut_1': xǁFilesPageǁhideEvent__mutmut_1
    }
    xǁFilesPageǁhideEvent__mutmut_orig.__name__ = 'xǁFilesPageǁhideEvent'

    def _build_file_list(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_build_file_list__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_build_file_list__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_build_file_list__mutmut_orig(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_1(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(None)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_2(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(False)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_3(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = None
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_4(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = True
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_5(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = None

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_6(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir and self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_7(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_8(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir != "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_9(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "XX/XX"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_10(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories or is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_11(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list or not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_12(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_13(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_14(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(None)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_15(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(True)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_16(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_17(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = None
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_18(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            None, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_19(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=None
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_20(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_21(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_22(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: None
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_23(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").upper()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_24(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get(None, "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_25(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", None).lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_26(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_27(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", ).lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_28(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("XXdirnameXX", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_29(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("DIRNAME", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_30(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "XXXX").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_31(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = None
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_32(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get(None, "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_33(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", None)
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_34(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_35(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", )
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_36(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("XXdirnameXX", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_37(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("DIRNAME", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_38(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "XXXX")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_39(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname or not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_40(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_41(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith(None):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_42(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("XX.XX"):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_43(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(None)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_44(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = None
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_45(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            None, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_46(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=None, reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_47(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=None
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_48(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_49(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_50(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_51(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: None, reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_52(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get(None, 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_53(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", None), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_54(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get(0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_55(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", ), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_56(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("XXmodifiedXX", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_57(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("MODIFIED", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_58(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 1), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_59(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=False
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_60(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = None
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_61(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get(None, file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_62(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", None)
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_63(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get(file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_64(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", )
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_65(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("XXfilenameXX", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_66(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("FILENAME", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_67(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get(None, ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_68(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", None))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_69(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get(""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_70(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_71(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("XXpathXX", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_72(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("PATH", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_73(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", "XXXX"))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_74(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_75(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                break

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_76(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(None)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_77(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(None):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_78(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.upper().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_79(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(None)

        self._list_widget.blockSignals(False)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_80(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(None)
        self._list_widget.update()

    def xǁFilesPageǁ_build_file_list__mutmut_81(self) -> None:
        """Build the complete file list display."""
        self._list_widget.blockSignals(True)
        self._model.clear()
        self._entry_delegate.clear()
        self._pending_action = False
        self._pending_metadata_requests.clear()
        self._metadata_retry_count.clear()

        # Determine if we're in root directory
        is_root = not self._curr_dir or self._curr_dir == "/"

        # Check for empty state in root directory
        if not self._file_list and not self._directories and is_root:
            self._show_placeholder()
            self._list_widget.blockSignals(False)
            return

        # We have content (or we're in a subdirectory), hide placeholder
        self._hide_placeholder()

        # Add back button if not in root
        if not is_root:
            self._add_back_folder_entry()

        # Add directories (sorted alphabetically)
        sorted_dirs = sorted(
            self._directories, key=lambda x: x.get("dirname", "").lower()
        )
        for dir_data in sorted_dirs:
            dirname = dir_data.get("dirname", "")
            if dirname and not dirname.startswith("."):
                self._add_directory_list_item(dir_data)

        # Add files immediately (sorted by modification time, newest first)
        sorted_files = sorted(
            self._file_list, key=lambda x: x.get("modified", 0), reverse=True
        )
        for file_item in sorted_files:
            filename = file_item.get("filename", file_item.get("path", ""))
            if not filename:
                continue

            # Add file to list immediately with basic info
            self._add_file_to_list(file_item)

            # Request metadata for gcode files (will update display later)
            if filename.lower().endswith(self.GCODE_EXTENSION):
                self._request_file_info(file_item)

        self._list_widget.blockSignals(True)
        self._list_widget.update()
    
    xǁFilesPageǁ_build_file_list__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_build_file_list__mutmut_1': xǁFilesPageǁ_build_file_list__mutmut_1, 
        'xǁFilesPageǁ_build_file_list__mutmut_2': xǁFilesPageǁ_build_file_list__mutmut_2, 
        'xǁFilesPageǁ_build_file_list__mutmut_3': xǁFilesPageǁ_build_file_list__mutmut_3, 
        'xǁFilesPageǁ_build_file_list__mutmut_4': xǁFilesPageǁ_build_file_list__mutmut_4, 
        'xǁFilesPageǁ_build_file_list__mutmut_5': xǁFilesPageǁ_build_file_list__mutmut_5, 
        'xǁFilesPageǁ_build_file_list__mutmut_6': xǁFilesPageǁ_build_file_list__mutmut_6, 
        'xǁFilesPageǁ_build_file_list__mutmut_7': xǁFilesPageǁ_build_file_list__mutmut_7, 
        'xǁFilesPageǁ_build_file_list__mutmut_8': xǁFilesPageǁ_build_file_list__mutmut_8, 
        'xǁFilesPageǁ_build_file_list__mutmut_9': xǁFilesPageǁ_build_file_list__mutmut_9, 
        'xǁFilesPageǁ_build_file_list__mutmut_10': xǁFilesPageǁ_build_file_list__mutmut_10, 
        'xǁFilesPageǁ_build_file_list__mutmut_11': xǁFilesPageǁ_build_file_list__mutmut_11, 
        'xǁFilesPageǁ_build_file_list__mutmut_12': xǁFilesPageǁ_build_file_list__mutmut_12, 
        'xǁFilesPageǁ_build_file_list__mutmut_13': xǁFilesPageǁ_build_file_list__mutmut_13, 
        'xǁFilesPageǁ_build_file_list__mutmut_14': xǁFilesPageǁ_build_file_list__mutmut_14, 
        'xǁFilesPageǁ_build_file_list__mutmut_15': xǁFilesPageǁ_build_file_list__mutmut_15, 
        'xǁFilesPageǁ_build_file_list__mutmut_16': xǁFilesPageǁ_build_file_list__mutmut_16, 
        'xǁFilesPageǁ_build_file_list__mutmut_17': xǁFilesPageǁ_build_file_list__mutmut_17, 
        'xǁFilesPageǁ_build_file_list__mutmut_18': xǁFilesPageǁ_build_file_list__mutmut_18, 
        'xǁFilesPageǁ_build_file_list__mutmut_19': xǁFilesPageǁ_build_file_list__mutmut_19, 
        'xǁFilesPageǁ_build_file_list__mutmut_20': xǁFilesPageǁ_build_file_list__mutmut_20, 
        'xǁFilesPageǁ_build_file_list__mutmut_21': xǁFilesPageǁ_build_file_list__mutmut_21, 
        'xǁFilesPageǁ_build_file_list__mutmut_22': xǁFilesPageǁ_build_file_list__mutmut_22, 
        'xǁFilesPageǁ_build_file_list__mutmut_23': xǁFilesPageǁ_build_file_list__mutmut_23, 
        'xǁFilesPageǁ_build_file_list__mutmut_24': xǁFilesPageǁ_build_file_list__mutmut_24, 
        'xǁFilesPageǁ_build_file_list__mutmut_25': xǁFilesPageǁ_build_file_list__mutmut_25, 
        'xǁFilesPageǁ_build_file_list__mutmut_26': xǁFilesPageǁ_build_file_list__mutmut_26, 
        'xǁFilesPageǁ_build_file_list__mutmut_27': xǁFilesPageǁ_build_file_list__mutmut_27, 
        'xǁFilesPageǁ_build_file_list__mutmut_28': xǁFilesPageǁ_build_file_list__mutmut_28, 
        'xǁFilesPageǁ_build_file_list__mutmut_29': xǁFilesPageǁ_build_file_list__mutmut_29, 
        'xǁFilesPageǁ_build_file_list__mutmut_30': xǁFilesPageǁ_build_file_list__mutmut_30, 
        'xǁFilesPageǁ_build_file_list__mutmut_31': xǁFilesPageǁ_build_file_list__mutmut_31, 
        'xǁFilesPageǁ_build_file_list__mutmut_32': xǁFilesPageǁ_build_file_list__mutmut_32, 
        'xǁFilesPageǁ_build_file_list__mutmut_33': xǁFilesPageǁ_build_file_list__mutmut_33, 
        'xǁFilesPageǁ_build_file_list__mutmut_34': xǁFilesPageǁ_build_file_list__mutmut_34, 
        'xǁFilesPageǁ_build_file_list__mutmut_35': xǁFilesPageǁ_build_file_list__mutmut_35, 
        'xǁFilesPageǁ_build_file_list__mutmut_36': xǁFilesPageǁ_build_file_list__mutmut_36, 
        'xǁFilesPageǁ_build_file_list__mutmut_37': xǁFilesPageǁ_build_file_list__mutmut_37, 
        'xǁFilesPageǁ_build_file_list__mutmut_38': xǁFilesPageǁ_build_file_list__mutmut_38, 
        'xǁFilesPageǁ_build_file_list__mutmut_39': xǁFilesPageǁ_build_file_list__mutmut_39, 
        'xǁFilesPageǁ_build_file_list__mutmut_40': xǁFilesPageǁ_build_file_list__mutmut_40, 
        'xǁFilesPageǁ_build_file_list__mutmut_41': xǁFilesPageǁ_build_file_list__mutmut_41, 
        'xǁFilesPageǁ_build_file_list__mutmut_42': xǁFilesPageǁ_build_file_list__mutmut_42, 
        'xǁFilesPageǁ_build_file_list__mutmut_43': xǁFilesPageǁ_build_file_list__mutmut_43, 
        'xǁFilesPageǁ_build_file_list__mutmut_44': xǁFilesPageǁ_build_file_list__mutmut_44, 
        'xǁFilesPageǁ_build_file_list__mutmut_45': xǁFilesPageǁ_build_file_list__mutmut_45, 
        'xǁFilesPageǁ_build_file_list__mutmut_46': xǁFilesPageǁ_build_file_list__mutmut_46, 
        'xǁFilesPageǁ_build_file_list__mutmut_47': xǁFilesPageǁ_build_file_list__mutmut_47, 
        'xǁFilesPageǁ_build_file_list__mutmut_48': xǁFilesPageǁ_build_file_list__mutmut_48, 
        'xǁFilesPageǁ_build_file_list__mutmut_49': xǁFilesPageǁ_build_file_list__mutmut_49, 
        'xǁFilesPageǁ_build_file_list__mutmut_50': xǁFilesPageǁ_build_file_list__mutmut_50, 
        'xǁFilesPageǁ_build_file_list__mutmut_51': xǁFilesPageǁ_build_file_list__mutmut_51, 
        'xǁFilesPageǁ_build_file_list__mutmut_52': xǁFilesPageǁ_build_file_list__mutmut_52, 
        'xǁFilesPageǁ_build_file_list__mutmut_53': xǁFilesPageǁ_build_file_list__mutmut_53, 
        'xǁFilesPageǁ_build_file_list__mutmut_54': xǁFilesPageǁ_build_file_list__mutmut_54, 
        'xǁFilesPageǁ_build_file_list__mutmut_55': xǁFilesPageǁ_build_file_list__mutmut_55, 
        'xǁFilesPageǁ_build_file_list__mutmut_56': xǁFilesPageǁ_build_file_list__mutmut_56, 
        'xǁFilesPageǁ_build_file_list__mutmut_57': xǁFilesPageǁ_build_file_list__mutmut_57, 
        'xǁFilesPageǁ_build_file_list__mutmut_58': xǁFilesPageǁ_build_file_list__mutmut_58, 
        'xǁFilesPageǁ_build_file_list__mutmut_59': xǁFilesPageǁ_build_file_list__mutmut_59, 
        'xǁFilesPageǁ_build_file_list__mutmut_60': xǁFilesPageǁ_build_file_list__mutmut_60, 
        'xǁFilesPageǁ_build_file_list__mutmut_61': xǁFilesPageǁ_build_file_list__mutmut_61, 
        'xǁFilesPageǁ_build_file_list__mutmut_62': xǁFilesPageǁ_build_file_list__mutmut_62, 
        'xǁFilesPageǁ_build_file_list__mutmut_63': xǁFilesPageǁ_build_file_list__mutmut_63, 
        'xǁFilesPageǁ_build_file_list__mutmut_64': xǁFilesPageǁ_build_file_list__mutmut_64, 
        'xǁFilesPageǁ_build_file_list__mutmut_65': xǁFilesPageǁ_build_file_list__mutmut_65, 
        'xǁFilesPageǁ_build_file_list__mutmut_66': xǁFilesPageǁ_build_file_list__mutmut_66, 
        'xǁFilesPageǁ_build_file_list__mutmut_67': xǁFilesPageǁ_build_file_list__mutmut_67, 
        'xǁFilesPageǁ_build_file_list__mutmut_68': xǁFilesPageǁ_build_file_list__mutmut_68, 
        'xǁFilesPageǁ_build_file_list__mutmut_69': xǁFilesPageǁ_build_file_list__mutmut_69, 
        'xǁFilesPageǁ_build_file_list__mutmut_70': xǁFilesPageǁ_build_file_list__mutmut_70, 
        'xǁFilesPageǁ_build_file_list__mutmut_71': xǁFilesPageǁ_build_file_list__mutmut_71, 
        'xǁFilesPageǁ_build_file_list__mutmut_72': xǁFilesPageǁ_build_file_list__mutmut_72, 
        'xǁFilesPageǁ_build_file_list__mutmut_73': xǁFilesPageǁ_build_file_list__mutmut_73, 
        'xǁFilesPageǁ_build_file_list__mutmut_74': xǁFilesPageǁ_build_file_list__mutmut_74, 
        'xǁFilesPageǁ_build_file_list__mutmut_75': xǁFilesPageǁ_build_file_list__mutmut_75, 
        'xǁFilesPageǁ_build_file_list__mutmut_76': xǁFilesPageǁ_build_file_list__mutmut_76, 
        'xǁFilesPageǁ_build_file_list__mutmut_77': xǁFilesPageǁ_build_file_list__mutmut_77, 
        'xǁFilesPageǁ_build_file_list__mutmut_78': xǁFilesPageǁ_build_file_list__mutmut_78, 
        'xǁFilesPageǁ_build_file_list__mutmut_79': xǁFilesPageǁ_build_file_list__mutmut_79, 
        'xǁFilesPageǁ_build_file_list__mutmut_80': xǁFilesPageǁ_build_file_list__mutmut_80, 
        'xǁFilesPageǁ_build_file_list__mutmut_81': xǁFilesPageǁ_build_file_list__mutmut_81
    }
    xǁFilesPageǁ_build_file_list__mutmut_orig.__name__ = 'xǁFilesPageǁ_build_file_list'

    def _delayed_scrollbar_update(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_delayed_scrollbar_update__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_delayed_scrollbar_update__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_delayed_scrollbar_update__mutmut_orig(self) -> None:
        """Update scrollbar after model changes."""
        QtCore.QTimer.singleShot(10, self._setup_scrollbar)

    def xǁFilesPageǁ_delayed_scrollbar_update__mutmut_1(self) -> None:
        """Update scrollbar after model changes."""
        QtCore.QTimer.singleShot(None, self._setup_scrollbar)

    def xǁFilesPageǁ_delayed_scrollbar_update__mutmut_2(self) -> None:
        """Update scrollbar after model changes."""
        QtCore.QTimer.singleShot(10, None)

    def xǁFilesPageǁ_delayed_scrollbar_update__mutmut_3(self) -> None:
        """Update scrollbar after model changes."""
        QtCore.QTimer.singleShot(self._setup_scrollbar)

    def xǁFilesPageǁ_delayed_scrollbar_update__mutmut_4(self) -> None:
        """Update scrollbar after model changes."""
        QtCore.QTimer.singleShot(10, )

    def xǁFilesPageǁ_delayed_scrollbar_update__mutmut_5(self) -> None:
        """Update scrollbar after model changes."""
        QtCore.QTimer.singleShot(11, self._setup_scrollbar)
    
    xǁFilesPageǁ_delayed_scrollbar_update__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_delayed_scrollbar_update__mutmut_1': xǁFilesPageǁ_delayed_scrollbar_update__mutmut_1, 
        'xǁFilesPageǁ_delayed_scrollbar_update__mutmut_2': xǁFilesPageǁ_delayed_scrollbar_update__mutmut_2, 
        'xǁFilesPageǁ_delayed_scrollbar_update__mutmut_3': xǁFilesPageǁ_delayed_scrollbar_update__mutmut_3, 
        'xǁFilesPageǁ_delayed_scrollbar_update__mutmut_4': xǁFilesPageǁ_delayed_scrollbar_update__mutmut_4, 
        'xǁFilesPageǁ_delayed_scrollbar_update__mutmut_5': xǁFilesPageǁ_delayed_scrollbar_update__mutmut_5
    }
    xǁFilesPageǁ_delayed_scrollbar_update__mutmut_orig.__name__ = 'xǁFilesPageǁ_delayed_scrollbar_update'

    def _add_file_to_list(self, file_item: dict) -> None:
        args = [file_item]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_add_file_to_list__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_add_file_to_list__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_add_file_to_list__mutmut_orig(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_1(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = None
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_2(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get(None, file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_3(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", None)
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_4(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get(file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_5(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", )
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_6(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("XXfilenameXX", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_7(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("FILENAME", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_8(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get(None, ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_9(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", None))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_10(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get(""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_11(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_12(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("XXpathXX", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_13(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("PATH", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_14(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", "XXXX"))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_15(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename and not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_16(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_17(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_18(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(None):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_19(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.upper().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_20(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = None

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_21(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(None)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_22(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(None):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_23(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = None
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_24(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(None)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_25(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = None

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_26(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(None)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_27(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = None
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_28(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(None)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_29(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = None

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_30(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=None,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_31(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text=None,
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_32(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=None,
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_33(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=None,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_34(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=None,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_35(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=None,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_36(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=None,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_37(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=None,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_38(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=None,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_39(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_40(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_41(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_42(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_43(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_44(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_45(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_46(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_47(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_48(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_49(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_50(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="XXUnknown Filament - Unknown timeXX",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_51(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="unknown filament - unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_52(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="UNKNOWN FILAMENT - UNKNOWN TIME",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_53(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get(None),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_54(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("XXright_arrowXX"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_55(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("RIGHT_ARROW"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_56(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=True,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_57(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=True,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_58(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=True,
            )

        if item:
            self._model.add_item(item)

    def xǁFilesPageǁ_add_file_to_list__mutmut_59(self, file_item: dict) -> None:
        """Add a file entry to the list with basic info."""
        filename = file_item.get("filename", file_item.get("path", ""))
        if not filename or not filename.lower().endswith(self.GCODE_EXTENSION):
            return

        # Get display name
        display_name = self._get_display_name(filename)

        # Check if already in list
        if self._model_contains_item(display_name):
            return

        # Use cached metadata if available, otherwise show unknown
        full_path = self._build_filepath(filename)
        cached = self._files_data.get(full_path)

        if cached:
            item = self._create_file_list_item(cached)
        else:
            item = ListItem(
                text=display_name,
                right_text="Unknown Filament - Unknown time",
                right_icon=self._icons.get("right_arrow"),
                left_icon=None,
                callback=None,
                selected=False,
                allow_check=False,
                _lfontsize=self.LEFT_FONT_SIZE,
                _rfontsize=self.RIGHT_FONT_SIZE,
                height=self.ITEM_HEIGHT,
                notificate=False,
            )

        if item:
            self._model.add_item(None)
    
    xǁFilesPageǁ_add_file_to_list__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_add_file_to_list__mutmut_1': xǁFilesPageǁ_add_file_to_list__mutmut_1, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_2': xǁFilesPageǁ_add_file_to_list__mutmut_2, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_3': xǁFilesPageǁ_add_file_to_list__mutmut_3, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_4': xǁFilesPageǁ_add_file_to_list__mutmut_4, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_5': xǁFilesPageǁ_add_file_to_list__mutmut_5, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_6': xǁFilesPageǁ_add_file_to_list__mutmut_6, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_7': xǁFilesPageǁ_add_file_to_list__mutmut_7, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_8': xǁFilesPageǁ_add_file_to_list__mutmut_8, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_9': xǁFilesPageǁ_add_file_to_list__mutmut_9, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_10': xǁFilesPageǁ_add_file_to_list__mutmut_10, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_11': xǁFilesPageǁ_add_file_to_list__mutmut_11, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_12': xǁFilesPageǁ_add_file_to_list__mutmut_12, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_13': xǁFilesPageǁ_add_file_to_list__mutmut_13, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_14': xǁFilesPageǁ_add_file_to_list__mutmut_14, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_15': xǁFilesPageǁ_add_file_to_list__mutmut_15, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_16': xǁFilesPageǁ_add_file_to_list__mutmut_16, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_17': xǁFilesPageǁ_add_file_to_list__mutmut_17, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_18': xǁFilesPageǁ_add_file_to_list__mutmut_18, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_19': xǁFilesPageǁ_add_file_to_list__mutmut_19, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_20': xǁFilesPageǁ_add_file_to_list__mutmut_20, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_21': xǁFilesPageǁ_add_file_to_list__mutmut_21, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_22': xǁFilesPageǁ_add_file_to_list__mutmut_22, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_23': xǁFilesPageǁ_add_file_to_list__mutmut_23, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_24': xǁFilesPageǁ_add_file_to_list__mutmut_24, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_25': xǁFilesPageǁ_add_file_to_list__mutmut_25, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_26': xǁFilesPageǁ_add_file_to_list__mutmut_26, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_27': xǁFilesPageǁ_add_file_to_list__mutmut_27, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_28': xǁFilesPageǁ_add_file_to_list__mutmut_28, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_29': xǁFilesPageǁ_add_file_to_list__mutmut_29, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_30': xǁFilesPageǁ_add_file_to_list__mutmut_30, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_31': xǁFilesPageǁ_add_file_to_list__mutmut_31, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_32': xǁFilesPageǁ_add_file_to_list__mutmut_32, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_33': xǁFilesPageǁ_add_file_to_list__mutmut_33, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_34': xǁFilesPageǁ_add_file_to_list__mutmut_34, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_35': xǁFilesPageǁ_add_file_to_list__mutmut_35, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_36': xǁFilesPageǁ_add_file_to_list__mutmut_36, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_37': xǁFilesPageǁ_add_file_to_list__mutmut_37, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_38': xǁFilesPageǁ_add_file_to_list__mutmut_38, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_39': xǁFilesPageǁ_add_file_to_list__mutmut_39, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_40': xǁFilesPageǁ_add_file_to_list__mutmut_40, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_41': xǁFilesPageǁ_add_file_to_list__mutmut_41, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_42': xǁFilesPageǁ_add_file_to_list__mutmut_42, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_43': xǁFilesPageǁ_add_file_to_list__mutmut_43, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_44': xǁFilesPageǁ_add_file_to_list__mutmut_44, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_45': xǁFilesPageǁ_add_file_to_list__mutmut_45, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_46': xǁFilesPageǁ_add_file_to_list__mutmut_46, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_47': xǁFilesPageǁ_add_file_to_list__mutmut_47, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_48': xǁFilesPageǁ_add_file_to_list__mutmut_48, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_49': xǁFilesPageǁ_add_file_to_list__mutmut_49, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_50': xǁFilesPageǁ_add_file_to_list__mutmut_50, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_51': xǁFilesPageǁ_add_file_to_list__mutmut_51, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_52': xǁFilesPageǁ_add_file_to_list__mutmut_52, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_53': xǁFilesPageǁ_add_file_to_list__mutmut_53, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_54': xǁFilesPageǁ_add_file_to_list__mutmut_54, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_55': xǁFilesPageǁ_add_file_to_list__mutmut_55, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_56': xǁFilesPageǁ_add_file_to_list__mutmut_56, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_57': xǁFilesPageǁ_add_file_to_list__mutmut_57, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_58': xǁFilesPageǁ_add_file_to_list__mutmut_58, 
        'xǁFilesPageǁ_add_file_to_list__mutmut_59': xǁFilesPageǁ_add_file_to_list__mutmut_59
    }
    xǁFilesPageǁ_add_file_to_list__mutmut_orig.__name__ = 'xǁFilesPageǁ_add_file_to_list'

    def _create_file_list_item(self, filedata: dict) -> typing.Optional[ListItem]:
        args = [filedata]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_create_file_list_item__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_create_file_list_item__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_create_file_list_item__mutmut_orig(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_1(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = None
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_2(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get(None, "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_3(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", None)
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_4(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_5(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", )
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_6(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("XXfilenameXX", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_7(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("FILENAME", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_8(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "XXXX")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_9(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_10(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = None
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_11(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get(None, 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_12(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", None)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_13(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get(0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_14(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", )
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_15(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("XXestimated_timeXX", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_16(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("ESTIMATED_TIME", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_17(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 1)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_18(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = None
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_19(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(None) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_20(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 1
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_21(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = None

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_22(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(None)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_23(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = None
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_24(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get(None)
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_25(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("XXfilament_typeXX")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_26(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("FILAMENT_TYPE")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_27(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = None
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_28(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") or text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_29(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith(None) and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_30(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("XX[XX") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_31(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith(None):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_32(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("XX]XX"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_33(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = None
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_34(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(None)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_35(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = None
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_36(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = None
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_37(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = None

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_38(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type and []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_39(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_40(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = None

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_41(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = None

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_42(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(None)

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_43(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = "XX,XX".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_44(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(None))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_45(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 and filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_46(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type and filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_47(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_48(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type != -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_49(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == +1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_50(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -2.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_51(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type != "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_52(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "XXUnknownXX":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_53(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_54(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "UNKNOWN":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_55(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = None

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_56(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "XXUnknown FilamentXX"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_57(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "unknown filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_58(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "UNKNOWN FILAMENT"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_59(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = None

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_60(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(None)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_61(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=None,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_62(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=None,
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_63(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=None,
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_64(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_65(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=None,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_66(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=None,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_67(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=None,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_68(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=None,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_69(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=None,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_70(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_71(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_72(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_73(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_74(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_75(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_76(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_77(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_78(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_79(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_80(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            )

    def xǁFilesPageǁ_create_file_list_item__mutmut_81(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get(None),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_82(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("XXright_arrowXX"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_83(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("RIGHT_ARROW"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_84(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=True,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_85(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=True,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )

    def xǁFilesPageǁ_create_file_list_item__mutmut_86(self, filedata: dict) -> typing.Optional[ListItem]:
        """Create a ListItem from file metadata."""
        filename = filedata.get("filename", "")
        if not filename:
            return None

        # Format estimated time
        estimated_time = filedata.get("estimated_time", 0)
        seconds = int(estimated_time) if isinstance(estimated_time, (int, float)) else 0
        time_str = self._format_print_time(seconds)

        # Get filament type
        filament_type = filedata.get("filament_type")
        if isinstance(filament_type, str):
            text = filament_type.strip()
            if text.startswith("[") and text.endswith("]"):
                try:
                    types = json.loads(text)
                except json.JSONDecodeError:
                    types = [text]
            else:
                types = [text]
        else:
            types = filament_type or []

        if not isinstance(types, list):
            types = [types]

        filament_type = ",".join(dict.fromkeys(types))

        if not filament_type or filament_type == -1.0 or filament_type == "Unknown":
            filament_type = "Unknown Filament"

        display_name = self._get_display_name(filename)

        return ListItem(
            text=display_name,
            right_text=f"{filament_type} - {time_str}",
            right_icon=self._icons.get("right_arrow"),
            left_icon=None,  # Files have no left icon
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=True,
        )
    
    xǁFilesPageǁ_create_file_list_item__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_create_file_list_item__mutmut_1': xǁFilesPageǁ_create_file_list_item__mutmut_1, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_2': xǁFilesPageǁ_create_file_list_item__mutmut_2, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_3': xǁFilesPageǁ_create_file_list_item__mutmut_3, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_4': xǁFilesPageǁ_create_file_list_item__mutmut_4, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_5': xǁFilesPageǁ_create_file_list_item__mutmut_5, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_6': xǁFilesPageǁ_create_file_list_item__mutmut_6, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_7': xǁFilesPageǁ_create_file_list_item__mutmut_7, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_8': xǁFilesPageǁ_create_file_list_item__mutmut_8, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_9': xǁFilesPageǁ_create_file_list_item__mutmut_9, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_10': xǁFilesPageǁ_create_file_list_item__mutmut_10, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_11': xǁFilesPageǁ_create_file_list_item__mutmut_11, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_12': xǁFilesPageǁ_create_file_list_item__mutmut_12, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_13': xǁFilesPageǁ_create_file_list_item__mutmut_13, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_14': xǁFilesPageǁ_create_file_list_item__mutmut_14, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_15': xǁFilesPageǁ_create_file_list_item__mutmut_15, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_16': xǁFilesPageǁ_create_file_list_item__mutmut_16, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_17': xǁFilesPageǁ_create_file_list_item__mutmut_17, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_18': xǁFilesPageǁ_create_file_list_item__mutmut_18, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_19': xǁFilesPageǁ_create_file_list_item__mutmut_19, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_20': xǁFilesPageǁ_create_file_list_item__mutmut_20, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_21': xǁFilesPageǁ_create_file_list_item__mutmut_21, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_22': xǁFilesPageǁ_create_file_list_item__mutmut_22, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_23': xǁFilesPageǁ_create_file_list_item__mutmut_23, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_24': xǁFilesPageǁ_create_file_list_item__mutmut_24, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_25': xǁFilesPageǁ_create_file_list_item__mutmut_25, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_26': xǁFilesPageǁ_create_file_list_item__mutmut_26, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_27': xǁFilesPageǁ_create_file_list_item__mutmut_27, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_28': xǁFilesPageǁ_create_file_list_item__mutmut_28, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_29': xǁFilesPageǁ_create_file_list_item__mutmut_29, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_30': xǁFilesPageǁ_create_file_list_item__mutmut_30, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_31': xǁFilesPageǁ_create_file_list_item__mutmut_31, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_32': xǁFilesPageǁ_create_file_list_item__mutmut_32, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_33': xǁFilesPageǁ_create_file_list_item__mutmut_33, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_34': xǁFilesPageǁ_create_file_list_item__mutmut_34, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_35': xǁFilesPageǁ_create_file_list_item__mutmut_35, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_36': xǁFilesPageǁ_create_file_list_item__mutmut_36, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_37': xǁFilesPageǁ_create_file_list_item__mutmut_37, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_38': xǁFilesPageǁ_create_file_list_item__mutmut_38, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_39': xǁFilesPageǁ_create_file_list_item__mutmut_39, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_40': xǁFilesPageǁ_create_file_list_item__mutmut_40, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_41': xǁFilesPageǁ_create_file_list_item__mutmut_41, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_42': xǁFilesPageǁ_create_file_list_item__mutmut_42, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_43': xǁFilesPageǁ_create_file_list_item__mutmut_43, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_44': xǁFilesPageǁ_create_file_list_item__mutmut_44, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_45': xǁFilesPageǁ_create_file_list_item__mutmut_45, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_46': xǁFilesPageǁ_create_file_list_item__mutmut_46, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_47': xǁFilesPageǁ_create_file_list_item__mutmut_47, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_48': xǁFilesPageǁ_create_file_list_item__mutmut_48, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_49': xǁFilesPageǁ_create_file_list_item__mutmut_49, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_50': xǁFilesPageǁ_create_file_list_item__mutmut_50, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_51': xǁFilesPageǁ_create_file_list_item__mutmut_51, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_52': xǁFilesPageǁ_create_file_list_item__mutmut_52, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_53': xǁFilesPageǁ_create_file_list_item__mutmut_53, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_54': xǁFilesPageǁ_create_file_list_item__mutmut_54, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_55': xǁFilesPageǁ_create_file_list_item__mutmut_55, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_56': xǁFilesPageǁ_create_file_list_item__mutmut_56, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_57': xǁFilesPageǁ_create_file_list_item__mutmut_57, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_58': xǁFilesPageǁ_create_file_list_item__mutmut_58, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_59': xǁFilesPageǁ_create_file_list_item__mutmut_59, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_60': xǁFilesPageǁ_create_file_list_item__mutmut_60, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_61': xǁFilesPageǁ_create_file_list_item__mutmut_61, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_62': xǁFilesPageǁ_create_file_list_item__mutmut_62, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_63': xǁFilesPageǁ_create_file_list_item__mutmut_63, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_64': xǁFilesPageǁ_create_file_list_item__mutmut_64, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_65': xǁFilesPageǁ_create_file_list_item__mutmut_65, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_66': xǁFilesPageǁ_create_file_list_item__mutmut_66, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_67': xǁFilesPageǁ_create_file_list_item__mutmut_67, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_68': xǁFilesPageǁ_create_file_list_item__mutmut_68, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_69': xǁFilesPageǁ_create_file_list_item__mutmut_69, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_70': xǁFilesPageǁ_create_file_list_item__mutmut_70, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_71': xǁFilesPageǁ_create_file_list_item__mutmut_71, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_72': xǁFilesPageǁ_create_file_list_item__mutmut_72, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_73': xǁFilesPageǁ_create_file_list_item__mutmut_73, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_74': xǁFilesPageǁ_create_file_list_item__mutmut_74, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_75': xǁFilesPageǁ_create_file_list_item__mutmut_75, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_76': xǁFilesPageǁ_create_file_list_item__mutmut_76, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_77': xǁFilesPageǁ_create_file_list_item__mutmut_77, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_78': xǁFilesPageǁ_create_file_list_item__mutmut_78, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_79': xǁFilesPageǁ_create_file_list_item__mutmut_79, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_80': xǁFilesPageǁ_create_file_list_item__mutmut_80, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_81': xǁFilesPageǁ_create_file_list_item__mutmut_81, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_82': xǁFilesPageǁ_create_file_list_item__mutmut_82, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_83': xǁFilesPageǁ_create_file_list_item__mutmut_83, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_84': xǁFilesPageǁ_create_file_list_item__mutmut_84, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_85': xǁFilesPageǁ_create_file_list_item__mutmut_85, 
        'xǁFilesPageǁ_create_file_list_item__mutmut_86': xǁFilesPageǁ_create_file_list_item__mutmut_86
    }
    xǁFilesPageǁ_create_file_list_item__mutmut_orig.__name__ = 'xǁFilesPageǁ_create_file_list_item'

    def _add_directory_list_item(self, dir_data: dict) -> None:
        args = [dir_data]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_add_directory_list_item__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_add_directory_list_item__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_orig(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_1(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = None
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_2(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get(None, "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_3(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", None)
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_4(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_5(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", )
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_6(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("XXdirnameXX", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_7(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("DIRNAME", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_8(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "XXXX")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_9(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_10(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = None
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_11(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get(None)
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_12(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("XXfolderXX")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_13(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("FOLDER")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_14(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(None, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_15(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, None):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_16(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_17(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, ):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_18(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = None

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_19(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get(None)

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_20(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("XXusbXX")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_21(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("USB")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_22(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = None
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_23(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=None,
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_24(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=None,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_25(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text=None,
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_26(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=None,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_27(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=None,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_28(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=None,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_29(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=None,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_30(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=None,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_31(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_32(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_33(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_34(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_35(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_36(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_37(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_38(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_39(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_40(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_41(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(None),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_42(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="XXXX",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_43(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=True,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_44(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=True,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_directory_list_item__mutmut_45(self, dir_data: dict) -> None:
        """Add a directory entry to the list."""
        dir_name = dir_data.get("dirname", "")
        if not dir_name:
            return

        # Choose appropriate icon
        icon = self._icons.get("folder")
        if self._is_usb_directory(self._curr_dir, dir_name):
            icon = self._icons.get("usb")

        item = ListItem(
            text=str(dir_name),
            left_icon=icon,
            right_text="",
            right_icon=None,
            selected=False,
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
        )
        self._model.add_item(None)
    
    xǁFilesPageǁ_add_directory_list_item__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_add_directory_list_item__mutmut_1': xǁFilesPageǁ_add_directory_list_item__mutmut_1, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_2': xǁFilesPageǁ_add_directory_list_item__mutmut_2, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_3': xǁFilesPageǁ_add_directory_list_item__mutmut_3, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_4': xǁFilesPageǁ_add_directory_list_item__mutmut_4, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_5': xǁFilesPageǁ_add_directory_list_item__mutmut_5, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_6': xǁFilesPageǁ_add_directory_list_item__mutmut_6, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_7': xǁFilesPageǁ_add_directory_list_item__mutmut_7, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_8': xǁFilesPageǁ_add_directory_list_item__mutmut_8, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_9': xǁFilesPageǁ_add_directory_list_item__mutmut_9, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_10': xǁFilesPageǁ_add_directory_list_item__mutmut_10, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_11': xǁFilesPageǁ_add_directory_list_item__mutmut_11, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_12': xǁFilesPageǁ_add_directory_list_item__mutmut_12, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_13': xǁFilesPageǁ_add_directory_list_item__mutmut_13, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_14': xǁFilesPageǁ_add_directory_list_item__mutmut_14, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_15': xǁFilesPageǁ_add_directory_list_item__mutmut_15, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_16': xǁFilesPageǁ_add_directory_list_item__mutmut_16, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_17': xǁFilesPageǁ_add_directory_list_item__mutmut_17, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_18': xǁFilesPageǁ_add_directory_list_item__mutmut_18, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_19': xǁFilesPageǁ_add_directory_list_item__mutmut_19, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_20': xǁFilesPageǁ_add_directory_list_item__mutmut_20, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_21': xǁFilesPageǁ_add_directory_list_item__mutmut_21, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_22': xǁFilesPageǁ_add_directory_list_item__mutmut_22, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_23': xǁFilesPageǁ_add_directory_list_item__mutmut_23, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_24': xǁFilesPageǁ_add_directory_list_item__mutmut_24, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_25': xǁFilesPageǁ_add_directory_list_item__mutmut_25, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_26': xǁFilesPageǁ_add_directory_list_item__mutmut_26, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_27': xǁFilesPageǁ_add_directory_list_item__mutmut_27, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_28': xǁFilesPageǁ_add_directory_list_item__mutmut_28, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_29': xǁFilesPageǁ_add_directory_list_item__mutmut_29, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_30': xǁFilesPageǁ_add_directory_list_item__mutmut_30, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_31': xǁFilesPageǁ_add_directory_list_item__mutmut_31, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_32': xǁFilesPageǁ_add_directory_list_item__mutmut_32, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_33': xǁFilesPageǁ_add_directory_list_item__mutmut_33, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_34': xǁFilesPageǁ_add_directory_list_item__mutmut_34, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_35': xǁFilesPageǁ_add_directory_list_item__mutmut_35, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_36': xǁFilesPageǁ_add_directory_list_item__mutmut_36, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_37': xǁFilesPageǁ_add_directory_list_item__mutmut_37, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_38': xǁFilesPageǁ_add_directory_list_item__mutmut_38, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_39': xǁFilesPageǁ_add_directory_list_item__mutmut_39, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_40': xǁFilesPageǁ_add_directory_list_item__mutmut_40, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_41': xǁFilesPageǁ_add_directory_list_item__mutmut_41, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_42': xǁFilesPageǁ_add_directory_list_item__mutmut_42, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_43': xǁFilesPageǁ_add_directory_list_item__mutmut_43, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_44': xǁFilesPageǁ_add_directory_list_item__mutmut_44, 
        'xǁFilesPageǁ_add_directory_list_item__mutmut_45': xǁFilesPageǁ_add_directory_list_item__mutmut_45
    }
    xǁFilesPageǁ_add_directory_list_item__mutmut_orig.__name__ = 'xǁFilesPageǁ_add_directory_list_item'

    def _add_back_folder_entry(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_add_back_folder_entry__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_add_back_folder_entry__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_orig(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_1(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = None
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_2(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text=None,
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_3(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text=None,
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_4(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=None,
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_5(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_6(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=None,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_7(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=None,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_8(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=None,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_9(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=None,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_10(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=None,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_11(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_12(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_13(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_14(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_15(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_16(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_17(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_18(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_19(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_20(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_21(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_22(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="XXGo BackXX",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_23(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="go back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_24(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="GO BACK",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_25(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="XXXX",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_26(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get(None),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_27(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("XXback_folderXX"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_28(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("BACK_FOLDER"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_29(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=True,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_30(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=True,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_31(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=True,
        )
        self._model.add_item(item)

    def xǁFilesPageǁ_add_back_folder_entry__mutmut_32(self) -> None:
        """Add the 'Go Back' navigation entry."""
        item = ListItem(
            text="Go Back",
            right_text="",
            right_icon=None,
            left_icon=self._icons.get("back_folder"),
            callback=None,
            selected=False,
            allow_check=False,
            _lfontsize=self.LEFT_FONT_SIZE,
            _rfontsize=self.RIGHT_FONT_SIZE,
            height=self.ITEM_HEIGHT,
            notificate=False,
        )
        self._model.add_item(None)
    
    xǁFilesPageǁ_add_back_folder_entry__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_add_back_folder_entry__mutmut_1': xǁFilesPageǁ_add_back_folder_entry__mutmut_1, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_2': xǁFilesPageǁ_add_back_folder_entry__mutmut_2, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_3': xǁFilesPageǁ_add_back_folder_entry__mutmut_3, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_4': xǁFilesPageǁ_add_back_folder_entry__mutmut_4, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_5': xǁFilesPageǁ_add_back_folder_entry__mutmut_5, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_6': xǁFilesPageǁ_add_back_folder_entry__mutmut_6, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_7': xǁFilesPageǁ_add_back_folder_entry__mutmut_7, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_8': xǁFilesPageǁ_add_back_folder_entry__mutmut_8, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_9': xǁFilesPageǁ_add_back_folder_entry__mutmut_9, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_10': xǁFilesPageǁ_add_back_folder_entry__mutmut_10, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_11': xǁFilesPageǁ_add_back_folder_entry__mutmut_11, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_12': xǁFilesPageǁ_add_back_folder_entry__mutmut_12, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_13': xǁFilesPageǁ_add_back_folder_entry__mutmut_13, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_14': xǁFilesPageǁ_add_back_folder_entry__mutmut_14, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_15': xǁFilesPageǁ_add_back_folder_entry__mutmut_15, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_16': xǁFilesPageǁ_add_back_folder_entry__mutmut_16, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_17': xǁFilesPageǁ_add_back_folder_entry__mutmut_17, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_18': xǁFilesPageǁ_add_back_folder_entry__mutmut_18, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_19': xǁFilesPageǁ_add_back_folder_entry__mutmut_19, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_20': xǁFilesPageǁ_add_back_folder_entry__mutmut_20, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_21': xǁFilesPageǁ_add_back_folder_entry__mutmut_21, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_22': xǁFilesPageǁ_add_back_folder_entry__mutmut_22, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_23': xǁFilesPageǁ_add_back_folder_entry__mutmut_23, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_24': xǁFilesPageǁ_add_back_folder_entry__mutmut_24, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_25': xǁFilesPageǁ_add_back_folder_entry__mutmut_25, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_26': xǁFilesPageǁ_add_back_folder_entry__mutmut_26, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_27': xǁFilesPageǁ_add_back_folder_entry__mutmut_27, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_28': xǁFilesPageǁ_add_back_folder_entry__mutmut_28, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_29': xǁFilesPageǁ_add_back_folder_entry__mutmut_29, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_30': xǁFilesPageǁ_add_back_folder_entry__mutmut_30, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_31': xǁFilesPageǁ_add_back_folder_entry__mutmut_31, 
        'xǁFilesPageǁ_add_back_folder_entry__mutmut_32': xǁFilesPageǁ_add_back_folder_entry__mutmut_32
    }
    xǁFilesPageǁ_add_back_folder_entry__mutmut_orig.__name__ = 'xǁFilesPageǁ_add_back_folder_entry'

    def _request_file_info(self, file_data_item: dict) -> None:
        args = [file_data_item]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_request_file_info__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_request_file_info__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_request_file_info__mutmut_orig(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_1(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_2(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = None
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_3(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get(None, file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_4(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", None)
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_5(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get(file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_6(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", )
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_7(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("XXpathXX", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_8(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("PATH", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_9(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get(None, ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_10(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", None))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_11(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get(""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_12(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_13(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("XXfilenameXX", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_14(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("FILENAME", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_15(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", "XXXX"))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_16(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name and not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_17(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_18(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_19(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(None):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_20(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.upper().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_21(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = None

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_22(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(None)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_23(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(None)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(file_path)

    def xǁFilesPageǁ_request_file_info__mutmut_24(self, file_data_item: dict) -> None:
        """Request metadata for a file item using retry mechanism."""
        if not file_data_item:
            return

        name = file_data_item.get("path", file_data_item.get("filename", ""))
        if not name or not name.lower().endswith(self.GCODE_EXTENSION):
            return

        # Build full path
        file_path = self._build_filepath(name)

        # Track pending request
        self._pending_metadata_requests.add(file_path)

        # Use retry mechanism (first attempt uses get_gcode_metadata)
        self.retry_metadata_request(None)
    
    xǁFilesPageǁ_request_file_info__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_request_file_info__mutmut_1': xǁFilesPageǁ_request_file_info__mutmut_1, 
        'xǁFilesPageǁ_request_file_info__mutmut_2': xǁFilesPageǁ_request_file_info__mutmut_2, 
        'xǁFilesPageǁ_request_file_info__mutmut_3': xǁFilesPageǁ_request_file_info__mutmut_3, 
        'xǁFilesPageǁ_request_file_info__mutmut_4': xǁFilesPageǁ_request_file_info__mutmut_4, 
        'xǁFilesPageǁ_request_file_info__mutmut_5': xǁFilesPageǁ_request_file_info__mutmut_5, 
        'xǁFilesPageǁ_request_file_info__mutmut_6': xǁFilesPageǁ_request_file_info__mutmut_6, 
        'xǁFilesPageǁ_request_file_info__mutmut_7': xǁFilesPageǁ_request_file_info__mutmut_7, 
        'xǁFilesPageǁ_request_file_info__mutmut_8': xǁFilesPageǁ_request_file_info__mutmut_8, 
        'xǁFilesPageǁ_request_file_info__mutmut_9': xǁFilesPageǁ_request_file_info__mutmut_9, 
        'xǁFilesPageǁ_request_file_info__mutmut_10': xǁFilesPageǁ_request_file_info__mutmut_10, 
        'xǁFilesPageǁ_request_file_info__mutmut_11': xǁFilesPageǁ_request_file_info__mutmut_11, 
        'xǁFilesPageǁ_request_file_info__mutmut_12': xǁFilesPageǁ_request_file_info__mutmut_12, 
        'xǁFilesPageǁ_request_file_info__mutmut_13': xǁFilesPageǁ_request_file_info__mutmut_13, 
        'xǁFilesPageǁ_request_file_info__mutmut_14': xǁFilesPageǁ_request_file_info__mutmut_14, 
        'xǁFilesPageǁ_request_file_info__mutmut_15': xǁFilesPageǁ_request_file_info__mutmut_15, 
        'xǁFilesPageǁ_request_file_info__mutmut_16': xǁFilesPageǁ_request_file_info__mutmut_16, 
        'xǁFilesPageǁ_request_file_info__mutmut_17': xǁFilesPageǁ_request_file_info__mutmut_17, 
        'xǁFilesPageǁ_request_file_info__mutmut_18': xǁFilesPageǁ_request_file_info__mutmut_18, 
        'xǁFilesPageǁ_request_file_info__mutmut_19': xǁFilesPageǁ_request_file_info__mutmut_19, 
        'xǁFilesPageǁ_request_file_info__mutmut_20': xǁFilesPageǁ_request_file_info__mutmut_20, 
        'xǁFilesPageǁ_request_file_info__mutmut_21': xǁFilesPageǁ_request_file_info__mutmut_21, 
        'xǁFilesPageǁ_request_file_info__mutmut_22': xǁFilesPageǁ_request_file_info__mutmut_22, 
        'xǁFilesPageǁ_request_file_info__mutmut_23': xǁFilesPageǁ_request_file_info__mutmut_23, 
        'xǁFilesPageǁ_request_file_info__mutmut_24': xǁFilesPageǁ_request_file_info__mutmut_24
    }
    xǁFilesPageǁ_request_file_info__mutmut_orig.__name__ = 'xǁFilesPageǁ_request_file_info'

    def _on_file_item_clicked(self, filename: str) -> None:
        args = [filename]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_on_file_item_clicked__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_on_file_item_clicked__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_orig(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_1(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = None
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_2(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix(None)
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_3(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removesuffix("/")
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_4(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("XX/XX")
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_5(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = None
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_6(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(None, {})
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_7(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(clean_filename, None)
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_8(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get({})
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_9(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(clean_filename, )
        self.file_selected.emit(clean_filename, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_10(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(None, file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_11(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(clean_filename, None)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_12(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(file_data)

    def xǁFilesPageǁ_on_file_item_clicked__mutmut_13(self, filename: str) -> None:
        """Handle file item click."""
        clean_filename = filename.removeprefix("/")
        file_data = self._files_data.get(clean_filename, {})
        self.file_selected.emit(clean_filename, )
    
    xǁFilesPageǁ_on_file_item_clicked__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_on_file_item_clicked__mutmut_1': xǁFilesPageǁ_on_file_item_clicked__mutmut_1, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_2': xǁFilesPageǁ_on_file_item_clicked__mutmut_2, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_3': xǁFilesPageǁ_on_file_item_clicked__mutmut_3, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_4': xǁFilesPageǁ_on_file_item_clicked__mutmut_4, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_5': xǁFilesPageǁ_on_file_item_clicked__mutmut_5, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_6': xǁFilesPageǁ_on_file_item_clicked__mutmut_6, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_7': xǁFilesPageǁ_on_file_item_clicked__mutmut_7, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_8': xǁFilesPageǁ_on_file_item_clicked__mutmut_8, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_9': xǁFilesPageǁ_on_file_item_clicked__mutmut_9, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_10': xǁFilesPageǁ_on_file_item_clicked__mutmut_10, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_11': xǁFilesPageǁ_on_file_item_clicked__mutmut_11, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_12': xǁFilesPageǁ_on_file_item_clicked__mutmut_12, 
        'xǁFilesPageǁ_on_file_item_clicked__mutmut_13': xǁFilesPageǁ_on_file_item_clicked__mutmut_13
    }
    xǁFilesPageǁ_on_file_item_clicked__mutmut_orig.__name__ = 'xǁFilesPageǁ_on_file_item_clicked'

    def _on_dir_item_clicked(self, directory: str) -> None:
        args = [directory]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_on_dir_item_clicked__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_on_dir_item_clicked__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_on_dir_item_clicked__mutmut_orig(self, directory: str) -> None:
        """Handle directory item click."""
        if self._pending_action:
            return

        self._curr_dir = self._curr_dir + directory
        self.request_dir_info[str].emit(self._curr_dir)
        self._pending_action = True

    def xǁFilesPageǁ_on_dir_item_clicked__mutmut_1(self, directory: str) -> None:
        """Handle directory item click."""
        if self._pending_action:
            return

        self._curr_dir = None
        self.request_dir_info[str].emit(self._curr_dir)
        self._pending_action = True

    def xǁFilesPageǁ_on_dir_item_clicked__mutmut_2(self, directory: str) -> None:
        """Handle directory item click."""
        if self._pending_action:
            return

        self._curr_dir = self._curr_dir - directory
        self.request_dir_info[str].emit(self._curr_dir)
        self._pending_action = True

    def xǁFilesPageǁ_on_dir_item_clicked__mutmut_3(self, directory: str) -> None:
        """Handle directory item click."""
        if self._pending_action:
            return

        self._curr_dir = self._curr_dir + directory
        self.request_dir_info[str].emit(None)
        self._pending_action = True

    def xǁFilesPageǁ_on_dir_item_clicked__mutmut_4(self, directory: str) -> None:
        """Handle directory item click."""
        if self._pending_action:
            return

        self._curr_dir = self._curr_dir + directory
        self.request_dir_info[str].emit(self._curr_dir)
        self._pending_action = None

    def xǁFilesPageǁ_on_dir_item_clicked__mutmut_5(self, directory: str) -> None:
        """Handle directory item click."""
        if self._pending_action:
            return

        self._curr_dir = self._curr_dir + directory
        self.request_dir_info[str].emit(self._curr_dir)
        self._pending_action = False
    
    xǁFilesPageǁ_on_dir_item_clicked__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_on_dir_item_clicked__mutmut_1': xǁFilesPageǁ_on_dir_item_clicked__mutmut_1, 
        'xǁFilesPageǁ_on_dir_item_clicked__mutmut_2': xǁFilesPageǁ_on_dir_item_clicked__mutmut_2, 
        'xǁFilesPageǁ_on_dir_item_clicked__mutmut_3': xǁFilesPageǁ_on_dir_item_clicked__mutmut_3, 
        'xǁFilesPageǁ_on_dir_item_clicked__mutmut_4': xǁFilesPageǁ_on_dir_item_clicked__mutmut_4, 
        'xǁFilesPageǁ_on_dir_item_clicked__mutmut_5': xǁFilesPageǁ_on_dir_item_clicked__mutmut_5
    }
    xǁFilesPageǁ_on_dir_item_clicked__mutmut_orig.__name__ = 'xǁFilesPageǁ_on_dir_item_clicked'

    def _on_go_back_dir(self, directory: str) -> None:
        args = [directory]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_on_go_back_dir__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_on_go_back_dir__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_on_go_back_dir__mutmut_orig(self, directory: str) -> None:
        """Handle go back navigation."""
        self.request_dir_info[str].emit(directory)
        self._curr_dir = directory

    def xǁFilesPageǁ_on_go_back_dir__mutmut_1(self, directory: str) -> None:
        """Handle go back navigation."""
        self.request_dir_info[str].emit(None)
        self._curr_dir = directory

    def xǁFilesPageǁ_on_go_back_dir__mutmut_2(self, directory: str) -> None:
        """Handle go back navigation."""
        self.request_dir_info[str].emit(directory)
        self._curr_dir = None
    
    xǁFilesPageǁ_on_go_back_dir__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_on_go_back_dir__mutmut_1': xǁFilesPageǁ_on_go_back_dir__mutmut_1, 
        'xǁFilesPageǁ_on_go_back_dir__mutmut_2': xǁFilesPageǁ_on_go_back_dir__mutmut_2
    }
    xǁFilesPageǁ_on_go_back_dir__mutmut_orig.__name__ = 'xǁFilesPageǁ_on_go_back_dir'

    def _show_placeholder(self) -> None:
        """Show the 'No Files found' placeholder."""
        self._scrollbar.hide()
        self._list_widget.hide()
        self._label.show()

    def _hide_placeholder(self) -> None:
        """Hide the placeholder and show the list."""
        self._label.hide()
        self._list_widget.show()

    def _check_empty_state(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_check_empty_state__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_check_empty_state__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_check_empty_state__mutmut_orig(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_1(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = None

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_2(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir and self._curr_dir == "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_3(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_4(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir != "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_5(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "XX/XX"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_6(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 0 or is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_7(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() != 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_8(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 1 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_9(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 or not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_10(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() != 0 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_11(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 1 and not is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()

    def xǁFilesPageǁ_check_empty_state__mutmut_12(self) -> None:
        """Check if list is empty and show placeholder if needed."""
        is_root = not self._curr_dir or self._curr_dir == "/"

        if self._model.rowCount() == 0 and is_root:
            self._show_placeholder()
        elif self._model.rowCount() == 0 and is_root:
            # In subdirectory with no files - just show "Go Back"
            self._add_back_folder_entry()
    
    xǁFilesPageǁ_check_empty_state__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_check_empty_state__mutmut_1': xǁFilesPageǁ_check_empty_state__mutmut_1, 
        'xǁFilesPageǁ_check_empty_state__mutmut_2': xǁFilesPageǁ_check_empty_state__mutmut_2, 
        'xǁFilesPageǁ_check_empty_state__mutmut_3': xǁFilesPageǁ_check_empty_state__mutmut_3, 
        'xǁFilesPageǁ_check_empty_state__mutmut_4': xǁFilesPageǁ_check_empty_state__mutmut_4, 
        'xǁFilesPageǁ_check_empty_state__mutmut_5': xǁFilesPageǁ_check_empty_state__mutmut_5, 
        'xǁFilesPageǁ_check_empty_state__mutmut_6': xǁFilesPageǁ_check_empty_state__mutmut_6, 
        'xǁFilesPageǁ_check_empty_state__mutmut_7': xǁFilesPageǁ_check_empty_state__mutmut_7, 
        'xǁFilesPageǁ_check_empty_state__mutmut_8': xǁFilesPageǁ_check_empty_state__mutmut_8, 
        'xǁFilesPageǁ_check_empty_state__mutmut_9': xǁFilesPageǁ_check_empty_state__mutmut_9, 
        'xǁFilesPageǁ_check_empty_state__mutmut_10': xǁFilesPageǁ_check_empty_state__mutmut_10, 
        'xǁFilesPageǁ_check_empty_state__mutmut_11': xǁFilesPageǁ_check_empty_state__mutmut_11, 
        'xǁFilesPageǁ_check_empty_state__mutmut_12': xǁFilesPageǁ_check_empty_state__mutmut_12
    }
    xǁFilesPageǁ_check_empty_state__mutmut_orig.__name__ = 'xǁFilesPageǁ_check_empty_state'

    def _model_contains_item(self, text: str) -> bool:
        args = [text]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_model_contains_item__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_model_contains_item__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_model_contains_item__mutmut_orig(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_1(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(None):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_2(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = None
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_3(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(None)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_4(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.rindex(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_5(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = None
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_6(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(None, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_7(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, None)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_8(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_9(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, )
            if item and item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_10(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item or item.text == text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_11(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text != text:
                return True
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_12(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return False
        return False

    def xǁFilesPageǁ_model_contains_item__mutmut_13(self, text: str) -> bool:
        """Check if model contains an item with the given text."""
        for i in range(self._model.rowCount()):
            index = self._model.index(i)
            item = self._model.data(index, QtCore.Qt.ItemDataRole.UserRole)
            if item and item.text == text:
                return True
        return True
    
    xǁFilesPageǁ_model_contains_item__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_model_contains_item__mutmut_1': xǁFilesPageǁ_model_contains_item__mutmut_1, 
        'xǁFilesPageǁ_model_contains_item__mutmut_2': xǁFilesPageǁ_model_contains_item__mutmut_2, 
        'xǁFilesPageǁ_model_contains_item__mutmut_3': xǁFilesPageǁ_model_contains_item__mutmut_3, 
        'xǁFilesPageǁ_model_contains_item__mutmut_4': xǁFilesPageǁ_model_contains_item__mutmut_4, 
        'xǁFilesPageǁ_model_contains_item__mutmut_5': xǁFilesPageǁ_model_contains_item__mutmut_5, 
        'xǁFilesPageǁ_model_contains_item__mutmut_6': xǁFilesPageǁ_model_contains_item__mutmut_6, 
        'xǁFilesPageǁ_model_contains_item__mutmut_7': xǁFilesPageǁ_model_contains_item__mutmut_7, 
        'xǁFilesPageǁ_model_contains_item__mutmut_8': xǁFilesPageǁ_model_contains_item__mutmut_8, 
        'xǁFilesPageǁ_model_contains_item__mutmut_9': xǁFilesPageǁ_model_contains_item__mutmut_9, 
        'xǁFilesPageǁ_model_contains_item__mutmut_10': xǁFilesPageǁ_model_contains_item__mutmut_10, 
        'xǁFilesPageǁ_model_contains_item__mutmut_11': xǁFilesPageǁ_model_contains_item__mutmut_11, 
        'xǁFilesPageǁ_model_contains_item__mutmut_12': xǁFilesPageǁ_model_contains_item__mutmut_12, 
        'xǁFilesPageǁ_model_contains_item__mutmut_13': xǁFilesPageǁ_model_contains_item__mutmut_13
    }
    xǁFilesPageǁ_model_contains_item__mutmut_orig.__name__ = 'xǁFilesPageǁ_model_contains_item'

    def _handle_scrollbar_value_changed(self, value: int) -> None:
        args = [value]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_orig(self, value: int) -> None:
        """Sync scrollbar with list widget."""
        self._scrollbar.blockSignals(True)
        self._scrollbar.setValue(value)
        self._scrollbar.blockSignals(False)

    def xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_1(self, value: int) -> None:
        """Sync scrollbar with list widget."""
        self._scrollbar.blockSignals(None)
        self._scrollbar.setValue(value)
        self._scrollbar.blockSignals(False)

    def xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_2(self, value: int) -> None:
        """Sync scrollbar with list widget."""
        self._scrollbar.blockSignals(False)
        self._scrollbar.setValue(value)
        self._scrollbar.blockSignals(False)

    def xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_3(self, value: int) -> None:
        """Sync scrollbar with list widget."""
        self._scrollbar.blockSignals(True)
        self._scrollbar.setValue(None)
        self._scrollbar.blockSignals(False)

    def xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_4(self, value: int) -> None:
        """Sync scrollbar with list widget."""
        self._scrollbar.blockSignals(True)
        self._scrollbar.setValue(value)
        self._scrollbar.blockSignals(None)

    def xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_5(self, value: int) -> None:
        """Sync scrollbar with list widget."""
        self._scrollbar.blockSignals(True)
        self._scrollbar.setValue(value)
        self._scrollbar.blockSignals(True)
    
    xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_1': xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_1, 
        'xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_2': xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_2, 
        'xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_3': xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_3, 
        'xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_4': xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_4, 
        'xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_5': xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_5
    }
    xǁFilesPageǁ_handle_scrollbar_value_changed__mutmut_orig.__name__ = 'xǁFilesPageǁ_handle_scrollbar_value_changed'

    def _setup_scrollbar(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_setup_scrollbar__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_setup_scrollbar__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_setup_scrollbar__mutmut_orig(self) -> None:
        """Configure scrollbar to match list size."""
        list_scrollbar = self._list_widget.verticalScrollBar()
        self._scrollbar.setMinimum(list_scrollbar.minimum())
        self._scrollbar.setMaximum(list_scrollbar.maximum())
        self._scrollbar.setPageStep(list_scrollbar.pageStep())

        if list_scrollbar.maximum() > 0:
            self._scrollbar.show()
        else:
            self._scrollbar.hide()

    def xǁFilesPageǁ_setup_scrollbar__mutmut_1(self) -> None:
        """Configure scrollbar to match list size."""
        list_scrollbar = None
        self._scrollbar.setMinimum(list_scrollbar.minimum())
        self._scrollbar.setMaximum(list_scrollbar.maximum())
        self._scrollbar.setPageStep(list_scrollbar.pageStep())

        if list_scrollbar.maximum() > 0:
            self._scrollbar.show()
        else:
            self._scrollbar.hide()

    def xǁFilesPageǁ_setup_scrollbar__mutmut_2(self) -> None:
        """Configure scrollbar to match list size."""
        list_scrollbar = self._list_widget.verticalScrollBar()
        self._scrollbar.setMinimum(None)
        self._scrollbar.setMaximum(list_scrollbar.maximum())
        self._scrollbar.setPageStep(list_scrollbar.pageStep())

        if list_scrollbar.maximum() > 0:
            self._scrollbar.show()
        else:
            self._scrollbar.hide()

    def xǁFilesPageǁ_setup_scrollbar__mutmut_3(self) -> None:
        """Configure scrollbar to match list size."""
        list_scrollbar = self._list_widget.verticalScrollBar()
        self._scrollbar.setMinimum(list_scrollbar.minimum())
        self._scrollbar.setMaximum(None)
        self._scrollbar.setPageStep(list_scrollbar.pageStep())

        if list_scrollbar.maximum() > 0:
            self._scrollbar.show()
        else:
            self._scrollbar.hide()

    def xǁFilesPageǁ_setup_scrollbar__mutmut_4(self) -> None:
        """Configure scrollbar to match list size."""
        list_scrollbar = self._list_widget.verticalScrollBar()
        self._scrollbar.setMinimum(list_scrollbar.minimum())
        self._scrollbar.setMaximum(list_scrollbar.maximum())
        self._scrollbar.setPageStep(None)

        if list_scrollbar.maximum() > 0:
            self._scrollbar.show()
        else:
            self._scrollbar.hide()

    def xǁFilesPageǁ_setup_scrollbar__mutmut_5(self) -> None:
        """Configure scrollbar to match list size."""
        list_scrollbar = self._list_widget.verticalScrollBar()
        self._scrollbar.setMinimum(list_scrollbar.minimum())
        self._scrollbar.setMaximum(list_scrollbar.maximum())
        self._scrollbar.setPageStep(list_scrollbar.pageStep())

        if list_scrollbar.maximum() >= 0:
            self._scrollbar.show()
        else:
            self._scrollbar.hide()

    def xǁFilesPageǁ_setup_scrollbar__mutmut_6(self) -> None:
        """Configure scrollbar to match list size."""
        list_scrollbar = self._list_widget.verticalScrollBar()
        self._scrollbar.setMinimum(list_scrollbar.minimum())
        self._scrollbar.setMaximum(list_scrollbar.maximum())
        self._scrollbar.setPageStep(list_scrollbar.pageStep())

        if list_scrollbar.maximum() > 1:
            self._scrollbar.show()
        else:
            self._scrollbar.hide()
    
    xǁFilesPageǁ_setup_scrollbar__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_setup_scrollbar__mutmut_1': xǁFilesPageǁ_setup_scrollbar__mutmut_1, 
        'xǁFilesPageǁ_setup_scrollbar__mutmut_2': xǁFilesPageǁ_setup_scrollbar__mutmut_2, 
        'xǁFilesPageǁ_setup_scrollbar__mutmut_3': xǁFilesPageǁ_setup_scrollbar__mutmut_3, 
        'xǁFilesPageǁ_setup_scrollbar__mutmut_4': xǁFilesPageǁ_setup_scrollbar__mutmut_4, 
        'xǁFilesPageǁ_setup_scrollbar__mutmut_5': xǁFilesPageǁ_setup_scrollbar__mutmut_5, 
        'xǁFilesPageǁ_setup_scrollbar__mutmut_6': xǁFilesPageǁ_setup_scrollbar__mutmut_6
    }
    xǁFilesPageǁ_setup_scrollbar__mutmut_orig.__name__ = 'xǁFilesPageǁ_setup_scrollbar'

    @staticmethod
    def _get_basename(path: str) -> str:
        """
        Get the basename of a path without using os.path.
        Works with paths from Moonraker (forward slashes only).
        """
        if not path:
            return ""
        # Remove trailing slashes and get last component
        path = path.rstrip("/")
        if "/" in path:
            return path.rsplit("/", 1)[-1]
        return path

    @staticmethod
    def _get_parent_directory(path: str) -> str:
        """
        Get the parent directory of a path without using os.path.
        Works with paths from Moonraker (forward slashes only).
        """
        if not path:
            return ""
        path = path.removeprefix("/").rstrip("/")
        if "/" in path:
            return path.rsplit("/", 1)[0]
        return ""

    def _build_filepath(self, filename: str) -> str:
        args = [filename]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_build_filepath__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_build_filepath__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_build_filepath__mutmut_orig(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removeprefix("/")
        if self._curr_dir:
            curr = self._curr_dir.removeprefix("/")
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_1(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = None
        if self._curr_dir:
            curr = self._curr_dir.removeprefix("/")
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_2(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removeprefix(None)
        if self._curr_dir:
            curr = self._curr_dir.removeprefix("/")
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_3(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removesuffix("/")
        if self._curr_dir:
            curr = self._curr_dir.removeprefix("/")
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_4(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removeprefix("XX/XX")
        if self._curr_dir:
            curr = self._curr_dir.removeprefix("/")
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_5(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removeprefix("/")
        if self._curr_dir:
            curr = None
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_6(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removeprefix("/")
        if self._curr_dir:
            curr = self._curr_dir.removeprefix(None)
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_7(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removeprefix("/")
        if self._curr_dir:
            curr = self._curr_dir.removesuffix("/")
            return f"{curr}/{filename}"
        return filename

    def xǁFilesPageǁ_build_filepath__mutmut_8(self, filename: str) -> str:
        """Build full file path from current directory and filename."""
        filename = filename.removeprefix("/")
        if self._curr_dir:
            curr = self._curr_dir.removeprefix("XX/XX")
            return f"{curr}/{filename}"
        return filename
    
    xǁFilesPageǁ_build_filepath__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_build_filepath__mutmut_1': xǁFilesPageǁ_build_filepath__mutmut_1, 
        'xǁFilesPageǁ_build_filepath__mutmut_2': xǁFilesPageǁ_build_filepath__mutmut_2, 
        'xǁFilesPageǁ_build_filepath__mutmut_3': xǁFilesPageǁ_build_filepath__mutmut_3, 
        'xǁFilesPageǁ_build_filepath__mutmut_4': xǁFilesPageǁ_build_filepath__mutmut_4, 
        'xǁFilesPageǁ_build_filepath__mutmut_5': xǁFilesPageǁ_build_filepath__mutmut_5, 
        'xǁFilesPageǁ_build_filepath__mutmut_6': xǁFilesPageǁ_build_filepath__mutmut_6, 
        'xǁFilesPageǁ_build_filepath__mutmut_7': xǁFilesPageǁ_build_filepath__mutmut_7, 
        'xǁFilesPageǁ_build_filepath__mutmut_8': xǁFilesPageǁ_build_filepath__mutmut_8
    }
    xǁFilesPageǁ_build_filepath__mutmut_orig.__name__ = 'xǁFilesPageǁ_build_filepath'

    @staticmethod
    def _is_usb_directory(parent_dir: str, directory_name: str) -> bool:
        """Check if directory is a USB mount in the root."""
        return parent_dir == "" and directory_name.startswith("USB-")

    @staticmethod
    def _format_print_time(seconds: int) -> str:
        """Format print time in human-readable form."""
        if seconds <= 0:
            return "Unknown time"
        if seconds < 60:
            return f"{seconds}s"

        days, hours, minutes, _ = helper_methods.estimate_print_time(seconds)

        if days > 0:
            return f"{days}d {hours}h {minutes}m"
        elif hours > 0:
            return f"{hours}h {minutes}m"
        else:
            return f"{minutes}m"

    def _get_display_name(self, filename: str) -> str:
        args = [filename]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_get_display_name__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_get_display_name__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_get_display_name__mutmut_orig(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = name[:-6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_1(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = None
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = name[:-6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_2(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(None)
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = name[:-6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_3(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = None

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = name[:-6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_4(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = helper_methods.get_file_name(None)

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = name[:-6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_5(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.lower().endswith(None):
            name = name[:-6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_6(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.upper().endswith(self.GCODE_EXTENSION):
            name = name[:-6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_7(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = None

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_8(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = name[:+6]

        return name

    def xǁFilesPageǁ_get_display_name__mutmut_9(self, filename: str) -> str:
        """Get display name from filename (without path and extension)."""
        basename = self._get_basename(filename)
        name = helper_methods.get_file_name(basename)

        # Remove .gcode extension
        if name.lower().endswith(self.GCODE_EXTENSION):
            name = name[:-7]

        return name
    
    xǁFilesPageǁ_get_display_name__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_get_display_name__mutmut_1': xǁFilesPageǁ_get_display_name__mutmut_1, 
        'xǁFilesPageǁ_get_display_name__mutmut_2': xǁFilesPageǁ_get_display_name__mutmut_2, 
        'xǁFilesPageǁ_get_display_name__mutmut_3': xǁFilesPageǁ_get_display_name__mutmut_3, 
        'xǁFilesPageǁ_get_display_name__mutmut_4': xǁFilesPageǁ_get_display_name__mutmut_4, 
        'xǁFilesPageǁ_get_display_name__mutmut_5': xǁFilesPageǁ_get_display_name__mutmut_5, 
        'xǁFilesPageǁ_get_display_name__mutmut_6': xǁFilesPageǁ_get_display_name__mutmut_6, 
        'xǁFilesPageǁ_get_display_name__mutmut_7': xǁFilesPageǁ_get_display_name__mutmut_7, 
        'xǁFilesPageǁ_get_display_name__mutmut_8': xǁFilesPageǁ_get_display_name__mutmut_8, 
        'xǁFilesPageǁ_get_display_name__mutmut_9': xǁFilesPageǁ_get_display_name__mutmut_9
    }
    xǁFilesPageǁ_get_display_name__mutmut_orig.__name__ = 'xǁFilesPageǁ_get_display_name'

    def _load_icons(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_load_icons__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_load_icons__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_load_icons__mutmut_orig(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_1(self) -> None:
        """Load all icons into cache."""
        self._icons = None

    def xǁFilesPageǁ_load_icons__mutmut_2(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "XXback_folderXX": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_3(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "BACK_FOLDER": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_4(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(None),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_5(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["XXback_folderXX"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_6(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["BACK_FOLDER"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_7(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "XXfolderXX": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_8(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "FOLDER": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_9(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(None),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_10(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["XXfolderXX"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_11(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["FOLDER"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_12(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "XXright_arrowXX": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_13(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "RIGHT_ARROW": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_14(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(None),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_15(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["XXright_arrowXX"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_16(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["RIGHT_ARROW"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_17(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "XXusbXX": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_18(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "USB": QtGui.QPixmap(self.ICON_PATHS["usb"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_19(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(None),
        }

    def xǁFilesPageǁ_load_icons__mutmut_20(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["XXusbXX"]),
        }

    def xǁFilesPageǁ_load_icons__mutmut_21(self) -> None:
        """Load all icons into cache."""
        self._icons = {
            "back_folder": QtGui.QPixmap(self.ICON_PATHS["back_folder"]),
            "folder": QtGui.QPixmap(self.ICON_PATHS["folder"]),
            "right_arrow": QtGui.QPixmap(self.ICON_PATHS["right_arrow"]),
            "usb": QtGui.QPixmap(self.ICON_PATHS["USB"]),
        }
    
    xǁFilesPageǁ_load_icons__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_load_icons__mutmut_1': xǁFilesPageǁ_load_icons__mutmut_1, 
        'xǁFilesPageǁ_load_icons__mutmut_2': xǁFilesPageǁ_load_icons__mutmut_2, 
        'xǁFilesPageǁ_load_icons__mutmut_3': xǁFilesPageǁ_load_icons__mutmut_3, 
        'xǁFilesPageǁ_load_icons__mutmut_4': xǁFilesPageǁ_load_icons__mutmut_4, 
        'xǁFilesPageǁ_load_icons__mutmut_5': xǁFilesPageǁ_load_icons__mutmut_5, 
        'xǁFilesPageǁ_load_icons__mutmut_6': xǁFilesPageǁ_load_icons__mutmut_6, 
        'xǁFilesPageǁ_load_icons__mutmut_7': xǁFilesPageǁ_load_icons__mutmut_7, 
        'xǁFilesPageǁ_load_icons__mutmut_8': xǁFilesPageǁ_load_icons__mutmut_8, 
        'xǁFilesPageǁ_load_icons__mutmut_9': xǁFilesPageǁ_load_icons__mutmut_9, 
        'xǁFilesPageǁ_load_icons__mutmut_10': xǁFilesPageǁ_load_icons__mutmut_10, 
        'xǁFilesPageǁ_load_icons__mutmut_11': xǁFilesPageǁ_load_icons__mutmut_11, 
        'xǁFilesPageǁ_load_icons__mutmut_12': xǁFilesPageǁ_load_icons__mutmut_12, 
        'xǁFilesPageǁ_load_icons__mutmut_13': xǁFilesPageǁ_load_icons__mutmut_13, 
        'xǁFilesPageǁ_load_icons__mutmut_14': xǁFilesPageǁ_load_icons__mutmut_14, 
        'xǁFilesPageǁ_load_icons__mutmut_15': xǁFilesPageǁ_load_icons__mutmut_15, 
        'xǁFilesPageǁ_load_icons__mutmut_16': xǁFilesPageǁ_load_icons__mutmut_16, 
        'xǁFilesPageǁ_load_icons__mutmut_17': xǁFilesPageǁ_load_icons__mutmut_17, 
        'xǁFilesPageǁ_load_icons__mutmut_18': xǁFilesPageǁ_load_icons__mutmut_18, 
        'xǁFilesPageǁ_load_icons__mutmut_19': xǁFilesPageǁ_load_icons__mutmut_19, 
        'xǁFilesPageǁ_load_icons__mutmut_20': xǁFilesPageǁ_load_icons__mutmut_20, 
        'xǁFilesPageǁ_load_icons__mutmut_21': xǁFilesPageǁ_load_icons__mutmut_21
    }
    xǁFilesPageǁ_load_icons__mutmut_orig.__name__ = 'xǁFilesPageǁ_load_icons'

    def _connect_signals(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_connect_signals__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_connect_signals__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_connect_signals__mutmut_orig(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_1(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            None
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_2(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: None
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_3(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(None)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_4(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(None)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_5(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            None
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_6(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(None)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_7(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            None
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_8(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: None
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_9(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(None)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(self._on_item_selected)

    def xǁFilesPageǁ_connect_signals__mutmut_10(self) -> None:
        """Connect internal signals."""
        # Button connections
        self._reload_button.clicked.connect(
            lambda: self.request_dir_info[str].emit(self._curr_dir)
        )
        self.back_btn.clicked.connect(self.reset_dir)

        # List widget connections
        self._list_widget.verticalScrollBar().valueChanged.connect(
            self._handle_scrollbar_value_changed
        )
        self._scrollbar.valueChanged.connect(self._handle_scrollbar_value_changed)
        self._scrollbar.valueChanged.connect(
            lambda value: self._list_widget.verticalScrollBar().setValue(value)
        )

        # Delegate connections
        self._entry_delegate.item_selected.connect(None)
    
    xǁFilesPageǁ_connect_signals__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_connect_signals__mutmut_1': xǁFilesPageǁ_connect_signals__mutmut_1, 
        'xǁFilesPageǁ_connect_signals__mutmut_2': xǁFilesPageǁ_connect_signals__mutmut_2, 
        'xǁFilesPageǁ_connect_signals__mutmut_3': xǁFilesPageǁ_connect_signals__mutmut_3, 
        'xǁFilesPageǁ_connect_signals__mutmut_4': xǁFilesPageǁ_connect_signals__mutmut_4, 
        'xǁFilesPageǁ_connect_signals__mutmut_5': xǁFilesPageǁ_connect_signals__mutmut_5, 
        'xǁFilesPageǁ_connect_signals__mutmut_6': xǁFilesPageǁ_connect_signals__mutmut_6, 
        'xǁFilesPageǁ_connect_signals__mutmut_7': xǁFilesPageǁ_connect_signals__mutmut_7, 
        'xǁFilesPageǁ_connect_signals__mutmut_8': xǁFilesPageǁ_connect_signals__mutmut_8, 
        'xǁFilesPageǁ_connect_signals__mutmut_9': xǁFilesPageǁ_connect_signals__mutmut_9, 
        'xǁFilesPageǁ_connect_signals__mutmut_10': xǁFilesPageǁ_connect_signals__mutmut_10
    }
    xǁFilesPageǁ_connect_signals__mutmut_orig.__name__ = 'xǁFilesPageǁ_connect_signals'

    def _setup_ui(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_setup_ui__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_setup_ui__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_setup_ui__mutmut_orig(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_1(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = None
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_2(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            None,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_3(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            None,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_4(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_5(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_6(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(None)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_7(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(2)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_8(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(None)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_9(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(2)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_10(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(None)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_11(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(None)

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_12(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(None, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_13(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, None))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_14(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_15(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, ))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_16(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(711, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_17(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 401))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_18(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = None
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_19(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(None)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_20(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(None)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_21(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(None)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_22(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(None)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_23(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(True)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_24(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet(None)

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_25(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("XX#file_page { background-color: transparent; }XX")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_26(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#FILE_PAGE { BACKGROUND-COLOR: TRANSPARENT; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_27(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = None
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_28(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(None)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_29(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName(None)

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_30(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("XXmain_layoutXX")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_31(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("MAIN_LAYOUT")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_32(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = None
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_33(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(None)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_34(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = None
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_35(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=None)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_36(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(None)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_37(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(None)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_38(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(None)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_39(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = None
        main_layout.addLayout(content_layout)

    def xǁFilesPageǁ_setup_ui__mutmut_40(self) -> None:
        """Set up the widget UI."""
        # Size policy
        size_policy = QtWidgets.QSizePolicy(
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
            QtWidgets.QSizePolicy.Policy.MinimumExpanding,
        )
        size_policy.setHorizontalStretch(1)
        size_policy.setVerticalStretch(1)
        self.setSizePolicy(size_policy)
        self.setMinimumSize(QtCore.QSize(710, 400))

        # Font
        font = QtGui.QFont()
        font.setStyleStrategy(QtGui.QFont.StyleStrategy.PreferAntialias)
        self.setFont(font)

        # Layout direction and style
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.setAutoFillBackground(False)
        self.setStyleSheet("#file_page { background-color: transparent; }")

        # Main layout
        main_layout = QtWidgets.QVBoxLayout(self)
        main_layout.setObjectName("main_layout")

        # Header layout
        header_layout = self._create_header_layout()
        main_layout.addLayout(header_layout)

        # Separator line
        line = QtWidgets.QFrame(parent=self)
        line.setFrameShape(QtWidgets.QFrame.Shape.HLine)
        line.setFrameShadow(QtWidgets.QFrame.Shadow.Sunken)
        main_layout.addWidget(line)

        # Content layout
        content_layout = self._create_content_layout()
        main_layout.addLayout(None)
    
    xǁFilesPageǁ_setup_ui__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_setup_ui__mutmut_1': xǁFilesPageǁ_setup_ui__mutmut_1, 
        'xǁFilesPageǁ_setup_ui__mutmut_2': xǁFilesPageǁ_setup_ui__mutmut_2, 
        'xǁFilesPageǁ_setup_ui__mutmut_3': xǁFilesPageǁ_setup_ui__mutmut_3, 
        'xǁFilesPageǁ_setup_ui__mutmut_4': xǁFilesPageǁ_setup_ui__mutmut_4, 
        'xǁFilesPageǁ_setup_ui__mutmut_5': xǁFilesPageǁ_setup_ui__mutmut_5, 
        'xǁFilesPageǁ_setup_ui__mutmut_6': xǁFilesPageǁ_setup_ui__mutmut_6, 
        'xǁFilesPageǁ_setup_ui__mutmut_7': xǁFilesPageǁ_setup_ui__mutmut_7, 
        'xǁFilesPageǁ_setup_ui__mutmut_8': xǁFilesPageǁ_setup_ui__mutmut_8, 
        'xǁFilesPageǁ_setup_ui__mutmut_9': xǁFilesPageǁ_setup_ui__mutmut_9, 
        'xǁFilesPageǁ_setup_ui__mutmut_10': xǁFilesPageǁ_setup_ui__mutmut_10, 
        'xǁFilesPageǁ_setup_ui__mutmut_11': xǁFilesPageǁ_setup_ui__mutmut_11, 
        'xǁFilesPageǁ_setup_ui__mutmut_12': xǁFilesPageǁ_setup_ui__mutmut_12, 
        'xǁFilesPageǁ_setup_ui__mutmut_13': xǁFilesPageǁ_setup_ui__mutmut_13, 
        'xǁFilesPageǁ_setup_ui__mutmut_14': xǁFilesPageǁ_setup_ui__mutmut_14, 
        'xǁFilesPageǁ_setup_ui__mutmut_15': xǁFilesPageǁ_setup_ui__mutmut_15, 
        'xǁFilesPageǁ_setup_ui__mutmut_16': xǁFilesPageǁ_setup_ui__mutmut_16, 
        'xǁFilesPageǁ_setup_ui__mutmut_17': xǁFilesPageǁ_setup_ui__mutmut_17, 
        'xǁFilesPageǁ_setup_ui__mutmut_18': xǁFilesPageǁ_setup_ui__mutmut_18, 
        'xǁFilesPageǁ_setup_ui__mutmut_19': xǁFilesPageǁ_setup_ui__mutmut_19, 
        'xǁFilesPageǁ_setup_ui__mutmut_20': xǁFilesPageǁ_setup_ui__mutmut_20, 
        'xǁFilesPageǁ_setup_ui__mutmut_21': xǁFilesPageǁ_setup_ui__mutmut_21, 
        'xǁFilesPageǁ_setup_ui__mutmut_22': xǁFilesPageǁ_setup_ui__mutmut_22, 
        'xǁFilesPageǁ_setup_ui__mutmut_23': xǁFilesPageǁ_setup_ui__mutmut_23, 
        'xǁFilesPageǁ_setup_ui__mutmut_24': xǁFilesPageǁ_setup_ui__mutmut_24, 
        'xǁFilesPageǁ_setup_ui__mutmut_25': xǁFilesPageǁ_setup_ui__mutmut_25, 
        'xǁFilesPageǁ_setup_ui__mutmut_26': xǁFilesPageǁ_setup_ui__mutmut_26, 
        'xǁFilesPageǁ_setup_ui__mutmut_27': xǁFilesPageǁ_setup_ui__mutmut_27, 
        'xǁFilesPageǁ_setup_ui__mutmut_28': xǁFilesPageǁ_setup_ui__mutmut_28, 
        'xǁFilesPageǁ_setup_ui__mutmut_29': xǁFilesPageǁ_setup_ui__mutmut_29, 
        'xǁFilesPageǁ_setup_ui__mutmut_30': xǁFilesPageǁ_setup_ui__mutmut_30, 
        'xǁFilesPageǁ_setup_ui__mutmut_31': xǁFilesPageǁ_setup_ui__mutmut_31, 
        'xǁFilesPageǁ_setup_ui__mutmut_32': xǁFilesPageǁ_setup_ui__mutmut_32, 
        'xǁFilesPageǁ_setup_ui__mutmut_33': xǁFilesPageǁ_setup_ui__mutmut_33, 
        'xǁFilesPageǁ_setup_ui__mutmut_34': xǁFilesPageǁ_setup_ui__mutmut_34, 
        'xǁFilesPageǁ_setup_ui__mutmut_35': xǁFilesPageǁ_setup_ui__mutmut_35, 
        'xǁFilesPageǁ_setup_ui__mutmut_36': xǁFilesPageǁ_setup_ui__mutmut_36, 
        'xǁFilesPageǁ_setup_ui__mutmut_37': xǁFilesPageǁ_setup_ui__mutmut_37, 
        'xǁFilesPageǁ_setup_ui__mutmut_38': xǁFilesPageǁ_setup_ui__mutmut_38, 
        'xǁFilesPageǁ_setup_ui__mutmut_39': xǁFilesPageǁ_setup_ui__mutmut_39, 
        'xǁFilesPageǁ_setup_ui__mutmut_40': xǁFilesPageǁ_setup_ui__mutmut_40
    }
    xǁFilesPageǁ_setup_ui__mutmut_orig.__name__ = 'xǁFilesPageǁ_setup_ui'

    def _create_header_layout(self) -> QtWidgets.QHBoxLayout:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_create_header_layout__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_create_header_layout__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_create_header_layout__mutmut_orig(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_1(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = None
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_2(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName(None)

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_3(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("XXheader_layoutXX")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_4(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("HEADER_LAYOUT")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_5(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = None
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_6(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=None)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_7(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(None)
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_8(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(None, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_9(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, None))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_10(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_11(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, ))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_12(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(61, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_13(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 61))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_14(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(None)
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_15(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(None, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_16(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, None))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_17(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_18(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, ))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_19(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(61, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_20(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 61))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_21(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(None)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_22(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(False)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_23(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty(None, QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_24(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", None)
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_25(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty(QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_26(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", )
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_27(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("XXicon_pixmapXX", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_28(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("ICON_PIXMAP", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_29(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(None))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_30(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["XXbackXX"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_31(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["BACK"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_32(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName(None)
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_33(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("XXback_btnXX")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_34(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("BACK_BTN")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_35(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(None, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_36(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, None, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_37(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, None)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_38(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_39(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_40(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, )

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_41(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 1, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_42(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = None
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_43(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=None)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_44(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(None)
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_45(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(None, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_46(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, None))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_47(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_48(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, ))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_49(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(61, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_50(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 61))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_51(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(None)
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_52(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(None, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_53(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, None))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_54(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_55(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, ))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_56(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(61, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_57(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 61))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_58(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(None)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_59(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(False)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_60(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            None, QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_61(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", None
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_62(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_63(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_64(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "XXicon_pixmapXX", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_65(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "ICON_PIXMAP", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_66(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(None)
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_67(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["XXrefreshXX"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_68(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["REFRESH"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_69(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName(None)
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_70(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("XXreload_buttonXX")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_71(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("RELOAD_BUTTON")
        layout.addWidget(self._reload_button, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_72(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(None, 0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_73(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, None, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_74(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, None)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_75(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(0, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_76(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_77(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 0, )

        return layout

    def xǁFilesPageǁ_create_header_layout__mutmut_78(self) -> QtWidgets.QHBoxLayout:
        """Create the header with back and reload buttons."""
        layout = QtWidgets.QHBoxLayout()
        layout.setObjectName("header_layout")

        # Back button
        self.back_btn = IconButton(parent=self)
        self.back_btn.setMinimumSize(QtCore.QSize(60, 60))
        self.back_btn.setMaximumSize(QtCore.QSize(60, 60))
        self.back_btn.setFlat(True)
        self.back_btn.setProperty("icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["back"]))
        self.back_btn.setObjectName("back_btn")
        layout.addWidget(self.back_btn, 0, QtCore.Qt.AlignmentFlag.AlignLeft)

        # Reload button
        self._reload_button = IconButton(parent=self)
        self._reload_button.setMinimumSize(QtCore.QSize(60, 60))
        self._reload_button.setMaximumSize(QtCore.QSize(60, 60))
        self._reload_button.setFlat(True)
        self._reload_button.setProperty(
            "icon_pixmap", QtGui.QPixmap(self.ICON_PATHS["refresh"])
        )
        self._reload_button.setObjectName("reload_button")
        layout.addWidget(self._reload_button, 1, QtCore.Qt.AlignmentFlag.AlignRight)

        return layout
    
    xǁFilesPageǁ_create_header_layout__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_create_header_layout__mutmut_1': xǁFilesPageǁ_create_header_layout__mutmut_1, 
        'xǁFilesPageǁ_create_header_layout__mutmut_2': xǁFilesPageǁ_create_header_layout__mutmut_2, 
        'xǁFilesPageǁ_create_header_layout__mutmut_3': xǁFilesPageǁ_create_header_layout__mutmut_3, 
        'xǁFilesPageǁ_create_header_layout__mutmut_4': xǁFilesPageǁ_create_header_layout__mutmut_4, 
        'xǁFilesPageǁ_create_header_layout__mutmut_5': xǁFilesPageǁ_create_header_layout__mutmut_5, 
        'xǁFilesPageǁ_create_header_layout__mutmut_6': xǁFilesPageǁ_create_header_layout__mutmut_6, 
        'xǁFilesPageǁ_create_header_layout__mutmut_7': xǁFilesPageǁ_create_header_layout__mutmut_7, 
        'xǁFilesPageǁ_create_header_layout__mutmut_8': xǁFilesPageǁ_create_header_layout__mutmut_8, 
        'xǁFilesPageǁ_create_header_layout__mutmut_9': xǁFilesPageǁ_create_header_layout__mutmut_9, 
        'xǁFilesPageǁ_create_header_layout__mutmut_10': xǁFilesPageǁ_create_header_layout__mutmut_10, 
        'xǁFilesPageǁ_create_header_layout__mutmut_11': xǁFilesPageǁ_create_header_layout__mutmut_11, 
        'xǁFilesPageǁ_create_header_layout__mutmut_12': xǁFilesPageǁ_create_header_layout__mutmut_12, 
        'xǁFilesPageǁ_create_header_layout__mutmut_13': xǁFilesPageǁ_create_header_layout__mutmut_13, 
        'xǁFilesPageǁ_create_header_layout__mutmut_14': xǁFilesPageǁ_create_header_layout__mutmut_14, 
        'xǁFilesPageǁ_create_header_layout__mutmut_15': xǁFilesPageǁ_create_header_layout__mutmut_15, 
        'xǁFilesPageǁ_create_header_layout__mutmut_16': xǁFilesPageǁ_create_header_layout__mutmut_16, 
        'xǁFilesPageǁ_create_header_layout__mutmut_17': xǁFilesPageǁ_create_header_layout__mutmut_17, 
        'xǁFilesPageǁ_create_header_layout__mutmut_18': xǁFilesPageǁ_create_header_layout__mutmut_18, 
        'xǁFilesPageǁ_create_header_layout__mutmut_19': xǁFilesPageǁ_create_header_layout__mutmut_19, 
        'xǁFilesPageǁ_create_header_layout__mutmut_20': xǁFilesPageǁ_create_header_layout__mutmut_20, 
        'xǁFilesPageǁ_create_header_layout__mutmut_21': xǁFilesPageǁ_create_header_layout__mutmut_21, 
        'xǁFilesPageǁ_create_header_layout__mutmut_22': xǁFilesPageǁ_create_header_layout__mutmut_22, 
        'xǁFilesPageǁ_create_header_layout__mutmut_23': xǁFilesPageǁ_create_header_layout__mutmut_23, 
        'xǁFilesPageǁ_create_header_layout__mutmut_24': xǁFilesPageǁ_create_header_layout__mutmut_24, 
        'xǁFilesPageǁ_create_header_layout__mutmut_25': xǁFilesPageǁ_create_header_layout__mutmut_25, 
        'xǁFilesPageǁ_create_header_layout__mutmut_26': xǁFilesPageǁ_create_header_layout__mutmut_26, 
        'xǁFilesPageǁ_create_header_layout__mutmut_27': xǁFilesPageǁ_create_header_layout__mutmut_27, 
        'xǁFilesPageǁ_create_header_layout__mutmut_28': xǁFilesPageǁ_create_header_layout__mutmut_28, 
        'xǁFilesPageǁ_create_header_layout__mutmut_29': xǁFilesPageǁ_create_header_layout__mutmut_29, 
        'xǁFilesPageǁ_create_header_layout__mutmut_30': xǁFilesPageǁ_create_header_layout__mutmut_30, 
        'xǁFilesPageǁ_create_header_layout__mutmut_31': xǁFilesPageǁ_create_header_layout__mutmut_31, 
        'xǁFilesPageǁ_create_header_layout__mutmut_32': xǁFilesPageǁ_create_header_layout__mutmut_32, 
        'xǁFilesPageǁ_create_header_layout__mutmut_33': xǁFilesPageǁ_create_header_layout__mutmut_33, 
        'xǁFilesPageǁ_create_header_layout__mutmut_34': xǁFilesPageǁ_create_header_layout__mutmut_34, 
        'xǁFilesPageǁ_create_header_layout__mutmut_35': xǁFilesPageǁ_create_header_layout__mutmut_35, 
        'xǁFilesPageǁ_create_header_layout__mutmut_36': xǁFilesPageǁ_create_header_layout__mutmut_36, 
        'xǁFilesPageǁ_create_header_layout__mutmut_37': xǁFilesPageǁ_create_header_layout__mutmut_37, 
        'xǁFilesPageǁ_create_header_layout__mutmut_38': xǁFilesPageǁ_create_header_layout__mutmut_38, 
        'xǁFilesPageǁ_create_header_layout__mutmut_39': xǁFilesPageǁ_create_header_layout__mutmut_39, 
        'xǁFilesPageǁ_create_header_layout__mutmut_40': xǁFilesPageǁ_create_header_layout__mutmut_40, 
        'xǁFilesPageǁ_create_header_layout__mutmut_41': xǁFilesPageǁ_create_header_layout__mutmut_41, 
        'xǁFilesPageǁ_create_header_layout__mutmut_42': xǁFilesPageǁ_create_header_layout__mutmut_42, 
        'xǁFilesPageǁ_create_header_layout__mutmut_43': xǁFilesPageǁ_create_header_layout__mutmut_43, 
        'xǁFilesPageǁ_create_header_layout__mutmut_44': xǁFilesPageǁ_create_header_layout__mutmut_44, 
        'xǁFilesPageǁ_create_header_layout__mutmut_45': xǁFilesPageǁ_create_header_layout__mutmut_45, 
        'xǁFilesPageǁ_create_header_layout__mutmut_46': xǁFilesPageǁ_create_header_layout__mutmut_46, 
        'xǁFilesPageǁ_create_header_layout__mutmut_47': xǁFilesPageǁ_create_header_layout__mutmut_47, 
        'xǁFilesPageǁ_create_header_layout__mutmut_48': xǁFilesPageǁ_create_header_layout__mutmut_48, 
        'xǁFilesPageǁ_create_header_layout__mutmut_49': xǁFilesPageǁ_create_header_layout__mutmut_49, 
        'xǁFilesPageǁ_create_header_layout__mutmut_50': xǁFilesPageǁ_create_header_layout__mutmut_50, 
        'xǁFilesPageǁ_create_header_layout__mutmut_51': xǁFilesPageǁ_create_header_layout__mutmut_51, 
        'xǁFilesPageǁ_create_header_layout__mutmut_52': xǁFilesPageǁ_create_header_layout__mutmut_52, 
        'xǁFilesPageǁ_create_header_layout__mutmut_53': xǁFilesPageǁ_create_header_layout__mutmut_53, 
        'xǁFilesPageǁ_create_header_layout__mutmut_54': xǁFilesPageǁ_create_header_layout__mutmut_54, 
        'xǁFilesPageǁ_create_header_layout__mutmut_55': xǁFilesPageǁ_create_header_layout__mutmut_55, 
        'xǁFilesPageǁ_create_header_layout__mutmut_56': xǁFilesPageǁ_create_header_layout__mutmut_56, 
        'xǁFilesPageǁ_create_header_layout__mutmut_57': xǁFilesPageǁ_create_header_layout__mutmut_57, 
        'xǁFilesPageǁ_create_header_layout__mutmut_58': xǁFilesPageǁ_create_header_layout__mutmut_58, 
        'xǁFilesPageǁ_create_header_layout__mutmut_59': xǁFilesPageǁ_create_header_layout__mutmut_59, 
        'xǁFilesPageǁ_create_header_layout__mutmut_60': xǁFilesPageǁ_create_header_layout__mutmut_60, 
        'xǁFilesPageǁ_create_header_layout__mutmut_61': xǁFilesPageǁ_create_header_layout__mutmut_61, 
        'xǁFilesPageǁ_create_header_layout__mutmut_62': xǁFilesPageǁ_create_header_layout__mutmut_62, 
        'xǁFilesPageǁ_create_header_layout__mutmut_63': xǁFilesPageǁ_create_header_layout__mutmut_63, 
        'xǁFilesPageǁ_create_header_layout__mutmut_64': xǁFilesPageǁ_create_header_layout__mutmut_64, 
        'xǁFilesPageǁ_create_header_layout__mutmut_65': xǁFilesPageǁ_create_header_layout__mutmut_65, 
        'xǁFilesPageǁ_create_header_layout__mutmut_66': xǁFilesPageǁ_create_header_layout__mutmut_66, 
        'xǁFilesPageǁ_create_header_layout__mutmut_67': xǁFilesPageǁ_create_header_layout__mutmut_67, 
        'xǁFilesPageǁ_create_header_layout__mutmut_68': xǁFilesPageǁ_create_header_layout__mutmut_68, 
        'xǁFilesPageǁ_create_header_layout__mutmut_69': xǁFilesPageǁ_create_header_layout__mutmut_69, 
        'xǁFilesPageǁ_create_header_layout__mutmut_70': xǁFilesPageǁ_create_header_layout__mutmut_70, 
        'xǁFilesPageǁ_create_header_layout__mutmut_71': xǁFilesPageǁ_create_header_layout__mutmut_71, 
        'xǁFilesPageǁ_create_header_layout__mutmut_72': xǁFilesPageǁ_create_header_layout__mutmut_72, 
        'xǁFilesPageǁ_create_header_layout__mutmut_73': xǁFilesPageǁ_create_header_layout__mutmut_73, 
        'xǁFilesPageǁ_create_header_layout__mutmut_74': xǁFilesPageǁ_create_header_layout__mutmut_74, 
        'xǁFilesPageǁ_create_header_layout__mutmut_75': xǁFilesPageǁ_create_header_layout__mutmut_75, 
        'xǁFilesPageǁ_create_header_layout__mutmut_76': xǁFilesPageǁ_create_header_layout__mutmut_76, 
        'xǁFilesPageǁ_create_header_layout__mutmut_77': xǁFilesPageǁ_create_header_layout__mutmut_77, 
        'xǁFilesPageǁ_create_header_layout__mutmut_78': xǁFilesPageǁ_create_header_layout__mutmut_78
    }
    xǁFilesPageǁ_create_header_layout__mutmut_orig.__name__ = 'xǁFilesPageǁ_create_header_layout'

    def _create_content_layout(self) -> QtWidgets.QHBoxLayout:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_create_content_layout__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_create_content_layout__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_create_content_layout__mutmut_orig(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_1(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = None
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_2(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(None, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_3(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, None, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_4(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, None, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_5(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, None)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_6(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_7(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_8(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_9(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, )
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_10(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(1, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_11(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 1, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_12(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 1, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_13(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 1)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_14(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName(None)

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_15(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("XXcontent_layoutXX")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_16(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("CONTENT_LAYOUT")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_17(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = None
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_18(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(None)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_19(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(26)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_20(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = None
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_21(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel(None)
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_22(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("XXNo Files foundXX")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_23(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("no files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_24(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("NO FILES FOUND")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_25(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(None)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_26(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet(None)
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_27(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("XXcolor: gray;XX")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_28(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("COLOR: GRAY;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_29(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = None

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_30(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = None
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_31(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            None,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_32(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=None,
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_33(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_34(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_35(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter & QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_36(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(None)
        layout.addWidget(self._scrollbar)

        return layout

    def xǁFilesPageǁ_create_content_layout__mutmut_37(self) -> QtWidgets.QHBoxLayout:
        """Create the content area with list and scrollbar."""
        layout = QtWidgets.QHBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setObjectName("content_layout")

        # Placeholder label
        font = QtGui.QFont()
        font.setPointSize(25)
        self._label = QtWidgets.QLabel("No Files found")
        self._label.setFont(font)
        self._label.setStyleSheet("color: gray;")
        self._label.hide()

        # List widget
        self._list_widget = self._create_list_widget()

        # Scrollbar
        self._scrollbar = CustomScrollBar()
        self._scrollbar.show()

        # Add widgets to layout
        layout.addWidget(
            self._label,
            alignment=(
                QtCore.Qt.AlignmentFlag.AlignHCenter
                | QtCore.Qt.AlignmentFlag.AlignVCenter
            ),
        )
        layout.addWidget(self._list_widget)
        layout.addWidget(None)

        return layout
    
    xǁFilesPageǁ_create_content_layout__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_create_content_layout__mutmut_1': xǁFilesPageǁ_create_content_layout__mutmut_1, 
        'xǁFilesPageǁ_create_content_layout__mutmut_2': xǁFilesPageǁ_create_content_layout__mutmut_2, 
        'xǁFilesPageǁ_create_content_layout__mutmut_3': xǁFilesPageǁ_create_content_layout__mutmut_3, 
        'xǁFilesPageǁ_create_content_layout__mutmut_4': xǁFilesPageǁ_create_content_layout__mutmut_4, 
        'xǁFilesPageǁ_create_content_layout__mutmut_5': xǁFilesPageǁ_create_content_layout__mutmut_5, 
        'xǁFilesPageǁ_create_content_layout__mutmut_6': xǁFilesPageǁ_create_content_layout__mutmut_6, 
        'xǁFilesPageǁ_create_content_layout__mutmut_7': xǁFilesPageǁ_create_content_layout__mutmut_7, 
        'xǁFilesPageǁ_create_content_layout__mutmut_8': xǁFilesPageǁ_create_content_layout__mutmut_8, 
        'xǁFilesPageǁ_create_content_layout__mutmut_9': xǁFilesPageǁ_create_content_layout__mutmut_9, 
        'xǁFilesPageǁ_create_content_layout__mutmut_10': xǁFilesPageǁ_create_content_layout__mutmut_10, 
        'xǁFilesPageǁ_create_content_layout__mutmut_11': xǁFilesPageǁ_create_content_layout__mutmut_11, 
        'xǁFilesPageǁ_create_content_layout__mutmut_12': xǁFilesPageǁ_create_content_layout__mutmut_12, 
        'xǁFilesPageǁ_create_content_layout__mutmut_13': xǁFilesPageǁ_create_content_layout__mutmut_13, 
        'xǁFilesPageǁ_create_content_layout__mutmut_14': xǁFilesPageǁ_create_content_layout__mutmut_14, 
        'xǁFilesPageǁ_create_content_layout__mutmut_15': xǁFilesPageǁ_create_content_layout__mutmut_15, 
        'xǁFilesPageǁ_create_content_layout__mutmut_16': xǁFilesPageǁ_create_content_layout__mutmut_16, 
        'xǁFilesPageǁ_create_content_layout__mutmut_17': xǁFilesPageǁ_create_content_layout__mutmut_17, 
        'xǁFilesPageǁ_create_content_layout__mutmut_18': xǁFilesPageǁ_create_content_layout__mutmut_18, 
        'xǁFilesPageǁ_create_content_layout__mutmut_19': xǁFilesPageǁ_create_content_layout__mutmut_19, 
        'xǁFilesPageǁ_create_content_layout__mutmut_20': xǁFilesPageǁ_create_content_layout__mutmut_20, 
        'xǁFilesPageǁ_create_content_layout__mutmut_21': xǁFilesPageǁ_create_content_layout__mutmut_21, 
        'xǁFilesPageǁ_create_content_layout__mutmut_22': xǁFilesPageǁ_create_content_layout__mutmut_22, 
        'xǁFilesPageǁ_create_content_layout__mutmut_23': xǁFilesPageǁ_create_content_layout__mutmut_23, 
        'xǁFilesPageǁ_create_content_layout__mutmut_24': xǁFilesPageǁ_create_content_layout__mutmut_24, 
        'xǁFilesPageǁ_create_content_layout__mutmut_25': xǁFilesPageǁ_create_content_layout__mutmut_25, 
        'xǁFilesPageǁ_create_content_layout__mutmut_26': xǁFilesPageǁ_create_content_layout__mutmut_26, 
        'xǁFilesPageǁ_create_content_layout__mutmut_27': xǁFilesPageǁ_create_content_layout__mutmut_27, 
        'xǁFilesPageǁ_create_content_layout__mutmut_28': xǁFilesPageǁ_create_content_layout__mutmut_28, 
        'xǁFilesPageǁ_create_content_layout__mutmut_29': xǁFilesPageǁ_create_content_layout__mutmut_29, 
        'xǁFilesPageǁ_create_content_layout__mutmut_30': xǁFilesPageǁ_create_content_layout__mutmut_30, 
        'xǁFilesPageǁ_create_content_layout__mutmut_31': xǁFilesPageǁ_create_content_layout__mutmut_31, 
        'xǁFilesPageǁ_create_content_layout__mutmut_32': xǁFilesPageǁ_create_content_layout__mutmut_32, 
        'xǁFilesPageǁ_create_content_layout__mutmut_33': xǁFilesPageǁ_create_content_layout__mutmut_33, 
        'xǁFilesPageǁ_create_content_layout__mutmut_34': xǁFilesPageǁ_create_content_layout__mutmut_34, 
        'xǁFilesPageǁ_create_content_layout__mutmut_35': xǁFilesPageǁ_create_content_layout__mutmut_35, 
        'xǁFilesPageǁ_create_content_layout__mutmut_36': xǁFilesPageǁ_create_content_layout__mutmut_36, 
        'xǁFilesPageǁ_create_content_layout__mutmut_37': xǁFilesPageǁ_create_content_layout__mutmut_37
    }
    xǁFilesPageǁ_create_content_layout__mutmut_orig.__name__ = 'xǁFilesPageǁ_create_content_layout'

    def _create_list_widget(self) -> QtWidgets.QListView:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁFilesPageǁ_create_list_widget__mutmut_orig'), object.__getattribute__(self, 'xǁFilesPageǁ_create_list_widget__mutmut_mutants'), args, kwargs, self)

    def xǁFilesPageǁ_create_list_widget__mutmut_orig(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_1(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = None
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_2(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=None)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_3(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(None)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_4(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(None)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_5(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(None)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_6(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(6)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_7(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty(None, False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_8(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", None)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_9(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty(False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_10(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", )
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_11(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("XXshowDropIndicatorXX", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_12(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showdropindicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_13(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("SHOWDROPINDICATOR", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_14(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", True)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_15(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty(None, "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_16(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", None)
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_17(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_18(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", )
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_19(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("XXselectionModeXX", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_20(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionmode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_21(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("SELECTIONMODE", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_22(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "XXNoSelectionXX")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_23(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "noselection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_24(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NOSELECTION")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_25(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet(None)
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_26(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("XXbackground: transparent;XX")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_27(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("BACKGROUND: TRANSPARENT;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_28(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(None)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_29(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(None)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_30(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(False)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_31(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName(None)
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_32(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("XXlist_widgetXX")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_33(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("LIST_WIDGET")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_34(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(None)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_35(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            None
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_36(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            None
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_37(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            None
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_38(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            None
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_39(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            None
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_40(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            None,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_41(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            None,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_42(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_43(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_44(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            None,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_45(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            None,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_46(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_47(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_48(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = None
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_49(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(None)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_50(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = None
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_51(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            None,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_52(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            None,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_53(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_54(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_55(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            1.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_56(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            None,
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_57(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            None,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_58(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            0.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_59(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_60(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            1.4,
        )
        scroller.setScrollerProperties(props)

        return list_widget

    def xǁFilesPageǁ_create_list_widget__mutmut_61(self) -> QtWidgets.QListView:
        """Create and configure the list view widget."""
        list_widget = QtWidgets.QListView(parent=self)
        list_widget.setModel(self._model)
        list_widget.setItemDelegate(self._entry_delegate)
        list_widget.setSpacing(5)
        list_widget.setProperty("showDropIndicator", False)
        list_widget.setProperty("selectionMode", "NoSelection")
        list_widget.setStyleSheet("background: transparent;")
        list_widget.setDefaultDropAction(QtCore.Qt.DropAction.IgnoreAction)
        list_widget.setUniformItemSizes(True)
        list_widget.setObjectName("list_widget")
        list_widget.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
        list_widget.setSelectionBehavior(
            QtWidgets.QAbstractItemView.SelectionBehavior.SelectItems
        )
        list_widget.setHorizontalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setVerticalScrollMode(
            QtWidgets.QAbstractItemView.ScrollMode.ScrollPerPixel
        )
        list_widget.setVerticalScrollBarPolicy(
            QtCore.Qt.ScrollBarPolicy.ScrollBarAlwaysOff
        )
        list_widget.setEditTriggers(
            QtWidgets.QAbstractItemView.EditTrigger.NoEditTriggers
        )

        # Enable touch gestures
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.TouchGesture,
        )
        QtWidgets.QScroller.grabGesture(
            list_widget,
            QtWidgets.QScroller.ScrollerGestureType.LeftMouseButtonGesture,
        )

        # Configure scroller properties
        scroller = QtWidgets.QScroller.scroller(list_widget)
        props = scroller.scrollerProperties()
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DragVelocitySmoothingFactor,
            0.05,
        )
        props.setScrollMetric(
            QtWidgets.QScrollerProperties.ScrollMetric.DecelerationFactor,
            0.4,
        )
        scroller.setScrollerProperties(None)

        return list_widget
    
    xǁFilesPageǁ_create_list_widget__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁFilesPageǁ_create_list_widget__mutmut_1': xǁFilesPageǁ_create_list_widget__mutmut_1, 
        'xǁFilesPageǁ_create_list_widget__mutmut_2': xǁFilesPageǁ_create_list_widget__mutmut_2, 
        'xǁFilesPageǁ_create_list_widget__mutmut_3': xǁFilesPageǁ_create_list_widget__mutmut_3, 
        'xǁFilesPageǁ_create_list_widget__mutmut_4': xǁFilesPageǁ_create_list_widget__mutmut_4, 
        'xǁFilesPageǁ_create_list_widget__mutmut_5': xǁFilesPageǁ_create_list_widget__mutmut_5, 
        'xǁFilesPageǁ_create_list_widget__mutmut_6': xǁFilesPageǁ_create_list_widget__mutmut_6, 
        'xǁFilesPageǁ_create_list_widget__mutmut_7': xǁFilesPageǁ_create_list_widget__mutmut_7, 
        'xǁFilesPageǁ_create_list_widget__mutmut_8': xǁFilesPageǁ_create_list_widget__mutmut_8, 
        'xǁFilesPageǁ_create_list_widget__mutmut_9': xǁFilesPageǁ_create_list_widget__mutmut_9, 
        'xǁFilesPageǁ_create_list_widget__mutmut_10': xǁFilesPageǁ_create_list_widget__mutmut_10, 
        'xǁFilesPageǁ_create_list_widget__mutmut_11': xǁFilesPageǁ_create_list_widget__mutmut_11, 
        'xǁFilesPageǁ_create_list_widget__mutmut_12': xǁFilesPageǁ_create_list_widget__mutmut_12, 
        'xǁFilesPageǁ_create_list_widget__mutmut_13': xǁFilesPageǁ_create_list_widget__mutmut_13, 
        'xǁFilesPageǁ_create_list_widget__mutmut_14': xǁFilesPageǁ_create_list_widget__mutmut_14, 
        'xǁFilesPageǁ_create_list_widget__mutmut_15': xǁFilesPageǁ_create_list_widget__mutmut_15, 
        'xǁFilesPageǁ_create_list_widget__mutmut_16': xǁFilesPageǁ_create_list_widget__mutmut_16, 
        'xǁFilesPageǁ_create_list_widget__mutmut_17': xǁFilesPageǁ_create_list_widget__mutmut_17, 
        'xǁFilesPageǁ_create_list_widget__mutmut_18': xǁFilesPageǁ_create_list_widget__mutmut_18, 
        'xǁFilesPageǁ_create_list_widget__mutmut_19': xǁFilesPageǁ_create_list_widget__mutmut_19, 
        'xǁFilesPageǁ_create_list_widget__mutmut_20': xǁFilesPageǁ_create_list_widget__mutmut_20, 
        'xǁFilesPageǁ_create_list_widget__mutmut_21': xǁFilesPageǁ_create_list_widget__mutmut_21, 
        'xǁFilesPageǁ_create_list_widget__mutmut_22': xǁFilesPageǁ_create_list_widget__mutmut_22, 
        'xǁFilesPageǁ_create_list_widget__mutmut_23': xǁFilesPageǁ_create_list_widget__mutmut_23, 
        'xǁFilesPageǁ_create_list_widget__mutmut_24': xǁFilesPageǁ_create_list_widget__mutmut_24, 
        'xǁFilesPageǁ_create_list_widget__mutmut_25': xǁFilesPageǁ_create_list_widget__mutmut_25, 
        'xǁFilesPageǁ_create_list_widget__mutmut_26': xǁFilesPageǁ_create_list_widget__mutmut_26, 
        'xǁFilesPageǁ_create_list_widget__mutmut_27': xǁFilesPageǁ_create_list_widget__mutmut_27, 
        'xǁFilesPageǁ_create_list_widget__mutmut_28': xǁFilesPageǁ_create_list_widget__mutmut_28, 
        'xǁFilesPageǁ_create_list_widget__mutmut_29': xǁFilesPageǁ_create_list_widget__mutmut_29, 
        'xǁFilesPageǁ_create_list_widget__mutmut_30': xǁFilesPageǁ_create_list_widget__mutmut_30, 
        'xǁFilesPageǁ_create_list_widget__mutmut_31': xǁFilesPageǁ_create_list_widget__mutmut_31, 
        'xǁFilesPageǁ_create_list_widget__mutmut_32': xǁFilesPageǁ_create_list_widget__mutmut_32, 
        'xǁFilesPageǁ_create_list_widget__mutmut_33': xǁFilesPageǁ_create_list_widget__mutmut_33, 
        'xǁFilesPageǁ_create_list_widget__mutmut_34': xǁFilesPageǁ_create_list_widget__mutmut_34, 
        'xǁFilesPageǁ_create_list_widget__mutmut_35': xǁFilesPageǁ_create_list_widget__mutmut_35, 
        'xǁFilesPageǁ_create_list_widget__mutmut_36': xǁFilesPageǁ_create_list_widget__mutmut_36, 
        'xǁFilesPageǁ_create_list_widget__mutmut_37': xǁFilesPageǁ_create_list_widget__mutmut_37, 
        'xǁFilesPageǁ_create_list_widget__mutmut_38': xǁFilesPageǁ_create_list_widget__mutmut_38, 
        'xǁFilesPageǁ_create_list_widget__mutmut_39': xǁFilesPageǁ_create_list_widget__mutmut_39, 
        'xǁFilesPageǁ_create_list_widget__mutmut_40': xǁFilesPageǁ_create_list_widget__mutmut_40, 
        'xǁFilesPageǁ_create_list_widget__mutmut_41': xǁFilesPageǁ_create_list_widget__mutmut_41, 
        'xǁFilesPageǁ_create_list_widget__mutmut_42': xǁFilesPageǁ_create_list_widget__mutmut_42, 
        'xǁFilesPageǁ_create_list_widget__mutmut_43': xǁFilesPageǁ_create_list_widget__mutmut_43, 
        'xǁFilesPageǁ_create_list_widget__mutmut_44': xǁFilesPageǁ_create_list_widget__mutmut_44, 
        'xǁFilesPageǁ_create_list_widget__mutmut_45': xǁFilesPageǁ_create_list_widget__mutmut_45, 
        'xǁFilesPageǁ_create_list_widget__mutmut_46': xǁFilesPageǁ_create_list_widget__mutmut_46, 
        'xǁFilesPageǁ_create_list_widget__mutmut_47': xǁFilesPageǁ_create_list_widget__mutmut_47, 
        'xǁFilesPageǁ_create_list_widget__mutmut_48': xǁFilesPageǁ_create_list_widget__mutmut_48, 
        'xǁFilesPageǁ_create_list_widget__mutmut_49': xǁFilesPageǁ_create_list_widget__mutmut_49, 
        'xǁFilesPageǁ_create_list_widget__mutmut_50': xǁFilesPageǁ_create_list_widget__mutmut_50, 
        'xǁFilesPageǁ_create_list_widget__mutmut_51': xǁFilesPageǁ_create_list_widget__mutmut_51, 
        'xǁFilesPageǁ_create_list_widget__mutmut_52': xǁFilesPageǁ_create_list_widget__mutmut_52, 
        'xǁFilesPageǁ_create_list_widget__mutmut_53': xǁFilesPageǁ_create_list_widget__mutmut_53, 
        'xǁFilesPageǁ_create_list_widget__mutmut_54': xǁFilesPageǁ_create_list_widget__mutmut_54, 
        'xǁFilesPageǁ_create_list_widget__mutmut_55': xǁFilesPageǁ_create_list_widget__mutmut_55, 
        'xǁFilesPageǁ_create_list_widget__mutmut_56': xǁFilesPageǁ_create_list_widget__mutmut_56, 
        'xǁFilesPageǁ_create_list_widget__mutmut_57': xǁFilesPageǁ_create_list_widget__mutmut_57, 
        'xǁFilesPageǁ_create_list_widget__mutmut_58': xǁFilesPageǁ_create_list_widget__mutmut_58, 
        'xǁFilesPageǁ_create_list_widget__mutmut_59': xǁFilesPageǁ_create_list_widget__mutmut_59, 
        'xǁFilesPageǁ_create_list_widget__mutmut_60': xǁFilesPageǁ_create_list_widget__mutmut_60, 
        'xǁFilesPageǁ_create_list_widget__mutmut_61': xǁFilesPageǁ_create_list_widget__mutmut_61
    }
    xǁFilesPageǁ_create_list_widget__mutmut_orig.__name__ = 'xǁFilesPageǁ_create_list_widget'
