# Moonraker api
import json
import logging
import threading

import websocket
from events import (
    WebSocketDisconnected,
    WebSocketError,
    WebSocketMessageReceived,
    WebSocketOpen,
)
from lib.moonrest import MoonRest
from lib.utils.RepeatedTimer import RepeatedTimer
from PyQt6 import QtCore, QtWidgets

logger = logging.getLogger(__name__)
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class OneShotTokenError(Exception):
    """Raised when unable to get oneshot token to connect to a websocket"""

    def __init__(self, message="Unable to get oneshot token", errors=None) -> None:
        args = [message, errors]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁOneShotTokenErrorǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁOneShotTokenErrorǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁOneShotTokenErrorǁ__init____mutmut_orig(self, message="Unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(message, errors)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_1(self, message="XXUnable to get oneshot tokenXX", errors=None) -> None:
        super(OneShotTokenError).__init__(message, errors)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_2(self, message="unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(message, errors)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_3(self, message="UNABLE TO GET ONESHOT TOKEN", errors=None) -> None:
        super(OneShotTokenError).__init__(message, errors)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_4(self, message="Unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(None, errors)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_5(self, message="Unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(message, None)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_6(self, message="Unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(errors)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_7(self, message="Unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(message, )
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_8(self, message="Unable to get oneshot token", errors=None) -> None:
        super(None).__init__(message, errors)
        self.errors = errors
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_9(self, message="Unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(message, errors)
        self.errors = None
        self.message = message

    def xǁOneShotTokenErrorǁ__init____mutmut_10(self, message="Unable to get oneshot token", errors=None) -> None:
        super(OneShotTokenError).__init__(message, errors)
        self.errors = errors
        self.message = None
    
    xǁOneShotTokenErrorǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁOneShotTokenErrorǁ__init____mutmut_1': xǁOneShotTokenErrorǁ__init____mutmut_1, 
        'xǁOneShotTokenErrorǁ__init____mutmut_2': xǁOneShotTokenErrorǁ__init____mutmut_2, 
        'xǁOneShotTokenErrorǁ__init____mutmut_3': xǁOneShotTokenErrorǁ__init____mutmut_3, 
        'xǁOneShotTokenErrorǁ__init____mutmut_4': xǁOneShotTokenErrorǁ__init____mutmut_4, 
        'xǁOneShotTokenErrorǁ__init____mutmut_5': xǁOneShotTokenErrorǁ__init____mutmut_5, 
        'xǁOneShotTokenErrorǁ__init____mutmut_6': xǁOneShotTokenErrorǁ__init____mutmut_6, 
        'xǁOneShotTokenErrorǁ__init____mutmut_7': xǁOneShotTokenErrorǁ__init____mutmut_7, 
        'xǁOneShotTokenErrorǁ__init____mutmut_8': xǁOneShotTokenErrorǁ__init____mutmut_8, 
        'xǁOneShotTokenErrorǁ__init____mutmut_9': xǁOneShotTokenErrorǁ__init____mutmut_9, 
        'xǁOneShotTokenErrorǁ__init____mutmut_10': xǁOneShotTokenErrorǁ__init____mutmut_10
    }
    xǁOneShotTokenErrorǁ__init____mutmut_orig.__name__ = 'xǁOneShotTokenErrorǁ__init__'


class MoonWebSocket(QtCore.QObject, threading.Thread):
    """MoonWebSocket class object for creating a websocket connection to Moonraker."""

    QUERY_KLIPPY_TIMEOUT: int = 2
    connected = False
    connecting = False
    callback_table = {}
    _reconnect_count = 0
    max_retries = 3
    timeout = 3

    connecting_signal = QtCore.pyqtSignal([int], [str], name="websocket_connecting")
    connected_signal = QtCore.pyqtSignal(name="websocket-connected")
    connection_lost = QtCore.pyqtSignal([str], name="websocket-connection-lost")
    klippy_connected_signal = QtCore.pyqtSignal(bool, name="klippy_connection_status")
    klippy_state_signal = QtCore.pyqtSignal(str, name="klippy_state")
    query_server_info_signal = QtCore.pyqtSignal(name="query_server_information")

    def __init__(self, parent: QtCore.QObject) -> None:
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁ__init____mutmut_orig(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_1(self, parent: QtCore.QObject) -> None:
        super().__init__(None)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_2(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = None

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_3(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = False

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_4(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = None
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_5(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get(None, parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_6(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=None, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_7(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default=None)
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_8(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get(parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_9(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_10(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, )
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_11(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("XXhostXX", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_12(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("HOST", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_13(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="XXlocalhostXX")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_14(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="LOCALHOST")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_15(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = None

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_16(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get(None, parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_17(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=None, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_18(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=None)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_19(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get(parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_20(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_21(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, )

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_22(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("XXportXX", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_23(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("PORT", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_24(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7126)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_25(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = ""
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_26(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = ""
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_27(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = ""
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_28(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = None
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_29(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 1
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_30(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = None
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_31(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = None
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_32(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=None, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_33(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=None)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_34(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_35(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, )
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_36(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = None
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_37(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(None)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_38(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(None)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_39(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(None)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_40(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = None

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_41(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            None, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_42(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, None
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_43(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_44(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_45(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(None)
        logger.info("Websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_46(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info(None)

    def xǁMoonWebSocketǁ__init____mutmut_47(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("XXWebsocket object initializedXX")

    def xǁMoonWebSocketǁ__init____mutmut_48(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("websocket object initialized")

    def xǁMoonWebSocketǁ__init____mutmut_49(self, parent: QtCore.QObject) -> None:
        super().__init__(parent)
        self.daemon = True

        self._host = parent.config.get("host", parser=str, default="localhost")
        self._port = parent.config.get("port", parser=int, default=7125)

        self.ws: websocket.WebSocketApp | None = None
        self._callback = None
        self._wst = None
        self._request_id = 0
        self.request_table = {}
        self._moonRest = MoonRest(host=self._host, port=self._port)
        self.api: MoonAPI = MoonAPI(self)
        self._retry_timer: RepeatedTimer
        websocket.setdefaulttimeout(self.timeout)

        self.query_server_info_signal.connect(self.api.api_query_server_info)
        self.query_klippy_status_timer = RepeatedTimer(
            self.QUERY_KLIPPY_TIMEOUT, self.query_server_info_signal.emit
        )

        self.klippy_state_signal.connect(self.api.request_printer_info)
        logger.info("WEBSOCKET OBJECT INITIALIZED")
    
    xǁMoonWebSocketǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁ__init____mutmut_1': xǁMoonWebSocketǁ__init____mutmut_1, 
        'xǁMoonWebSocketǁ__init____mutmut_2': xǁMoonWebSocketǁ__init____mutmut_2, 
        'xǁMoonWebSocketǁ__init____mutmut_3': xǁMoonWebSocketǁ__init____mutmut_3, 
        'xǁMoonWebSocketǁ__init____mutmut_4': xǁMoonWebSocketǁ__init____mutmut_4, 
        'xǁMoonWebSocketǁ__init____mutmut_5': xǁMoonWebSocketǁ__init____mutmut_5, 
        'xǁMoonWebSocketǁ__init____mutmut_6': xǁMoonWebSocketǁ__init____mutmut_6, 
        'xǁMoonWebSocketǁ__init____mutmut_7': xǁMoonWebSocketǁ__init____mutmut_7, 
        'xǁMoonWebSocketǁ__init____mutmut_8': xǁMoonWebSocketǁ__init____mutmut_8, 
        'xǁMoonWebSocketǁ__init____mutmut_9': xǁMoonWebSocketǁ__init____mutmut_9, 
        'xǁMoonWebSocketǁ__init____mutmut_10': xǁMoonWebSocketǁ__init____mutmut_10, 
        'xǁMoonWebSocketǁ__init____mutmut_11': xǁMoonWebSocketǁ__init____mutmut_11, 
        'xǁMoonWebSocketǁ__init____mutmut_12': xǁMoonWebSocketǁ__init____mutmut_12, 
        'xǁMoonWebSocketǁ__init____mutmut_13': xǁMoonWebSocketǁ__init____mutmut_13, 
        'xǁMoonWebSocketǁ__init____mutmut_14': xǁMoonWebSocketǁ__init____mutmut_14, 
        'xǁMoonWebSocketǁ__init____mutmut_15': xǁMoonWebSocketǁ__init____mutmut_15, 
        'xǁMoonWebSocketǁ__init____mutmut_16': xǁMoonWebSocketǁ__init____mutmut_16, 
        'xǁMoonWebSocketǁ__init____mutmut_17': xǁMoonWebSocketǁ__init____mutmut_17, 
        'xǁMoonWebSocketǁ__init____mutmut_18': xǁMoonWebSocketǁ__init____mutmut_18, 
        'xǁMoonWebSocketǁ__init____mutmut_19': xǁMoonWebSocketǁ__init____mutmut_19, 
        'xǁMoonWebSocketǁ__init____mutmut_20': xǁMoonWebSocketǁ__init____mutmut_20, 
        'xǁMoonWebSocketǁ__init____mutmut_21': xǁMoonWebSocketǁ__init____mutmut_21, 
        'xǁMoonWebSocketǁ__init____mutmut_22': xǁMoonWebSocketǁ__init____mutmut_22, 
        'xǁMoonWebSocketǁ__init____mutmut_23': xǁMoonWebSocketǁ__init____mutmut_23, 
        'xǁMoonWebSocketǁ__init____mutmut_24': xǁMoonWebSocketǁ__init____mutmut_24, 
        'xǁMoonWebSocketǁ__init____mutmut_25': xǁMoonWebSocketǁ__init____mutmut_25, 
        'xǁMoonWebSocketǁ__init____mutmut_26': xǁMoonWebSocketǁ__init____mutmut_26, 
        'xǁMoonWebSocketǁ__init____mutmut_27': xǁMoonWebSocketǁ__init____mutmut_27, 
        'xǁMoonWebSocketǁ__init____mutmut_28': xǁMoonWebSocketǁ__init____mutmut_28, 
        'xǁMoonWebSocketǁ__init____mutmut_29': xǁMoonWebSocketǁ__init____mutmut_29, 
        'xǁMoonWebSocketǁ__init____mutmut_30': xǁMoonWebSocketǁ__init____mutmut_30, 
        'xǁMoonWebSocketǁ__init____mutmut_31': xǁMoonWebSocketǁ__init____mutmut_31, 
        'xǁMoonWebSocketǁ__init____mutmut_32': xǁMoonWebSocketǁ__init____mutmut_32, 
        'xǁMoonWebSocketǁ__init____mutmut_33': xǁMoonWebSocketǁ__init____mutmut_33, 
        'xǁMoonWebSocketǁ__init____mutmut_34': xǁMoonWebSocketǁ__init____mutmut_34, 
        'xǁMoonWebSocketǁ__init____mutmut_35': xǁMoonWebSocketǁ__init____mutmut_35, 
        'xǁMoonWebSocketǁ__init____mutmut_36': xǁMoonWebSocketǁ__init____mutmut_36, 
        'xǁMoonWebSocketǁ__init____mutmut_37': xǁMoonWebSocketǁ__init____mutmut_37, 
        'xǁMoonWebSocketǁ__init____mutmut_38': xǁMoonWebSocketǁ__init____mutmut_38, 
        'xǁMoonWebSocketǁ__init____mutmut_39': xǁMoonWebSocketǁ__init____mutmut_39, 
        'xǁMoonWebSocketǁ__init____mutmut_40': xǁMoonWebSocketǁ__init____mutmut_40, 
        'xǁMoonWebSocketǁ__init____mutmut_41': xǁMoonWebSocketǁ__init____mutmut_41, 
        'xǁMoonWebSocketǁ__init____mutmut_42': xǁMoonWebSocketǁ__init____mutmut_42, 
        'xǁMoonWebSocketǁ__init____mutmut_43': xǁMoonWebSocketǁ__init____mutmut_43, 
        'xǁMoonWebSocketǁ__init____mutmut_44': xǁMoonWebSocketǁ__init____mutmut_44, 
        'xǁMoonWebSocketǁ__init____mutmut_45': xǁMoonWebSocketǁ__init____mutmut_45, 
        'xǁMoonWebSocketǁ__init____mutmut_46': xǁMoonWebSocketǁ__init____mutmut_46, 
        'xǁMoonWebSocketǁ__init____mutmut_47': xǁMoonWebSocketǁ__init____mutmut_47, 
        'xǁMoonWebSocketǁ__init____mutmut_48': xǁMoonWebSocketǁ__init____mutmut_48, 
        'xǁMoonWebSocketǁ__init____mutmut_49': xǁMoonWebSocketǁ__init____mutmut_49
    }
    xǁMoonWebSocketǁ__init____mutmut_orig.__name__ = 'xǁMoonWebSocketǁ__init__'

    @QtCore.pyqtSlot(name="retry_wb_conn")
    def retry_wb_conn(self):
        """Retry websocket connection"""
        if self.connecting is True and self.connected is False:
            return False
        self._reconnect_count = 0
        self.try_connection()

    def try_connection(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁtry_connection__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁtry_connection__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁtry_connection__mutmut_orig(self):
        """Try connecting to websocket"""
        self.connecting = True
        self._retry_timer = RepeatedTimer(self.timeout, self.reconnect)
        return self.connect()

    def xǁMoonWebSocketǁtry_connection__mutmut_1(self):
        """Try connecting to websocket"""
        self.connecting = None
        self._retry_timer = RepeatedTimer(self.timeout, self.reconnect)
        return self.connect()

    def xǁMoonWebSocketǁtry_connection__mutmut_2(self):
        """Try connecting to websocket"""
        self.connecting = False
        self._retry_timer = RepeatedTimer(self.timeout, self.reconnect)
        return self.connect()

    def xǁMoonWebSocketǁtry_connection__mutmut_3(self):
        """Try connecting to websocket"""
        self.connecting = True
        self._retry_timer = None
        return self.connect()

    def xǁMoonWebSocketǁtry_connection__mutmut_4(self):
        """Try connecting to websocket"""
        self.connecting = True
        self._retry_timer = RepeatedTimer(None, self.reconnect)
        return self.connect()

    def xǁMoonWebSocketǁtry_connection__mutmut_5(self):
        """Try connecting to websocket"""
        self.connecting = True
        self._retry_timer = RepeatedTimer(self.timeout, None)
        return self.connect()

    def xǁMoonWebSocketǁtry_connection__mutmut_6(self):
        """Try connecting to websocket"""
        self.connecting = True
        self._retry_timer = RepeatedTimer(self.reconnect)
        return self.connect()

    def xǁMoonWebSocketǁtry_connection__mutmut_7(self):
        """Try connecting to websocket"""
        self.connecting = True
        self._retry_timer = RepeatedTimer(self.timeout, )
        return self.connect()
    
    xǁMoonWebSocketǁtry_connection__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁtry_connection__mutmut_1': xǁMoonWebSocketǁtry_connection__mutmut_1, 
        'xǁMoonWebSocketǁtry_connection__mutmut_2': xǁMoonWebSocketǁtry_connection__mutmut_2, 
        'xǁMoonWebSocketǁtry_connection__mutmut_3': xǁMoonWebSocketǁtry_connection__mutmut_3, 
        'xǁMoonWebSocketǁtry_connection__mutmut_4': xǁMoonWebSocketǁtry_connection__mutmut_4, 
        'xǁMoonWebSocketǁtry_connection__mutmut_5': xǁMoonWebSocketǁtry_connection__mutmut_5, 
        'xǁMoonWebSocketǁtry_connection__mutmut_6': xǁMoonWebSocketǁtry_connection__mutmut_6, 
        'xǁMoonWebSocketǁtry_connection__mutmut_7': xǁMoonWebSocketǁtry_connection__mutmut_7
    }
    xǁMoonWebSocketǁtry_connection__mutmut_orig.__name__ = 'xǁMoonWebSocketǁtry_connection'

    def reconnect(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁreconnect__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁreconnect__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁreconnect__mutmut_orig(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_1(self):
        """Reconnect to websocket"""
        if self.connected:
            return False

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_2(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count > self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_3(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = None
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_4(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data=None
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_5(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="XXUnable to establish connection to WebsocketXX"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_6(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="unable to establish connection to websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_7(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="UNABLE TO ESTABLISH CONNECTION TO WEBSOCKET"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_8(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(None)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_9(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(1)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_10(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = None
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_11(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = True
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_12(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = None
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_13(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_14(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(None, unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_15(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), None)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_16(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_17(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), )
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_18(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError(None)
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_19(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("XXQApplication.instance expected ad non-None valueXX")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_20(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("qapplication.instance expected ad non-none value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_21(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QAPPLICATION.INSTANCE EXPECTED AD NON-NONE VALUE")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_22(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    None
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_23(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                None
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_24(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "XXMaximum number of connection retries reached, Unable to establish connection with MoonrakerXX"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_25(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "maximum number of connection retries reached, unable to establish connection with moonraker"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_26(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "MAXIMUM NUMBER OF CONNECTION RETRIES REACHED, UNABLE TO ESTABLISH CONNECTION WITH MOONRAKER"
            )
            return False
        return self.connect()

    def xǁMoonWebSocketǁreconnect__mutmut_27(self):
        """Reconnect to websocket"""
        if self.connected:
            return True

        if self._reconnect_count >= self.max_retries:
            self._retry_timer.stopTimer()
            unable_to_connect_event = WebSocketError(
                data="Unable to establish connection to Websocket"
            )
            self.connecting_signal[int].emit(0)
            self.connecting = False
            try:
                instance = QtWidgets.QApplication.instance()
                if instance is not None:
                    instance.sendEvent(self.parent(), unable_to_connect_event)
                else:
                    raise TypeError("QApplication.instance expected ad non-None value")
            except Exception as e:
                logger.error(
                    f"Error on sending Event {unable_to_connect_event.__class__.__name__} | Error message: {e}"
                )
            logger.info(
                "Maximum number of connection retries reached, Unable to establish connection with Moonraker"
            )
            return True
        return self.connect()
    
    xǁMoonWebSocketǁreconnect__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁreconnect__mutmut_1': xǁMoonWebSocketǁreconnect__mutmut_1, 
        'xǁMoonWebSocketǁreconnect__mutmut_2': xǁMoonWebSocketǁreconnect__mutmut_2, 
        'xǁMoonWebSocketǁreconnect__mutmut_3': xǁMoonWebSocketǁreconnect__mutmut_3, 
        'xǁMoonWebSocketǁreconnect__mutmut_4': xǁMoonWebSocketǁreconnect__mutmut_4, 
        'xǁMoonWebSocketǁreconnect__mutmut_5': xǁMoonWebSocketǁreconnect__mutmut_5, 
        'xǁMoonWebSocketǁreconnect__mutmut_6': xǁMoonWebSocketǁreconnect__mutmut_6, 
        'xǁMoonWebSocketǁreconnect__mutmut_7': xǁMoonWebSocketǁreconnect__mutmut_7, 
        'xǁMoonWebSocketǁreconnect__mutmut_8': xǁMoonWebSocketǁreconnect__mutmut_8, 
        'xǁMoonWebSocketǁreconnect__mutmut_9': xǁMoonWebSocketǁreconnect__mutmut_9, 
        'xǁMoonWebSocketǁreconnect__mutmut_10': xǁMoonWebSocketǁreconnect__mutmut_10, 
        'xǁMoonWebSocketǁreconnect__mutmut_11': xǁMoonWebSocketǁreconnect__mutmut_11, 
        'xǁMoonWebSocketǁreconnect__mutmut_12': xǁMoonWebSocketǁreconnect__mutmut_12, 
        'xǁMoonWebSocketǁreconnect__mutmut_13': xǁMoonWebSocketǁreconnect__mutmut_13, 
        'xǁMoonWebSocketǁreconnect__mutmut_14': xǁMoonWebSocketǁreconnect__mutmut_14, 
        'xǁMoonWebSocketǁreconnect__mutmut_15': xǁMoonWebSocketǁreconnect__mutmut_15, 
        'xǁMoonWebSocketǁreconnect__mutmut_16': xǁMoonWebSocketǁreconnect__mutmut_16, 
        'xǁMoonWebSocketǁreconnect__mutmut_17': xǁMoonWebSocketǁreconnect__mutmut_17, 
        'xǁMoonWebSocketǁreconnect__mutmut_18': xǁMoonWebSocketǁreconnect__mutmut_18, 
        'xǁMoonWebSocketǁreconnect__mutmut_19': xǁMoonWebSocketǁreconnect__mutmut_19, 
        'xǁMoonWebSocketǁreconnect__mutmut_20': xǁMoonWebSocketǁreconnect__mutmut_20, 
        'xǁMoonWebSocketǁreconnect__mutmut_21': xǁMoonWebSocketǁreconnect__mutmut_21, 
        'xǁMoonWebSocketǁreconnect__mutmut_22': xǁMoonWebSocketǁreconnect__mutmut_22, 
        'xǁMoonWebSocketǁreconnect__mutmut_23': xǁMoonWebSocketǁreconnect__mutmut_23, 
        'xǁMoonWebSocketǁreconnect__mutmut_24': xǁMoonWebSocketǁreconnect__mutmut_24, 
        'xǁMoonWebSocketǁreconnect__mutmut_25': xǁMoonWebSocketǁreconnect__mutmut_25, 
        'xǁMoonWebSocketǁreconnect__mutmut_26': xǁMoonWebSocketǁreconnect__mutmut_26, 
        'xǁMoonWebSocketǁreconnect__mutmut_27': xǁMoonWebSocketǁreconnect__mutmut_27
    }
    xǁMoonWebSocketǁreconnect__mutmut_orig.__name__ = 'xǁMoonWebSocketǁreconnect'

    def connect(self) -> bool:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁconnect__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁconnect__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁconnect__mutmut_orig(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_1(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info(None)
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_2(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("XXConnection establishedXX")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_3(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_4(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("CONNECTION ESTABLISHED")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_5(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return False
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_6(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count = 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_7(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count -= 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_8(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 2
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_9(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(None)
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_10(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(None))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_11(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            None
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_12(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = None
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_13(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is not None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_14(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError(None)
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_15(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("XXUnable to retrieve oneshot tokenXX")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_16(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_17(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("UNABLE TO RETRIEVE ONESHOT TOKEN")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_18(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                None
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_19(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return True

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_20(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = None
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_21(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = None
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_22(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            None,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_23(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=None,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_24(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=None,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_25(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=None,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_26(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=None,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_27(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_28(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_29(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_30(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_31(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_32(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = None  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_33(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"XXreconnectXX": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_34(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"RECONNECT": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_35(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = None
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_36(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name=None,
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_37(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=None,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_38(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=None,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_39(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_40(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_41(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_42(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="XXwebsocket.run_foreverXX",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_43(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="WEBSOCKET.RUN_FOREVER",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_44(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=False,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_45(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info(None)
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_46(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("XXWebsocket Start...XX")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_47(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("websocket start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_48(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("WEBSOCKET START...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_49(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(None)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_50(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(None)
            return False
        return True

    def xǁMoonWebSocketǁconnect__mutmut_51(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return True
        return True

    def xǁMoonWebSocketǁconnect__mutmut_52(self) -> bool:
        """Connect to websocket"""
        if self.connected:
            logger.info("Connection established")
            return True
        self._reconnect_count += 1
        self.connecting_signal[int].emit(int(self._reconnect_count))
        logger.debug(
            f"Establishing connection to Moonraker...\n Try number {self._reconnect_count}"
        )
        # TODO Handle if i cannot connect to moonraker, request server.info and see if i get a result
        try:
            _oneshot_token = self._moonRest.get_oneshot_token()
            if _oneshot_token is None:
                raise OneShotTokenError("Unable to retrieve oneshot token")
        except Exception as e:
            logger.info(
                f"Unexpected error occurred when trying to acquire oneshot token: {e}"
            )
            return False

        _url = f"ws://{self._host}:{self._port}/websocket?token={_oneshot_token}"
        self.ws = websocket.WebSocketApp(
            _url,
            on_open=self.on_open,
            on_close=self.on_close,
            on_error=self.on_error,
            on_message=self.on_message,
        )
        _kwargs = {"reconnect": self.timeout}  # FIXME: This goes nowhere

        self._wst = threading.Thread(
            name="websocket.run_forever",
            target=self.ws.run_forever,
            daemon=True,
        )
        try:
            logger.info("Websocket Start...")
            logger.debug(self.ws.url)
            self._wst.start()
        except Exception as e:
            logger.info(f"Unexpected while starting websocket {self._wst.name}: {e}")
            return False
        return False
    
    xǁMoonWebSocketǁconnect__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁconnect__mutmut_1': xǁMoonWebSocketǁconnect__mutmut_1, 
        'xǁMoonWebSocketǁconnect__mutmut_2': xǁMoonWebSocketǁconnect__mutmut_2, 
        'xǁMoonWebSocketǁconnect__mutmut_3': xǁMoonWebSocketǁconnect__mutmut_3, 
        'xǁMoonWebSocketǁconnect__mutmut_4': xǁMoonWebSocketǁconnect__mutmut_4, 
        'xǁMoonWebSocketǁconnect__mutmut_5': xǁMoonWebSocketǁconnect__mutmut_5, 
        'xǁMoonWebSocketǁconnect__mutmut_6': xǁMoonWebSocketǁconnect__mutmut_6, 
        'xǁMoonWebSocketǁconnect__mutmut_7': xǁMoonWebSocketǁconnect__mutmut_7, 
        'xǁMoonWebSocketǁconnect__mutmut_8': xǁMoonWebSocketǁconnect__mutmut_8, 
        'xǁMoonWebSocketǁconnect__mutmut_9': xǁMoonWebSocketǁconnect__mutmut_9, 
        'xǁMoonWebSocketǁconnect__mutmut_10': xǁMoonWebSocketǁconnect__mutmut_10, 
        'xǁMoonWebSocketǁconnect__mutmut_11': xǁMoonWebSocketǁconnect__mutmut_11, 
        'xǁMoonWebSocketǁconnect__mutmut_12': xǁMoonWebSocketǁconnect__mutmut_12, 
        'xǁMoonWebSocketǁconnect__mutmut_13': xǁMoonWebSocketǁconnect__mutmut_13, 
        'xǁMoonWebSocketǁconnect__mutmut_14': xǁMoonWebSocketǁconnect__mutmut_14, 
        'xǁMoonWebSocketǁconnect__mutmut_15': xǁMoonWebSocketǁconnect__mutmut_15, 
        'xǁMoonWebSocketǁconnect__mutmut_16': xǁMoonWebSocketǁconnect__mutmut_16, 
        'xǁMoonWebSocketǁconnect__mutmut_17': xǁMoonWebSocketǁconnect__mutmut_17, 
        'xǁMoonWebSocketǁconnect__mutmut_18': xǁMoonWebSocketǁconnect__mutmut_18, 
        'xǁMoonWebSocketǁconnect__mutmut_19': xǁMoonWebSocketǁconnect__mutmut_19, 
        'xǁMoonWebSocketǁconnect__mutmut_20': xǁMoonWebSocketǁconnect__mutmut_20, 
        'xǁMoonWebSocketǁconnect__mutmut_21': xǁMoonWebSocketǁconnect__mutmut_21, 
        'xǁMoonWebSocketǁconnect__mutmut_22': xǁMoonWebSocketǁconnect__mutmut_22, 
        'xǁMoonWebSocketǁconnect__mutmut_23': xǁMoonWebSocketǁconnect__mutmut_23, 
        'xǁMoonWebSocketǁconnect__mutmut_24': xǁMoonWebSocketǁconnect__mutmut_24, 
        'xǁMoonWebSocketǁconnect__mutmut_25': xǁMoonWebSocketǁconnect__mutmut_25, 
        'xǁMoonWebSocketǁconnect__mutmut_26': xǁMoonWebSocketǁconnect__mutmut_26, 
        'xǁMoonWebSocketǁconnect__mutmut_27': xǁMoonWebSocketǁconnect__mutmut_27, 
        'xǁMoonWebSocketǁconnect__mutmut_28': xǁMoonWebSocketǁconnect__mutmut_28, 
        'xǁMoonWebSocketǁconnect__mutmut_29': xǁMoonWebSocketǁconnect__mutmut_29, 
        'xǁMoonWebSocketǁconnect__mutmut_30': xǁMoonWebSocketǁconnect__mutmut_30, 
        'xǁMoonWebSocketǁconnect__mutmut_31': xǁMoonWebSocketǁconnect__mutmut_31, 
        'xǁMoonWebSocketǁconnect__mutmut_32': xǁMoonWebSocketǁconnect__mutmut_32, 
        'xǁMoonWebSocketǁconnect__mutmut_33': xǁMoonWebSocketǁconnect__mutmut_33, 
        'xǁMoonWebSocketǁconnect__mutmut_34': xǁMoonWebSocketǁconnect__mutmut_34, 
        'xǁMoonWebSocketǁconnect__mutmut_35': xǁMoonWebSocketǁconnect__mutmut_35, 
        'xǁMoonWebSocketǁconnect__mutmut_36': xǁMoonWebSocketǁconnect__mutmut_36, 
        'xǁMoonWebSocketǁconnect__mutmut_37': xǁMoonWebSocketǁconnect__mutmut_37, 
        'xǁMoonWebSocketǁconnect__mutmut_38': xǁMoonWebSocketǁconnect__mutmut_38, 
        'xǁMoonWebSocketǁconnect__mutmut_39': xǁMoonWebSocketǁconnect__mutmut_39, 
        'xǁMoonWebSocketǁconnect__mutmut_40': xǁMoonWebSocketǁconnect__mutmut_40, 
        'xǁMoonWebSocketǁconnect__mutmut_41': xǁMoonWebSocketǁconnect__mutmut_41, 
        'xǁMoonWebSocketǁconnect__mutmut_42': xǁMoonWebSocketǁconnect__mutmut_42, 
        'xǁMoonWebSocketǁconnect__mutmut_43': xǁMoonWebSocketǁconnect__mutmut_43, 
        'xǁMoonWebSocketǁconnect__mutmut_44': xǁMoonWebSocketǁconnect__mutmut_44, 
        'xǁMoonWebSocketǁconnect__mutmut_45': xǁMoonWebSocketǁconnect__mutmut_45, 
        'xǁMoonWebSocketǁconnect__mutmut_46': xǁMoonWebSocketǁconnect__mutmut_46, 
        'xǁMoonWebSocketǁconnect__mutmut_47': xǁMoonWebSocketǁconnect__mutmut_47, 
        'xǁMoonWebSocketǁconnect__mutmut_48': xǁMoonWebSocketǁconnect__mutmut_48, 
        'xǁMoonWebSocketǁconnect__mutmut_49': xǁMoonWebSocketǁconnect__mutmut_49, 
        'xǁMoonWebSocketǁconnect__mutmut_50': xǁMoonWebSocketǁconnect__mutmut_50, 
        'xǁMoonWebSocketǁconnect__mutmut_51': xǁMoonWebSocketǁconnect__mutmut_51, 
        'xǁMoonWebSocketǁconnect__mutmut_52': xǁMoonWebSocketǁconnect__mutmut_52
    }
    xǁMoonWebSocketǁconnect__mutmut_orig.__name__ = 'xǁMoonWebSocketǁconnect'

    def wb_disconnect(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁwb_disconnect__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁwb_disconnect__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁwb_disconnect__mutmut_orig(self) -> None:
        """Websocket disconnect"""
        if self._wst is not None and self.ws is not None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info("Websocket closed")

    def xǁMoonWebSocketǁwb_disconnect__mutmut_1(self) -> None:
        """Websocket disconnect"""
        if self._wst is not None or self.ws is not None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info("Websocket closed")

    def xǁMoonWebSocketǁwb_disconnect__mutmut_2(self) -> None:
        """Websocket disconnect"""
        if self._wst is None and self.ws is not None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info("Websocket closed")

    def xǁMoonWebSocketǁwb_disconnect__mutmut_3(self) -> None:
        """Websocket disconnect"""
        if self._wst is not None and self.ws is None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info("Websocket closed")

    def xǁMoonWebSocketǁwb_disconnect__mutmut_4(self) -> None:
        """Websocket disconnect"""
        if self._wst is not None and self.ws is not None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info(None)

    def xǁMoonWebSocketǁwb_disconnect__mutmut_5(self) -> None:
        """Websocket disconnect"""
        if self._wst is not None and self.ws is not None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info("XXWebsocket closedXX")

    def xǁMoonWebSocketǁwb_disconnect__mutmut_6(self) -> None:
        """Websocket disconnect"""
        if self._wst is not None and self.ws is not None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info("websocket closed")

    def xǁMoonWebSocketǁwb_disconnect__mutmut_7(self) -> None:
        """Websocket disconnect"""
        if self._wst is not None and self.ws is not None:
            self.ws.close()
            if self._wst.is_alive():
                self._wst.join()
            logger.info("WEBSOCKET CLOSED")
    
    xǁMoonWebSocketǁwb_disconnect__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁwb_disconnect__mutmut_1': xǁMoonWebSocketǁwb_disconnect__mutmut_1, 
        'xǁMoonWebSocketǁwb_disconnect__mutmut_2': xǁMoonWebSocketǁwb_disconnect__mutmut_2, 
        'xǁMoonWebSocketǁwb_disconnect__mutmut_3': xǁMoonWebSocketǁwb_disconnect__mutmut_3, 
        'xǁMoonWebSocketǁwb_disconnect__mutmut_4': xǁMoonWebSocketǁwb_disconnect__mutmut_4, 
        'xǁMoonWebSocketǁwb_disconnect__mutmut_5': xǁMoonWebSocketǁwb_disconnect__mutmut_5, 
        'xǁMoonWebSocketǁwb_disconnect__mutmut_6': xǁMoonWebSocketǁwb_disconnect__mutmut_6, 
        'xǁMoonWebSocketǁwb_disconnect__mutmut_7': xǁMoonWebSocketǁwb_disconnect__mutmut_7
    }
    xǁMoonWebSocketǁwb_disconnect__mutmut_orig.__name__ = 'xǁMoonWebSocketǁwb_disconnect'

    def on_error(self, *args) -> None:
        args = [*args]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁon_error__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁon_error__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁon_error__mutmut_orig(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 2 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_1(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = None
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_2(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[2] if len(args) == 2 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_3(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) != 2 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_4(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 3 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_5(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 2 else args[1]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_6(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 2 else args[0]
        logger.info(None)
        self.connected = False
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_7(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 2 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = None
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_8(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 2 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = True
        self.disconnected = True

    def xǁMoonWebSocketǁon_error__mutmut_9(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 2 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = None

    def xǁMoonWebSocketǁon_error__mutmut_10(self, *args) -> None:
        """Websocket error callback"""
        # First argument is ws second is error message
        # TODO: Handle error messages
        _error = args[1] if len(args) == 2 else args[0]
        logger.info(f"Websocket error, disconnected: {_error}")
        self.connected = False
        self.disconnected = False
    
    xǁMoonWebSocketǁon_error__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁon_error__mutmut_1': xǁMoonWebSocketǁon_error__mutmut_1, 
        'xǁMoonWebSocketǁon_error__mutmut_2': xǁMoonWebSocketǁon_error__mutmut_2, 
        'xǁMoonWebSocketǁon_error__mutmut_3': xǁMoonWebSocketǁon_error__mutmut_3, 
        'xǁMoonWebSocketǁon_error__mutmut_4': xǁMoonWebSocketǁon_error__mutmut_4, 
        'xǁMoonWebSocketǁon_error__mutmut_5': xǁMoonWebSocketǁon_error__mutmut_5, 
        'xǁMoonWebSocketǁon_error__mutmut_6': xǁMoonWebSocketǁon_error__mutmut_6, 
        'xǁMoonWebSocketǁon_error__mutmut_7': xǁMoonWebSocketǁon_error__mutmut_7, 
        'xǁMoonWebSocketǁon_error__mutmut_8': xǁMoonWebSocketǁon_error__mutmut_8, 
        'xǁMoonWebSocketǁon_error__mutmut_9': xǁMoonWebSocketǁon_error__mutmut_9, 
        'xǁMoonWebSocketǁon_error__mutmut_10': xǁMoonWebSocketǁon_error__mutmut_10
    }
    xǁMoonWebSocketǁon_error__mutmut_orig.__name__ = 'xǁMoonWebSocketǁon_error'

    def on_close(self, *args) -> None:
        args = [*args]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁon_close__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁon_close__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁon_close__mutmut_orig(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_1(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is not None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_2(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_3(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[2] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_4(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) != 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_5(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 4 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_6(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_7(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[3] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_8(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) != 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_9(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 4 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_10(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = None
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_11(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = True
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_12(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = None
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_13(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = True
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_14(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            None
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_15(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = None
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_16(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data=None, args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_17(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=None
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_18(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_19(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_20(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="XXDisconnectedXX", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_21(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_22(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="DISCONNECTED", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_23(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = None
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_24(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_25(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(None, close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_26(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), None)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_27(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_28(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), )
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_29(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError(None)
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_30(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("XXQApplication.instance expected non None valueXX")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_31(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("qapplication.instance expected non none value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_32(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QAPPLICATION.INSTANCE EXPECTED NON NONE VALUE")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_33(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                None
            )

        logger.info(
            f"Websocket closed, code: {_close_status_code}, message: {_close_message}"
        )

    def xǁMoonWebSocketǁon_close__mutmut_34(self, *args) -> None:
        """Websocket on close callback

        Raises:
            TypeError: When websocket Events cannot be sent because QApplication.instance `is` None
        """
        # First argument is ws, second is close status code, third is close message
        if self.ws is None:
            return
        _close_status_code = args[1] if len(args) == 3 else None
        _close_message = args[2] if len(args) == 3 else None
        self.connected = False
        self.ws.keep_running = False
        self.connection_lost[str].emit(
            f"code: {_close_status_code} | message {_close_message}"
        )
        close_event = WebSocketDisconnected(
            data="Disconnected", args=[_close_status_code, _close_message]
        )
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), close_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(
                f"Unexpected error when sending websocket close_event on disconnection: {e}"
            )

        logger.info(
            None
        )
    
    xǁMoonWebSocketǁon_close__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁon_close__mutmut_1': xǁMoonWebSocketǁon_close__mutmut_1, 
        'xǁMoonWebSocketǁon_close__mutmut_2': xǁMoonWebSocketǁon_close__mutmut_2, 
        'xǁMoonWebSocketǁon_close__mutmut_3': xǁMoonWebSocketǁon_close__mutmut_3, 
        'xǁMoonWebSocketǁon_close__mutmut_4': xǁMoonWebSocketǁon_close__mutmut_4, 
        'xǁMoonWebSocketǁon_close__mutmut_5': xǁMoonWebSocketǁon_close__mutmut_5, 
        'xǁMoonWebSocketǁon_close__mutmut_6': xǁMoonWebSocketǁon_close__mutmut_6, 
        'xǁMoonWebSocketǁon_close__mutmut_7': xǁMoonWebSocketǁon_close__mutmut_7, 
        'xǁMoonWebSocketǁon_close__mutmut_8': xǁMoonWebSocketǁon_close__mutmut_8, 
        'xǁMoonWebSocketǁon_close__mutmut_9': xǁMoonWebSocketǁon_close__mutmut_9, 
        'xǁMoonWebSocketǁon_close__mutmut_10': xǁMoonWebSocketǁon_close__mutmut_10, 
        'xǁMoonWebSocketǁon_close__mutmut_11': xǁMoonWebSocketǁon_close__mutmut_11, 
        'xǁMoonWebSocketǁon_close__mutmut_12': xǁMoonWebSocketǁon_close__mutmut_12, 
        'xǁMoonWebSocketǁon_close__mutmut_13': xǁMoonWebSocketǁon_close__mutmut_13, 
        'xǁMoonWebSocketǁon_close__mutmut_14': xǁMoonWebSocketǁon_close__mutmut_14, 
        'xǁMoonWebSocketǁon_close__mutmut_15': xǁMoonWebSocketǁon_close__mutmut_15, 
        'xǁMoonWebSocketǁon_close__mutmut_16': xǁMoonWebSocketǁon_close__mutmut_16, 
        'xǁMoonWebSocketǁon_close__mutmut_17': xǁMoonWebSocketǁon_close__mutmut_17, 
        'xǁMoonWebSocketǁon_close__mutmut_18': xǁMoonWebSocketǁon_close__mutmut_18, 
        'xǁMoonWebSocketǁon_close__mutmut_19': xǁMoonWebSocketǁon_close__mutmut_19, 
        'xǁMoonWebSocketǁon_close__mutmut_20': xǁMoonWebSocketǁon_close__mutmut_20, 
        'xǁMoonWebSocketǁon_close__mutmut_21': xǁMoonWebSocketǁon_close__mutmut_21, 
        'xǁMoonWebSocketǁon_close__mutmut_22': xǁMoonWebSocketǁon_close__mutmut_22, 
        'xǁMoonWebSocketǁon_close__mutmut_23': xǁMoonWebSocketǁon_close__mutmut_23, 
        'xǁMoonWebSocketǁon_close__mutmut_24': xǁMoonWebSocketǁon_close__mutmut_24, 
        'xǁMoonWebSocketǁon_close__mutmut_25': xǁMoonWebSocketǁon_close__mutmut_25, 
        'xǁMoonWebSocketǁon_close__mutmut_26': xǁMoonWebSocketǁon_close__mutmut_26, 
        'xǁMoonWebSocketǁon_close__mutmut_27': xǁMoonWebSocketǁon_close__mutmut_27, 
        'xǁMoonWebSocketǁon_close__mutmut_28': xǁMoonWebSocketǁon_close__mutmut_28, 
        'xǁMoonWebSocketǁon_close__mutmut_29': xǁMoonWebSocketǁon_close__mutmut_29, 
        'xǁMoonWebSocketǁon_close__mutmut_30': xǁMoonWebSocketǁon_close__mutmut_30, 
        'xǁMoonWebSocketǁon_close__mutmut_31': xǁMoonWebSocketǁon_close__mutmut_31, 
        'xǁMoonWebSocketǁon_close__mutmut_32': xǁMoonWebSocketǁon_close__mutmut_32, 
        'xǁMoonWebSocketǁon_close__mutmut_33': xǁMoonWebSocketǁon_close__mutmut_33, 
        'xǁMoonWebSocketǁon_close__mutmut_34': xǁMoonWebSocketǁon_close__mutmut_34
    }
    xǁMoonWebSocketǁon_close__mutmut_orig.__name__ = 'xǁMoonWebSocketǁon_close'

    @QtCore.pyqtSlot(name="evaluate_klippy_status")
    def evaluate_klippy_status(self) -> None:
        """Query server information for klippy status"""
        self.query_klippy_status_timer.startTimer()
        self.query_server_info_signal.emit()

    def on_open(self, *args) -> None:
        args = [*args]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁon_open__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁon_open__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁon_open__mutmut_orig(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_1(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_2(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[1] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_3(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) != 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_4(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 2 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_5(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = None
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_6(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = True
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_7(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = None
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_8(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = False
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_9(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = None
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_10(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data=None)
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_11(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="XXConnectedXX")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_12(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_13(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="CONNECTED")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_14(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = None
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_15(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_16(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(None, open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_17(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), None)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_18(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_19(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), )
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_20(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError(None)
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_21(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("XXQApplication.instance expected non None valueXX")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_22(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("qapplication.instance expected non none value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_23(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QAPPLICATION.INSTANCE EXPECTED NON NONE VALUE")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_24(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(None)

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(f"Connection to websocket achieved on {_ws}")

    def xǁMoonWebSocketǁon_open__mutmut_25(self, *args) -> None:
        """Websocket on open callback

        Raises:
            TypeError: When QApplication.instance `is` None
        """
        _ws = args[0] if len(args) == 1 else None
        self.connecting = False
        self.connected = True
        self.evaluate_klippy_status()
        open_event = WebSocketOpen(data="Connected")
        try:
            instance = QtWidgets.QApplication.instance()
            if instance is not None:
                instance.postEvent(self.parent(), open_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error opening websocket: {e}")

        self.connected_signal.emit()
        self._retry_timer.stopTimer()
        logger.info(None)
    
    xǁMoonWebSocketǁon_open__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁon_open__mutmut_1': xǁMoonWebSocketǁon_open__mutmut_1, 
        'xǁMoonWebSocketǁon_open__mutmut_2': xǁMoonWebSocketǁon_open__mutmut_2, 
        'xǁMoonWebSocketǁon_open__mutmut_3': xǁMoonWebSocketǁon_open__mutmut_3, 
        'xǁMoonWebSocketǁon_open__mutmut_4': xǁMoonWebSocketǁon_open__mutmut_4, 
        'xǁMoonWebSocketǁon_open__mutmut_5': xǁMoonWebSocketǁon_open__mutmut_5, 
        'xǁMoonWebSocketǁon_open__mutmut_6': xǁMoonWebSocketǁon_open__mutmut_6, 
        'xǁMoonWebSocketǁon_open__mutmut_7': xǁMoonWebSocketǁon_open__mutmut_7, 
        'xǁMoonWebSocketǁon_open__mutmut_8': xǁMoonWebSocketǁon_open__mutmut_8, 
        'xǁMoonWebSocketǁon_open__mutmut_9': xǁMoonWebSocketǁon_open__mutmut_9, 
        'xǁMoonWebSocketǁon_open__mutmut_10': xǁMoonWebSocketǁon_open__mutmut_10, 
        'xǁMoonWebSocketǁon_open__mutmut_11': xǁMoonWebSocketǁon_open__mutmut_11, 
        'xǁMoonWebSocketǁon_open__mutmut_12': xǁMoonWebSocketǁon_open__mutmut_12, 
        'xǁMoonWebSocketǁon_open__mutmut_13': xǁMoonWebSocketǁon_open__mutmut_13, 
        'xǁMoonWebSocketǁon_open__mutmut_14': xǁMoonWebSocketǁon_open__mutmut_14, 
        'xǁMoonWebSocketǁon_open__mutmut_15': xǁMoonWebSocketǁon_open__mutmut_15, 
        'xǁMoonWebSocketǁon_open__mutmut_16': xǁMoonWebSocketǁon_open__mutmut_16, 
        'xǁMoonWebSocketǁon_open__mutmut_17': xǁMoonWebSocketǁon_open__mutmut_17, 
        'xǁMoonWebSocketǁon_open__mutmut_18': xǁMoonWebSocketǁon_open__mutmut_18, 
        'xǁMoonWebSocketǁon_open__mutmut_19': xǁMoonWebSocketǁon_open__mutmut_19, 
        'xǁMoonWebSocketǁon_open__mutmut_20': xǁMoonWebSocketǁon_open__mutmut_20, 
        'xǁMoonWebSocketǁon_open__mutmut_21': xǁMoonWebSocketǁon_open__mutmut_21, 
        'xǁMoonWebSocketǁon_open__mutmut_22': xǁMoonWebSocketǁon_open__mutmut_22, 
        'xǁMoonWebSocketǁon_open__mutmut_23': xǁMoonWebSocketǁon_open__mutmut_23, 
        'xǁMoonWebSocketǁon_open__mutmut_24': xǁMoonWebSocketǁon_open__mutmut_24, 
        'xǁMoonWebSocketǁon_open__mutmut_25': xǁMoonWebSocketǁon_open__mutmut_25
    }
    xǁMoonWebSocketǁon_open__mutmut_orig.__name__ = 'xǁMoonWebSocketǁon_open'

    def on_message(self, *args) -> None:
        args = [*args]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁon_message__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁon_message__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁon_message__mutmut_orig(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_1(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = None  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_2(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[2] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_3(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) != 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_4(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 3 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_5(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[1]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_6(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = None
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_7(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(None)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_8(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response or response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_9(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "XXidXX" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_10(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "ID" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_11(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" not in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_12(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["XXidXX"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_13(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["ID"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_14(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] not in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_15(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = None
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_16(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(None)
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_17(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["XXidXX"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_18(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["ID"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_19(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "XXserver.infoXX" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_20(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "SERVER.INFO" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_21(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" not in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_22(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[1]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_23(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["XXresultXX"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_24(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["RESULT"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_25(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["XXklippy_stateXX"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_26(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["KLIPPY_STATE"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_27(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] != "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_28(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "XXreadyXX":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_29(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "READY":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_30(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["XXresultXX"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_31(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["RESULT"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_32(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["XXklippy_stateXX"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_33(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["KLIPPY_STATE"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_34(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] != "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_35(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "XXstartupXX":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_36(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "STARTUP":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_37(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_38(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["XXresultXX"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_39(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["RESULT"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_40(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["XXklippy_stateXX"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_41(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["KLIPPY_STATE"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_42(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] != "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_43(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "XXdisconnectedXX":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_44(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "DISCONNECTED":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_45(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_46(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    None
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_47(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["XXresultXX"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_48(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["RESULT"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_49(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["XXklippy_connectedXX"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_50(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["KLIPPY_CONNECTED"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_51(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(None)
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_52(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["XXresultXX"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_53(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["RESULT"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_54(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["XXklippy_stateXX"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_55(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["KLIPPY_STATE"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_56(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_57(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[3] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_58(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) >= 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_59(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 3 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_60(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_61(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "XXerrorXX" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_62(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "ERROR" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_63(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_64(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(None)
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_65(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get(None))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_66(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("XXresultXX"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_67(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("RESULT"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_68(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            None, _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_69(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", None, response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_70(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], None
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_71(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_72(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_73(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_74(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "XXWS request %s error: %sXX", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_75(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "ws request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_76(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS REQUEST %S ERROR: %S", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_77(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[1], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_78(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get(None)
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_79(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("XXerrorXX")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_80(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("ERROR")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_81(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "XXerrorXX" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_82(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "ERROR" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_83(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" not in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_84(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = None
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_85(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method=None,
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_86(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=None,
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_87(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=None,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_88(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_89(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_90(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_91(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="XXerrorXX",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_92(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="ERROR",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_93(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["XXerrorXX"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_94(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["ERROR"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_95(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = None
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_96(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=None,
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_97(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=None,
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_98(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=None,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_99(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_100(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_101(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_102(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(None),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_103(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[1]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_104(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["XXresultXX"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_105(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["RESULT"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_106(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "XXmethodXX" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_107(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "METHOD" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_108(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" not in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_109(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).upper() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_110(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(None).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_111(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["XXmethodXX"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_112(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["METHOD"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_113(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() not in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_114(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "XXnotify_klippy_disconnectedXX",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_115(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "NOTIFY_KLIPPY_DISCONNECTED",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_116(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "XXnotify_klippy_shutdownXX",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_117(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "NOTIFY_KLIPPY_SHUTDOWN",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_118(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = None

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_119(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=None,
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_120(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=None,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_121(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_122(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_123(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_124(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(None),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_125(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["XXmethodXX"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_126(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["METHOD"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_127(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = None
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_128(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(None, message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_129(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), None)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_130(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_131(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), )
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_132(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError(None)
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_133(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("XXQApplication.instance expected non None valueXX")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_134(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("qapplication.instance expected non none value")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_135(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QAPPLICATION.INSTANCE EXPECTED NON NONE VALUE")
        except Exception as e:
            logger.info(f"Unexpected error while creating websocket message event: {e}")

    def xǁMoonWebSocketǁon_message__mutmut_136(self, *args) -> None:
        """Websocket on message callback

        Raises:
            TypeError: Raised when events cannot be sent because QApplication.instance is None
        """
        _message = (
            args[1] if len(args) == 2 else args[0]
        )  # First argument is ws second is message

        response: dict = json.loads(_message)
        if "id" in response and response["id"] in self.request_table:
            _entry = self.request_table.pop(response["id"])
            if "server.info" in _entry[0]:
                if response["result"]["klippy_state"] == "ready":
                    self.query_klippy_status_timer.stopTimer()
                    self.api.update_status()  # Request update status immediately after klippy ready DEVDEBT
                elif response["result"]["klippy_state"] == "startup":
                    # request server.info in 2 seconds
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                elif response["result"]["klippy_state"] == "disconnected":
                    if not self.query_klippy_status_timer.running:
                        self.query_klippy_status_timer.startTimer()
                self.klippy_connected_signal.emit(
                    response["result"]["klippy_connected"]
                )
                self.klippy_state_signal.emit(response["result"]["klippy_state"])
                return
            else:
                _callback = _entry[2] if len(_entry) > 2 else None
                if _callback is not None:
                    if "error" not in response:
                        _callback(response.get("result"))
                    else:
                        logger.error(
                            "WS request %s error: %s", _entry[0], response.get("error")
                        )
                    return
                if "error" in response:
                    message_event = WebSocketMessageReceived(
                        method="error",
                        data=response["error"],
                        metadata=_entry,
                    )
                else:
                    message_event = WebSocketMessageReceived(
                        method=str(_entry[0]),
                        data=response["result"],
                        metadata=_entry,
                    )
        elif "method" in response:
            if str(response["method"]).lower() in (
                "notify_klippy_disconnected",
                "notify_klippy_shutdown",
            ):
                self.evaluate_klippy_status()

            message_event = (
                WebSocketMessageReceived(  # mainly used to pass websocket notifications
                    method=str(response["method"]),
                    data=response,
                    metadata=None,
                )
            )

        try:
            instance = QtWidgets.QApplication.instance()
            if instance:
                instance.postEvent(self.parent(), message_event)
            else:
                raise TypeError("QApplication.instance expected non None value")
        except Exception as e:
            logger.info(None)
    
    xǁMoonWebSocketǁon_message__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁon_message__mutmut_1': xǁMoonWebSocketǁon_message__mutmut_1, 
        'xǁMoonWebSocketǁon_message__mutmut_2': xǁMoonWebSocketǁon_message__mutmut_2, 
        'xǁMoonWebSocketǁon_message__mutmut_3': xǁMoonWebSocketǁon_message__mutmut_3, 
        'xǁMoonWebSocketǁon_message__mutmut_4': xǁMoonWebSocketǁon_message__mutmut_4, 
        'xǁMoonWebSocketǁon_message__mutmut_5': xǁMoonWebSocketǁon_message__mutmut_5, 
        'xǁMoonWebSocketǁon_message__mutmut_6': xǁMoonWebSocketǁon_message__mutmut_6, 
        'xǁMoonWebSocketǁon_message__mutmut_7': xǁMoonWebSocketǁon_message__mutmut_7, 
        'xǁMoonWebSocketǁon_message__mutmut_8': xǁMoonWebSocketǁon_message__mutmut_8, 
        'xǁMoonWebSocketǁon_message__mutmut_9': xǁMoonWebSocketǁon_message__mutmut_9, 
        'xǁMoonWebSocketǁon_message__mutmut_10': xǁMoonWebSocketǁon_message__mutmut_10, 
        'xǁMoonWebSocketǁon_message__mutmut_11': xǁMoonWebSocketǁon_message__mutmut_11, 
        'xǁMoonWebSocketǁon_message__mutmut_12': xǁMoonWebSocketǁon_message__mutmut_12, 
        'xǁMoonWebSocketǁon_message__mutmut_13': xǁMoonWebSocketǁon_message__mutmut_13, 
        'xǁMoonWebSocketǁon_message__mutmut_14': xǁMoonWebSocketǁon_message__mutmut_14, 
        'xǁMoonWebSocketǁon_message__mutmut_15': xǁMoonWebSocketǁon_message__mutmut_15, 
        'xǁMoonWebSocketǁon_message__mutmut_16': xǁMoonWebSocketǁon_message__mutmut_16, 
        'xǁMoonWebSocketǁon_message__mutmut_17': xǁMoonWebSocketǁon_message__mutmut_17, 
        'xǁMoonWebSocketǁon_message__mutmut_18': xǁMoonWebSocketǁon_message__mutmut_18, 
        'xǁMoonWebSocketǁon_message__mutmut_19': xǁMoonWebSocketǁon_message__mutmut_19, 
        'xǁMoonWebSocketǁon_message__mutmut_20': xǁMoonWebSocketǁon_message__mutmut_20, 
        'xǁMoonWebSocketǁon_message__mutmut_21': xǁMoonWebSocketǁon_message__mutmut_21, 
        'xǁMoonWebSocketǁon_message__mutmut_22': xǁMoonWebSocketǁon_message__mutmut_22, 
        'xǁMoonWebSocketǁon_message__mutmut_23': xǁMoonWebSocketǁon_message__mutmut_23, 
        'xǁMoonWebSocketǁon_message__mutmut_24': xǁMoonWebSocketǁon_message__mutmut_24, 
        'xǁMoonWebSocketǁon_message__mutmut_25': xǁMoonWebSocketǁon_message__mutmut_25, 
        'xǁMoonWebSocketǁon_message__mutmut_26': xǁMoonWebSocketǁon_message__mutmut_26, 
        'xǁMoonWebSocketǁon_message__mutmut_27': xǁMoonWebSocketǁon_message__mutmut_27, 
        'xǁMoonWebSocketǁon_message__mutmut_28': xǁMoonWebSocketǁon_message__mutmut_28, 
        'xǁMoonWebSocketǁon_message__mutmut_29': xǁMoonWebSocketǁon_message__mutmut_29, 
        'xǁMoonWebSocketǁon_message__mutmut_30': xǁMoonWebSocketǁon_message__mutmut_30, 
        'xǁMoonWebSocketǁon_message__mutmut_31': xǁMoonWebSocketǁon_message__mutmut_31, 
        'xǁMoonWebSocketǁon_message__mutmut_32': xǁMoonWebSocketǁon_message__mutmut_32, 
        'xǁMoonWebSocketǁon_message__mutmut_33': xǁMoonWebSocketǁon_message__mutmut_33, 
        'xǁMoonWebSocketǁon_message__mutmut_34': xǁMoonWebSocketǁon_message__mutmut_34, 
        'xǁMoonWebSocketǁon_message__mutmut_35': xǁMoonWebSocketǁon_message__mutmut_35, 
        'xǁMoonWebSocketǁon_message__mutmut_36': xǁMoonWebSocketǁon_message__mutmut_36, 
        'xǁMoonWebSocketǁon_message__mutmut_37': xǁMoonWebSocketǁon_message__mutmut_37, 
        'xǁMoonWebSocketǁon_message__mutmut_38': xǁMoonWebSocketǁon_message__mutmut_38, 
        'xǁMoonWebSocketǁon_message__mutmut_39': xǁMoonWebSocketǁon_message__mutmut_39, 
        'xǁMoonWebSocketǁon_message__mutmut_40': xǁMoonWebSocketǁon_message__mutmut_40, 
        'xǁMoonWebSocketǁon_message__mutmut_41': xǁMoonWebSocketǁon_message__mutmut_41, 
        'xǁMoonWebSocketǁon_message__mutmut_42': xǁMoonWebSocketǁon_message__mutmut_42, 
        'xǁMoonWebSocketǁon_message__mutmut_43': xǁMoonWebSocketǁon_message__mutmut_43, 
        'xǁMoonWebSocketǁon_message__mutmut_44': xǁMoonWebSocketǁon_message__mutmut_44, 
        'xǁMoonWebSocketǁon_message__mutmut_45': xǁMoonWebSocketǁon_message__mutmut_45, 
        'xǁMoonWebSocketǁon_message__mutmut_46': xǁMoonWebSocketǁon_message__mutmut_46, 
        'xǁMoonWebSocketǁon_message__mutmut_47': xǁMoonWebSocketǁon_message__mutmut_47, 
        'xǁMoonWebSocketǁon_message__mutmut_48': xǁMoonWebSocketǁon_message__mutmut_48, 
        'xǁMoonWebSocketǁon_message__mutmut_49': xǁMoonWebSocketǁon_message__mutmut_49, 
        'xǁMoonWebSocketǁon_message__mutmut_50': xǁMoonWebSocketǁon_message__mutmut_50, 
        'xǁMoonWebSocketǁon_message__mutmut_51': xǁMoonWebSocketǁon_message__mutmut_51, 
        'xǁMoonWebSocketǁon_message__mutmut_52': xǁMoonWebSocketǁon_message__mutmut_52, 
        'xǁMoonWebSocketǁon_message__mutmut_53': xǁMoonWebSocketǁon_message__mutmut_53, 
        'xǁMoonWebSocketǁon_message__mutmut_54': xǁMoonWebSocketǁon_message__mutmut_54, 
        'xǁMoonWebSocketǁon_message__mutmut_55': xǁMoonWebSocketǁon_message__mutmut_55, 
        'xǁMoonWebSocketǁon_message__mutmut_56': xǁMoonWebSocketǁon_message__mutmut_56, 
        'xǁMoonWebSocketǁon_message__mutmut_57': xǁMoonWebSocketǁon_message__mutmut_57, 
        'xǁMoonWebSocketǁon_message__mutmut_58': xǁMoonWebSocketǁon_message__mutmut_58, 
        'xǁMoonWebSocketǁon_message__mutmut_59': xǁMoonWebSocketǁon_message__mutmut_59, 
        'xǁMoonWebSocketǁon_message__mutmut_60': xǁMoonWebSocketǁon_message__mutmut_60, 
        'xǁMoonWebSocketǁon_message__mutmut_61': xǁMoonWebSocketǁon_message__mutmut_61, 
        'xǁMoonWebSocketǁon_message__mutmut_62': xǁMoonWebSocketǁon_message__mutmut_62, 
        'xǁMoonWebSocketǁon_message__mutmut_63': xǁMoonWebSocketǁon_message__mutmut_63, 
        'xǁMoonWebSocketǁon_message__mutmut_64': xǁMoonWebSocketǁon_message__mutmut_64, 
        'xǁMoonWebSocketǁon_message__mutmut_65': xǁMoonWebSocketǁon_message__mutmut_65, 
        'xǁMoonWebSocketǁon_message__mutmut_66': xǁMoonWebSocketǁon_message__mutmut_66, 
        'xǁMoonWebSocketǁon_message__mutmut_67': xǁMoonWebSocketǁon_message__mutmut_67, 
        'xǁMoonWebSocketǁon_message__mutmut_68': xǁMoonWebSocketǁon_message__mutmut_68, 
        'xǁMoonWebSocketǁon_message__mutmut_69': xǁMoonWebSocketǁon_message__mutmut_69, 
        'xǁMoonWebSocketǁon_message__mutmut_70': xǁMoonWebSocketǁon_message__mutmut_70, 
        'xǁMoonWebSocketǁon_message__mutmut_71': xǁMoonWebSocketǁon_message__mutmut_71, 
        'xǁMoonWebSocketǁon_message__mutmut_72': xǁMoonWebSocketǁon_message__mutmut_72, 
        'xǁMoonWebSocketǁon_message__mutmut_73': xǁMoonWebSocketǁon_message__mutmut_73, 
        'xǁMoonWebSocketǁon_message__mutmut_74': xǁMoonWebSocketǁon_message__mutmut_74, 
        'xǁMoonWebSocketǁon_message__mutmut_75': xǁMoonWebSocketǁon_message__mutmut_75, 
        'xǁMoonWebSocketǁon_message__mutmut_76': xǁMoonWebSocketǁon_message__mutmut_76, 
        'xǁMoonWebSocketǁon_message__mutmut_77': xǁMoonWebSocketǁon_message__mutmut_77, 
        'xǁMoonWebSocketǁon_message__mutmut_78': xǁMoonWebSocketǁon_message__mutmut_78, 
        'xǁMoonWebSocketǁon_message__mutmut_79': xǁMoonWebSocketǁon_message__mutmut_79, 
        'xǁMoonWebSocketǁon_message__mutmut_80': xǁMoonWebSocketǁon_message__mutmut_80, 
        'xǁMoonWebSocketǁon_message__mutmut_81': xǁMoonWebSocketǁon_message__mutmut_81, 
        'xǁMoonWebSocketǁon_message__mutmut_82': xǁMoonWebSocketǁon_message__mutmut_82, 
        'xǁMoonWebSocketǁon_message__mutmut_83': xǁMoonWebSocketǁon_message__mutmut_83, 
        'xǁMoonWebSocketǁon_message__mutmut_84': xǁMoonWebSocketǁon_message__mutmut_84, 
        'xǁMoonWebSocketǁon_message__mutmut_85': xǁMoonWebSocketǁon_message__mutmut_85, 
        'xǁMoonWebSocketǁon_message__mutmut_86': xǁMoonWebSocketǁon_message__mutmut_86, 
        'xǁMoonWebSocketǁon_message__mutmut_87': xǁMoonWebSocketǁon_message__mutmut_87, 
        'xǁMoonWebSocketǁon_message__mutmut_88': xǁMoonWebSocketǁon_message__mutmut_88, 
        'xǁMoonWebSocketǁon_message__mutmut_89': xǁMoonWebSocketǁon_message__mutmut_89, 
        'xǁMoonWebSocketǁon_message__mutmut_90': xǁMoonWebSocketǁon_message__mutmut_90, 
        'xǁMoonWebSocketǁon_message__mutmut_91': xǁMoonWebSocketǁon_message__mutmut_91, 
        'xǁMoonWebSocketǁon_message__mutmut_92': xǁMoonWebSocketǁon_message__mutmut_92, 
        'xǁMoonWebSocketǁon_message__mutmut_93': xǁMoonWebSocketǁon_message__mutmut_93, 
        'xǁMoonWebSocketǁon_message__mutmut_94': xǁMoonWebSocketǁon_message__mutmut_94, 
        'xǁMoonWebSocketǁon_message__mutmut_95': xǁMoonWebSocketǁon_message__mutmut_95, 
        'xǁMoonWebSocketǁon_message__mutmut_96': xǁMoonWebSocketǁon_message__mutmut_96, 
        'xǁMoonWebSocketǁon_message__mutmut_97': xǁMoonWebSocketǁon_message__mutmut_97, 
        'xǁMoonWebSocketǁon_message__mutmut_98': xǁMoonWebSocketǁon_message__mutmut_98, 
        'xǁMoonWebSocketǁon_message__mutmut_99': xǁMoonWebSocketǁon_message__mutmut_99, 
        'xǁMoonWebSocketǁon_message__mutmut_100': xǁMoonWebSocketǁon_message__mutmut_100, 
        'xǁMoonWebSocketǁon_message__mutmut_101': xǁMoonWebSocketǁon_message__mutmut_101, 
        'xǁMoonWebSocketǁon_message__mutmut_102': xǁMoonWebSocketǁon_message__mutmut_102, 
        'xǁMoonWebSocketǁon_message__mutmut_103': xǁMoonWebSocketǁon_message__mutmut_103, 
        'xǁMoonWebSocketǁon_message__mutmut_104': xǁMoonWebSocketǁon_message__mutmut_104, 
        'xǁMoonWebSocketǁon_message__mutmut_105': xǁMoonWebSocketǁon_message__mutmut_105, 
        'xǁMoonWebSocketǁon_message__mutmut_106': xǁMoonWebSocketǁon_message__mutmut_106, 
        'xǁMoonWebSocketǁon_message__mutmut_107': xǁMoonWebSocketǁon_message__mutmut_107, 
        'xǁMoonWebSocketǁon_message__mutmut_108': xǁMoonWebSocketǁon_message__mutmut_108, 
        'xǁMoonWebSocketǁon_message__mutmut_109': xǁMoonWebSocketǁon_message__mutmut_109, 
        'xǁMoonWebSocketǁon_message__mutmut_110': xǁMoonWebSocketǁon_message__mutmut_110, 
        'xǁMoonWebSocketǁon_message__mutmut_111': xǁMoonWebSocketǁon_message__mutmut_111, 
        'xǁMoonWebSocketǁon_message__mutmut_112': xǁMoonWebSocketǁon_message__mutmut_112, 
        'xǁMoonWebSocketǁon_message__mutmut_113': xǁMoonWebSocketǁon_message__mutmut_113, 
        'xǁMoonWebSocketǁon_message__mutmut_114': xǁMoonWebSocketǁon_message__mutmut_114, 
        'xǁMoonWebSocketǁon_message__mutmut_115': xǁMoonWebSocketǁon_message__mutmut_115, 
        'xǁMoonWebSocketǁon_message__mutmut_116': xǁMoonWebSocketǁon_message__mutmut_116, 
        'xǁMoonWebSocketǁon_message__mutmut_117': xǁMoonWebSocketǁon_message__mutmut_117, 
        'xǁMoonWebSocketǁon_message__mutmut_118': xǁMoonWebSocketǁon_message__mutmut_118, 
        'xǁMoonWebSocketǁon_message__mutmut_119': xǁMoonWebSocketǁon_message__mutmut_119, 
        'xǁMoonWebSocketǁon_message__mutmut_120': xǁMoonWebSocketǁon_message__mutmut_120, 
        'xǁMoonWebSocketǁon_message__mutmut_121': xǁMoonWebSocketǁon_message__mutmut_121, 
        'xǁMoonWebSocketǁon_message__mutmut_122': xǁMoonWebSocketǁon_message__mutmut_122, 
        'xǁMoonWebSocketǁon_message__mutmut_123': xǁMoonWebSocketǁon_message__mutmut_123, 
        'xǁMoonWebSocketǁon_message__mutmut_124': xǁMoonWebSocketǁon_message__mutmut_124, 
        'xǁMoonWebSocketǁon_message__mutmut_125': xǁMoonWebSocketǁon_message__mutmut_125, 
        'xǁMoonWebSocketǁon_message__mutmut_126': xǁMoonWebSocketǁon_message__mutmut_126, 
        'xǁMoonWebSocketǁon_message__mutmut_127': xǁMoonWebSocketǁon_message__mutmut_127, 
        'xǁMoonWebSocketǁon_message__mutmut_128': xǁMoonWebSocketǁon_message__mutmut_128, 
        'xǁMoonWebSocketǁon_message__mutmut_129': xǁMoonWebSocketǁon_message__mutmut_129, 
        'xǁMoonWebSocketǁon_message__mutmut_130': xǁMoonWebSocketǁon_message__mutmut_130, 
        'xǁMoonWebSocketǁon_message__mutmut_131': xǁMoonWebSocketǁon_message__mutmut_131, 
        'xǁMoonWebSocketǁon_message__mutmut_132': xǁMoonWebSocketǁon_message__mutmut_132, 
        'xǁMoonWebSocketǁon_message__mutmut_133': xǁMoonWebSocketǁon_message__mutmut_133, 
        'xǁMoonWebSocketǁon_message__mutmut_134': xǁMoonWebSocketǁon_message__mutmut_134, 
        'xǁMoonWebSocketǁon_message__mutmut_135': xǁMoonWebSocketǁon_message__mutmut_135, 
        'xǁMoonWebSocketǁon_message__mutmut_136': xǁMoonWebSocketǁon_message__mutmut_136
    }
    xǁMoonWebSocketǁon_message__mutmut_orig.__name__ = 'xǁMoonWebSocketǁon_message'

    def send_request(self, method: str, params: dict = {}, callback=None) -> bool:
        args = [method, params, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonWebSocketǁsend_request__mutmut_orig'), object.__getattribute__(self, 'xǁMoonWebSocketǁsend_request__mutmut_mutants'), args, kwargs, self)

    def xǁMoonWebSocketǁsend_request__mutmut_orig(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_1(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected and self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_2(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_3(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is not None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_4(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return True

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_5(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id = 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_6(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id -= 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_7(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 2
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_8(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = None
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_9(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = None
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_10(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "XXjsonrpcXX": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_11(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "JSONRPC": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_12(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "XX2.0XX",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_13(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "XXmethodXX": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_14(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "METHOD": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_15(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "XXparamsXX": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_16(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "PARAMS": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_17(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "XXidXX": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_18(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "ID": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_19(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(None)
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_20(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(None))
        return True

    def xǁMoonWebSocketǁsend_request__mutmut_21(self, method: str, params: dict = {}, callback=None) -> bool:
        """Send a request over the websocket

        Args:
            method (str): Websocket method name
            params (dict, optional): parameters for the websocket method. Defaults to {}.
            callback (callable, optional): Called with ``response["result"]`` when the
            response arrives. If None, the response is routed as a
            ``WebSocketMessageReceived`` event to the parent widget. Defaults to None.
        Returns:
            bool: Whether the method finished and a request was sent
        """
        if not self.connected or self.ws is None:
            return False

        self._request_id += 1
        self.request_table[self._request_id] = [method, params, callback]
        packet = {
            "jsonrpc": "2.0",
            "method": method,
            "params": params,
            "id": self._request_id,
        }
        self.ws.send(json.dumps(packet))
        return False
    
    xǁMoonWebSocketǁsend_request__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonWebSocketǁsend_request__mutmut_1': xǁMoonWebSocketǁsend_request__mutmut_1, 
        'xǁMoonWebSocketǁsend_request__mutmut_2': xǁMoonWebSocketǁsend_request__mutmut_2, 
        'xǁMoonWebSocketǁsend_request__mutmut_3': xǁMoonWebSocketǁsend_request__mutmut_3, 
        'xǁMoonWebSocketǁsend_request__mutmut_4': xǁMoonWebSocketǁsend_request__mutmut_4, 
        'xǁMoonWebSocketǁsend_request__mutmut_5': xǁMoonWebSocketǁsend_request__mutmut_5, 
        'xǁMoonWebSocketǁsend_request__mutmut_6': xǁMoonWebSocketǁsend_request__mutmut_6, 
        'xǁMoonWebSocketǁsend_request__mutmut_7': xǁMoonWebSocketǁsend_request__mutmut_7, 
        'xǁMoonWebSocketǁsend_request__mutmut_8': xǁMoonWebSocketǁsend_request__mutmut_8, 
        'xǁMoonWebSocketǁsend_request__mutmut_9': xǁMoonWebSocketǁsend_request__mutmut_9, 
        'xǁMoonWebSocketǁsend_request__mutmut_10': xǁMoonWebSocketǁsend_request__mutmut_10, 
        'xǁMoonWebSocketǁsend_request__mutmut_11': xǁMoonWebSocketǁsend_request__mutmut_11, 
        'xǁMoonWebSocketǁsend_request__mutmut_12': xǁMoonWebSocketǁsend_request__mutmut_12, 
        'xǁMoonWebSocketǁsend_request__mutmut_13': xǁMoonWebSocketǁsend_request__mutmut_13, 
        'xǁMoonWebSocketǁsend_request__mutmut_14': xǁMoonWebSocketǁsend_request__mutmut_14, 
        'xǁMoonWebSocketǁsend_request__mutmut_15': xǁMoonWebSocketǁsend_request__mutmut_15, 
        'xǁMoonWebSocketǁsend_request__mutmut_16': xǁMoonWebSocketǁsend_request__mutmut_16, 
        'xǁMoonWebSocketǁsend_request__mutmut_17': xǁMoonWebSocketǁsend_request__mutmut_17, 
        'xǁMoonWebSocketǁsend_request__mutmut_18': xǁMoonWebSocketǁsend_request__mutmut_18, 
        'xǁMoonWebSocketǁsend_request__mutmut_19': xǁMoonWebSocketǁsend_request__mutmut_19, 
        'xǁMoonWebSocketǁsend_request__mutmut_20': xǁMoonWebSocketǁsend_request__mutmut_20, 
        'xǁMoonWebSocketǁsend_request__mutmut_21': xǁMoonWebSocketǁsend_request__mutmut_21
    }
    xǁMoonWebSocketǁsend_request__mutmut_orig.__name__ = 'xǁMoonWebSocketǁsend_request'


class MoonAPI(QtCore.QObject):
    def __init__(self, ws: MoonWebSocket):
        args = [ws]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁ__init____mutmut_mutants'), args, kwargs, self)
    def xǁMoonAPIǁ__init____mutmut_orig(self, ws: MoonWebSocket):
        super(MoonAPI, self).__init__(ws)
        self._ws: MoonWebSocket = ws
    def xǁMoonAPIǁ__init____mutmut_1(self, ws: MoonWebSocket):
        super(MoonAPI, self).__init__(None)
        self._ws: MoonWebSocket = ws
    def xǁMoonAPIǁ__init____mutmut_2(self, ws: MoonWebSocket):
        super(None, self).__init__(ws)
        self._ws: MoonWebSocket = ws
    def xǁMoonAPIǁ__init____mutmut_3(self, ws: MoonWebSocket):
        super(MoonAPI, None).__init__(ws)
        self._ws: MoonWebSocket = ws
    def xǁMoonAPIǁ__init____mutmut_4(self, ws: MoonWebSocket):
        super(self).__init__(ws)
        self._ws: MoonWebSocket = ws
    def xǁMoonAPIǁ__init____mutmut_5(self, ws: MoonWebSocket):
        super(MoonAPI, ).__init__(ws)
        self._ws: MoonWebSocket = ws
    def xǁMoonAPIǁ__init____mutmut_6(self, ws: MoonWebSocket):
        super(MoonAPI, self).__init__(ws)
        self._ws: MoonWebSocket = None
    
    xǁMoonAPIǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁ__init____mutmut_1': xǁMoonAPIǁ__init____mutmut_1, 
        'xǁMoonAPIǁ__init____mutmut_2': xǁMoonAPIǁ__init____mutmut_2, 
        'xǁMoonAPIǁ__init____mutmut_3': xǁMoonAPIǁ__init____mutmut_3, 
        'xǁMoonAPIǁ__init____mutmut_4': xǁMoonAPIǁ__init____mutmut_4, 
        'xǁMoonAPIǁ__init____mutmut_5': xǁMoonAPIǁ__init____mutmut_5, 
        'xǁMoonAPIǁ__init____mutmut_6': xǁMoonAPIǁ__init____mutmut_6
    }
    xǁMoonAPIǁ__init____mutmut_orig.__name__ = 'xǁMoonAPIǁ__init__'

    @QtCore.pyqtSlot(name="api_query_server_info")
    def api_query_server_info(self):
        """Query server information"""
        return self._ws.send_request(method="server.info")

    def identify_connection(
        self, client_name, version, type, url, access_token, api_key
    ):
        args = [client_name, version, type, url, access_token, api_key]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁidentify_connection__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁidentify_connection__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁidentify_connection__mutmut_orig(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_1(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method=None,
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_2(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params=None,
        )

    def xǁMoonAPIǁidentify_connection__mutmut_3(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_4(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            )

    def xǁMoonAPIǁidentify_connection__mutmut_5(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="XXserver.connection.identifyXX",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_6(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="SERVER.CONNECTION.IDENTIFY",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_7(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "XXclient_nameXX": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_8(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "CLIENT_NAME": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_9(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "XXversionXX": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_10(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "VERSION": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_11(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "XXtypeXX": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_12(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "TYPE": type,
                "url": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_13(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "XXurlXX": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_14(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "URL": url,
                "access_token": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_15(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "XXaccess_tokenXX": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_16(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "ACCESS_TOKEN": access_token,
                "api_key": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_17(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "XXapi_keyXX": api_key,
            },
        )

    def xǁMoonAPIǁidentify_connection__mutmut_18(
        self, client_name, version, type, url, access_token, api_key
    ):
        """Request moonraker to identify connection"""
        return self._ws.send_request(
            method="server.connection.identify",
            params={
                "client_name": client_name,
                "version": version,
                "type": type,
                "url": url,
                "access_token": access_token,
                "API_KEY": api_key,
            },
        )
    
    xǁMoonAPIǁidentify_connection__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁidentify_connection__mutmut_1': xǁMoonAPIǁidentify_connection__mutmut_1, 
        'xǁMoonAPIǁidentify_connection__mutmut_2': xǁMoonAPIǁidentify_connection__mutmut_2, 
        'xǁMoonAPIǁidentify_connection__mutmut_3': xǁMoonAPIǁidentify_connection__mutmut_3, 
        'xǁMoonAPIǁidentify_connection__mutmut_4': xǁMoonAPIǁidentify_connection__mutmut_4, 
        'xǁMoonAPIǁidentify_connection__mutmut_5': xǁMoonAPIǁidentify_connection__mutmut_5, 
        'xǁMoonAPIǁidentify_connection__mutmut_6': xǁMoonAPIǁidentify_connection__mutmut_6, 
        'xǁMoonAPIǁidentify_connection__mutmut_7': xǁMoonAPIǁidentify_connection__mutmut_7, 
        'xǁMoonAPIǁidentify_connection__mutmut_8': xǁMoonAPIǁidentify_connection__mutmut_8, 
        'xǁMoonAPIǁidentify_connection__mutmut_9': xǁMoonAPIǁidentify_connection__mutmut_9, 
        'xǁMoonAPIǁidentify_connection__mutmut_10': xǁMoonAPIǁidentify_connection__mutmut_10, 
        'xǁMoonAPIǁidentify_connection__mutmut_11': xǁMoonAPIǁidentify_connection__mutmut_11, 
        'xǁMoonAPIǁidentify_connection__mutmut_12': xǁMoonAPIǁidentify_connection__mutmut_12, 
        'xǁMoonAPIǁidentify_connection__mutmut_13': xǁMoonAPIǁidentify_connection__mutmut_13, 
        'xǁMoonAPIǁidentify_connection__mutmut_14': xǁMoonAPIǁidentify_connection__mutmut_14, 
        'xǁMoonAPIǁidentify_connection__mutmut_15': xǁMoonAPIǁidentify_connection__mutmut_15, 
        'xǁMoonAPIǁidentify_connection__mutmut_16': xǁMoonAPIǁidentify_connection__mutmut_16, 
        'xǁMoonAPIǁidentify_connection__mutmut_17': xǁMoonAPIǁidentify_connection__mutmut_17, 
        'xǁMoonAPIǁidentify_connection__mutmut_18': xǁMoonAPIǁidentify_connection__mutmut_18
    }
    xǁMoonAPIǁidentify_connection__mutmut_orig.__name__ = 'xǁMoonAPIǁidentify_connection'

    def request_temperature_cached_data(self, include_monitors: bool = False):
        args = [include_monitors]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_orig(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="server.temperature_store",
            params={"include_monitors": include_monitors},
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_1(self, include_monitors: bool = True):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="server.temperature_store",
            params={"include_monitors": include_monitors},
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_2(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method=None,
            params={"include_monitors": include_monitors},
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_3(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="server.temperature_store",
            params=None,
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_4(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            params={"include_monitors": include_monitors},
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_5(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="server.temperature_store",
            )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_6(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="XXserver.temperature_storeXX",
            params={"include_monitors": include_monitors},
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_7(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="SERVER.TEMPERATURE_STORE",
            params={"include_monitors": include_monitors},
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_8(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="server.temperature_store",
            params={"XXinclude_monitorsXX": include_monitors},
        )

    def xǁMoonAPIǁrequest_temperature_cached_data__mutmut_9(self, include_monitors: bool = False):
        """Request stored temperature monitors"""
        return self._ws.send_request(
            method="server.temperature_store",
            params={"INCLUDE_MONITORS": include_monitors},
        )
    
    xǁMoonAPIǁrequest_temperature_cached_data__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_1': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_1, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_2': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_2, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_3': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_3, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_4': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_4, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_5': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_5, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_6': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_6, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_7': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_7, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_8': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_8, 
        'xǁMoonAPIǁrequest_temperature_cached_data__mutmut_9': xǁMoonAPIǁrequest_temperature_cached_data__mutmut_9
    }
    xǁMoonAPIǁrequest_temperature_cached_data__mutmut_orig.__name__ = 'xǁMoonAPIǁrequest_temperature_cached_data'

    @QtCore.pyqtSlot(name="query_printer_info")
    def request_printer_info(self):
        """Requested printer information"""
        return self._ws.send_request(method="printer.info")

    @QtCore.pyqtSlot(name="get_available_objects")
    def get_available_objects(self):
        """Request available printer objects"""
        return self._ws.send_request(method="printer.objects.list")

    @QtCore.pyqtSlot(dict, name="query_object")
    def object_query(self, objects: dict):
        """Query printer object"""
        return self._ws.send_request(
            method="printer.objects.query", params={"objects": objects}
        )

    @QtCore.pyqtSlot(dict, name="object_subscription")
    def object_subscription(self, objects: dict):
        """Subscribe to printer object"""
        return self._ws.send_request(
            method="printer.objects.subscribe", params={"objects": objects}
        )

    @QtCore.pyqtSlot(name="ws_query_endstops")
    def query_endstops(self):
        """Query printer endstops"""
        return self._ws.send_request(method="printer.query_endstops.status")

    @QtCore.pyqtSlot(str, name="run_gcode")
    def run_gcode(self, gcode: str):
        """Run Gcode"""
        if isinstance(gcode, str) is False or gcode is None:
            return False
        return self._ws.send_request(
            method="printer.gcode.script", params={"script": gcode}
        )

    def gcode_help(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁgcode_help__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁgcode_help__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁgcode_help__mutmut_orig(self):
        """Request Gcode information"""
        return self._ws.send_request(method="printer.gcode.help")

    def xǁMoonAPIǁgcode_help__mutmut_1(self):
        """Request Gcode information"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁgcode_help__mutmut_2(self):
        """Request Gcode information"""
        return self._ws.send_request(method="XXprinter.gcode.helpXX")

    def xǁMoonAPIǁgcode_help__mutmut_3(self):
        """Request Gcode information"""
        return self._ws.send_request(method="PRINTER.GCODE.HELP")
    
    xǁMoonAPIǁgcode_help__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁgcode_help__mutmut_1': xǁMoonAPIǁgcode_help__mutmut_1, 
        'xǁMoonAPIǁgcode_help__mutmut_2': xǁMoonAPIǁgcode_help__mutmut_2, 
        'xǁMoonAPIǁgcode_help__mutmut_3': xǁMoonAPIǁgcode_help__mutmut_3
    }
    xǁMoonAPIǁgcode_help__mutmut_orig.__name__ = 'xǁMoonAPIǁgcode_help'

    @QtCore.pyqtSlot(str, name="start_print")
    def start_print(self, filename):
        """Start print job"""
        return self._ws.send_request(
            method="printer.print.start", params={"filename": filename}
        )

    @QtCore.pyqtSlot(name="pause_print")
    def pause_print(self):
        """Pause print job"""
        return self._ws.send_request(method="printer.print.pause")

    @QtCore.pyqtSlot(name="resume_print")
    def resume_print(self):
        """Resume print job"""
        return self._ws.send_request(method="printer.print.resume")

    @QtCore.pyqtSlot(name="stop_print")
    def cancel_print(self):
        """Cancel print job"""
        return self._ws.send_request(method="printer.print.cancel")

    def machine_shutdown(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁmachine_shutdown__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁmachine_shutdown__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁmachine_shutdown__mutmut_orig(self):
        """Request machine shutdown"""
        return self._ws.send_request(method="machine.shutdown")

    def xǁMoonAPIǁmachine_shutdown__mutmut_1(self):
        """Request machine shutdown"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁmachine_shutdown__mutmut_2(self):
        """Request machine shutdown"""
        return self._ws.send_request(method="XXmachine.shutdownXX")

    def xǁMoonAPIǁmachine_shutdown__mutmut_3(self):
        """Request machine shutdown"""
        return self._ws.send_request(method="MACHINE.SHUTDOWN")
    
    xǁMoonAPIǁmachine_shutdown__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁmachine_shutdown__mutmut_1': xǁMoonAPIǁmachine_shutdown__mutmut_1, 
        'xǁMoonAPIǁmachine_shutdown__mutmut_2': xǁMoonAPIǁmachine_shutdown__mutmut_2, 
        'xǁMoonAPIǁmachine_shutdown__mutmut_3': xǁMoonAPIǁmachine_shutdown__mutmut_3
    }
    xǁMoonAPIǁmachine_shutdown__mutmut_orig.__name__ = 'xǁMoonAPIǁmachine_shutdown'

    def machine_reboot(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁmachine_reboot__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁmachine_reboot__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁmachine_reboot__mutmut_orig(self):
        """Request machine reboot"""
        return self._ws.send_request(method="machine.reboot")

    def xǁMoonAPIǁmachine_reboot__mutmut_1(self):
        """Request machine reboot"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁmachine_reboot__mutmut_2(self):
        """Request machine reboot"""
        return self._ws.send_request(method="XXmachine.rebootXX")

    def xǁMoonAPIǁmachine_reboot__mutmut_3(self):
        """Request machine reboot"""
        return self._ws.send_request(method="MACHINE.REBOOT")
    
    xǁMoonAPIǁmachine_reboot__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁmachine_reboot__mutmut_1': xǁMoonAPIǁmachine_reboot__mutmut_1, 
        'xǁMoonAPIǁmachine_reboot__mutmut_2': xǁMoonAPIǁmachine_reboot__mutmut_2, 
        'xǁMoonAPIǁmachine_reboot__mutmut_3': xǁMoonAPIǁmachine_reboot__mutmut_3
    }
    xǁMoonAPIǁmachine_reboot__mutmut_orig.__name__ = 'xǁMoonAPIǁmachine_reboot'

    def restart_server(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁrestart_server__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁrestart_server__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁrestart_server__mutmut_orig(self):
        """Request server restart"""
        return self._ws.send_request(method="server.restart")

    def xǁMoonAPIǁrestart_server__mutmut_1(self):
        """Request server restart"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁrestart_server__mutmut_2(self):
        """Request server restart"""
        return self._ws.send_request(method="XXserver.restartXX")

    def xǁMoonAPIǁrestart_server__mutmut_3(self):
        """Request server restart"""
        return self._ws.send_request(method="SERVER.RESTART")
    
    xǁMoonAPIǁrestart_server__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁrestart_server__mutmut_1': xǁMoonAPIǁrestart_server__mutmut_1, 
        'xǁMoonAPIǁrestart_server__mutmut_2': xǁMoonAPIǁrestart_server__mutmut_2, 
        'xǁMoonAPIǁrestart_server__mutmut_3': xǁMoonAPIǁrestart_server__mutmut_3
    }
    xǁMoonAPIǁrestart_server__mutmut_orig.__name__ = 'xǁMoonAPIǁrestart_server'

    def restart_service(self, service):
        args = [service]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁrestart_service__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁrestart_service__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁrestart_service__mutmut_orig(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_1(self, service):
        """Request service restart"""
        if service is None and isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_2(self, service):
        """Request service restart"""
        if service is not None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_3(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is not False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_4(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is True:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_5(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return True
        return self._ws.send_request(
            method="machine.services.restart", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_6(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method=None, params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_7(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params=None
        )

    def xǁMoonAPIǁrestart_service__mutmut_8(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_9(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", )

    def xǁMoonAPIǁrestart_service__mutmut_10(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="XXmachine.services.restartXX", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_11(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="MACHINE.SERVICES.RESTART", params={"service": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_12(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params={"XXserviceXX": service}
        )

    def xǁMoonAPIǁrestart_service__mutmut_13(self, service):
        """Request service restart"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.restart", params={"SERVICE": service}
        )
    
    xǁMoonAPIǁrestart_service__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁrestart_service__mutmut_1': xǁMoonAPIǁrestart_service__mutmut_1, 
        'xǁMoonAPIǁrestart_service__mutmut_2': xǁMoonAPIǁrestart_service__mutmut_2, 
        'xǁMoonAPIǁrestart_service__mutmut_3': xǁMoonAPIǁrestart_service__mutmut_3, 
        'xǁMoonAPIǁrestart_service__mutmut_4': xǁMoonAPIǁrestart_service__mutmut_4, 
        'xǁMoonAPIǁrestart_service__mutmut_5': xǁMoonAPIǁrestart_service__mutmut_5, 
        'xǁMoonAPIǁrestart_service__mutmut_6': xǁMoonAPIǁrestart_service__mutmut_6, 
        'xǁMoonAPIǁrestart_service__mutmut_7': xǁMoonAPIǁrestart_service__mutmut_7, 
        'xǁMoonAPIǁrestart_service__mutmut_8': xǁMoonAPIǁrestart_service__mutmut_8, 
        'xǁMoonAPIǁrestart_service__mutmut_9': xǁMoonAPIǁrestart_service__mutmut_9, 
        'xǁMoonAPIǁrestart_service__mutmut_10': xǁMoonAPIǁrestart_service__mutmut_10, 
        'xǁMoonAPIǁrestart_service__mutmut_11': xǁMoonAPIǁrestart_service__mutmut_11, 
        'xǁMoonAPIǁrestart_service__mutmut_12': xǁMoonAPIǁrestart_service__mutmut_12, 
        'xǁMoonAPIǁrestart_service__mutmut_13': xǁMoonAPIǁrestart_service__mutmut_13
    }
    xǁMoonAPIǁrestart_service__mutmut_orig.__name__ = 'xǁMoonAPIǁrestart_service'

    @QtCore.pyqtSlot(name="firmware_restart")
    def firmware_restart(self):
        """Request Klipper firmware restart

        HTTP_REQUEST: POST /printer/firmware_restart

        JSON_RPC_REQUEST: printer.firmware_restart
        """
        return self._ws.send_request(method="printer.firmware_restart")

    def stop_service(self, service):
        args = [service]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁstop_service__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁstop_service__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁstop_service__mutmut_orig(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_1(self, service):
        """Request service stop"""
        if service is None and isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_2(self, service):
        """Request service stop"""
        if service is not None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_3(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is not False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_4(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is True:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_5(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return True
        return self._ws.send_request(
            method="machine.services.stop", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_6(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method=None, params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_7(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params=None
        )

    def xǁMoonAPIǁstop_service__mutmut_8(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_9(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", )

    def xǁMoonAPIǁstop_service__mutmut_10(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="XXmachine.services.stopXX", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_11(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="MACHINE.SERVICES.STOP", params={"service": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_12(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params={"XXserviceXX": service}
        )

    def xǁMoonAPIǁstop_service__mutmut_13(self, service):
        """Request service stop"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.stop", params={"SERVICE": service}
        )
    
    xǁMoonAPIǁstop_service__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁstop_service__mutmut_1': xǁMoonAPIǁstop_service__mutmut_1, 
        'xǁMoonAPIǁstop_service__mutmut_2': xǁMoonAPIǁstop_service__mutmut_2, 
        'xǁMoonAPIǁstop_service__mutmut_3': xǁMoonAPIǁstop_service__mutmut_3, 
        'xǁMoonAPIǁstop_service__mutmut_4': xǁMoonAPIǁstop_service__mutmut_4, 
        'xǁMoonAPIǁstop_service__mutmut_5': xǁMoonAPIǁstop_service__mutmut_5, 
        'xǁMoonAPIǁstop_service__mutmut_6': xǁMoonAPIǁstop_service__mutmut_6, 
        'xǁMoonAPIǁstop_service__mutmut_7': xǁMoonAPIǁstop_service__mutmut_7, 
        'xǁMoonAPIǁstop_service__mutmut_8': xǁMoonAPIǁstop_service__mutmut_8, 
        'xǁMoonAPIǁstop_service__mutmut_9': xǁMoonAPIǁstop_service__mutmut_9, 
        'xǁMoonAPIǁstop_service__mutmut_10': xǁMoonAPIǁstop_service__mutmut_10, 
        'xǁMoonAPIǁstop_service__mutmut_11': xǁMoonAPIǁstop_service__mutmut_11, 
        'xǁMoonAPIǁstop_service__mutmut_12': xǁMoonAPIǁstop_service__mutmut_12, 
        'xǁMoonAPIǁstop_service__mutmut_13': xǁMoonAPIǁstop_service__mutmut_13
    }
    xǁMoonAPIǁstop_service__mutmut_orig.__name__ = 'xǁMoonAPIǁstop_service'

    def start_service(self, service):
        args = [service]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁstart_service__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁstart_service__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁstart_service__mutmut_orig(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.start", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_1(self, service):
        """Request service start"""
        if service is None and isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.start", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_2(self, service):
        """Request service start"""
        if service is not None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.start", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_3(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is not False:
            return False
        return self._ws.send_request(
            method="machine.services.start", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_4(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is True:
            return False
        return self._ws.send_request(
            method="machine.services.start", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_5(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return True
        return self._ws.send_request(
            method="machine.services.start", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_6(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method=None, params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_7(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.start", params=None
        )

    def xǁMoonAPIǁstart_service__mutmut_8(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_9(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.start", )

    def xǁMoonAPIǁstart_service__mutmut_10(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="XXmachine.services.startXX", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_11(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="MACHINE.SERVICES.START", params={"service": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_12(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.start", params={"XXserviceXX": service}
        )

    def xǁMoonAPIǁstart_service__mutmut_13(self, service):
        """Request service start"""
        if service is None or isinstance(service, str) is False:
            return False
        return self._ws.send_request(
            method="machine.services.start", params={"SERVICE": service}
        )
    
    xǁMoonAPIǁstart_service__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁstart_service__mutmut_1': xǁMoonAPIǁstart_service__mutmut_1, 
        'xǁMoonAPIǁstart_service__mutmut_2': xǁMoonAPIǁstart_service__mutmut_2, 
        'xǁMoonAPIǁstart_service__mutmut_3': xǁMoonAPIǁstart_service__mutmut_3, 
        'xǁMoonAPIǁstart_service__mutmut_4': xǁMoonAPIǁstart_service__mutmut_4, 
        'xǁMoonAPIǁstart_service__mutmut_5': xǁMoonAPIǁstart_service__mutmut_5, 
        'xǁMoonAPIǁstart_service__mutmut_6': xǁMoonAPIǁstart_service__mutmut_6, 
        'xǁMoonAPIǁstart_service__mutmut_7': xǁMoonAPIǁstart_service__mutmut_7, 
        'xǁMoonAPIǁstart_service__mutmut_8': xǁMoonAPIǁstart_service__mutmut_8, 
        'xǁMoonAPIǁstart_service__mutmut_9': xǁMoonAPIǁstart_service__mutmut_9, 
        'xǁMoonAPIǁstart_service__mutmut_10': xǁMoonAPIǁstart_service__mutmut_10, 
        'xǁMoonAPIǁstart_service__mutmut_11': xǁMoonAPIǁstart_service__mutmut_11, 
        'xǁMoonAPIǁstart_service__mutmut_12': xǁMoonAPIǁstart_service__mutmut_12, 
        'xǁMoonAPIǁstart_service__mutmut_13': xǁMoonAPIǁstart_service__mutmut_13
    }
    xǁMoonAPIǁstart_service__mutmut_orig.__name__ = 'xǁMoonAPIǁstart_service'

    def get_sudo_info(self, permission: bool = False):
        args = [permission]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_sudo_info__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_sudo_info__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_sudo_info__mutmut_orig(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_1(self, permission: bool = True):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_2(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is not False:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_3(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is True:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_4(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return True
        return self._ws.send_request(
            method="machine.sudo.info", params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_5(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method=None, params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_6(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", params=None
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_7(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_8(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", )

    def xǁMoonAPIǁget_sudo_info__mutmut_9(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="XXmachine.sudo.infoXX", params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_10(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="MACHINE.SUDO.INFO", params={"check_access": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_11(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", params={"XXcheck_accessXX": permission}
        )

    def xǁMoonAPIǁget_sudo_info__mutmut_12(self, permission: bool = False):
        """Request sudo privileges information"""
        if isinstance(permission, bool) is False:
            return False
        return self._ws.send_request(
            method="machine.sudo.info", params={"CHECK_ACCESS": permission}
        )
    
    xǁMoonAPIǁget_sudo_info__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_sudo_info__mutmut_1': xǁMoonAPIǁget_sudo_info__mutmut_1, 
        'xǁMoonAPIǁget_sudo_info__mutmut_2': xǁMoonAPIǁget_sudo_info__mutmut_2, 
        'xǁMoonAPIǁget_sudo_info__mutmut_3': xǁMoonAPIǁget_sudo_info__mutmut_3, 
        'xǁMoonAPIǁget_sudo_info__mutmut_4': xǁMoonAPIǁget_sudo_info__mutmut_4, 
        'xǁMoonAPIǁget_sudo_info__mutmut_5': xǁMoonAPIǁget_sudo_info__mutmut_5, 
        'xǁMoonAPIǁget_sudo_info__mutmut_6': xǁMoonAPIǁget_sudo_info__mutmut_6, 
        'xǁMoonAPIǁget_sudo_info__mutmut_7': xǁMoonAPIǁget_sudo_info__mutmut_7, 
        'xǁMoonAPIǁget_sudo_info__mutmut_8': xǁMoonAPIǁget_sudo_info__mutmut_8, 
        'xǁMoonAPIǁget_sudo_info__mutmut_9': xǁMoonAPIǁget_sudo_info__mutmut_9, 
        'xǁMoonAPIǁget_sudo_info__mutmut_10': xǁMoonAPIǁget_sudo_info__mutmut_10, 
        'xǁMoonAPIǁget_sudo_info__mutmut_11': xǁMoonAPIǁget_sudo_info__mutmut_11, 
        'xǁMoonAPIǁget_sudo_info__mutmut_12': xǁMoonAPIǁget_sudo_info__mutmut_12
    }
    xǁMoonAPIǁget_sudo_info__mutmut_orig.__name__ = 'xǁMoonAPIǁget_sudo_info'

    def get_usb_devices(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_usb_devices__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_usb_devices__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_usb_devices__mutmut_orig(self):
        """Request available usb devices"""
        return self._ws.send_request(method="machine.peripherals.usb")

    def xǁMoonAPIǁget_usb_devices__mutmut_1(self):
        """Request available usb devices"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁget_usb_devices__mutmut_2(self):
        """Request available usb devices"""
        return self._ws.send_request(method="XXmachine.peripherals.usbXX")

    def xǁMoonAPIǁget_usb_devices__mutmut_3(self):
        """Request available usb devices"""
        return self._ws.send_request(method="MACHINE.PERIPHERALS.USB")
    
    xǁMoonAPIǁget_usb_devices__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_usb_devices__mutmut_1': xǁMoonAPIǁget_usb_devices__mutmut_1, 
        'xǁMoonAPIǁget_usb_devices__mutmut_2': xǁMoonAPIǁget_usb_devices__mutmut_2, 
        'xǁMoonAPIǁget_usb_devices__mutmut_3': xǁMoonAPIǁget_usb_devices__mutmut_3
    }
    xǁMoonAPIǁget_usb_devices__mutmut_orig.__name__ = 'xǁMoonAPIǁget_usb_devices'

    def get_serial_devices(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_serial_devices__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_serial_devices__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_serial_devices__mutmut_orig(self):
        """Request available serial devices"""
        return self._ws.send_request(method="machine.peripherals.serial")

    def xǁMoonAPIǁget_serial_devices__mutmut_1(self):
        """Request available serial devices"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁget_serial_devices__mutmut_2(self):
        """Request available serial devices"""
        return self._ws.send_request(method="XXmachine.peripherals.serialXX")

    def xǁMoonAPIǁget_serial_devices__mutmut_3(self):
        """Request available serial devices"""
        return self._ws.send_request(method="MACHINE.PERIPHERALS.SERIAL")
    
    xǁMoonAPIǁget_serial_devices__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_serial_devices__mutmut_1': xǁMoonAPIǁget_serial_devices__mutmut_1, 
        'xǁMoonAPIǁget_serial_devices__mutmut_2': xǁMoonAPIǁget_serial_devices__mutmut_2, 
        'xǁMoonAPIǁget_serial_devices__mutmut_3': xǁMoonAPIǁget_serial_devices__mutmut_3
    }
    xǁMoonAPIǁget_serial_devices__mutmut_orig.__name__ = 'xǁMoonAPIǁget_serial_devices'

    def get_video_devices(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_video_devices__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_video_devices__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_video_devices__mutmut_orig(self):
        """Request available video devices"""
        return self._ws.send_request(method="machine.peripherals.video")

    def xǁMoonAPIǁget_video_devices__mutmut_1(self):
        """Request available video devices"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁget_video_devices__mutmut_2(self):
        """Request available video devices"""
        return self._ws.send_request(method="XXmachine.peripherals.videoXX")

    def xǁMoonAPIǁget_video_devices__mutmut_3(self):
        """Request available video devices"""
        return self._ws.send_request(method="MACHINE.PERIPHERALS.VIDEO")
    
    xǁMoonAPIǁget_video_devices__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_video_devices__mutmut_1': xǁMoonAPIǁget_video_devices__mutmut_1, 
        'xǁMoonAPIǁget_video_devices__mutmut_2': xǁMoonAPIǁget_video_devices__mutmut_2, 
        'xǁMoonAPIǁget_video_devices__mutmut_3': xǁMoonAPIǁget_video_devices__mutmut_3
    }
    xǁMoonAPIǁget_video_devices__mutmut_orig.__name__ = 'xǁMoonAPIǁget_video_devices'

    def get_cabus_devices(self, interface: str = "can0"):
        args = [interface]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_cabus_devices__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_cabus_devices__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_cabus_devices__mutmut_orig(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="machine.peripherals.canbus",
            params={"interface": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_1(self, interface: str = "XXcan0XX"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="machine.peripherals.canbus",
            params={"interface": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_2(self, interface: str = "CAN0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="machine.peripherals.canbus",
            params={"interface": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_3(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method=None,
            params={"interface": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_4(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="machine.peripherals.canbus",
            params=None,
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_5(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            params={"interface": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_6(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="machine.peripherals.canbus",
            )

    def xǁMoonAPIǁget_cabus_devices__mutmut_7(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="XXmachine.peripherals.canbusXX",
            params={"interface": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_8(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="MACHINE.PERIPHERALS.CANBUS",
            params={"interface": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_9(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="machine.peripherals.canbus",
            params={"XXinterfaceXX": interface},
        )

    def xǁMoonAPIǁget_cabus_devices__mutmut_10(self, interface: str = "can0"):
        """Request available CAN devices"""
        return self._ws.send_request(
            method="machine.peripherals.canbus",
            params={"INTERFACE": interface},
        )
    
    xǁMoonAPIǁget_cabus_devices__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_cabus_devices__mutmut_1': xǁMoonAPIǁget_cabus_devices__mutmut_1, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_2': xǁMoonAPIǁget_cabus_devices__mutmut_2, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_3': xǁMoonAPIǁget_cabus_devices__mutmut_3, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_4': xǁMoonAPIǁget_cabus_devices__mutmut_4, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_5': xǁMoonAPIǁget_cabus_devices__mutmut_5, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_6': xǁMoonAPIǁget_cabus_devices__mutmut_6, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_7': xǁMoonAPIǁget_cabus_devices__mutmut_7, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_8': xǁMoonAPIǁget_cabus_devices__mutmut_8, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_9': xǁMoonAPIǁget_cabus_devices__mutmut_9, 
        'xǁMoonAPIǁget_cabus_devices__mutmut_10': xǁMoonAPIǁget_cabus_devices__mutmut_10
    }
    xǁMoonAPIǁget_cabus_devices__mutmut_orig.__name__ = 'xǁMoonAPIǁget_cabus_devices'

    @QtCore.pyqtSlot(name="api-request-file-list")
    @QtCore.pyqtSlot(str, name="api-request-file-list")
    def get_file_list(self, root_folder: str = "gcodes"):
        """Get available files"""
        return self._ws.send_request(
            method="server.files.list", params={"root": root_folder}
        )

    @QtCore.pyqtSlot(name="api-list-roots")
    def list_registered_roots(self):
        """Get available root directories"""
        return self._ws.send_request(method="server.files.roots")

    @QtCore.pyqtSlot(str, name="api_request_file_list")
    def get_gcode_metadata(self, filename_dir: str):
        """Request gcode metadata"""
        if not isinstance(filename_dir, str) or not filename_dir:
            return False
        return self._ws.send_request(
            method="server.files.metadata", params={"filename": filename_dir}
        )

    @QtCore.pyqtSlot(str, name="api-scan-gcode-metadata")
    def scan_gcode_metadata(self, filename_dir: str):
        """Scan gcode metadata"""
        if isinstance(filename_dir, str) is False or filename_dir is None:
            return False
        return self._ws.send_request(
            method="server.files.metascan", params={"filename": filename_dir}
        )

    @QtCore.pyqtSlot(name="api_get_gcode_thumbnail")
    def get_gcode_thumbnail(self, filename_dir: str):
        """Request gcode thumbnail"""
        if isinstance(filename_dir, str) is False or filename_dir is None:
            return False
        return self._ws.send_request(
            method="server.files.thumbnails", params={"filename": filename_dir}
        )

    @QtCore.pyqtSlot(str, str, name="api-delete-file")
    @QtCore.pyqtSlot(str, name="api-delete-file")
    def delete_file(self, filename: str, root_dir: str = "gcodes"):
        """Request file deletion"""
        filepath = f"{root_dir}/{filename}"
        filepath = f"gcodes/{root_dir}/{filename}" if root_dir != "gcodes" else filepath
        return self._ws.send_request(
            method="server.files.delete_file",
            params={"path": filepath},
        )

    @QtCore.pyqtSlot(str, str, name="api-file_download")
    def download_file(self, root: str, filename: str):
        """Retrieves file *filename* at root *root*, the filename must include the relative path if
        it is not in the root folder

        Args:
            root (str): root directory where the file lies
            filename (str): file to download

        Returns:
            _type_: _description_
        """
        if not isinstance(filename, str) or not isinstance(root, str):
            return False

        return self._ws._moonRest.get_request(f"/server/files/{root}/{filename}")

    @QtCore.pyqtSlot(name="api-get-dir-info")
    @QtCore.pyqtSlot(str, name="api-get-dir-info")
    @QtCore.pyqtSlot(str, bool, name="api-get-dir-info")
    def get_dir_information(self, directory: str = "", extended: bool = True):
        """Request directory information"""
        if not isinstance(directory, str):
            return False
        return self._ws.send_request(
            method="server.files.get_directory",
            params={"path": f"gcodes/{directory}", "extended": extended},
        )

    def create_directory(self, directory: str):
        args = [directory]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁcreate_directory__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁcreate_directory__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁcreate_directory__mutmut_orig(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_1(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False and directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_2(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is not False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_3(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is True or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_4(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is not None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_5(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return True
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_6(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method=None,
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_7(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params=None,
        )

    def xǁMoonAPIǁcreate_directory__mutmut_8(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_9(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            )

    def xǁMoonAPIǁcreate_directory__mutmut_10(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="XXserver.files.post_directoryXX",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_11(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="SERVER.FILES.POST_DIRECTORY",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_12(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "XXpathXX": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁcreate_directory__mutmut_13(self, directory: str):
        """Create directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.post_directory",
            params={
                "PATH": f"gcodes/{directory}",
            },
        )
    
    xǁMoonAPIǁcreate_directory__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁcreate_directory__mutmut_1': xǁMoonAPIǁcreate_directory__mutmut_1, 
        'xǁMoonAPIǁcreate_directory__mutmut_2': xǁMoonAPIǁcreate_directory__mutmut_2, 
        'xǁMoonAPIǁcreate_directory__mutmut_3': xǁMoonAPIǁcreate_directory__mutmut_3, 
        'xǁMoonAPIǁcreate_directory__mutmut_4': xǁMoonAPIǁcreate_directory__mutmut_4, 
        'xǁMoonAPIǁcreate_directory__mutmut_5': xǁMoonAPIǁcreate_directory__mutmut_5, 
        'xǁMoonAPIǁcreate_directory__mutmut_6': xǁMoonAPIǁcreate_directory__mutmut_6, 
        'xǁMoonAPIǁcreate_directory__mutmut_7': xǁMoonAPIǁcreate_directory__mutmut_7, 
        'xǁMoonAPIǁcreate_directory__mutmut_8': xǁMoonAPIǁcreate_directory__mutmut_8, 
        'xǁMoonAPIǁcreate_directory__mutmut_9': xǁMoonAPIǁcreate_directory__mutmut_9, 
        'xǁMoonAPIǁcreate_directory__mutmut_10': xǁMoonAPIǁcreate_directory__mutmut_10, 
        'xǁMoonAPIǁcreate_directory__mutmut_11': xǁMoonAPIǁcreate_directory__mutmut_11, 
        'xǁMoonAPIǁcreate_directory__mutmut_12': xǁMoonAPIǁcreate_directory__mutmut_12, 
        'xǁMoonAPIǁcreate_directory__mutmut_13': xǁMoonAPIǁcreate_directory__mutmut_13
    }
    xǁMoonAPIǁcreate_directory__mutmut_orig.__name__ = 'xǁMoonAPIǁcreate_directory'

    def delete_directory(self, directory: str):
        args = [directory]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁdelete_directory__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁdelete_directory__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁdelete_directory__mutmut_orig(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_1(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False and directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_2(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is not False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_3(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is True or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_4(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is not None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_5(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return True
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_6(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method=None,
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_7(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params=None,
        )

    def xǁMoonAPIǁdelete_directory__mutmut_8(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_9(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            )

    def xǁMoonAPIǁdelete_directory__mutmut_10(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="XXserver.files.delete_directoryXX",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_11(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="SERVER.FILES.DELETE_DIRECTORY",
            params={
                "path": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_12(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "XXpathXX": f"gcodes/{directory}",
            },
        )

    def xǁMoonAPIǁdelete_directory__mutmut_13(self, directory: str):
        """Delete directory"""
        if isinstance(directory, str) is False or directory is None:
            return False
        return self._ws.send_request(
            method="server.files.delete_directory",
            params={
                "PATH": f"gcodes/{directory}",
            },
        )
    
    xǁMoonAPIǁdelete_directory__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁdelete_directory__mutmut_1': xǁMoonAPIǁdelete_directory__mutmut_1, 
        'xǁMoonAPIǁdelete_directory__mutmut_2': xǁMoonAPIǁdelete_directory__mutmut_2, 
        'xǁMoonAPIǁdelete_directory__mutmut_3': xǁMoonAPIǁdelete_directory__mutmut_3, 
        'xǁMoonAPIǁdelete_directory__mutmut_4': xǁMoonAPIǁdelete_directory__mutmut_4, 
        'xǁMoonAPIǁdelete_directory__mutmut_5': xǁMoonAPIǁdelete_directory__mutmut_5, 
        'xǁMoonAPIǁdelete_directory__mutmut_6': xǁMoonAPIǁdelete_directory__mutmut_6, 
        'xǁMoonAPIǁdelete_directory__mutmut_7': xǁMoonAPIǁdelete_directory__mutmut_7, 
        'xǁMoonAPIǁdelete_directory__mutmut_8': xǁMoonAPIǁdelete_directory__mutmut_8, 
        'xǁMoonAPIǁdelete_directory__mutmut_9': xǁMoonAPIǁdelete_directory__mutmut_9, 
        'xǁMoonAPIǁdelete_directory__mutmut_10': xǁMoonAPIǁdelete_directory__mutmut_10, 
        'xǁMoonAPIǁdelete_directory__mutmut_11': xǁMoonAPIǁdelete_directory__mutmut_11, 
        'xǁMoonAPIǁdelete_directory__mutmut_12': xǁMoonAPIǁdelete_directory__mutmut_12, 
        'xǁMoonAPIǁdelete_directory__mutmut_13': xǁMoonAPIǁdelete_directory__mutmut_13
    }
    xǁMoonAPIǁdelete_directory__mutmut_orig.__name__ = 'xǁMoonAPIǁdelete_directory'

    def move_file(self, source_dir: str, dest_dir: str):
        args = [source_dir, dest_dir]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁmove_file__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁmove_file__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁmove_file__mutmut_orig(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_1(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None and dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_2(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False and source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_3(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False and isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_4(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is not False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_5(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is True
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_6(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is not False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_7(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is True
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_8(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is not None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_9(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is not False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_10(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is True
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_11(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return True
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_12(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method=None,
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_13(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params=None,
        )

    def xǁMoonAPIǁmove_file__mutmut_14(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_15(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            )

    def xǁMoonAPIǁmove_file__mutmut_16(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="XXserver.files.moveXX",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_17(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="SERVER.FILES.MOVE",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_18(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"XXsourceXX": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_19(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"SOURCE": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_20(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "XXdestXX": dest_dir},
        )

    def xǁMoonAPIǁmove_file__mutmut_21(self, source_dir: str, dest_dir: str):
        """Move file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.move",
            params={"source": source_dir, "DEST": dest_dir},
        )
    
    xǁMoonAPIǁmove_file__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁmove_file__mutmut_1': xǁMoonAPIǁmove_file__mutmut_1, 
        'xǁMoonAPIǁmove_file__mutmut_2': xǁMoonAPIǁmove_file__mutmut_2, 
        'xǁMoonAPIǁmove_file__mutmut_3': xǁMoonAPIǁmove_file__mutmut_3, 
        'xǁMoonAPIǁmove_file__mutmut_4': xǁMoonAPIǁmove_file__mutmut_4, 
        'xǁMoonAPIǁmove_file__mutmut_5': xǁMoonAPIǁmove_file__mutmut_5, 
        'xǁMoonAPIǁmove_file__mutmut_6': xǁMoonAPIǁmove_file__mutmut_6, 
        'xǁMoonAPIǁmove_file__mutmut_7': xǁMoonAPIǁmove_file__mutmut_7, 
        'xǁMoonAPIǁmove_file__mutmut_8': xǁMoonAPIǁmove_file__mutmut_8, 
        'xǁMoonAPIǁmove_file__mutmut_9': xǁMoonAPIǁmove_file__mutmut_9, 
        'xǁMoonAPIǁmove_file__mutmut_10': xǁMoonAPIǁmove_file__mutmut_10, 
        'xǁMoonAPIǁmove_file__mutmut_11': xǁMoonAPIǁmove_file__mutmut_11, 
        'xǁMoonAPIǁmove_file__mutmut_12': xǁMoonAPIǁmove_file__mutmut_12, 
        'xǁMoonAPIǁmove_file__mutmut_13': xǁMoonAPIǁmove_file__mutmut_13, 
        'xǁMoonAPIǁmove_file__mutmut_14': xǁMoonAPIǁmove_file__mutmut_14, 
        'xǁMoonAPIǁmove_file__mutmut_15': xǁMoonAPIǁmove_file__mutmut_15, 
        'xǁMoonAPIǁmove_file__mutmut_16': xǁMoonAPIǁmove_file__mutmut_16, 
        'xǁMoonAPIǁmove_file__mutmut_17': xǁMoonAPIǁmove_file__mutmut_17, 
        'xǁMoonAPIǁmove_file__mutmut_18': xǁMoonAPIǁmove_file__mutmut_18, 
        'xǁMoonAPIǁmove_file__mutmut_19': xǁMoonAPIǁmove_file__mutmut_19, 
        'xǁMoonAPIǁmove_file__mutmut_20': xǁMoonAPIǁmove_file__mutmut_20, 
        'xǁMoonAPIǁmove_file__mutmut_21': xǁMoonAPIǁmove_file__mutmut_21
    }
    xǁMoonAPIǁmove_file__mutmut_orig.__name__ = 'xǁMoonAPIǁmove_file'

    def copy_file(self, source_dir: str, dest_dir: str):
        args = [source_dir, dest_dir]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁcopy_file__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁcopy_file__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁcopy_file__mutmut_orig(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_1(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None and dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_2(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False and source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_3(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False and isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_4(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is not False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_5(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is True
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_6(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is not False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_7(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is True
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_8(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is not None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_9(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is not False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_10(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is True
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_11(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return True
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_12(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method=None,
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_13(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params=None,
        )

    def xǁMoonAPIǁcopy_file__mutmut_14(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_15(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            )

    def xǁMoonAPIǁcopy_file__mutmut_16(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="XXserver.files.copyXX",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_17(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="SERVER.FILES.COPY",
            params={"source": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_18(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"XXsourceXX": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_19(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"SOURCE": source_dir, "dest": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_20(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "XXdestXX": dest_dir},
        )

    def xǁMoonAPIǁcopy_file__mutmut_21(self, source_dir: str, dest_dir: str):
        """Copy file"""
        if (
            isinstance(source_dir, str) is False
            or isinstance(dest_dir, str) is False
            or source_dir is None
            or dest_dir is False
        ):
            return False
        return self._ws.send_request(
            method="server.files.copy",
            params={"source": source_dir, "DEST": dest_dir},
        )
    
    xǁMoonAPIǁcopy_file__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁcopy_file__mutmut_1': xǁMoonAPIǁcopy_file__mutmut_1, 
        'xǁMoonAPIǁcopy_file__mutmut_2': xǁMoonAPIǁcopy_file__mutmut_2, 
        'xǁMoonAPIǁcopy_file__mutmut_3': xǁMoonAPIǁcopy_file__mutmut_3, 
        'xǁMoonAPIǁcopy_file__mutmut_4': xǁMoonAPIǁcopy_file__mutmut_4, 
        'xǁMoonAPIǁcopy_file__mutmut_5': xǁMoonAPIǁcopy_file__mutmut_5, 
        'xǁMoonAPIǁcopy_file__mutmut_6': xǁMoonAPIǁcopy_file__mutmut_6, 
        'xǁMoonAPIǁcopy_file__mutmut_7': xǁMoonAPIǁcopy_file__mutmut_7, 
        'xǁMoonAPIǁcopy_file__mutmut_8': xǁMoonAPIǁcopy_file__mutmut_8, 
        'xǁMoonAPIǁcopy_file__mutmut_9': xǁMoonAPIǁcopy_file__mutmut_9, 
        'xǁMoonAPIǁcopy_file__mutmut_10': xǁMoonAPIǁcopy_file__mutmut_10, 
        'xǁMoonAPIǁcopy_file__mutmut_11': xǁMoonAPIǁcopy_file__mutmut_11, 
        'xǁMoonAPIǁcopy_file__mutmut_12': xǁMoonAPIǁcopy_file__mutmut_12, 
        'xǁMoonAPIǁcopy_file__mutmut_13': xǁMoonAPIǁcopy_file__mutmut_13, 
        'xǁMoonAPIǁcopy_file__mutmut_14': xǁMoonAPIǁcopy_file__mutmut_14, 
        'xǁMoonAPIǁcopy_file__mutmut_15': xǁMoonAPIǁcopy_file__mutmut_15, 
        'xǁMoonAPIǁcopy_file__mutmut_16': xǁMoonAPIǁcopy_file__mutmut_16, 
        'xǁMoonAPIǁcopy_file__mutmut_17': xǁMoonAPIǁcopy_file__mutmut_17, 
        'xǁMoonAPIǁcopy_file__mutmut_18': xǁMoonAPIǁcopy_file__mutmut_18, 
        'xǁMoonAPIǁcopy_file__mutmut_19': xǁMoonAPIǁcopy_file__mutmut_19, 
        'xǁMoonAPIǁcopy_file__mutmut_20': xǁMoonAPIǁcopy_file__mutmut_20, 
        'xǁMoonAPIǁcopy_file__mutmut_21': xǁMoonAPIǁcopy_file__mutmut_21
    }
    xǁMoonAPIǁcopy_file__mutmut_orig.__name__ = 'xǁMoonAPIǁcopy_file'

    def list_announcements(self, include_dismissed: bool = False):
        args = [include_dismissed]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁlist_announcements__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁlist_announcements__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁlist_announcements__mutmut_orig(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method="server.announcements.list",
            params={"include_dismissed": include_dismissed},
        )

    def xǁMoonAPIǁlist_announcements__mutmut_1(self, include_dismissed: bool = True):
        """Request available announcements"""
        return self._ws.send_request(
            method="server.announcements.list",
            params={"include_dismissed": include_dismissed},
        )

    def xǁMoonAPIǁlist_announcements__mutmut_2(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method=None,
            params={"include_dismissed": include_dismissed},
        )

    def xǁMoonAPIǁlist_announcements__mutmut_3(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method="server.announcements.list",
            params=None,
        )

    def xǁMoonAPIǁlist_announcements__mutmut_4(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            params={"include_dismissed": include_dismissed},
        )

    def xǁMoonAPIǁlist_announcements__mutmut_5(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method="server.announcements.list",
            )

    def xǁMoonAPIǁlist_announcements__mutmut_6(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method="XXserver.announcements.listXX",
            params={"include_dismissed": include_dismissed},
        )

    def xǁMoonAPIǁlist_announcements__mutmut_7(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method="SERVER.ANNOUNCEMENTS.LIST",
            params={"include_dismissed": include_dismissed},
        )

    def xǁMoonAPIǁlist_announcements__mutmut_8(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method="server.announcements.list",
            params={"XXinclude_dismissedXX": include_dismissed},
        )

    def xǁMoonAPIǁlist_announcements__mutmut_9(self, include_dismissed: bool = False):
        """Request available announcements"""
        return self._ws.send_request(
            method="server.announcements.list",
            params={"INCLUDE_DISMISSED": include_dismissed},
        )
    
    xǁMoonAPIǁlist_announcements__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁlist_announcements__mutmut_1': xǁMoonAPIǁlist_announcements__mutmut_1, 
        'xǁMoonAPIǁlist_announcements__mutmut_2': xǁMoonAPIǁlist_announcements__mutmut_2, 
        'xǁMoonAPIǁlist_announcements__mutmut_3': xǁMoonAPIǁlist_announcements__mutmut_3, 
        'xǁMoonAPIǁlist_announcements__mutmut_4': xǁMoonAPIǁlist_announcements__mutmut_4, 
        'xǁMoonAPIǁlist_announcements__mutmut_5': xǁMoonAPIǁlist_announcements__mutmut_5, 
        'xǁMoonAPIǁlist_announcements__mutmut_6': xǁMoonAPIǁlist_announcements__mutmut_6, 
        'xǁMoonAPIǁlist_announcements__mutmut_7': xǁMoonAPIǁlist_announcements__mutmut_7, 
        'xǁMoonAPIǁlist_announcements__mutmut_8': xǁMoonAPIǁlist_announcements__mutmut_8, 
        'xǁMoonAPIǁlist_announcements__mutmut_9': xǁMoonAPIǁlist_announcements__mutmut_9
    }
    xǁMoonAPIǁlist_announcements__mutmut_orig.__name__ = 'xǁMoonAPIǁlist_announcements'

    def update_announcements(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁupdate_announcements__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁupdate_announcements__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁupdate_announcements__mutmut_orig(self):
        """Request announcements update to moonraker"""
        return self._ws.send_request(method="server.announcements.update")

    def xǁMoonAPIǁupdate_announcements__mutmut_1(self):
        """Request announcements update to moonraker"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁupdate_announcements__mutmut_2(self):
        """Request announcements update to moonraker"""
        return self._ws.send_request(method="XXserver.announcements.updateXX")

    def xǁMoonAPIǁupdate_announcements__mutmut_3(self):
        """Request announcements update to moonraker"""
        return self._ws.send_request(method="SERVER.ANNOUNCEMENTS.UPDATE")
    
    xǁMoonAPIǁupdate_announcements__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁupdate_announcements__mutmut_1': xǁMoonAPIǁupdate_announcements__mutmut_1, 
        'xǁMoonAPIǁupdate_announcements__mutmut_2': xǁMoonAPIǁupdate_announcements__mutmut_2, 
        'xǁMoonAPIǁupdate_announcements__mutmut_3': xǁMoonAPIǁupdate_announcements__mutmut_3
    }
    xǁMoonAPIǁupdate_announcements__mutmut_orig.__name__ = 'xǁMoonAPIǁupdate_announcements'

    def dismiss_announcements(self, entry_id: str, wake_time: int = 600):
        args = [entry_id, wake_time]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁdismiss_announcements__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁdismiss_announcements__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁdismiss_announcements__mutmut_orig(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_1(self, entry_id: str, wake_time: int = 601):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_2(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None and isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_3(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False and entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_4(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is not False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_5(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is True
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_6(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is not None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_7(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is not False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_8(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is True
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_9(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return True
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_10(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method=None,
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_11(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params=None,
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_12(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_13(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            )

    def xǁMoonAPIǁdismiss_announcements__mutmut_14(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="XXserver.announcements.dismissXX",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_15(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="SERVER.ANNOUNCEMENTS.DISMISS",
            params={"entry_id": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_16(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"XXentry_idXX": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_17(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"ENTRY_ID": entry_id, "wake_time": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_18(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "XXwake_timeXX": wake_time},
        )

    def xǁMoonAPIǁdismiss_announcements__mutmut_19(self, entry_id: str, wake_time: int = 600):
        """Dismiss announcements"""
        if (
            isinstance(entry_id, str) is False
            or entry_id is None
            or isinstance(wake_time, int) is False
        ):
            return False
        return self._ws.send_request(
            method="server.announcements.dismiss",
            params={"entry_id": entry_id, "WAKE_TIME": wake_time},
        )
    
    xǁMoonAPIǁdismiss_announcements__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁdismiss_announcements__mutmut_1': xǁMoonAPIǁdismiss_announcements__mutmut_1, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_2': xǁMoonAPIǁdismiss_announcements__mutmut_2, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_3': xǁMoonAPIǁdismiss_announcements__mutmut_3, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_4': xǁMoonAPIǁdismiss_announcements__mutmut_4, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_5': xǁMoonAPIǁdismiss_announcements__mutmut_5, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_6': xǁMoonAPIǁdismiss_announcements__mutmut_6, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_7': xǁMoonAPIǁdismiss_announcements__mutmut_7, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_8': xǁMoonAPIǁdismiss_announcements__mutmut_8, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_9': xǁMoonAPIǁdismiss_announcements__mutmut_9, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_10': xǁMoonAPIǁdismiss_announcements__mutmut_10, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_11': xǁMoonAPIǁdismiss_announcements__mutmut_11, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_12': xǁMoonAPIǁdismiss_announcements__mutmut_12, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_13': xǁMoonAPIǁdismiss_announcements__mutmut_13, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_14': xǁMoonAPIǁdismiss_announcements__mutmut_14, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_15': xǁMoonAPIǁdismiss_announcements__mutmut_15, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_16': xǁMoonAPIǁdismiss_announcements__mutmut_16, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_17': xǁMoonAPIǁdismiss_announcements__mutmut_17, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_18': xǁMoonAPIǁdismiss_announcements__mutmut_18, 
        'xǁMoonAPIǁdismiss_announcements__mutmut_19': xǁMoonAPIǁdismiss_announcements__mutmut_19
    }
    xǁMoonAPIǁdismiss_announcements__mutmut_orig.__name__ = 'xǁMoonAPIǁdismiss_announcements'

    def list_announcements_feeds(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁlist_announcements_feeds__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁlist_announcements_feeds__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁlist_announcements_feeds__mutmut_orig(self):
        """List announcement feeds"""
        return self._ws.send_request(method="server.announcements.feeds")

    def xǁMoonAPIǁlist_announcements_feeds__mutmut_1(self):
        """List announcement feeds"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁlist_announcements_feeds__mutmut_2(self):
        """List announcement feeds"""
        return self._ws.send_request(method="XXserver.announcements.feedsXX")

    def xǁMoonAPIǁlist_announcements_feeds__mutmut_3(self):
        """List announcement feeds"""
        return self._ws.send_request(method="SERVER.ANNOUNCEMENTS.FEEDS")
    
    xǁMoonAPIǁlist_announcements_feeds__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁlist_announcements_feeds__mutmut_1': xǁMoonAPIǁlist_announcements_feeds__mutmut_1, 
        'xǁMoonAPIǁlist_announcements_feeds__mutmut_2': xǁMoonAPIǁlist_announcements_feeds__mutmut_2, 
        'xǁMoonAPIǁlist_announcements_feeds__mutmut_3': xǁMoonAPIǁlist_announcements_feeds__mutmut_3
    }
    xǁMoonAPIǁlist_announcements_feeds__mutmut_orig.__name__ = 'xǁMoonAPIǁlist_announcements_feeds'

    def post_announcement_feed(self, announcement_name: str):
        args = [announcement_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁpost_announcement_feed__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁpost_announcement_feed__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁpost_announcement_feed__mutmut_orig(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_1(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False and announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_2(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is not False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_3(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is True or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_4(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is not None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_5(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return True
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_6(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method=None,
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_7(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params=None,
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_8(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_9(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_10(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="XXserver.announcements.post_feedXX",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_11(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="SERVER.ANNOUNCEMENTS.POST_FEED",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_12(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"XXnameXX": announcement_name},
        )

    def xǁMoonAPIǁpost_announcement_feed__mutmut_13(self, announcement_name: str):
        """Post annoucement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.post_feed",
            params={"NAME": announcement_name},
        )
    
    xǁMoonAPIǁpost_announcement_feed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁpost_announcement_feed__mutmut_1': xǁMoonAPIǁpost_announcement_feed__mutmut_1, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_2': xǁMoonAPIǁpost_announcement_feed__mutmut_2, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_3': xǁMoonAPIǁpost_announcement_feed__mutmut_3, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_4': xǁMoonAPIǁpost_announcement_feed__mutmut_4, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_5': xǁMoonAPIǁpost_announcement_feed__mutmut_5, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_6': xǁMoonAPIǁpost_announcement_feed__mutmut_6, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_7': xǁMoonAPIǁpost_announcement_feed__mutmut_7, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_8': xǁMoonAPIǁpost_announcement_feed__mutmut_8, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_9': xǁMoonAPIǁpost_announcement_feed__mutmut_9, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_10': xǁMoonAPIǁpost_announcement_feed__mutmut_10, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_11': xǁMoonAPIǁpost_announcement_feed__mutmut_11, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_12': xǁMoonAPIǁpost_announcement_feed__mutmut_12, 
        'xǁMoonAPIǁpost_announcement_feed__mutmut_13': xǁMoonAPIǁpost_announcement_feed__mutmut_13
    }
    xǁMoonAPIǁpost_announcement_feed__mutmut_orig.__name__ = 'xǁMoonAPIǁpost_announcement_feed'

    def delete_announcement_feed(self, announcement_name: str):
        args = [announcement_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁdelete_announcement_feed__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁdelete_announcement_feed__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_orig(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_1(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False and announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_2(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is not False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_3(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is True or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_4(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is not None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_5(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return True
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_6(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method=None,
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_7(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params=None,
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_8(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_9(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_10(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="XXserver.announcements.delete_feedXX",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_11(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="SERVER.ANNOUNCEMENTS.DELETE_FEED",
            params={"name": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_12(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"XXnameXX": announcement_name},
        )

    def xǁMoonAPIǁdelete_announcement_feed__mutmut_13(self, announcement_name: str):
        """Delete announcement feeds"""
        if isinstance(announcement_name, str) is False or announcement_name is None:
            return False
        return self._ws.send_request(
            method="server.announcements.delete_feed",
            params={"NAME": announcement_name},
        )
    
    xǁMoonAPIǁdelete_announcement_feed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁdelete_announcement_feed__mutmut_1': xǁMoonAPIǁdelete_announcement_feed__mutmut_1, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_2': xǁMoonAPIǁdelete_announcement_feed__mutmut_2, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_3': xǁMoonAPIǁdelete_announcement_feed__mutmut_3, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_4': xǁMoonAPIǁdelete_announcement_feed__mutmut_4, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_5': xǁMoonAPIǁdelete_announcement_feed__mutmut_5, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_6': xǁMoonAPIǁdelete_announcement_feed__mutmut_6, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_7': xǁMoonAPIǁdelete_announcement_feed__mutmut_7, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_8': xǁMoonAPIǁdelete_announcement_feed__mutmut_8, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_9': xǁMoonAPIǁdelete_announcement_feed__mutmut_9, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_10': xǁMoonAPIǁdelete_announcement_feed__mutmut_10, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_11': xǁMoonAPIǁdelete_announcement_feed__mutmut_11, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_12': xǁMoonAPIǁdelete_announcement_feed__mutmut_12, 
        'xǁMoonAPIǁdelete_announcement_feed__mutmut_13': xǁMoonAPIǁdelete_announcement_feed__mutmut_13
    }
    xǁMoonAPIǁdelete_announcement_feed__mutmut_orig.__name__ = 'xǁMoonAPIǁdelete_announcement_feed'

    def list_webcams(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁlist_webcams__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁlist_webcams__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁlist_webcams__mutmut_orig(self):
        """List available webcams"""
        return self._ws.send_request(method="server.webcams.list")

    def xǁMoonAPIǁlist_webcams__mutmut_1(self):
        """List available webcams"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁlist_webcams__mutmut_2(self):
        """List available webcams"""
        return self._ws.send_request(method="XXserver.webcams.listXX")

    def xǁMoonAPIǁlist_webcams__mutmut_3(self):
        """List available webcams"""
        return self._ws.send_request(method="SERVER.WEBCAMS.LIST")
    
    xǁMoonAPIǁlist_webcams__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁlist_webcams__mutmut_1': xǁMoonAPIǁlist_webcams__mutmut_1, 
        'xǁMoonAPIǁlist_webcams__mutmut_2': xǁMoonAPIǁlist_webcams__mutmut_2, 
        'xǁMoonAPIǁlist_webcams__mutmut_3': xǁMoonAPIǁlist_webcams__mutmut_3
    }
    xǁMoonAPIǁlist_webcams__mutmut_orig.__name__ = 'xǁMoonAPIǁlist_webcams'

    def get_webcam_info(self, uid: str):
        args = [uid]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_webcam_info__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_webcam_info__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_webcam_info__mutmut_orig(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_1(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False and uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_2(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is not False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_3(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is True or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_4(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is not None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_5(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return True
        return self._ws.send_request(
            method="server.webcams.get_info", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_6(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method=None, params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_7(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params=None
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_8(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_9(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", )

    def xǁMoonAPIǁget_webcam_info__mutmut_10(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="XXserver.webcams.get_infoXX", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_11(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="SERVER.WEBCAMS.GET_INFO", params={"uid": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_12(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params={"XXuidXX": uid}
        )

    def xǁMoonAPIǁget_webcam_info__mutmut_13(self, uid: str):
        """Get webcamera information"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.get_info", params={"UID": uid}
        )
    
    xǁMoonAPIǁget_webcam_info__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_webcam_info__mutmut_1': xǁMoonAPIǁget_webcam_info__mutmut_1, 
        'xǁMoonAPIǁget_webcam_info__mutmut_2': xǁMoonAPIǁget_webcam_info__mutmut_2, 
        'xǁMoonAPIǁget_webcam_info__mutmut_3': xǁMoonAPIǁget_webcam_info__mutmut_3, 
        'xǁMoonAPIǁget_webcam_info__mutmut_4': xǁMoonAPIǁget_webcam_info__mutmut_4, 
        'xǁMoonAPIǁget_webcam_info__mutmut_5': xǁMoonAPIǁget_webcam_info__mutmut_5, 
        'xǁMoonAPIǁget_webcam_info__mutmut_6': xǁMoonAPIǁget_webcam_info__mutmut_6, 
        'xǁMoonAPIǁget_webcam_info__mutmut_7': xǁMoonAPIǁget_webcam_info__mutmut_7, 
        'xǁMoonAPIǁget_webcam_info__mutmut_8': xǁMoonAPIǁget_webcam_info__mutmut_8, 
        'xǁMoonAPIǁget_webcam_info__mutmut_9': xǁMoonAPIǁget_webcam_info__mutmut_9, 
        'xǁMoonAPIǁget_webcam_info__mutmut_10': xǁMoonAPIǁget_webcam_info__mutmut_10, 
        'xǁMoonAPIǁget_webcam_info__mutmut_11': xǁMoonAPIǁget_webcam_info__mutmut_11, 
        'xǁMoonAPIǁget_webcam_info__mutmut_12': xǁMoonAPIǁget_webcam_info__mutmut_12, 
        'xǁMoonAPIǁget_webcam_info__mutmut_13': xǁMoonAPIǁget_webcam_info__mutmut_13
    }
    xǁMoonAPIǁget_webcam_info__mutmut_orig.__name__ = 'xǁMoonAPIǁget_webcam_info'

    def add_update_webcam(self, cam_name: str, snapshot_url: str, stream_url: str):
        args = [cam_name, snapshot_url, stream_url]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁadd_update_webcam__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁadd_update_webcam__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁadd_update_webcam__mutmut_orig(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_1(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None and stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_2(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None and snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_3(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False and cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_4(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False and isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_5(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False and isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_6(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is not False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_7(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is True
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_8(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is not False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_9(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is True
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_10(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is not False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_11(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is True
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_12(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is not None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_13(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is not None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_14(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is not None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_15(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return True
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_16(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method=None,
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_17(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params=None,
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_18(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_19(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            )

    def xǁMoonAPIǁadd_update_webcam__mutmut_20(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="XXserver.webcams.post_itemXX",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_21(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="SERVER.WEBCAMS.POST_ITEM",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_22(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "XXnameXX": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_23(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "NAME": cam_name,
                "snapshot_url": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_24(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "XXsnapshot_urlXX": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_25(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "SNAPSHOT_URL": snapshot_url,
                "stream_url": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_26(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "XXstream_urlXX": stream_url,
            },
        )

    def xǁMoonAPIǁadd_update_webcam__mutmut_27(self, cam_name: str, snapshot_url: str, stream_url: str):
        """Add or update webcamera"""
        if (
            isinstance(cam_name, str) is False
            or isinstance(snapshot_url, str) is False
            or isinstance(stream_url, str) is False
            or cam_name is None
            or snapshot_url is None
            or stream_url is None
        ):
            return False
        return self._ws.send_request(
            method="server.webcams.post_item",
            params={
                "name": cam_name,
                "snapshot_url": snapshot_url,
                "STREAM_URL": stream_url,
            },
        )
    
    xǁMoonAPIǁadd_update_webcam__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁadd_update_webcam__mutmut_1': xǁMoonAPIǁadd_update_webcam__mutmut_1, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_2': xǁMoonAPIǁadd_update_webcam__mutmut_2, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_3': xǁMoonAPIǁadd_update_webcam__mutmut_3, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_4': xǁMoonAPIǁadd_update_webcam__mutmut_4, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_5': xǁMoonAPIǁadd_update_webcam__mutmut_5, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_6': xǁMoonAPIǁadd_update_webcam__mutmut_6, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_7': xǁMoonAPIǁadd_update_webcam__mutmut_7, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_8': xǁMoonAPIǁadd_update_webcam__mutmut_8, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_9': xǁMoonAPIǁadd_update_webcam__mutmut_9, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_10': xǁMoonAPIǁadd_update_webcam__mutmut_10, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_11': xǁMoonAPIǁadd_update_webcam__mutmut_11, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_12': xǁMoonAPIǁadd_update_webcam__mutmut_12, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_13': xǁMoonAPIǁadd_update_webcam__mutmut_13, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_14': xǁMoonAPIǁadd_update_webcam__mutmut_14, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_15': xǁMoonAPIǁadd_update_webcam__mutmut_15, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_16': xǁMoonAPIǁadd_update_webcam__mutmut_16, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_17': xǁMoonAPIǁadd_update_webcam__mutmut_17, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_18': xǁMoonAPIǁadd_update_webcam__mutmut_18, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_19': xǁMoonAPIǁadd_update_webcam__mutmut_19, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_20': xǁMoonAPIǁadd_update_webcam__mutmut_20, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_21': xǁMoonAPIǁadd_update_webcam__mutmut_21, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_22': xǁMoonAPIǁadd_update_webcam__mutmut_22, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_23': xǁMoonAPIǁadd_update_webcam__mutmut_23, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_24': xǁMoonAPIǁadd_update_webcam__mutmut_24, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_25': xǁMoonAPIǁadd_update_webcam__mutmut_25, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_26': xǁMoonAPIǁadd_update_webcam__mutmut_26, 
        'xǁMoonAPIǁadd_update_webcam__mutmut_27': xǁMoonAPIǁadd_update_webcam__mutmut_27
    }
    xǁMoonAPIǁadd_update_webcam__mutmut_orig.__name__ = 'xǁMoonAPIǁadd_update_webcam'

    def delete_webcam(self, uid: str):
        args = [uid]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁdelete_webcam__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁdelete_webcam__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁdelete_webcam__mutmut_orig(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_1(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False and uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_2(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is not False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_3(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is True or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_4(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is not None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_5(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return True
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_6(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method=None, params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_7(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params=None
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_8(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_9(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", )

    def xǁMoonAPIǁdelete_webcam__mutmut_10(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="XXserver.webcams.delete_itemXX", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_11(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="SERVER.WEBCAMS.DELETE_ITEM", params={"uid": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_12(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"XXuidXX": uid}
        )

    def xǁMoonAPIǁdelete_webcam__mutmut_13(self, uid: str):
        """Delete webcamera"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(
            method="server.webcams.delete_item", params={"UID": uid}
        )
    
    xǁMoonAPIǁdelete_webcam__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁdelete_webcam__mutmut_1': xǁMoonAPIǁdelete_webcam__mutmut_1, 
        'xǁMoonAPIǁdelete_webcam__mutmut_2': xǁMoonAPIǁdelete_webcam__mutmut_2, 
        'xǁMoonAPIǁdelete_webcam__mutmut_3': xǁMoonAPIǁdelete_webcam__mutmut_3, 
        'xǁMoonAPIǁdelete_webcam__mutmut_4': xǁMoonAPIǁdelete_webcam__mutmut_4, 
        'xǁMoonAPIǁdelete_webcam__mutmut_5': xǁMoonAPIǁdelete_webcam__mutmut_5, 
        'xǁMoonAPIǁdelete_webcam__mutmut_6': xǁMoonAPIǁdelete_webcam__mutmut_6, 
        'xǁMoonAPIǁdelete_webcam__mutmut_7': xǁMoonAPIǁdelete_webcam__mutmut_7, 
        'xǁMoonAPIǁdelete_webcam__mutmut_8': xǁMoonAPIǁdelete_webcam__mutmut_8, 
        'xǁMoonAPIǁdelete_webcam__mutmut_9': xǁMoonAPIǁdelete_webcam__mutmut_9, 
        'xǁMoonAPIǁdelete_webcam__mutmut_10': xǁMoonAPIǁdelete_webcam__mutmut_10, 
        'xǁMoonAPIǁdelete_webcam__mutmut_11': xǁMoonAPIǁdelete_webcam__mutmut_11, 
        'xǁMoonAPIǁdelete_webcam__mutmut_12': xǁMoonAPIǁdelete_webcam__mutmut_12, 
        'xǁMoonAPIǁdelete_webcam__mutmut_13': xǁMoonAPIǁdelete_webcam__mutmut_13
    }
    xǁMoonAPIǁdelete_webcam__mutmut_orig.__name__ = 'xǁMoonAPIǁdelete_webcam'

    def test_webcam(self, uid: str):
        args = [uid]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁtest_webcam__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁtest_webcam__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁtest_webcam__mutmut_orig(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_1(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False and uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_2(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is not False or uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_3(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is True or uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_4(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is not None:
            return False
        return self._ws.send_request(method="server.webcams.test", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_5(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return True
        return self._ws.send_request(method="server.webcams.test", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_6(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method=None, params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_7(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", params=None)

    def xǁMoonAPIǁtest_webcam__mutmut_8(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_9(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", )

    def xǁMoonAPIǁtest_webcam__mutmut_10(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method="XXserver.webcams.testXX", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_11(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method="SERVER.WEBCAMS.TEST", params={"uid": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_12(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", params={"XXuidXX": uid})

    def xǁMoonAPIǁtest_webcam__mutmut_13(self, uid: str):
        """Test webcamera connection"""
        if isinstance(uid, str) is False or uid is None:
            return False
        return self._ws.send_request(method="server.webcams.test", params={"UID": uid})
    
    xǁMoonAPIǁtest_webcam__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁtest_webcam__mutmut_1': xǁMoonAPIǁtest_webcam__mutmut_1, 
        'xǁMoonAPIǁtest_webcam__mutmut_2': xǁMoonAPIǁtest_webcam__mutmut_2, 
        'xǁMoonAPIǁtest_webcam__mutmut_3': xǁMoonAPIǁtest_webcam__mutmut_3, 
        'xǁMoonAPIǁtest_webcam__mutmut_4': xǁMoonAPIǁtest_webcam__mutmut_4, 
        'xǁMoonAPIǁtest_webcam__mutmut_5': xǁMoonAPIǁtest_webcam__mutmut_5, 
        'xǁMoonAPIǁtest_webcam__mutmut_6': xǁMoonAPIǁtest_webcam__mutmut_6, 
        'xǁMoonAPIǁtest_webcam__mutmut_7': xǁMoonAPIǁtest_webcam__mutmut_7, 
        'xǁMoonAPIǁtest_webcam__mutmut_8': xǁMoonAPIǁtest_webcam__mutmut_8, 
        'xǁMoonAPIǁtest_webcam__mutmut_9': xǁMoonAPIǁtest_webcam__mutmut_9, 
        'xǁMoonAPIǁtest_webcam__mutmut_10': xǁMoonAPIǁtest_webcam__mutmut_10, 
        'xǁMoonAPIǁtest_webcam__mutmut_11': xǁMoonAPIǁtest_webcam__mutmut_11, 
        'xǁMoonAPIǁtest_webcam__mutmut_12': xǁMoonAPIǁtest_webcam__mutmut_12, 
        'xǁMoonAPIǁtest_webcam__mutmut_13': xǁMoonAPIǁtest_webcam__mutmut_13
    }
    xǁMoonAPIǁtest_webcam__mutmut_orig.__name__ = 'xǁMoonAPIǁtest_webcam'

    def list_notifiers(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁlist_notifiers__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁlist_notifiers__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁlist_notifiers__mutmut_orig(self):
        """List configured notifiers"""
        return self._ws.send_request(method="server.notifiers.list")

    def xǁMoonAPIǁlist_notifiers__mutmut_1(self):
        """List configured notifiers"""
        return self._ws.send_request(method=None)

    def xǁMoonAPIǁlist_notifiers__mutmut_2(self):
        """List configured notifiers"""
        return self._ws.send_request(method="XXserver.notifiers.listXX")

    def xǁMoonAPIǁlist_notifiers__mutmut_3(self):
        """List configured notifiers"""
        return self._ws.send_request(method="SERVER.NOTIFIERS.LIST")
    
    xǁMoonAPIǁlist_notifiers__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁlist_notifiers__mutmut_1': xǁMoonAPIǁlist_notifiers__mutmut_1, 
        'xǁMoonAPIǁlist_notifiers__mutmut_2': xǁMoonAPIǁlist_notifiers__mutmut_2, 
        'xǁMoonAPIǁlist_notifiers__mutmut_3': xǁMoonAPIǁlist_notifiers__mutmut_3
    }
    xǁMoonAPIǁlist_notifiers__mutmut_orig.__name__ = 'xǁMoonAPIǁlist_notifiers'

    @QtCore.pyqtSlot(bool, name="update-status")
    def update_status(self, refresh: bool = False) -> bool:
        """Get packages state"""
        return self._ws.send_request(
            method="machine.update.status", params={"refresh": refresh}
        )

    @QtCore.pyqtSlot(name="update-refresh")
    @QtCore.pyqtSlot(str, name="update-refresh")
    def refresh_update_status(self, name: str = None) -> bool:
        """Refresh packages state"""
        if isinstance(name, str):
            return self._ws.send_request(
                method="machine.update.refresh", params={"name": name}
            )
        else:
            return self._ws.send_request(
                method="machine.update.refresh",
            )

    @QtCore.pyqtSlot(name="update-full")
    def full_update(self) -> bool:
        """Issue full upgrade to all packages"""
        return self._ws.send_request(method="machine.update.full")

    @QtCore.pyqtSlot(name="update-moonraker")
    def update_moonraker(self) -> bool:
        """Issue moonraker update"""
        return self._ws.send_request(method="machine.update.moonraker")

    @QtCore.pyqtSlot(name="update-klipper")
    def update_klipper(self) -> bool:
        """Issue klipper update"""
        return self._ws.send_request(method="machine.update.klipper")

    @QtCore.pyqtSlot(str, name="update-client")
    def update_client(self, client_name: str = "") -> bool:
        """Issue client update"""
        if not isinstance(client_name, str) or not client_name:
            return False
        return self._ws.send_request(method="machine.update.client")

    @QtCore.pyqtSlot(name="update-system")
    def update_system(self):
        """Issue system update"""
        return self._ws.send_request(method="machine.update.system")

    @QtCore.pyqtSlot(str, name="recover-repo")
    @QtCore.pyqtSlot(str, bool, name="recover-repo")
    def recover_corrupt_repo(self, name: str, hard: bool = False):
        """Issue package recovery"""
        if isinstance(name, str) is False or name is None:
            return False
        return self._ws.send_request(
            method="machine.update.recover",
            params={"name": name, "hard": hard},
        )

    @QtCore.pyqtSlot(str, name="rollback-update")
    def rollback_update(self, name: str):
        """Issue rollback update"""
        if not isinstance(name, str) or not name:
            return False
        return self._ws.send_request(
            method="machine,update.rollback", params={"name": name}
        )

    def history_list(self, limit, start, since, before, order):
        """Request Job history list"""
        raise NotImplementedError

    def history_job_totals(self):
        """Request total job history"""
        raise NotImplementedError

    def history_reset_totals(self):
        """Request history reset"""
        raise NotImplementedError

    def history_get_job(self, uid: str):
        """Request job history"""
        raise NotImplementedError

    def history_delete_job(self, uid: str):
        """Request delete job history"""
        raise NotImplementedError

    #   ---------------------------------AMU----------------------------------

    def spoolman_proxy(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        args = [request_method, path, query, body, use_v2_response, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁspoolman_proxy__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁspoolman_proxy__mutmut_mutants'), args, kwargs, self)

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_orig(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_1(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = False,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_2(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = None
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_3(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "XXuse_v2_responseXX": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_4(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "USE_V2_RESPONSE": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_5(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "XXrequest_methodXX": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_6(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "REQUEST_METHOD": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_7(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "XXpathXX": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_8(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "PATH": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_9(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_10(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = None
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_11(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["XXqueryXX"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_12(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["QUERY"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_13(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_14(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = None
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_15(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["XXbodyXX"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_16(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["BODY"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_17(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method=None,
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_18(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=None,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_19(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            callback=None,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_20(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_21(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_22(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="server.spoolman.proxy",
            params=params,
            )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_23(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="XXserver.spoolman.proxyXX",
            params=params,
            callback=callback,
        )

    #   ---------------------------------AMU----------------------------------

    def xǁMoonAPIǁspoolman_proxy__mutmut_24(
        self,
        request_method: str,
        path: str,
        query: str | None = None,
        body: dict | None = None,
        use_v2_response: bool = True,
        callback=None,
    ) -> bool:
        """Proxy a request to the Spoolman API via Moonraker (server.spoolman.proxy)."""
        params = {
            "use_v2_response": use_v2_response,
            "request_method": request_method,
            "path": path,
        }
        if query is not None:
            params["query"] = query
        if body is not None:
            params["body"] = body
        return self._ws.send_request(
            method="SERVER.SPOOLMAN.PROXY",
            params=params,
            callback=callback,
        )
    
    xǁMoonAPIǁspoolman_proxy__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁspoolman_proxy__mutmut_1': xǁMoonAPIǁspoolman_proxy__mutmut_1, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_2': xǁMoonAPIǁspoolman_proxy__mutmut_2, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_3': xǁMoonAPIǁspoolman_proxy__mutmut_3, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_4': xǁMoonAPIǁspoolman_proxy__mutmut_4, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_5': xǁMoonAPIǁspoolman_proxy__mutmut_5, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_6': xǁMoonAPIǁspoolman_proxy__mutmut_6, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_7': xǁMoonAPIǁspoolman_proxy__mutmut_7, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_8': xǁMoonAPIǁspoolman_proxy__mutmut_8, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_9': xǁMoonAPIǁspoolman_proxy__mutmut_9, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_10': xǁMoonAPIǁspoolman_proxy__mutmut_10, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_11': xǁMoonAPIǁspoolman_proxy__mutmut_11, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_12': xǁMoonAPIǁspoolman_proxy__mutmut_12, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_13': xǁMoonAPIǁspoolman_proxy__mutmut_13, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_14': xǁMoonAPIǁspoolman_proxy__mutmut_14, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_15': xǁMoonAPIǁspoolman_proxy__mutmut_15, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_16': xǁMoonAPIǁspoolman_proxy__mutmut_16, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_17': xǁMoonAPIǁspoolman_proxy__mutmut_17, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_18': xǁMoonAPIǁspoolman_proxy__mutmut_18, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_19': xǁMoonAPIǁspoolman_proxy__mutmut_19, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_20': xǁMoonAPIǁspoolman_proxy__mutmut_20, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_21': xǁMoonAPIǁspoolman_proxy__mutmut_21, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_22': xǁMoonAPIǁspoolman_proxy__mutmut_22, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_23': xǁMoonAPIǁspoolman_proxy__mutmut_23, 
        'xǁMoonAPIǁspoolman_proxy__mutmut_24': xǁMoonAPIǁspoolman_proxy__mutmut_24
    }
    xǁMoonAPIǁspoolman_proxy__mutmut_orig.__name__ = 'xǁMoonAPIǁspoolman_proxy'

    def get_filaments(self, callback) -> bool:
        args = [callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_filaments__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_filaments__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_filaments__mutmut_orig(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="GET",
            path="/v1/filament",
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_1(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method=None,
            path="/v1/filament",
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_2(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="GET",
            path=None,
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_3(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="GET",
            path="/v1/filament",
            callback=None,
        )

    def xǁMoonAPIǁget_filaments__mutmut_4(self, callback) -> bool:
        return self.spoolman_proxy(
            path="/v1/filament",
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_5(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="GET",
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_6(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="GET",
            path="/v1/filament",
            )

    def xǁMoonAPIǁget_filaments__mutmut_7(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="XXGETXX",
            path="/v1/filament",
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_8(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="get",
            path="/v1/filament",
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_9(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="GET",
            path="XX/v1/filamentXX",
            callback=callback,
        )

    def xǁMoonAPIǁget_filaments__mutmut_10(self, callback) -> bool:
        return self.spoolman_proxy(
            request_method="GET",
            path="/V1/FILAMENT",
            callback=callback,
        )
    
    xǁMoonAPIǁget_filaments__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_filaments__mutmut_1': xǁMoonAPIǁget_filaments__mutmut_1, 
        'xǁMoonAPIǁget_filaments__mutmut_2': xǁMoonAPIǁget_filaments__mutmut_2, 
        'xǁMoonAPIǁget_filaments__mutmut_3': xǁMoonAPIǁget_filaments__mutmut_3, 
        'xǁMoonAPIǁget_filaments__mutmut_4': xǁMoonAPIǁget_filaments__mutmut_4, 
        'xǁMoonAPIǁget_filaments__mutmut_5': xǁMoonAPIǁget_filaments__mutmut_5, 
        'xǁMoonAPIǁget_filaments__mutmut_6': xǁMoonAPIǁget_filaments__mutmut_6, 
        'xǁMoonAPIǁget_filaments__mutmut_7': xǁMoonAPIǁget_filaments__mutmut_7, 
        'xǁMoonAPIǁget_filaments__mutmut_8': xǁMoonAPIǁget_filaments__mutmut_8, 
        'xǁMoonAPIǁget_filaments__mutmut_9': xǁMoonAPIǁget_filaments__mutmut_9, 
        'xǁMoonAPIǁget_filaments__mutmut_10': xǁMoonAPIǁget_filaments__mutmut_10
    }
    xǁMoonAPIǁget_filaments__mutmut_orig.__name__ = 'xǁMoonAPIǁget_filaments'

    def get_spool_id(self, callback=None) -> bool:
        args = [callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_spool_id__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_spool_id__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_spool_id__mutmut_orig(self, callback=None) -> bool:
        """Get the currently active spool ID (server.spoolman.get_spool_id)."""
        return self._ws.send_request(
            method="server.spoolman.get_spool_id",
            callback=callback,
        )

    def xǁMoonAPIǁget_spool_id__mutmut_1(self, callback=None) -> bool:
        """Get the currently active spool ID (server.spoolman.get_spool_id)."""
        return self._ws.send_request(
            method=None,
            callback=callback,
        )

    def xǁMoonAPIǁget_spool_id__mutmut_2(self, callback=None) -> bool:
        """Get the currently active spool ID (server.spoolman.get_spool_id)."""
        return self._ws.send_request(
            method="server.spoolman.get_spool_id",
            callback=None,
        )

    def xǁMoonAPIǁget_spool_id__mutmut_3(self, callback=None) -> bool:
        """Get the currently active spool ID (server.spoolman.get_spool_id)."""
        return self._ws.send_request(
            callback=callback,
        )

    def xǁMoonAPIǁget_spool_id__mutmut_4(self, callback=None) -> bool:
        """Get the currently active spool ID (server.spoolman.get_spool_id)."""
        return self._ws.send_request(
            method="server.spoolman.get_spool_id",
            )

    def xǁMoonAPIǁget_spool_id__mutmut_5(self, callback=None) -> bool:
        """Get the currently active spool ID (server.spoolman.get_spool_id)."""
        return self._ws.send_request(
            method="XXserver.spoolman.get_spool_idXX",
            callback=callback,
        )

    def xǁMoonAPIǁget_spool_id__mutmut_6(self, callback=None) -> bool:
        """Get the currently active spool ID (server.spoolman.get_spool_id)."""
        return self._ws.send_request(
            method="SERVER.SPOOLMAN.GET_SPOOL_ID",
            callback=callback,
        )
    
    xǁMoonAPIǁget_spool_id__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_spool_id__mutmut_1': xǁMoonAPIǁget_spool_id__mutmut_1, 
        'xǁMoonAPIǁget_spool_id__mutmut_2': xǁMoonAPIǁget_spool_id__mutmut_2, 
        'xǁMoonAPIǁget_spool_id__mutmut_3': xǁMoonAPIǁget_spool_id__mutmut_3, 
        'xǁMoonAPIǁget_spool_id__mutmut_4': xǁMoonAPIǁget_spool_id__mutmut_4, 
        'xǁMoonAPIǁget_spool_id__mutmut_5': xǁMoonAPIǁget_spool_id__mutmut_5, 
        'xǁMoonAPIǁget_spool_id__mutmut_6': xǁMoonAPIǁget_spool_id__mutmut_6
    }
    xǁMoonAPIǁget_spool_id__mutmut_orig.__name__ = 'xǁMoonAPIǁget_spool_id'

    def set_spool_id(self, spool_id: int | None, callback=None) -> bool:
        args = [spool_id, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁset_spool_id__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁset_spool_id__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁset_spool_id__mutmut_orig(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="server.spoolman.post_spool_id",
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_1(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method=None,
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_2(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="server.spoolman.post_spool_id",
            params=None,
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_3(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="server.spoolman.post_spool_id",
            params={"spool_id": spool_id},
            callback=None,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_4(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_5(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="server.spoolman.post_spool_id",
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_6(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="server.spoolman.post_spool_id",
            params={"spool_id": spool_id},
            )

    def xǁMoonAPIǁset_spool_id__mutmut_7(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="XXserver.spoolman.post_spool_idXX",
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_8(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="SERVER.SPOOLMAN.POST_SPOOL_ID",
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_9(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="server.spoolman.post_spool_id",
            params={"XXspool_idXX": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁset_spool_id__mutmut_10(self, spool_id: int | None, callback=None) -> bool:
        """Set the active spool ID (server.spoolman.post_spool_id). Pass None to unset."""
        return self._ws.send_request(
            method="server.spoolman.post_spool_id",
            params={"SPOOL_ID": spool_id},
            callback=callback,
        )
    
    xǁMoonAPIǁset_spool_id__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁset_spool_id__mutmut_1': xǁMoonAPIǁset_spool_id__mutmut_1, 
        'xǁMoonAPIǁset_spool_id__mutmut_2': xǁMoonAPIǁset_spool_id__mutmut_2, 
        'xǁMoonAPIǁset_spool_id__mutmut_3': xǁMoonAPIǁset_spool_id__mutmut_3, 
        'xǁMoonAPIǁset_spool_id__mutmut_4': xǁMoonAPIǁset_spool_id__mutmut_4, 
        'xǁMoonAPIǁset_spool_id__mutmut_5': xǁMoonAPIǁset_spool_id__mutmut_5, 
        'xǁMoonAPIǁset_spool_id__mutmut_6': xǁMoonAPIǁset_spool_id__mutmut_6, 
        'xǁMoonAPIǁset_spool_id__mutmut_7': xǁMoonAPIǁset_spool_id__mutmut_7, 
        'xǁMoonAPIǁset_spool_id__mutmut_8': xǁMoonAPIǁset_spool_id__mutmut_8, 
        'xǁMoonAPIǁset_spool_id__mutmut_9': xǁMoonAPIǁset_spool_id__mutmut_9, 
        'xǁMoonAPIǁset_spool_id__mutmut_10': xǁMoonAPIǁset_spool_id__mutmut_10
    }
    xǁMoonAPIǁset_spool_id__mutmut_orig.__name__ = 'xǁMoonAPIǁset_spool_id'

    def get_spool(self, spool_id: int, callback) -> bool:
        args = [spool_id, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁget_spool__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁget_spool__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁget_spool__mutmut_orig(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="server.spoolman.get_spool",
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_1(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method=None,
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_2(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="server.spoolman.get_spool",
            params=None,
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_3(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="server.spoolman.get_spool",
            params={"spool_id": spool_id},
            callback=None,
        )

    def xǁMoonAPIǁget_spool__mutmut_4(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_5(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="server.spoolman.get_spool",
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_6(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="server.spoolman.get_spool",
            params={"spool_id": spool_id},
            )

    def xǁMoonAPIǁget_spool__mutmut_7(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="XXserver.spoolman.get_spoolXX",
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_8(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="SERVER.SPOOLMAN.GET_SPOOL",
            params={"spool_id": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_9(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="server.spoolman.get_spool",
            params={"XXspool_idXX": spool_id},
            callback=callback,
        )

    def xǁMoonAPIǁget_spool__mutmut_10(self, spool_id: int, callback) -> bool:
        """Request spool data from Moonraker's Spoolman proxy"""
        return self._ws.send_request(
            method="server.spoolman.get_spool",
            params={"SPOOL_ID": spool_id},
            callback=callback,
        )
    
    xǁMoonAPIǁget_spool__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁget_spool__mutmut_1': xǁMoonAPIǁget_spool__mutmut_1, 
        'xǁMoonAPIǁget_spool__mutmut_2': xǁMoonAPIǁget_spool__mutmut_2, 
        'xǁMoonAPIǁget_spool__mutmut_3': xǁMoonAPIǁget_spool__mutmut_3, 
        'xǁMoonAPIǁget_spool__mutmut_4': xǁMoonAPIǁget_spool__mutmut_4, 
        'xǁMoonAPIǁget_spool__mutmut_5': xǁMoonAPIǁget_spool__mutmut_5, 
        'xǁMoonAPIǁget_spool__mutmut_6': xǁMoonAPIǁget_spool__mutmut_6, 
        'xǁMoonAPIǁget_spool__mutmut_7': xǁMoonAPIǁget_spool__mutmut_7, 
        'xǁMoonAPIǁget_spool__mutmut_8': xǁMoonAPIǁget_spool__mutmut_8, 
        'xǁMoonAPIǁget_spool__mutmut_9': xǁMoonAPIǁget_spool__mutmut_9, 
        'xǁMoonAPIǁget_spool__mutmut_10': xǁMoonAPIǁget_spool__mutmut_10
    }
    xǁMoonAPIǁget_spool__mutmut_orig.__name__ = 'xǁMoonAPIǁget_spool'

    def add_spool(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        args = [filament_id, body, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁadd_spool__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁadd_spool__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁadd_spool__mutmut_orig(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_1(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = None
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_2(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"XXfilament_idXX": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_3(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"FILAMENT_ID": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_4(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_5(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(None)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_6(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy(None, "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_7(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", None, body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_8(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=None, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_9(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, callback=None)

    def xǁMoonAPIǁadd_spool__mutmut_10(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_11(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_12(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_13(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/v1/spool", body=payload, )

    def xǁMoonAPIǁadd_spool__mutmut_14(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("XXPOSTXX", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_15(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("post", "/v1/spool", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_16(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "XX/v1/spoolXX", body=payload, callback=callback)

    def xǁMoonAPIǁadd_spool__mutmut_17(
        self, filament_id: int, body: dict | None = None, callback=None
    ) -> bool:
        """Create a new spool (POST /v1/spool)."""
        payload: dict = {"filament_id": filament_id}
        if body is not None:
            payload.update(body)
        return self.spoolman_proxy("POST", "/V1/SPOOL", body=payload, callback=callback)
    
    xǁMoonAPIǁadd_spool__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁadd_spool__mutmut_1': xǁMoonAPIǁadd_spool__mutmut_1, 
        'xǁMoonAPIǁadd_spool__mutmut_2': xǁMoonAPIǁadd_spool__mutmut_2, 
        'xǁMoonAPIǁadd_spool__mutmut_3': xǁMoonAPIǁadd_spool__mutmut_3, 
        'xǁMoonAPIǁadd_spool__mutmut_4': xǁMoonAPIǁadd_spool__mutmut_4, 
        'xǁMoonAPIǁadd_spool__mutmut_5': xǁMoonAPIǁadd_spool__mutmut_5, 
        'xǁMoonAPIǁadd_spool__mutmut_6': xǁMoonAPIǁadd_spool__mutmut_6, 
        'xǁMoonAPIǁadd_spool__mutmut_7': xǁMoonAPIǁadd_spool__mutmut_7, 
        'xǁMoonAPIǁadd_spool__mutmut_8': xǁMoonAPIǁadd_spool__mutmut_8, 
        'xǁMoonAPIǁadd_spool__mutmut_9': xǁMoonAPIǁadd_spool__mutmut_9, 
        'xǁMoonAPIǁadd_spool__mutmut_10': xǁMoonAPIǁadd_spool__mutmut_10, 
        'xǁMoonAPIǁadd_spool__mutmut_11': xǁMoonAPIǁadd_spool__mutmut_11, 
        'xǁMoonAPIǁadd_spool__mutmut_12': xǁMoonAPIǁadd_spool__mutmut_12, 
        'xǁMoonAPIǁadd_spool__mutmut_13': xǁMoonAPIǁadd_spool__mutmut_13, 
        'xǁMoonAPIǁadd_spool__mutmut_14': xǁMoonAPIǁadd_spool__mutmut_14, 
        'xǁMoonAPIǁadd_spool__mutmut_15': xǁMoonAPIǁadd_spool__mutmut_15, 
        'xǁMoonAPIǁadd_spool__mutmut_16': xǁMoonAPIǁadd_spool__mutmut_16, 
        'xǁMoonAPIǁadd_spool__mutmut_17': xǁMoonAPIǁadd_spool__mutmut_17
    }
    xǁMoonAPIǁadd_spool__mutmut_orig.__name__ = 'xǁMoonAPIǁadd_spool'

    def delete_spool(self, spool_id: int, callback=None) -> bool:
        args = [spool_id, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁdelete_spool__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁdelete_spool__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁdelete_spool__mutmut_orig(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy("DELETE", f"/v1/spool/{spool_id}", callback=callback)

    def xǁMoonAPIǁdelete_spool__mutmut_1(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy(None, f"/v1/spool/{spool_id}", callback=callback)

    def xǁMoonAPIǁdelete_spool__mutmut_2(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy("DELETE", None, callback=callback)

    def xǁMoonAPIǁdelete_spool__mutmut_3(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy("DELETE", f"/v1/spool/{spool_id}", callback=None)

    def xǁMoonAPIǁdelete_spool__mutmut_4(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy(f"/v1/spool/{spool_id}", callback=callback)

    def xǁMoonAPIǁdelete_spool__mutmut_5(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy("DELETE", callback=callback)

    def xǁMoonAPIǁdelete_spool__mutmut_6(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy("DELETE", f"/v1/spool/{spool_id}", )

    def xǁMoonAPIǁdelete_spool__mutmut_7(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy("XXDELETEXX", f"/v1/spool/{spool_id}", callback=callback)

    def xǁMoonAPIǁdelete_spool__mutmut_8(self, spool_id: int, callback=None) -> bool:
        """Delete a spool (DELETE /v1/spool/{id})."""
        return self.spoolman_proxy("delete", f"/v1/spool/{spool_id}", callback=callback)
    
    xǁMoonAPIǁdelete_spool__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁdelete_spool__mutmut_1': xǁMoonAPIǁdelete_spool__mutmut_1, 
        'xǁMoonAPIǁdelete_spool__mutmut_2': xǁMoonAPIǁdelete_spool__mutmut_2, 
        'xǁMoonAPIǁdelete_spool__mutmut_3': xǁMoonAPIǁdelete_spool__mutmut_3, 
        'xǁMoonAPIǁdelete_spool__mutmut_4': xǁMoonAPIǁdelete_spool__mutmut_4, 
        'xǁMoonAPIǁdelete_spool__mutmut_5': xǁMoonAPIǁdelete_spool__mutmut_5, 
        'xǁMoonAPIǁdelete_spool__mutmut_6': xǁMoonAPIǁdelete_spool__mutmut_6, 
        'xǁMoonAPIǁdelete_spool__mutmut_7': xǁMoonAPIǁdelete_spool__mutmut_7, 
        'xǁMoonAPIǁdelete_spool__mutmut_8': xǁMoonAPIǁdelete_spool__mutmut_8
    }
    xǁMoonAPIǁdelete_spool__mutmut_orig.__name__ = 'xǁMoonAPIǁdelete_spool'

    def add_filament(self, body: dict, callback=None) -> bool:
        args = [body, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁadd_filament__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁadd_filament__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁadd_filament__mutmut_orig(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", "/v1/filament", body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_1(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy(None, "/v1/filament", body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_2(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", None, body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_3(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", "/v1/filament", body=None, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_4(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", "/v1/filament", body=body, callback=None)

    def xǁMoonAPIǁadd_filament__mutmut_5(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("/v1/filament", body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_6(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_7(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", "/v1/filament", callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_8(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", "/v1/filament", body=body, )

    def xǁMoonAPIǁadd_filament__mutmut_9(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("XXPOSTXX", "/v1/filament", body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_10(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("post", "/v1/filament", body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_11(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", "XX/v1/filamentXX", body=body, callback=callback)

    def xǁMoonAPIǁadd_filament__mutmut_12(self, body: dict, callback=None) -> bool:
        """Create a new filament (POST /v1/filament)."""
        return self.spoolman_proxy("POST", "/V1/FILAMENT", body=body, callback=callback)
    
    xǁMoonAPIǁadd_filament__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁadd_filament__mutmut_1': xǁMoonAPIǁadd_filament__mutmut_1, 
        'xǁMoonAPIǁadd_filament__mutmut_2': xǁMoonAPIǁadd_filament__mutmut_2, 
        'xǁMoonAPIǁadd_filament__mutmut_3': xǁMoonAPIǁadd_filament__mutmut_3, 
        'xǁMoonAPIǁadd_filament__mutmut_4': xǁMoonAPIǁadd_filament__mutmut_4, 
        'xǁMoonAPIǁadd_filament__mutmut_5': xǁMoonAPIǁadd_filament__mutmut_5, 
        'xǁMoonAPIǁadd_filament__mutmut_6': xǁMoonAPIǁadd_filament__mutmut_6, 
        'xǁMoonAPIǁadd_filament__mutmut_7': xǁMoonAPIǁadd_filament__mutmut_7, 
        'xǁMoonAPIǁadd_filament__mutmut_8': xǁMoonAPIǁadd_filament__mutmut_8, 
        'xǁMoonAPIǁadd_filament__mutmut_9': xǁMoonAPIǁadd_filament__mutmut_9, 
        'xǁMoonAPIǁadd_filament__mutmut_10': xǁMoonAPIǁadd_filament__mutmut_10, 
        'xǁMoonAPIǁadd_filament__mutmut_11': xǁMoonAPIǁadd_filament__mutmut_11, 
        'xǁMoonAPIǁadd_filament__mutmut_12': xǁMoonAPIǁadd_filament__mutmut_12
    }
    xǁMoonAPIǁadd_filament__mutmut_orig.__name__ = 'xǁMoonAPIǁadd_filament'

    def update_spool(self, spool_id: int, body: dict, callback=None) -> bool:
        args = [spool_id, body, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁMoonAPIǁupdate_spool__mutmut_orig'), object.__getattribute__(self, 'xǁMoonAPIǁupdate_spool__mutmut_mutants'), args, kwargs, self)

    def xǁMoonAPIǁupdate_spool__mutmut_orig(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "PATCH", f"/v1/spool/{spool_id}", body=body, callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_1(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            None, f"/v1/spool/{spool_id}", body=body, callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_2(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "PATCH", None, body=body, callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_3(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "PATCH", f"/v1/spool/{spool_id}", body=None, callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_4(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "PATCH", f"/v1/spool/{spool_id}", body=body, callback=None
        )

    def xǁMoonAPIǁupdate_spool__mutmut_5(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            f"/v1/spool/{spool_id}", body=body, callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_6(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "PATCH", body=body, callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_7(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "PATCH", f"/v1/spool/{spool_id}", callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_8(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "PATCH", f"/v1/spool/{spool_id}", body=body, )

    def xǁMoonAPIǁupdate_spool__mutmut_9(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "XXPATCHXX", f"/v1/spool/{spool_id}", body=body, callback=callback
        )

    def xǁMoonAPIǁupdate_spool__mutmut_10(self, spool_id: int, body: dict, callback=None) -> bool:
        """Update spool attributes (PATCH /v1/spool/{id})."""
        return self.spoolman_proxy(
            "patch", f"/v1/spool/{spool_id}", body=body, callback=callback
        )
    
    xǁMoonAPIǁupdate_spool__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁMoonAPIǁupdate_spool__mutmut_1': xǁMoonAPIǁupdate_spool__mutmut_1, 
        'xǁMoonAPIǁupdate_spool__mutmut_2': xǁMoonAPIǁupdate_spool__mutmut_2, 
        'xǁMoonAPIǁupdate_spool__mutmut_3': xǁMoonAPIǁupdate_spool__mutmut_3, 
        'xǁMoonAPIǁupdate_spool__mutmut_4': xǁMoonAPIǁupdate_spool__mutmut_4, 
        'xǁMoonAPIǁupdate_spool__mutmut_5': xǁMoonAPIǁupdate_spool__mutmut_5, 
        'xǁMoonAPIǁupdate_spool__mutmut_6': xǁMoonAPIǁupdate_spool__mutmut_6, 
        'xǁMoonAPIǁupdate_spool__mutmut_7': xǁMoonAPIǁupdate_spool__mutmut_7, 
        'xǁMoonAPIǁupdate_spool__mutmut_8': xǁMoonAPIǁupdate_spool__mutmut_8, 
        'xǁMoonAPIǁupdate_spool__mutmut_9': xǁMoonAPIǁupdate_spool__mutmut_9, 
        'xǁMoonAPIǁupdate_spool__mutmut_10': xǁMoonAPIǁupdate_spool__mutmut_10
    }
    xǁMoonAPIǁupdate_spool__mutmut_orig.__name__ = 'xǁMoonAPIǁupdate_spool'
